#!/bin/sh

mime=$(file -bL --mime-type "$1")
category=${mime%%/*}

if [ -d "$1" ]; then
  lsd -1 --color=always "$1"
elif [ "$category" = text ]; then
  bat --color=always --theme=base16 "$1"
else
  lesspipe.sh "$1" | bat --color=always --theme=base16 "$1"
fi
