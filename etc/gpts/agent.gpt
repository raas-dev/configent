Name: Agent
Description: Help uset choose correct fabric prompts, execute them and provide output
Type: Tool
Context: github.com/gptscript-ai/context/cli, fabric
Tools: sys.prompt, sys.read, sys.stat, sys.find
Chat: true

Ask clarifying questions about the initial user requirements if required, provide the best possible prompts for the user to choose from, execute the selected prompt, and provide the output to the user. Always use fabric prompts for the user to choose from .
IMPORTANT: Please provide the correct fabric prompts for the user to choose from.

---
Name: fabric

#!/usr/bin/env bash

echo "The following is the help text for the fabric cli and all available prompts. Feel free to use those prompts if as additional tools if the need arises."
echo "IMPORTANT: Remember that you need to pipe user's request to the fabric prompt to make it work. Example: echo 'user input' | fabric -p explain_code or if reading from a file echo /path/to/file | fabric -p explain_code"
echo "Here is the CLI help and available prompts:"
fabric --help
fabric -l
