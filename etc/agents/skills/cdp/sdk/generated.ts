/* eslint-disable */
// AUTO-GENERATED by gen.ts. Do not edit by hand.
// Run `bun gen.ts` to regenerate from browser_protocol.json + js_protocol.json.

export interface Transport {
  _call(method: string, params?: unknown): Promise<unknown>;
}


export namespace Accessibility {

  /** Unique accessibility node identifier. */
  export type AXNodeId = string;

  /** Enum of possible property types. */
  export type AXValueType = "boolean" | "tristate" | "booleanOrUndefined" | "idref" | "idrefList" | "integer" | "node" | "nodeList" | "number" | "string" | "computedString" | "token" | "tokenList" | "domRelation" | "role" | "internalRole" | "valueUndefined";

  /** Enum of possible property sources. */
  export type AXValueSourceType = "attribute" | "implicit" | "style" | "contents" | "placeholder" | "relatedElement";

  /** Enum of possible native property sources (as a subtype of a particular AXValueSourceType). */
  export type AXValueNativeSourceType = "description" | "figcaption" | "label" | "labelfor" | "labelwrapped" | "legend" | "rubyannotation" | "tablecaption" | "title" | "other";

  /** A single source for a computed AX property. */
  export interface AXValueSource {
    /** What type of source this is. */
    type: Accessibility.AXValueSourceType;
    /** The value of this property source. */
    value?: Accessibility.AXValue;
    /** The name of the relevant attribute, if any. */
    attribute?: string;
    /** The value of the relevant attribute, if any. */
    attributeValue?: Accessibility.AXValue;
    /** Whether this source is superseded by a higher priority source. */
    superseded?: boolean;
    /** The native markup source for this value, e.g. a `<label>` element. */
    nativeSource?: Accessibility.AXValueNativeSourceType;
    /** The value, such as a node or node list, of the native source. */
    nativeSourceValue?: Accessibility.AXValue;
    /** Whether the value for this property is invalid. */
    invalid?: boolean;
    /** Reason for the value being invalid, if it is. */
    invalidReason?: string;
  }

  export interface AXRelatedNode {
    /** The BackendNodeId of the related DOM node. */
    backendDOMNodeId: DOM.BackendNodeId;
    /** The IDRef value provided, if any. */
    idref?: string;
    /** The text alternative of this node in the current context. */
    text?: string;
  }

  export interface AXProperty {
    /** The name of this property. */
    name: Accessibility.AXPropertyName;
    /** The value of this property. */
    value: Accessibility.AXValue;
  }

  /** A single computed AX property. */
  export interface AXValue {
    /** The type of this value. */
    type: Accessibility.AXValueType;
    /** The computed value of this property. */
    value?: unknown;
    /** One or more related nodes, if applicable. */
    relatedNodes?: Accessibility.AXRelatedNode[];
    /** The sources which contributed to the computation of this property. */
    sources?: Accessibility.AXValueSource[];
  }

  /**
   * Values of AXProperty name:
   * - from 'busy' to 'roledescription': states which apply to every AX node
   * - from 'live' to 'root': attributes which apply to nodes in live regions
   * - from 'autocomplete' to 'valuetext': attributes which apply to widgets
   * - from 'checked' to 'selected': states which apply to widgets
   * - from 'activedescendant' to 'owns': relationships between elements other than parent/child/sibling
   * - from 'activeFullscreenElement' to 'uninteresting': reasons why this noode is hidden
   */
  export type AXPropertyName = "actions" | "busy" | "disabled" | "editable" | "focusable" | "focused" | "hidden" | "hiddenRoot" | "invalid" | "keyshortcuts" | "settable" | "roledescription" | "live" | "atomic" | "relevant" | "root" | "autocomplete" | "hasPopup" | "level" | "multiselectable" | "orientation" | "multiline" | "readonly" | "required" | "valuemin" | "valuemax" | "valuetext" | "checked" | "expanded" | "modal" | "pressed" | "selected" | "activedescendant" | "controls" | "describedby" | "details" | "errormessage" | "flowto" | "labelledby" | "owns" | "url" | "activeFullscreenElement" | "activeModalDialog" | "activeAriaModalDialog" | "ariaHiddenElement" | "ariaHiddenSubtree" | "emptyAlt" | "emptyText" | "inertElement" | "inertSubtree" | "labelContainer" | "labelFor" | "notRendered" | "notVisible" | "presentationalRole" | "probablyPresentational" | "inactiveCarouselTabContent" | "uninteresting";

  /** A node in the accessibility tree. */
  export interface AXNode {
    /** Unique identifier for this node. */
    nodeId: Accessibility.AXNodeId;
    /** Whether this node is ignored for accessibility */
    ignored: boolean;
    /** Collection of reasons why this node is hidden. */
    ignoredReasons?: Accessibility.AXProperty[];
    /** This `Node`'s role, whether explicit or implicit. */
    role?: Accessibility.AXValue;
    /** This `Node`'s Chrome raw role. */
    chromeRole?: Accessibility.AXValue;
    /** The accessible name for this `Node`. */
    name?: Accessibility.AXValue;
    /** The accessible description for this `Node`. */
    description?: Accessibility.AXValue;
    /** The value for this `Node`. */
    value?: Accessibility.AXValue;
    /** All other properties */
    properties?: Accessibility.AXProperty[];
    /** ID for this node's parent. */
    parentId?: Accessibility.AXNodeId;
    /** IDs for each of this node's child nodes. */
    childIds?: Accessibility.AXNodeId[];
    /** The backend ID for the associated DOM node, if any. */
    backendDOMNodeId?: DOM.BackendNodeId;
    /** The frame ID for the frame associated with this nodes document. */
    frameId?: Page.FrameId;
  }

}

export namespace Animation {

  /** Animation instance. */
  export interface Animation {
    /** `Animation`'s id. */
    id: string;
    /** `Animation`'s name. */
    name: string;
    /** `Animation`'s internal paused state. */
    pausedState: boolean;
    /** `Animation`'s play state. */
    playState: string;
    /** `Animation`'s playback rate. */
    playbackRate: number;
    /**
     * `Animation`'s start time.
     * Milliseconds for time based animations and
     * percentage [0 - 100] for scroll driven animations
     * (i.e. when viewOrScrollTimeline exists).
     */
    startTime: number;
    /** `Animation`'s current time. */
    currentTime: number;
    /** Animation type of `Animation`. */
    type: "CSSTransition" | "CSSAnimation" | "WebAnimation";
    /** `Animation`'s source animation node. */
    source?: Animation.AnimationEffect;
    /**
     * A unique ID for `Animation` representing the sources that triggered this CSS
     * animation/transition.
     */
    cssId?: string;
    /** View or scroll timeline */
    viewOrScrollTimeline?: Animation.ViewOrScrollTimeline;
  }

  /** Timeline instance */
  export interface ViewOrScrollTimeline {
    /** Scroll container node */
    sourceNodeId?: DOM.BackendNodeId;
    /**
     * Represents the starting scroll position of the timeline
     * as a length offset in pixels from scroll origin.
     */
    startOffset?: number;
    /**
     * Represents the ending scroll position of the timeline
     * as a length offset in pixels from scroll origin.
     */
    endOffset?: number;
    /**
     * The element whose principal box's visibility in the
     * scrollport defined the progress of the timeline.
     * Does not exist for animations with ScrollTimeline
     */
    subjectNodeId?: DOM.BackendNodeId;
    /** Orientation of the scroll */
    axis: DOM.ScrollOrientation;
  }

  /** AnimationEffect instance */
  export interface AnimationEffect {
    /** `AnimationEffect`'s delay. */
    delay: number;
    /** `AnimationEffect`'s end delay. */
    endDelay: number;
    /** `AnimationEffect`'s iteration start. */
    iterationStart: number;
    /** `AnimationEffect`'s iterations. Omitted if the value is infinite. */
    iterations?: number;
    /**
     * `AnimationEffect`'s iteration duration.
     * Milliseconds for time based animations and
     * percentage [0 - 100] for scroll driven animations
     * (i.e. when viewOrScrollTimeline exists).
     */
    duration: number;
    /** `AnimationEffect`'s playback direction. */
    direction: string;
    /** `AnimationEffect`'s fill mode. */
    fill: string;
    /** `AnimationEffect`'s target node. */
    backendNodeId?: DOM.BackendNodeId;
    /** `AnimationEffect`'s keyframes. */
    keyframesRule?: Animation.KeyframesRule;
    /** `AnimationEffect`'s timing function. */
    easing: string;
  }

  /** Keyframes Rule */
  export interface KeyframesRule {
    /** CSS keyframed animation's name. */
    name?: string;
    /** List of animation keyframes. */
    keyframes: Animation.KeyframeStyle[];
  }

  /** Keyframe Style */
  export interface KeyframeStyle {
    /** Keyframe's time offset. */
    offset: string;
    /** `AnimationEffect`'s timing function. */
    easing: string;
  }

}

export namespace Audits {

  /** Information about a cookie that is affected by an inspector issue. */
  export interface AffectedCookie {
    /** The following three properties uniquely identify a cookie */
    name: string;
    path: string;
    domain: string;
  }

  /** Information about a request that is affected by an inspector issue. */
  export interface AffectedRequest {
    /** The unique request id. */
    requestId?: Network.RequestId;
    url: string;
  }

  /** Information about the frame affected by an inspector issue. */
  export interface AffectedFrame {
    frameId: Page.FrameId;
  }

  export type CookieExclusionReason = "ExcludeSameSiteUnspecifiedTreatedAsLax" | "ExcludeSameSiteNoneInsecure" | "ExcludeSameSiteLax" | "ExcludeSameSiteStrict" | "ExcludeDomainNonASCII" | "ExcludeThirdPartyCookieBlockedInFirstPartySet" | "ExcludeThirdPartyPhaseout" | "ExcludePortMismatch" | "ExcludeSchemeMismatch";

  export type CookieWarningReason = "WarnSameSiteUnspecifiedCrossSiteContext" | "WarnSameSiteNoneInsecure" | "WarnSameSiteUnspecifiedLaxAllowUnsafe" | "WarnSameSiteStrictLaxDowngradeStrict" | "WarnSameSiteStrictCrossDowngradeStrict" | "WarnSameSiteStrictCrossDowngradeLax" | "WarnSameSiteLaxCrossDowngradeStrict" | "WarnSameSiteLaxCrossDowngradeLax" | "WarnAttributeValueExceedsMaxSize" | "WarnDomainNonASCII" | "WarnThirdPartyPhaseout" | "WarnCrossSiteRedirectDowngradeChangesInclusion" | "WarnDeprecationTrialMetadata" | "WarnThirdPartyCookieHeuristic";

  export type CookieOperation = "SetCookie" | "ReadCookie";

  /** Represents the category of insight that a cookie issue falls under. */
  export type InsightType = "GitHubResource" | "GracePeriod" | "Heuristics";

  /** Information about the suggested solution to a cookie issue. */
  export interface CookieIssueInsight {
    type: Audits.InsightType;
    /** Link to table entry in third-party cookie migration readiness list. */
    tableEntryUrl?: string;
  }

  /**
   * This information is currently necessary, as the front-end has a difficult
   * time finding a specific cookie. With this, we can convey specific error
   * information without the cookie.
   */
  export interface CookieIssueDetails {
    /**
     * If AffectedCookie is not set then rawCookieLine contains the raw
     * Set-Cookie header string. This hints at a problem where the
     * cookie line is syntactically or semantically malformed in a way
     * that no valid cookie could be created.
     */
    cookie?: Audits.AffectedCookie;
    rawCookieLine?: string;
    cookieWarningReasons: Audits.CookieWarningReason[];
    cookieExclusionReasons: Audits.CookieExclusionReason[];
    /**
     * Optionally identifies the site-for-cookies and the cookie url, which
     * may be used by the front-end as additional context.
     */
    operation: Audits.CookieOperation;
    siteForCookies?: string;
    cookieUrl?: string;
    request?: Audits.AffectedRequest;
    /** The recommended solution to the issue. */
    insight?: Audits.CookieIssueInsight;
  }

  export type PerformanceIssueType = "DocumentCookie";

  /** Details for a performance issue. */
  export interface PerformanceIssueDetails {
    performanceIssueType: Audits.PerformanceIssueType;
    sourceCodeLocation?: Audits.SourceCodeLocation;
  }

  export type MixedContentResolutionStatus = "MixedContentBlocked" | "MixedContentAutomaticallyUpgraded" | "MixedContentWarning";

  export type MixedContentResourceType = "AttributionSrc" | "Audio" | "Beacon" | "CSPReport" | "Download" | "EventSource" | "Favicon" | "Font" | "Form" | "Frame" | "Image" | "Import" | "JSON" | "Manifest" | "Ping" | "PluginData" | "PluginResource" | "Prefetch" | "Resource" | "Script" | "ServiceWorker" | "SharedWorker" | "SpeculationRules" | "Stylesheet" | "Track" | "Video" | "Worker" | "XMLHttpRequest" | "XSLT";

  export interface MixedContentIssueDetails {
    /**
     * The type of resource causing the mixed content issue (css, js, iframe,
     * form,...). Marked as optional because it is mapped to from
     * blink::mojom::RequestContextType, which will be replaced
     * by network::mojom::RequestDestination
     */
    resourceType?: Audits.MixedContentResourceType;
    /** The way the mixed content issue is being resolved. */
    resolutionStatus: Audits.MixedContentResolutionStatus;
    /** The unsafe http url causing the mixed content issue. */
    insecureURL: string;
    /** The url responsible for the call to an unsafe url. */
    mainResourceURL: string;
    /**
     * The mixed content request.
     * Does not always exist (e.g. for unsafe form submission urls).
     */
    request?: Audits.AffectedRequest;
    /** Optional because not every mixed content issue is necessarily linked to a frame. */
    frame?: Audits.AffectedFrame;
  }

  /**
   * Enum indicating the reason a response has been blocked. These reasons are
   * refinements of the net error BLOCKED_BY_RESPONSE.
   */
  export type BlockedByResponseReason = "CoepFrameResourceNeedsCoepHeader" | "CoopSandboxedIFrameCannotNavigateToCoopPage" | "CorpNotSameOrigin" | "CorpNotSameOriginAfterDefaultedToSameOriginByCoep" | "CorpNotSameOriginAfterDefaultedToSameOriginByDip" | "CorpNotSameOriginAfterDefaultedToSameOriginByCoepAndDip" | "CorpNotSameSite" | "SRIMessageSignatureMismatch";

  /**
   * Details for a request that has been blocked with the BLOCKED_BY_RESPONSE
   * code. Currently only used for COEP/COOP, but may be extended to include
   * some CSP errors in the future.
   */
  export interface BlockedByResponseIssueDetails {
    request: Audits.AffectedRequest;
    parentFrame?: Audits.AffectedFrame;
    blockedFrame?: Audits.AffectedFrame;
    reason: Audits.BlockedByResponseReason;
  }

  export type HeavyAdResolutionStatus = "HeavyAdBlocked" | "HeavyAdWarning";

  export type HeavyAdReason = "NetworkTotalLimit" | "CpuTotalLimit" | "CpuPeakLimit";

  export interface HeavyAdIssueDetails {
    /** The resolution status, either blocking the content or warning. */
    resolution: Audits.HeavyAdResolutionStatus;
    /** The reason the ad was blocked, total network or cpu or peak cpu. */
    reason: Audits.HeavyAdReason;
    /** The frame that was blocked. */
    frame: Audits.AffectedFrame;
  }

  export type ContentSecurityPolicyViolationType = "kInlineViolation" | "kEvalViolation" | "kURLViolation" | "kSRIViolation" | "kTrustedTypesSinkViolation" | "kTrustedTypesPolicyViolation" | "kWasmEvalViolation";

  export interface SourceCodeLocation {
    scriptId?: Runtime.ScriptId;
    url: string;
    lineNumber: number;
    columnNumber: number;
  }

  export interface ContentSecurityPolicyIssueDetails {
    /** The url not included in allowed sources. */
    blockedURL?: string;
    /** Specific directive that is violated, causing the CSP issue. */
    violatedDirective: string;
    isReportOnly: boolean;
    contentSecurityPolicyViolationType: Audits.ContentSecurityPolicyViolationType;
    frameAncestor?: Audits.AffectedFrame;
    sourceCodeLocation?: Audits.SourceCodeLocation;
    violatingNodeId?: DOM.BackendNodeId;
  }

  export type SharedArrayBufferIssueType = "TransferIssue" | "CreationIssue";

  /**
   * Details for a issue arising from an SAB being instantiated in, or
   * transferred to a context that is not cross-origin isolated.
   */
  export interface SharedArrayBufferIssueDetails {
    sourceCodeLocation: Audits.SourceCodeLocation;
    isWarning: boolean;
    type: Audits.SharedArrayBufferIssueType;
  }

  /**
   * Details for a CORS related issue, e.g. a warning or error related to
   * CORS RFC1918 enforcement.
   */
  export interface CorsIssueDetails {
    corsErrorStatus: Network.CorsErrorStatus;
    isWarning: boolean;
    request: Audits.AffectedRequest;
    location?: Audits.SourceCodeLocation;
    initiatorOrigin?: string;
    resourceIPAddressSpace?: Network.IPAddressSpace;
    clientSecurityState?: Network.ClientSecurityState;
  }

  export type AttributionReportingIssueType = "PermissionPolicyDisabled" | "UntrustworthyReportingOrigin" | "InsecureContext" | "InvalidHeader" | "InvalidRegisterTriggerHeader" | "SourceAndTriggerHeaders" | "SourceIgnored" | "TriggerIgnored" | "OsSourceIgnored" | "OsTriggerIgnored" | "InvalidRegisterOsSourceHeader" | "InvalidRegisterOsTriggerHeader" | "WebAndOsHeaders" | "NoWebOrOsSupport" | "NavigationRegistrationWithoutTransientUserActivation" | "InvalidInfoHeader" | "NoRegisterSourceHeader" | "NoRegisterTriggerHeader" | "NoRegisterOsSourceHeader" | "NoRegisterOsTriggerHeader" | "NavigationRegistrationUniqueScopeAlreadySet";

  export type SharedDictionaryError = "UseErrorCrossOriginNoCorsRequest" | "UseErrorDictionaryLoadFailure" | "UseErrorMatchingDictionaryNotUsed" | "UseErrorUnexpectedContentDictionaryHeader" | "WriteErrorCossOriginNoCorsRequest" | "WriteErrorDisallowedBySettings" | "WriteErrorExpiredResponse" | "WriteErrorFeatureDisabled" | "WriteErrorInsufficientResources" | "WriteErrorInvalidMatchField" | "WriteErrorInvalidStructuredHeader" | "WriteErrorInvalidTTLField" | "WriteErrorNavigationRequest" | "WriteErrorNoMatchField" | "WriteErrorNonIntegerTTLField" | "WriteErrorNonListMatchDestField" | "WriteErrorNonSecureContext" | "WriteErrorNonStringIdField" | "WriteErrorNonStringInMatchDestList" | "WriteErrorNonStringMatchField" | "WriteErrorNonTokenTypeField" | "WriteErrorRequestAborted" | "WriteErrorShuttingDown" | "WriteErrorTooLongIdField" | "WriteErrorUnsupportedType";

  export type SRIMessageSignatureError = "MissingSignatureHeader" | "MissingSignatureInputHeader" | "InvalidSignatureHeader" | "InvalidSignatureInputHeader" | "SignatureHeaderValueIsNotByteSequence" | "SignatureHeaderValueIsParameterized" | "SignatureHeaderValueIsIncorrectLength" | "SignatureInputHeaderMissingLabel" | "SignatureInputHeaderValueNotInnerList" | "SignatureInputHeaderValueMissingComponents" | "SignatureInputHeaderInvalidComponentType" | "SignatureInputHeaderInvalidComponentName" | "SignatureInputHeaderInvalidHeaderComponentParameter" | "SignatureInputHeaderInvalidDerivedComponentParameter" | "SignatureInputHeaderKeyIdLength" | "SignatureInputHeaderInvalidParameter" | "SignatureInputHeaderMissingRequiredParameters" | "ValidationFailedSignatureExpired" | "ValidationFailedInvalidLength" | "ValidationFailedSignatureMismatch" | "ValidationFailedIntegrityMismatch";

  export type UnencodedDigestError = "MalformedDictionary" | "UnknownAlgorithm" | "IncorrectDigestType" | "IncorrectDigestLength";

  export type ConnectionAllowlistError = "InvalidHeader" | "MoreThanOneList" | "ItemNotInnerList" | "InvalidAllowlistItemType" | "ReportingEndpointNotToken" | "InvalidUrlPattern";

  /**
   * Details for issues around "Attribution Reporting API" usage.
   * Explainer: https://github.com/WICG/attribution-reporting-api
   */
  export interface AttributionReportingIssueDetails {
    violationType: Audits.AttributionReportingIssueType;
    request?: Audits.AffectedRequest;
    violatingNodeId?: DOM.BackendNodeId;
    invalidParameter?: string;
  }

  /**
   * Details for issues about documents in Quirks Mode
   * or Limited Quirks Mode that affects page layouting.
   */
  export interface QuirksModeIssueDetails {
    /**
     * If false, it means the document's mode is "quirks"
     * instead of "limited-quirks".
     */
    isLimitedQuirksMode: boolean;
    documentNodeId: DOM.BackendNodeId;
    url: string;
    frameId: Page.FrameId;
    loaderId: Network.LoaderId;
  }

  export interface NavigatorUserAgentIssueDetails {
    url: string;
    location?: Audits.SourceCodeLocation;
  }

  export interface SharedDictionaryIssueDetails {
    sharedDictionaryError: Audits.SharedDictionaryError;
    request: Audits.AffectedRequest;
  }

  export interface SRIMessageSignatureIssueDetails {
    error: Audits.SRIMessageSignatureError;
    signatureBase: string;
    integrityAssertions: string[];
    request: Audits.AffectedRequest;
  }

  export interface UnencodedDigestIssueDetails {
    error: Audits.UnencodedDigestError;
    request: Audits.AffectedRequest;
  }

  export interface ConnectionAllowlistIssueDetails {
    error: Audits.ConnectionAllowlistError;
    request: Audits.AffectedRequest;
  }

  export type GenericIssueErrorType = "FormLabelForNameError" | "FormDuplicateIdForInputError" | "FormInputWithNoLabelError" | "FormAutocompleteAttributeEmptyError" | "FormEmptyIdAndNameAttributesForInputError" | "FormAriaLabelledByToNonExistingIdError" | "FormInputAssignedAutocompleteValueToIdOrNameAttributeError" | "FormLabelHasNeitherForNorNestedInputError" | "FormLabelForMatchesNonExistingIdError" | "FormInputHasWrongButWellIntendedAutocompleteValueError" | "ResponseWasBlockedByORB" | "NavigationEntryMarkedSkippable" | "AutofillAndManualTextPolicyControlledFeaturesInfo" | "AutofillPolicyControlledFeatureInfo" | "ManualTextPolicyControlledFeatureInfo" | "FormModelContextParameterMissingTitleAndDescription" | "FormModelContextMissingToolName" | "FormModelContextMissingToolDescription" | "FormModelContextRequiredParameterMissingName" | "FormModelContextParameterMissingName";

  /** Depending on the concrete errorType, different properties are set. */
  export interface GenericIssueDetails {
    /** Issues with the same errorType are aggregated in the frontend. */
    errorType: Audits.GenericIssueErrorType;
    frameId?: Page.FrameId;
    violatingNodeId?: DOM.BackendNodeId;
    violatingNodeAttribute?: string;
    request?: Audits.AffectedRequest;
  }

  /**
   * This issue tracks information needed to print a deprecation message.
   * https://source.chromium.org/chromium/chromium/src/+/main:third_party/blink/renderer/core/frame/third_party/blink/renderer/core/frame/deprecation/README.md
   */
  export interface DeprecationIssueDetails {
    affectedFrame?: Audits.AffectedFrame;
    sourceCodeLocation: Audits.SourceCodeLocation;
    /** One of the deprecation names from third_party/blink/renderer/core/frame/deprecation/deprecation.json5 */
    type: string;
  }

  /**
   * This issue warns about sites in the redirect chain of a finished navigation
   * that may be flagged as trackers and have their state cleared if they don't
   * receive a user interaction. Note that in this context 'site' means eTLD+1.
   * For example, if the URL `https://example.test:80/bounce` was in the
   * redirect chain, the site reported would be `example.test`.
   */
  export interface BounceTrackingIssueDetails {
    trackingSites: string[];
  }

  /**
   * This issue warns about third-party sites that are accessing cookies on the
   * current page, and have been permitted due to having a global metadata grant.
   * Note that in this context 'site' means eTLD+1. For example, if the URL
   * `https://example.test:80/web_page` was accessing cookies, the site reported
   * would be `example.test`.
   */
  export interface CookieDeprecationMetadataIssueDetails {
    allowedSites: string[];
    optOutPercentage: number;
    isOptOutTopLevel: boolean;
    operation: Audits.CookieOperation;
  }

  export type ClientHintIssueReason = "MetaTagAllowListInvalidOrigin" | "MetaTagModifiedHTML";

  export interface FederatedAuthRequestIssueDetails {
    federatedAuthRequestIssueReason: Audits.FederatedAuthRequestIssueReason;
  }

  /**
   * Represents the failure reason when a federated authentication reason fails.
   * Should be updated alongside RequestIdTokenStatus in
   * third_party/blink/public/mojom/devtools/inspector_issue.mojom to include
   * all cases except for success.
   */
  export type FederatedAuthRequestIssueReason = "ShouldEmbargo" | "TooManyRequests" | "WellKnownHttpNotFound" | "WellKnownNoResponse" | "WellKnownInvalidResponse" | "WellKnownListEmpty" | "WellKnownInvalidContentType" | "ConfigNotInWellKnown" | "WellKnownTooBig" | "ConfigHttpNotFound" | "ConfigNoResponse" | "ConfigInvalidResponse" | "ConfigInvalidContentType" | "IdpNotPotentiallyTrustworthy" | "DisabledInSettings" | "DisabledInFlags" | "ErrorFetchingSignin" | "InvalidSigninResponse" | "AccountsHttpNotFound" | "AccountsNoResponse" | "AccountsInvalidResponse" | "AccountsListEmpty" | "AccountsInvalidContentType" | "IdTokenHttpNotFound" | "IdTokenNoResponse" | "IdTokenInvalidResponse" | "IdTokenIdpErrorResponse" | "IdTokenCrossSiteIdpErrorResponse" | "IdTokenInvalidRequest" | "IdTokenInvalidContentType" | "ErrorIdToken" | "Canceled" | "RpPageNotVisible" | "SilentMediationFailure" | "NotSignedInWithIdp" | "MissingTransientUserActivation" | "ReplacedByActiveMode" | "RelyingPartyOriginIsOpaque" | "TypeNotMatching" | "UiDismissedNoEmbargo" | "CorsError" | "SuppressedBySegmentationPlatform";

  export interface FederatedAuthUserInfoRequestIssueDetails {
    federatedAuthUserInfoRequestIssueReason: Audits.FederatedAuthUserInfoRequestIssueReason;
  }

  /**
   * Represents the failure reason when a getUserInfo() call fails.
   * Should be updated alongside FederatedAuthUserInfoRequestResult in
   * third_party/blink/public/mojom/devtools/inspector_issue.mojom.
   */
  export type FederatedAuthUserInfoRequestIssueReason = "NotSameOrigin" | "NotIframe" | "NotPotentiallyTrustworthy" | "NoApiPermission" | "NotSignedInWithIdp" | "NoAccountSharingPermission" | "InvalidConfigOrWellKnown" | "InvalidAccountsResponse" | "NoReturningUserFromFetchedAccounts";

  /**
   * This issue tracks client hints related issues. It's used to deprecate old
   * features, encourage the use of new ones, and provide general guidance.
   */
  export interface ClientHintIssueDetails {
    sourceCodeLocation: Audits.SourceCodeLocation;
    clientHintIssueReason: Audits.ClientHintIssueReason;
  }

  export interface FailedRequestInfo {
    /** The URL that failed to load. */
    url: string;
    /** The failure message for the failed request. */
    failureMessage: string;
    requestId?: Network.RequestId;
  }

  export type PartitioningBlobURLInfo = "BlockedCrossPartitionFetching" | "EnforceNoopenerForNavigation";

  export interface PartitioningBlobURLIssueDetails {
    /** The BlobURL that failed to load. */
    url: string;
    /** Additional information about the Partitioning Blob URL issue. */
    partitioningBlobURLInfo: Audits.PartitioningBlobURLInfo;
  }

  export type ElementAccessibilityIssueReason = "DisallowedSelectChild" | "DisallowedOptGroupChild" | "NonPhrasingContentOptionChild" | "InteractiveContentOptionChild" | "InteractiveContentLegendChild" | "InteractiveContentSummaryDescendant";

  /** This issue warns about errors in the select or summary element content model. */
  export interface ElementAccessibilityIssueDetails {
    nodeId: DOM.BackendNodeId;
    elementAccessibilityIssueReason: Audits.ElementAccessibilityIssueReason;
    hasDisallowedAttributes: boolean;
  }

  export type StyleSheetLoadingIssueReason = "LateImportRule" | "RequestFailed";

  /** This issue warns when a referenced stylesheet couldn't be loaded. */
  export interface StylesheetLoadingIssueDetails {
    /** Source code position that referenced the failing stylesheet. */
    sourceCodeLocation: Audits.SourceCodeLocation;
    /** Reason why the stylesheet couldn't be loaded. */
    styleSheetLoadingIssueReason: Audits.StyleSheetLoadingIssueReason;
    /** Contains additional info when the failure was due to a request. */
    failedRequestInfo?: Audits.FailedRequestInfo;
  }

  export type PropertyRuleIssueReason = "InvalidSyntax" | "InvalidInitialValue" | "InvalidInherits" | "InvalidName";

  /**
   * This issue warns about errors in property rules that lead to property
   * registrations being ignored.
   */
  export interface PropertyRuleIssueDetails {
    /** Source code position of the property rule. */
    sourceCodeLocation: Audits.SourceCodeLocation;
    /** Reason why the property rule was discarded. */
    propertyRuleIssueReason: Audits.PropertyRuleIssueReason;
    /** The value of the property rule property that failed to parse */
    propertyValue?: string;
  }

  export type UserReidentificationIssueType = "BlockedFrameNavigation" | "BlockedSubresource" | "NoisedCanvasReadback";

  /**
   * This issue warns about uses of APIs that may be considered misuse to
   * re-identify users.
   */
  export interface UserReidentificationIssueDetails {
    type: Audits.UserReidentificationIssueType;
    /** Applies to BlockedFrameNavigation and BlockedSubresource issue types. */
    request?: Audits.AffectedRequest;
    /** Applies to NoisedCanvasReadback issue type. */
    sourceCodeLocation?: Audits.SourceCodeLocation;
  }

  export type PermissionElementIssueType = "InvalidType" | "FencedFrameDisallowed" | "CspFrameAncestorsMissing" | "PermissionsPolicyBlocked" | "PaddingRightUnsupported" | "PaddingBottomUnsupported" | "InsetBoxShadowUnsupported" | "RequestInProgress" | "UntrustedEvent" | "RegistrationFailed" | "TypeNotSupported" | "InvalidTypeActivation" | "SecurityChecksFailed" | "ActivationDisabled" | "GeolocationDeprecated" | "InvalidDisplayStyle" | "NonOpaqueColor" | "LowContrast" | "FontSizeTooSmall" | "FontSizeTooLarge" | "InvalidSizeValue";

  /** This issue warns about improper usage of the <permission> element. */
  export interface PermissionElementIssueDetails {
    issueType: Audits.PermissionElementIssueType;
    /** The value of the type attribute. */
    type?: string;
    /** The node ID of the <permission> element. */
    nodeId?: DOM.BackendNodeId;
    /** True if the issue is a warning, false if it is an error. */
    isWarning?: boolean;
    /**
     * Fields for message construction:
     * Used for messages that reference a specific permission name
     */
    permissionName?: string;
    /** Used for messages about occlusion */
    occluderNodeInfo?: string;
    /** Used for messages about occluder's parent */
    occluderParentNodeInfo?: string;
    /** Used for messages about activation disabled reason */
    disableReason?: string;
  }

  /**
   * The issue warns about blocked calls to privacy sensitive APIs via the
   * Selective Permissions Intervention.
   */
  export interface SelectivePermissionsInterventionIssueDetails {
    /** Which API was intervened on. */
    apiName: string;
    /** Why the ad script using the API is considered an ad. */
    adAncestry: Network.AdAncestry;
    /** The stack trace at the time of the intervention. */
    stackTrace?: Runtime.StackTrace;
  }

  /**
   * A unique identifier for the type of issue. Each type may use one of the
   * optional fields in InspectorIssueDetails to convey more specific
   * information about the kind of issue.
   */
  export type InspectorIssueCode = "CookieIssue" | "MixedContentIssue" | "BlockedByResponseIssue" | "HeavyAdIssue" | "ContentSecurityPolicyIssue" | "SharedArrayBufferIssue" | "CorsIssue" | "AttributionReportingIssue" | "QuirksModeIssue" | "PartitioningBlobURLIssue" | "NavigatorUserAgentIssue" | "GenericIssue" | "DeprecationIssue" | "ClientHintIssue" | "FederatedAuthRequestIssue" | "BounceTrackingIssue" | "CookieDeprecationMetadataIssue" | "StylesheetLoadingIssue" | "FederatedAuthUserInfoRequestIssue" | "PropertyRuleIssue" | "SharedDictionaryIssue" | "ElementAccessibilityIssue" | "SRIMessageSignatureIssue" | "UnencodedDigestIssue" | "ConnectionAllowlistIssue" | "UserReidentificationIssue" | "PermissionElementIssue" | "PerformanceIssue" | "SelectivePermissionsInterventionIssue";

  /**
   * This struct holds a list of optional fields with additional information
   * specific to the kind of issue. When adding a new issue code, please also
   * add a new optional field to this type.
   */
  export interface InspectorIssueDetails {
    cookieIssueDetails?: Audits.CookieIssueDetails;
    mixedContentIssueDetails?: Audits.MixedContentIssueDetails;
    blockedByResponseIssueDetails?: Audits.BlockedByResponseIssueDetails;
    heavyAdIssueDetails?: Audits.HeavyAdIssueDetails;
    contentSecurityPolicyIssueDetails?: Audits.ContentSecurityPolicyIssueDetails;
    sharedArrayBufferIssueDetails?: Audits.SharedArrayBufferIssueDetails;
    corsIssueDetails?: Audits.CorsIssueDetails;
    attributionReportingIssueDetails?: Audits.AttributionReportingIssueDetails;
    quirksModeIssueDetails?: Audits.QuirksModeIssueDetails;
    partitioningBlobURLIssueDetails?: Audits.PartitioningBlobURLIssueDetails;
    navigatorUserAgentIssueDetails?: Audits.NavigatorUserAgentIssueDetails;
    genericIssueDetails?: Audits.GenericIssueDetails;
    deprecationIssueDetails?: Audits.DeprecationIssueDetails;
    clientHintIssueDetails?: Audits.ClientHintIssueDetails;
    federatedAuthRequestIssueDetails?: Audits.FederatedAuthRequestIssueDetails;
    bounceTrackingIssueDetails?: Audits.BounceTrackingIssueDetails;
    cookieDeprecationMetadataIssueDetails?: Audits.CookieDeprecationMetadataIssueDetails;
    stylesheetLoadingIssueDetails?: Audits.StylesheetLoadingIssueDetails;
    propertyRuleIssueDetails?: Audits.PropertyRuleIssueDetails;
    federatedAuthUserInfoRequestIssueDetails?: Audits.FederatedAuthUserInfoRequestIssueDetails;
    sharedDictionaryIssueDetails?: Audits.SharedDictionaryIssueDetails;
    elementAccessibilityIssueDetails?: Audits.ElementAccessibilityIssueDetails;
    sriMessageSignatureIssueDetails?: Audits.SRIMessageSignatureIssueDetails;
    unencodedDigestIssueDetails?: Audits.UnencodedDigestIssueDetails;
    connectionAllowlistIssueDetails?: Audits.ConnectionAllowlistIssueDetails;
    userReidentificationIssueDetails?: Audits.UserReidentificationIssueDetails;
    permissionElementIssueDetails?: Audits.PermissionElementIssueDetails;
    performanceIssueDetails?: Audits.PerformanceIssueDetails;
    selectivePermissionsInterventionIssueDetails?: Audits.SelectivePermissionsInterventionIssueDetails;
  }

  /**
   * A unique id for a DevTools inspector issue. Allows other entities (e.g.
   * exceptions, CDP message, console messages, etc.) to reference an issue.
   */
  export type IssueId = string;

  /** An inspector issue reported from the back-end. */
  export interface InspectorIssue {
    code: Audits.InspectorIssueCode;
    details: Audits.InspectorIssueDetails;
    /**
     * A unique id for this issue. May be omitted if no other entity (e.g.
     * exception, CDP message, etc.) is referencing this issue.
     */
    issueId?: Audits.IssueId;
  }

}

export namespace Autofill {

  export interface CreditCard {
    /** 16-digit credit card number. */
    number: string;
    /** Name of the credit card owner. */
    name: string;
    /** 2-digit expiry month. */
    expiryMonth: string;
    /** 4-digit expiry year. */
    expiryYear: string;
    /** 3-digit card verification code. */
    cvc: string;
  }

  export interface AddressField {
    /**
     * address field name, for example GIVEN_NAME.
     * The full list of supported field names:
     * https://source.chromium.org/chromium/chromium/src/+/main:components/autofill/core/browser/field_types.cc;l=38
     */
    name: string;
    /** address field value, for example Jon Doe. */
    value: string;
  }

  /** A list of address fields. */
  export interface AddressFields {
    fields: Autofill.AddressField[];
  }

  export interface Address {
    /** fields and values defining an address. */
    fields: Autofill.AddressField[];
  }

  /**
   * Defines how an address can be displayed like in chrome://settings/addresses.
   * Address UI is a two dimensional array, each inner array is an "address information line", and when rendered in a UI surface should be displayed as such.
   * The following address UI for instance:
   * [[{name: "GIVE_NAME", value: "Jon"}, {name: "FAMILY_NAME", value: "Doe"}], [{name: "CITY", value: "Munich"}, {name: "ZIP", value: "81456"}]]
   * should allow the receiver to render:
   * Jon Doe
   * Munich 81456
   */
  export interface AddressUI {
    /** A two dimension array containing the representation of values from an address profile. */
    addressFields: Autofill.AddressFields[];
  }

  /** Specified whether a filled field was done so by using the html autocomplete attribute or autofill heuristics. */
  export type FillingStrategy = "autocompleteAttribute" | "autofillInferred";

  export interface FilledField {
    /** The type of the field, e.g text, password etc. */
    htmlType: string;
    /** the html id */
    id: string;
    /** the html name */
    name: string;
    /** the field value */
    value: string;
    /** The actual field type, e.g FAMILY_NAME */
    autofillType: string;
    /** The filling strategy */
    fillingStrategy: Autofill.FillingStrategy;
    /** The frame the field belongs to */
    frameId: Page.FrameId;
    /** The form field's DOM node */
    fieldId: DOM.BackendNodeId;
  }

}

export namespace BackgroundService {

  /**
   * The Background Service that will be associated with the commands/events.
   * Every Background Service operates independently, but they share the same
   * API.
   */
  export type ServiceName = "backgroundFetch" | "backgroundSync" | "pushMessaging" | "notifications" | "paymentHandler" | "periodicBackgroundSync";

  /** A key-value pair for additional event information to pass along. */
  export interface EventMetadata {
    key: string;
    value: string;
  }

  export interface BackgroundServiceEvent {
    /** Timestamp of the event (in seconds). */
    timestamp: Network.TimeSinceEpoch;
    /** The origin this event belongs to. */
    origin: string;
    /** The Service Worker ID that initiated the event. */
    serviceWorkerRegistrationId: ServiceWorker.RegistrationID;
    /** The Background Service this event belongs to. */
    service: BackgroundService.ServiceName;
    /** A description of the event. */
    eventName: string;
    /** An identifier that groups related events together. */
    instanceId: string;
    /** A list of event-specific information. */
    eventMetadata: BackgroundService.EventMetadata[];
    /** Storage key this event belongs to. */
    storageKey: string;
  }

}

export namespace BluetoothEmulation {

  /** Indicates the various states of Central. */
  export type CentralState = "absent" | "powered-off" | "powered-on";

  /** Indicates the various types of GATT event. */
  export type GATTOperationType = "connection" | "discovery";

  /** Indicates the various types of characteristic write. */
  export type CharacteristicWriteType = "write-default-deprecated" | "write-with-response" | "write-without-response";

  /** Indicates the various types of characteristic operation. */
  export type CharacteristicOperationType = "read" | "write" | "subscribe-to-notifications" | "unsubscribe-from-notifications";

  /** Indicates the various types of descriptor operation. */
  export type DescriptorOperationType = "read" | "write";

  /** Stores the manufacturer data */
  export interface ManufacturerData {
    /**
     * Company identifier
     * https://bitbucket.org/bluetooth-SIG/public/src/main/assigned_numbers/company_identifiers/company_identifiers.yaml
     * https://usb.org/developers
     */
    key: number;
    /** Manufacturer-specific data (Encoded as a base64 string when passed over JSON) */
    data: string;
  }

  /** Stores the byte data of the advertisement packet sent by a Bluetooth device. */
  export interface ScanRecord {
    name?: string;
    uuids?: string[];
    /** Stores the external appearance description of the device. */
    appearance?: number;
    /** Stores the transmission power of a broadcasting device. */
    txPower?: number;
    /**
     * Key is the company identifier and the value is an array of bytes of
     * manufacturer specific data.
     */
    manufacturerData?: BluetoothEmulation.ManufacturerData[];
  }

  /** Stores the advertisement packet information that is sent by a Bluetooth device. */
  export interface ScanEntry {
    deviceAddress: string;
    rssi: number;
    scanRecord: BluetoothEmulation.ScanRecord;
  }

  /**
   * Describes the properties of a characteristic. This follows Bluetooth Core
   * Specification BT 4.2 Vol 3 Part G 3.3.1. Characteristic Properties.
   */
  export interface CharacteristicProperties {
    broadcast?: boolean;
    read?: boolean;
    writeWithoutResponse?: boolean;
    write?: boolean;
    notify?: boolean;
    indicate?: boolean;
    authenticatedSignedWrites?: boolean;
    extendedProperties?: boolean;
  }

}

export namespace Browser {

  export type BrowserContextID = string;

  export type WindowID = number;

  /** The state of the browser window. */
  export type WindowState = "normal" | "minimized" | "maximized" | "fullscreen";

  /** Browser window bounds information */
  export interface Bounds {
    /** The offset from the left edge of the screen to the window in pixels. */
    left?: number;
    /** The offset from the top edge of the screen to the window in pixels. */
    top?: number;
    /** The window width in pixels. */
    width?: number;
    /** The window height in pixels. */
    height?: number;
    /** The window state. Default to normal. */
    windowState?: Browser.WindowState;
  }

  export type PermissionType = "ar" | "audioCapture" | "automaticFullscreen" | "backgroundFetch" | "backgroundSync" | "cameraPanTiltZoom" | "capturedSurfaceControl" | "clipboardReadWrite" | "clipboardSanitizedWrite" | "displayCapture" | "durableStorage" | "geolocation" | "handTracking" | "idleDetection" | "keyboardLock" | "localFonts" | "localNetwork" | "localNetworkAccess" | "loopbackNetwork" | "midi" | "midiSysex" | "nfc" | "notifications" | "paymentHandler" | "periodicBackgroundSync" | "pointerLock" | "protectedMediaIdentifier" | "sensors" | "smartCard" | "speakerSelection" | "storageAccess" | "topLevelStorageAccess" | "videoCapture" | "vr" | "wakeLockScreen" | "wakeLockSystem" | "webAppInstallation" | "webPrinting" | "windowManagement";

  export type PermissionSetting = "granted" | "denied" | "prompt";

  /**
   * Definition of PermissionDescriptor defined in the Permissions API:
   * https://w3c.github.io/permissions/#dom-permissiondescriptor.
   */
  export interface PermissionDescriptor {
    /**
     * Name of permission.
     * See https://cs.chromium.org/chromium/src/third_party/blink/renderer/modules/permissions/permission_descriptor.idl for valid permission names.
     */
    name: string;
    /** For "midi" permission, may also specify sysex control. */
    sysex?: boolean;
    /**
     * For "push" permission, may specify userVisibleOnly.
     * Note that userVisibleOnly = true is the only currently supported type.
     */
    userVisibleOnly?: boolean;
    /** For "clipboard" permission, may specify allowWithoutSanitization. */
    allowWithoutSanitization?: boolean;
    /** For "fullscreen" permission, must specify allowWithoutGesture:true. */
    allowWithoutGesture?: boolean;
    /** For "camera" permission, may specify panTiltZoom. */
    panTiltZoom?: boolean;
  }

  /** Browser command ids used by executeBrowserCommand. */
  export type BrowserCommandId = "openTabSearch" | "closeTabSearch" | "openGlic";

  /** Chrome histogram bucket. */
  export interface Bucket {
    /** Minimum value (inclusive). */
    low: number;
    /** Maximum value (exclusive). */
    high: number;
    /** Number of samples. */
    count: number;
  }

  /** Chrome histogram. */
  export interface Histogram {
    /** Name. */
    name: string;
    /** Sum of sample values. */
    sum: number;
    /** Total number of samples. */
    count: number;
    /** Buckets. */
    buckets: Browser.Bucket[];
  }

  export type PrivacySandboxAPI = "BiddingAndAuctionServices" | "TrustedKeyValue";

}

export namespace CacheStorage {

  /** Unique identifier of the Cache object. */
  export type CacheId = string;

  /** type of HTTP response cached */
  export type CachedResponseType = "basic" | "cors" | "default" | "error" | "opaqueResponse" | "opaqueRedirect";

  /** Data entry. */
  export interface DataEntry {
    /** Request URL. */
    requestURL: string;
    /** Request method. */
    requestMethod: string;
    /** Request headers */
    requestHeaders: CacheStorage.Header[];
    /** Number of seconds since epoch. */
    responseTime: number;
    /** HTTP response status code. */
    responseStatus: number;
    /** HTTP response status text. */
    responseStatusText: string;
    /** HTTP response type */
    responseType: CacheStorage.CachedResponseType;
    /** Response headers */
    responseHeaders: CacheStorage.Header[];
  }

  /** Cache identifier. */
  export interface Cache {
    /** An opaque unique id of the cache. */
    cacheId: CacheStorage.CacheId;
    /** Security origin of the cache. */
    securityOrigin: string;
    /** Storage key of the cache. */
    storageKey: string;
    /** Storage bucket of the cache. */
    storageBucket?: Storage.StorageBucket;
    /** The name of the cache. */
    cacheName: string;
  }

  export interface Header {
    name: string;
    value: string;
  }

  /** Cached response */
  export interface CachedResponse {
    /** Entry content, base64-encoded. (Encoded as a base64 string when passed over JSON) */
    body: string;
  }

}

export namespace Cast {

  export interface Sink {
    name: string;
    id: string;
    /**
     * Text describing the current session. Present only if there is an active
     * session on the sink.
     */
    session?: string;
  }

}

export namespace Console {

  /** Console message. */
  export interface ConsoleMessage {
    /** Message source. */
    source: "xml" | "javascript" | "network" | "console-api" | "storage" | "appcache" | "rendering" | "security" | "other" | "deprecation" | "worker";
    /** Message severity. */
    level: "log" | "warning" | "error" | "debug" | "info";
    /** Message text. */
    text: string;
    /** URL of the message origin. */
    url?: string;
    /** Line number in the resource that generated this message (1-based). */
    line?: number;
    /** Column number in the resource that generated this message (1-based). */
    column?: number;
  }

}

export namespace CrashReportContext {

  /** Key-value pair in CrashReportContext. */
  export interface CrashReportContextEntry {
    key: string;
    value: string;
    /** The ID of the frame where the key-value pair was set. */
    frameId: Page.FrameId;
  }

}

export namespace CSS {

  /**
   * Stylesheet type: "injected" for stylesheets injected via extension, "user-agent" for user-agent
   * stylesheets, "inspector" for stylesheets created by the inspector (i.e. those holding the "via
   * inspector" rules), "regular" for regular stylesheets.
   */
  export type StyleSheetOrigin = "injected" | "user-agent" | "inspector" | "regular";

  /** CSS rule collection for a single pseudo style. */
  export interface PseudoElementMatches {
    /** Pseudo element type. */
    pseudoType: DOM.PseudoType;
    /** Pseudo element custom ident. */
    pseudoIdentifier?: string;
    /** Matches of CSS rules applicable to the pseudo style. */
    matches: CSS.RuleMatch[];
  }

  /** CSS style coming from animations with the name of the animation. */
  export interface CSSAnimationStyle {
    /** The name of the animation. */
    name?: string;
    /** The style coming from the animation. */
    style: CSS.CSSStyle;
  }

  /** Inherited CSS rule collection from ancestor node. */
  export interface InheritedStyleEntry {
    /** The ancestor node's inline style, if any, in the style inheritance chain. */
    inlineStyle?: CSS.CSSStyle;
    /** Matches of CSS rules matching the ancestor node in the style inheritance chain. */
    matchedCSSRules: CSS.RuleMatch[];
  }

  /** Inherited CSS style collection for animated styles from ancestor node. */
  export interface InheritedAnimatedStyleEntry {
    /** Styles coming from the animations of the ancestor, if any, in the style inheritance chain. */
    animationStyles?: CSS.CSSAnimationStyle[];
    /** The style coming from the transitions of the ancestor, if any, in the style inheritance chain. */
    transitionsStyle?: CSS.CSSStyle;
  }

  /** Inherited pseudo element matches from pseudos of an ancestor node. */
  export interface InheritedPseudoElementMatches {
    /** Matches of pseudo styles from the pseudos of an ancestor node. */
    pseudoElements: CSS.PseudoElementMatches[];
  }

  /** Match data for a CSS rule. */
  export interface RuleMatch {
    /** CSS rule in the match. */
    rule: CSS.CSSRule;
    /** Matching selector indices in the rule's selectorList selectors (0-based). */
    matchingSelectors: number[];
  }

  /** Data for a simple selector (these are delimited by commas in a selector list). */
  export interface Value {
    /** Value text. */
    text: string;
    /** Value range in the underlying resource (if available). */
    range?: CSS.SourceRange;
    /** Specificity of the selector. */
    specificity?: CSS.Specificity;
  }

  /**
   * Specificity:
   * https://drafts.csswg.org/selectors/#specificity-rules
   */
  export interface Specificity {
    /** The a component, which represents the number of ID selectors. */
    a: number;
    /**
     * The b component, which represents the number of class selectors, attributes selectors, and
     * pseudo-classes.
     */
    b: number;
    /** The c component, which represents the number of type selectors and pseudo-elements. */
    c: number;
  }

  /** Selector list data. */
  export interface SelectorList {
    /** Selectors in the list. */
    selectors: CSS.Value[];
    /** Rule selector text. */
    text: string;
  }

  /** CSS stylesheet metainformation. */
  export interface CSSStyleSheetHeader {
    /** The stylesheet identifier. */
    styleSheetId: DOM.StyleSheetId;
    /** Owner frame identifier. */
    frameId: Page.FrameId;
    /**
     * Stylesheet resource URL. Empty if this is a constructed stylesheet created using
     * new CSSStyleSheet() (but non-empty if this is a constructed stylesheet imported
     * as a CSS module script).
     */
    sourceURL: string;
    /** URL of source map associated with the stylesheet (if any). */
    sourceMapURL?: string;
    /** Stylesheet origin. */
    origin: CSS.StyleSheetOrigin;
    /** Stylesheet title. */
    title: string;
    /** The backend id for the owner node of the stylesheet. */
    ownerNode?: DOM.BackendNodeId;
    /** Denotes whether the stylesheet is disabled. */
    disabled: boolean;
    /** Whether the sourceURL field value comes from the sourceURL comment. */
    hasSourceURL?: boolean;
    /**
     * Whether this stylesheet is created for STYLE tag by parser. This flag is not set for
     * document.written STYLE tags.
     */
    isInline: boolean;
    /**
     * Whether this stylesheet is mutable. Inline stylesheets become mutable
     * after they have been modified via CSSOM API.
     * `<link>` element's stylesheets become mutable only if DevTools modifies them.
     * Constructed stylesheets (new CSSStyleSheet()) are mutable immediately after creation.
     */
    isMutable: boolean;
    /**
     * True if this stylesheet is created through new CSSStyleSheet() or imported as a
     * CSS module script.
     */
    isConstructed: boolean;
    /** Line offset of the stylesheet within the resource (zero based). */
    startLine: number;
    /** Column offset of the stylesheet within the resource (zero based). */
    startColumn: number;
    /** Size of the content (in characters). */
    length: number;
    /** Line offset of the end of the stylesheet within the resource (zero based). */
    endLine: number;
    /** Column offset of the end of the stylesheet within the resource (zero based). */
    endColumn: number;
    /** If the style sheet was loaded from a network resource, this indicates when the resource failed to load */
    loadingFailed?: boolean;
  }

  /** CSS rule representation. */
  export interface CSSRule {
    /**
     * The css style sheet identifier (absent for user agent stylesheet and user-specified
     * stylesheet rules) this rule came from.
     */
    styleSheetId?: DOM.StyleSheetId;
    /** Rule selector data. */
    selectorList: CSS.SelectorList;
    /** Array of selectors from ancestor style rules, sorted by distance from the current rule. */
    nestingSelectors?: string[];
    /** Parent stylesheet's origin. */
    origin: CSS.StyleSheetOrigin;
    /** Associated style declaration. */
    style: CSS.CSSStyle;
    /** The BackendNodeId of the DOM node that constitutes the origin tree scope of this rule. */
    originTreeScopeNodeId?: DOM.BackendNodeId;
    /**
     * Media list array (for rules involving media queries). The array enumerates media queries
     * starting with the innermost one, going outwards.
     */
    media?: CSS.CSSMedia[];
    /**
     * Container query list array (for rules involving container queries).
     * The array enumerates container queries starting with the innermost one, going outwards.
     */
    containerQueries?: CSS.CSSContainerQuery[];
    /**
     * @supports CSS at-rule array.
     * The array enumerates @supports at-rules starting with the innermost one, going outwards.
     */
    supports?: CSS.CSSSupports[];
    /**
     * Cascade layer array. Contains the layer hierarchy that this rule belongs to starting
     * with the innermost layer and going outwards.
     */
    layers?: CSS.CSSLayer[];
    /**
     * @scope CSS at-rule array.
     * The array enumerates @scope at-rules starting with the innermost one, going outwards.
     */
    scopes?: CSS.CSSScope[];
    /** The array keeps the types of ancestor CSSRules from the innermost going outwards. */
    ruleTypes?: CSS.CSSRuleType[];
    /**
     * @starting-style CSS at-rule array.
     * The array enumerates @starting-style at-rules starting with the innermost one, going outwards.
     */
    startingStyles?: CSS.CSSStartingStyle[];
    /**
     * @navigation CSS at-rule array.
     * The array enumerates @navigation at-rules starting with the innermost one, going outwards.
     */
    navigations?: CSS.CSSNavigation[];
  }

  /**
   * Enum indicating the type of a CSS rule, used to represent the order of a style rule's ancestors.
   * This list only contains rule types that are collected during the ancestor rule collection.
   */
  export type CSSRuleType = "MediaRule" | "SupportsRule" | "ContainerRule" | "LayerRule" | "ScopeRule" | "StyleRule" | "StartingStyleRule" | "NavigationRule";

  /** CSS coverage information. */
  export interface RuleUsage {
    /**
     * The css style sheet identifier (absent for user agent stylesheet and user-specified
     * stylesheet rules) this rule came from.
     */
    styleSheetId: DOM.StyleSheetId;
    /** Offset of the start of the rule (including selector) from the beginning of the stylesheet. */
    startOffset: number;
    /** Offset of the end of the rule body from the beginning of the stylesheet. */
    endOffset: number;
    /** Indicates whether the rule was actually used by some element in the page. */
    used: boolean;
  }

  /** Text range within a resource. All numbers are zero-based. */
  export interface SourceRange {
    /** Start line of range. */
    startLine: number;
    /** Start column of range (inclusive). */
    startColumn: number;
    /** End line of range */
    endLine: number;
    /** End column of range (exclusive). */
    endColumn: number;
  }

  export interface ShorthandEntry {
    /** Shorthand name. */
    name: string;
    /** Shorthand value. */
    value: string;
    /** Whether the property has "!important" annotation (implies `false` if absent). */
    important?: boolean;
  }

  export interface CSSComputedStyleProperty {
    /** Computed style property name. */
    name: string;
    /** Computed style property value. */
    value: string;
  }

  export interface ComputedStyleExtraFields {
    /**
     * Returns whether or not this node is being rendered with base appearance,
     * which happens when it has its appearance property set to base/base-select
     * or it is in the subtree of an element being rendered with base appearance.
     */
    isAppearanceBase: boolean;
  }

  /** CSS style representation. */
  export interface CSSStyle {
    /**
     * The css style sheet identifier (absent for user agent stylesheet and user-specified
     * stylesheet rules) this rule came from.
     */
    styleSheetId?: DOM.StyleSheetId;
    /** CSS properties in the style. */
    cssProperties: CSS.CSSProperty[];
    /** Computed values for all shorthands found in the style. */
    shorthandEntries: CSS.ShorthandEntry[];
    /** Style declaration text (if available). */
    cssText?: string;
    /** Style declaration range in the enclosing stylesheet (if available). */
    range?: CSS.SourceRange;
  }

  /** CSS property declaration data. */
  export interface CSSProperty {
    /** The property name. */
    name: string;
    /** The property value. */
    value: string;
    /** Whether the property has "!important" annotation (implies `false` if absent). */
    important?: boolean;
    /** Whether the property is implicit (implies `false` if absent). */
    implicit?: boolean;
    /** The full property text as specified in the style. */
    text?: string;
    /** Whether the property is understood by the browser (implies `true` if absent). */
    parsedOk?: boolean;
    /** Whether the property is disabled by the user (present for source-based properties only). */
    disabled?: boolean;
    /** The entire property range in the enclosing style declaration (if available). */
    range?: CSS.SourceRange;
    /**
     * Parsed longhand components of this property if it is a shorthand.
     * This field will be empty if the given property is not a shorthand.
     */
    longhandProperties?: CSS.CSSProperty[];
  }

  /** CSS media rule descriptor. */
  export interface CSSMedia {
    /** Media query text. */
    text: string;
    /**
     * Source of the media query: "mediaRule" if specified by a @media rule, "importRule" if
     * specified by an @import rule, "linkedSheet" if specified by a "media" attribute in a linked
     * stylesheet's LINK tag, "inlineSheet" if specified by a "media" attribute in an inline
     * stylesheet's STYLE tag.
     */
    source: "mediaRule" | "importRule" | "linkedSheet" | "inlineSheet";
    /** URL of the document containing the media query description. */
    sourceURL?: string;
    /**
     * The associated rule (@media or @import) header range in the enclosing stylesheet (if
     * available).
     */
    range?: CSS.SourceRange;
    /** Identifier of the stylesheet containing this object (if exists). */
    styleSheetId?: DOM.StyleSheetId;
    /** Array of media queries. */
    mediaList?: CSS.MediaQuery[];
  }

  /** Media query descriptor. */
  export interface MediaQuery {
    /** Array of media query expressions. */
    expressions: CSS.MediaQueryExpression[];
    /** Whether the media query condition is satisfied. */
    active: boolean;
  }

  /** Media query expression descriptor. */
  export interface MediaQueryExpression {
    /** Media query expression value. */
    value: number;
    /** Media query expression units. */
    unit: string;
    /** Media query expression feature. */
    feature: string;
    /** The associated range of the value text in the enclosing stylesheet (if available). */
    valueRange?: CSS.SourceRange;
    /** Computed length of media query expression (if applicable). */
    computedLength?: number;
  }

  /** CSS container query rule descriptor. */
  export interface CSSContainerQuery {
    /** Container query text. */
    text: string;
    /**
     * The associated rule header range in the enclosing stylesheet (if
     * available).
     */
    range?: CSS.SourceRange;
    /** Identifier of the stylesheet containing this object (if exists). */
    styleSheetId?: DOM.StyleSheetId;
    /** Optional name for the container. */
    name?: string;
    /** Optional physical axes queried for the container. */
    physicalAxes?: DOM.PhysicalAxes;
    /** Optional logical axes queried for the container. */
    logicalAxes?: DOM.LogicalAxes;
    /** true if the query contains scroll-state() queries. */
    queriesScrollState?: boolean;
    /** true if the query contains anchored() queries. */
    queriesAnchored?: boolean;
  }

  /** CSS Supports at-rule descriptor. */
  export interface CSSSupports {
    /** Supports rule text. */
    text: string;
    /** Whether the supports condition is satisfied. */
    active: boolean;
    /**
     * The associated rule header range in the enclosing stylesheet (if
     * available).
     */
    range?: CSS.SourceRange;
    /** Identifier of the stylesheet containing this object (if exists). */
    styleSheetId?: DOM.StyleSheetId;
  }

  /** CSS Navigation at-rule descriptor. */
  export interface CSSNavigation {
    /** Navigation rule text. */
    text: string;
    /** Whether the navigation condition is satisfied. */
    active?: boolean;
    /**
     * The associated rule header range in the enclosing stylesheet (if
     * available).
     */
    range?: CSS.SourceRange;
    /** Identifier of the stylesheet containing this object (if exists). */
    styleSheetId?: DOM.StyleSheetId;
  }

  /** CSS Scope at-rule descriptor. */
  export interface CSSScope {
    /** Scope rule text. */
    text: string;
    /**
     * The associated rule header range in the enclosing stylesheet (if
     * available).
     */
    range?: CSS.SourceRange;
    /** Identifier of the stylesheet containing this object (if exists). */
    styleSheetId?: DOM.StyleSheetId;
  }

  /** CSS Layer at-rule descriptor. */
  export interface CSSLayer {
    /** Layer name. */
    text: string;
    /**
     * The associated rule header range in the enclosing stylesheet (if
     * available).
     */
    range?: CSS.SourceRange;
    /** Identifier of the stylesheet containing this object (if exists). */
    styleSheetId?: DOM.StyleSheetId;
  }

  /** CSS Starting Style at-rule descriptor. */
  export interface CSSStartingStyle {
    /**
     * The associated rule header range in the enclosing stylesheet (if
     * available).
     */
    range?: CSS.SourceRange;
    /** Identifier of the stylesheet containing this object (if exists). */
    styleSheetId?: DOM.StyleSheetId;
  }

  /** CSS Layer data. */
  export interface CSSLayerData {
    /** Layer name. */
    name: string;
    /** Direct sub-layers */
    subLayers?: CSS.CSSLayerData[];
    /**
     * Layer order. The order determines the order of the layer in the cascade order.
     * A higher number has higher priority in the cascade order.
     */
    order: number;
  }

  /** Information about amount of glyphs that were rendered with given font. */
  export interface PlatformFontUsage {
    /** Font's family name reported by platform. */
    familyName: string;
    /** Font's PostScript name reported by platform. */
    postScriptName: string;
    /** Indicates if the font was downloaded or resolved locally. */
    isCustomFont: boolean;
    /** Amount of glyphs that were rendered with this font. */
    glyphCount: number;
  }

  /** Information about font variation axes for variable fonts */
  export interface FontVariationAxis {
    /** The font-variation-setting tag (a.k.a. "axis tag"). */
    tag: string;
    /** Human-readable variation name in the default language (normally, "en"). */
    name: string;
    /** The minimum value (inclusive) the font supports for this tag. */
    minValue: number;
    /** The maximum value (inclusive) the font supports for this tag. */
    maxValue: number;
    /** The default value. */
    defaultValue: number;
  }

  /**
   * Properties of a web font: https://www.w3.org/TR/2008/REC-CSS2-20080411/fonts.html#font-descriptions
   * and additional information such as platformFontFamily and fontVariationAxes.
   */
  export interface FontFace {
    /** The font-family. */
    fontFamily: string;
    /** The font-style. */
    fontStyle: string;
    /** The font-variant. */
    fontVariant: string;
    /** The font-weight. */
    fontWeight: string;
    /** The font-stretch. */
    fontStretch: string;
    /** The font-display. */
    fontDisplay: string;
    /** The unicode-range. */
    unicodeRange: string;
    /** The src. */
    src: string;
    /** The resolved platform font family */
    platformFontFamily: string;
    /** Available variation settings (a.k.a. "axes"). */
    fontVariationAxes?: CSS.FontVariationAxis[];
  }

  /** CSS try rule representation. */
  export interface CSSTryRule {
    /**
     * The css style sheet identifier (absent for user agent stylesheet and user-specified
     * stylesheet rules) this rule came from.
     */
    styleSheetId?: DOM.StyleSheetId;
    /** Parent stylesheet's origin. */
    origin: CSS.StyleSheetOrigin;
    /** Associated style declaration. */
    style: CSS.CSSStyle;
  }

  /** CSS @position-try rule representation. */
  export interface CSSPositionTryRule {
    /** The prelude dashed-ident name */
    name: CSS.Value;
    /**
     * The css style sheet identifier (absent for user agent stylesheet and user-specified
     * stylesheet rules) this rule came from.
     */
    styleSheetId?: DOM.StyleSheetId;
    /** Parent stylesheet's origin. */
    origin: CSS.StyleSheetOrigin;
    /** Associated style declaration. */
    style: CSS.CSSStyle;
    active: boolean;
  }

  /** CSS keyframes rule representation. */
  export interface CSSKeyframesRule {
    /** Animation name. */
    animationName: CSS.Value;
    /** List of keyframes. */
    keyframes: CSS.CSSKeyframeRule[];
  }

  /** Representation of a custom property registration through CSS.registerProperty */
  export interface CSSPropertyRegistration {
    propertyName: string;
    initialValue?: CSS.Value;
    inherits: boolean;
    syntax: string;
  }

  /** CSS generic @rule representation. */
  export interface CSSAtRule {
    /** Type of at-rule. */
    type: "font-face" | "font-feature-values" | "font-palette-values";
    /** Subsection of font-feature-values, if this is a subsection. */
    subsection?: "swash" | "annotation" | "ornaments" | "stylistic" | "styleset" | "character-variant";
    /**
     * LINT.ThenChange(//third_party/blink/renderer/core/inspector/inspector_style_sheet.cc:FontVariantAlternatesFeatureType,//third_party/blink/renderer/core/inspector/inspector_css_agent.cc:FontVariantAlternatesFeatureType)
     * Associated name, if applicable.
     */
    name?: CSS.Value;
    /**
     * The css style sheet identifier (absent for user agent stylesheet and user-specified
     * stylesheet rules) this rule came from.
     */
    styleSheetId?: DOM.StyleSheetId;
    /** Parent stylesheet's origin. */
    origin: CSS.StyleSheetOrigin;
    /** Associated style declaration. */
    style: CSS.CSSStyle;
  }

  /** CSS property at-rule representation. */
  export interface CSSPropertyRule {
    /**
     * The css style sheet identifier (absent for user agent stylesheet and user-specified
     * stylesheet rules) this rule came from.
     */
    styleSheetId?: DOM.StyleSheetId;
    /** Parent stylesheet's origin. */
    origin: CSS.StyleSheetOrigin;
    /** Associated property name. */
    propertyName: CSS.Value;
    /** Associated style declaration. */
    style: CSS.CSSStyle;
  }

  /** CSS function argument representation. */
  export interface CSSFunctionParameter {
    /** The parameter name. */
    name: string;
    /** The parameter type. */
    type: string;
  }

  /** CSS function conditional block representation. */
  export interface CSSFunctionConditionNode {
    /** Media query for this conditional block. Only one type of condition should be set. */
    media?: CSS.CSSMedia;
    /** Container query for this conditional block. Only one type of condition should be set. */
    containerQueries?: CSS.CSSContainerQuery;
    /** @supports CSS at-rule condition. Only one type of condition should be set. */
    supports?: CSS.CSSSupports;
    /** @navigation condition. Only one type of condition should be set. */
    navigation?: CSS.CSSNavigation;
    /** Block body. */
    children: CSS.CSSFunctionNode[];
    /** The condition text. */
    conditionText: string;
  }

  /** Section of the body of a CSS function rule. */
  export interface CSSFunctionNode {
    /** A conditional block. If set, style should not be set. */
    condition?: CSS.CSSFunctionConditionNode;
    /** Values set by this node. If set, condition should not be set. */
    style?: CSS.CSSStyle;
  }

  /** CSS function at-rule representation. */
  export interface CSSFunctionRule {
    /** Name of the function. */
    name: CSS.Value;
    /**
     * The css style sheet identifier (absent for user agent stylesheet and user-specified
     * stylesheet rules) this rule came from.
     */
    styleSheetId?: DOM.StyleSheetId;
    /** Parent stylesheet's origin. */
    origin: CSS.StyleSheetOrigin;
    /** List of parameters. */
    parameters: CSS.CSSFunctionParameter[];
    /** Function body. */
    children: CSS.CSSFunctionNode[];
    /** The BackendNodeId of the DOM node that constitutes the origin tree scope of this rule. */
    originTreeScopeNodeId?: DOM.BackendNodeId;
  }

  /** CSS keyframe rule representation. */
  export interface CSSKeyframeRule {
    /**
     * The css style sheet identifier (absent for user agent stylesheet and user-specified
     * stylesheet rules) this rule came from.
     */
    styleSheetId?: DOM.StyleSheetId;
    /** Parent stylesheet's origin. */
    origin: CSS.StyleSheetOrigin;
    /** Associated key text. */
    keyText: CSS.Value;
    /** Associated style declaration. */
    style: CSS.CSSStyle;
  }

  /** A descriptor of operation to mutate style declaration text. */
  export interface StyleDeclarationEdit {
    /** The css style sheet identifier. */
    styleSheetId: DOM.StyleSheetId;
    /** The range of the style text in the enclosing stylesheet. */
    range: CSS.SourceRange;
    /** New style text. */
    text: string;
  }

}

export namespace Debugger {

  /** Breakpoint identifier. */
  export type BreakpointId = string;

  /** Call frame identifier. */
  export type CallFrameId = string;

  /** Location in the source code. */
  export interface Location {
    /** Script identifier as reported in the `Debugger.scriptParsed`. */
    scriptId: Runtime.ScriptId;
    /** Line number in the script (0-based). */
    lineNumber: number;
    /** Column number in the script (0-based). */
    columnNumber?: number;
  }

  /** Location in the source code. */
  export interface ScriptPosition {
    lineNumber: number;
    columnNumber: number;
  }

  /** Location range within one script. */
  export interface LocationRange {
    scriptId: Runtime.ScriptId;
    start: Debugger.ScriptPosition;
    end: Debugger.ScriptPosition;
  }

  /** JavaScript call frame. Array of call frames form the call stack. */
  export interface CallFrame {
    /** Call frame identifier. This identifier is only valid while the virtual machine is paused. */
    callFrameId: Debugger.CallFrameId;
    /** Name of the JavaScript function called on this call frame. */
    functionName: string;
    /** Location in the source code. */
    functionLocation?: Debugger.Location;
    /** Location in the source code. */
    location: Debugger.Location;
    /**
     * JavaScript script name or url.
     * Deprecated in favor of using the `location.scriptId` to resolve the URL via a previously
     * sent `Debugger.scriptParsed` event.
     */
    url: string;
    /** Scope chain for this call frame. */
    scopeChain: Debugger.Scope[];
    /** `this` object for this call frame. */
    "this": Runtime.RemoteObject;
    /** The value being returned, if the function is at return point. */
    returnValue?: Runtime.RemoteObject;
    /**
     * Valid only while the VM is paused and indicates whether this frame
     * can be restarted or not. Note that a `true` value here does not
     * guarantee that Debugger#restartFrame with this CallFrameId will be
     * successful, but it is very likely.
     */
    canBeRestarted?: boolean;
  }

  /** Scope description. */
  export interface Scope {
    /** Scope type. */
    type: "global" | "local" | "with" | "closure" | "catch" | "block" | "script" | "eval" | "module" | "wasm-expression-stack";
    /**
     * Object representing the scope. For `global` and `with` scopes it represents the actual
     * object; for the rest of the scopes, it is artificial transient object enumerating scope
     * variables as its properties.
     */
    object: Runtime.RemoteObject;
    name?: string;
    /** Location in the source code where scope starts */
    startLocation?: Debugger.Location;
    /** Location in the source code where scope ends */
    endLocation?: Debugger.Location;
  }

  /** Search match for resource. */
  export interface SearchMatch {
    /** Line number in resource content. */
    lineNumber: number;
    /** Line with match content. */
    lineContent: string;
  }

  export interface BreakLocation {
    /** Script identifier as reported in the `Debugger.scriptParsed`. */
    scriptId: Runtime.ScriptId;
    /** Line number in the script (0-based). */
    lineNumber: number;
    /** Column number in the script (0-based). */
    columnNumber?: number;
    type?: "debuggerStatement" | "call" | "return";
  }

  export interface WasmDisassemblyChunk {
    /** The next chunk of disassembled lines. */
    lines: string[];
    /** The bytecode offsets describing the start of each line. */
    bytecodeOffsets: number[];
  }

  /** Enum of possible script languages. */
  export type ScriptLanguage = "JavaScript" | "WebAssembly";

  /** Debug symbols available for a wasm script. */
  export interface DebugSymbols {
    /** Type of the debug symbols. */
    type: "SourceMap" | "EmbeddedDWARF" | "ExternalDWARF";
    /** URL of the external symbol source. */
    externalURL?: string;
  }

  export interface ResolvedBreakpoint {
    /** Breakpoint unique identifier. */
    breakpointId: Debugger.BreakpointId;
    /** Actual breakpoint location. */
    location: Debugger.Location;
  }

}

export namespace DeviceAccess {

  /** Device request id. */
  export type RequestId = string;

  /** A device id. */
  export type DeviceId = string;

  /** Device information displayed in a user prompt to select a device. */
  export interface PromptDevice {
    id: DeviceAccess.DeviceId;
    /** Display name as it appears in a device request user prompt. */
    name: string;
  }

}

export namespace DeviceOrientation {
  // (no types)
}

export namespace DOM {

  /** Unique DOM node identifier. */
  export type NodeId = number;

  /**
   * Unique DOM node identifier used to reference a node that may not have been pushed to the
   * front-end.
   */
  export type BackendNodeId = number;

  /** Unique identifier for a CSS stylesheet. */
  export type StyleSheetId = string;

  /** Backend node with a friendly name. */
  export interface BackendNode {
    /** `Node`'s nodeType. */
    nodeType: number;
    /** `Node`'s nodeName. */
    nodeName: string;
    backendNodeId: DOM.BackendNodeId;
  }

  /** Pseudo element type. */
  export type PseudoType = "first-line" | "first-letter" | "checkmark" | "before" | "after" | "expand-icon" | "picker-icon" | "interest-hint" | "marker" | "backdrop" | "column" | "selection" | "search-text" | "target-text" | "spelling-error" | "grammar-error" | "highlight" | "first-line-inherited" | "scroll-marker" | "scroll-marker-group" | "scroll-button" | "scrollbar" | "scrollbar-thumb" | "scrollbar-button" | "scrollbar-track" | "scrollbar-track-piece" | "scrollbar-corner" | "resizer" | "input-list-button" | "view-transition" | "view-transition-group" | "view-transition-image-pair" | "view-transition-group-children" | "view-transition-old" | "view-transition-new" | "placeholder" | "file-selector-button" | "details-content" | "picker" | "permission-icon" | "overscroll-area-parent";

  /** Shadow root type. */
  export type ShadowRootType = "user-agent" | "open" | "closed";

  /** Document compatibility mode. */
  export type CompatibilityMode = "QuirksMode" | "LimitedQuirksMode" | "NoQuirksMode";

  /** ContainerSelector physical axes */
  export type PhysicalAxes = "Horizontal" | "Vertical" | "Both";

  /** ContainerSelector logical axes */
  export type LogicalAxes = "Inline" | "Block" | "Both";

  /** Physical scroll orientation */
  export type ScrollOrientation = "horizontal" | "vertical";

  /**
   * DOM interaction is implemented in terms of mirror objects that represent the actual DOM nodes.
   * DOMNode is a base node mirror type.
   */
  export interface Node {
    /**
     * Node identifier that is passed into the rest of the DOM messages as the `nodeId`. Backend
     * will only push node with given `id` once. It is aware of all requested nodes and will only
     * fire DOM events for nodes known to the client.
     */
    nodeId: DOM.NodeId;
    /** The id of the parent node if any. */
    parentId?: DOM.NodeId;
    /** The BackendNodeId for this node. */
    backendNodeId: DOM.BackendNodeId;
    /** `Node`'s nodeType. */
    nodeType: number;
    /** `Node`'s nodeName. */
    nodeName: string;
    /** `Node`'s localName. */
    localName: string;
    /** `Node`'s nodeValue. */
    nodeValue: string;
    /** Child count for `Container` nodes. */
    childNodeCount?: number;
    /** Child nodes of this node when requested with children. */
    children?: DOM.Node[];
    /** Attributes of the `Element` node in the form of flat array `[name1, value1, name2, value2]`. */
    attributes?: string[];
    /** Document URL that `Document` or `FrameOwner` node points to. */
    documentURL?: string;
    /** Base URL that `Document` or `FrameOwner` node uses for URL completion. */
    baseURL?: string;
    /** `DocumentType`'s publicId. */
    publicId?: string;
    /** `DocumentType`'s systemId. */
    systemId?: string;
    /** `DocumentType`'s internalSubset. */
    internalSubset?: string;
    /** `Document`'s XML version in case of XML documents. */
    xmlVersion?: string;
    /** `Attr`'s name. */
    name?: string;
    /** `Attr`'s value. */
    value?: string;
    /** Pseudo element type for this node. */
    pseudoType?: DOM.PseudoType;
    /**
     * Pseudo element identifier for this node. Only present if there is a
     * valid pseudoType.
     */
    pseudoIdentifier?: string;
    /** Shadow root type. */
    shadowRootType?: DOM.ShadowRootType;
    /** Frame ID for frame owner elements. */
    frameId?: Page.FrameId;
    /** Content document for frame owner elements. */
    contentDocument?: DOM.Node;
    /** Shadow root list for given element host. */
    shadowRoots?: DOM.Node[];
    /** Content document fragment for template elements. */
    templateContent?: DOM.Node;
    /** Pseudo elements associated with this node. */
    pseudoElements?: DOM.Node[];
    /**
     * Deprecated, as the HTML Imports API has been removed (crbug.com/937746).
     * This property used to return the imported document for the HTMLImport links.
     * The property is always undefined now.
     */
    importedDocument?: DOM.Node;
    /** Distributed nodes for given insertion point. */
    distributedNodes?: DOM.BackendNode[];
    /** Whether the node is SVG. */
    isSVG?: boolean;
    compatibilityMode?: DOM.CompatibilityMode;
    assignedSlot?: DOM.BackendNode;
    isScrollable?: boolean;
    affectedByStartingStyles?: boolean;
    adoptedStyleSheets?: DOM.StyleSheetId[];
    adProvenance?: Network.AdProvenance;
  }

  /** A structure to hold the top-level node of a detached tree and an array of its retained descendants. */
  export interface DetachedElementInfo {
    treeNode: DOM.Node;
    retainedNodeIds: DOM.NodeId[];
  }

  /** A structure holding an RGBA color. */
  export interface RGBA {
    /** The red component, in the [0-255] range. */
    r: number;
    /** The green component, in the [0-255] range. */
    g: number;
    /** The blue component, in the [0-255] range. */
    b: number;
    /** The alpha component, in the [0-1] range (default: 1). */
    a?: number;
  }

  /** An array of quad vertices, x immediately followed by y for each point, points clock-wise. */
  export type Quad = number[];

  /** Box model. */
  export interface BoxModel {
    /** Content box */
    content: DOM.Quad;
    /** Padding box */
    padding: DOM.Quad;
    /** Border box */
    border: DOM.Quad;
    /** Margin box */
    margin: DOM.Quad;
    /** Node width */
    width: number;
    /** Node height */
    height: number;
    /** Shape outside coordinates */
    shapeOutside?: DOM.ShapeOutsideInfo;
  }

  /** CSS Shape Outside details. */
  export interface ShapeOutsideInfo {
    /** Shape bounds */
    bounds: DOM.Quad;
    /** Shape coordinate details */
    shape: unknown[];
    /** Margin shape bounds */
    marginShape: unknown[];
  }

  /** Rectangle. */
  export interface Rect {
    /** X coordinate */
    x: number;
    /** Y coordinate */
    y: number;
    /** Rectangle width */
    width: number;
    /** Rectangle height */
    height: number;
  }

  export interface CSSComputedStyleProperty {
    /** Computed style property name. */
    name: string;
    /** Computed style property value. */
    value: string;
  }

}

export namespace DOMDebugger {

  /** DOM breakpoint type. */
  export type DOMBreakpointType = "subtree-modified" | "attribute-modified" | "node-removed";

  /** CSP Violation type. */
  export type CSPViolationType = "trustedtype-sink-violation" | "trustedtype-policy-violation";

  /** Object event listener. */
  export interface EventListener {
    /** `EventListener`'s type. */
    type: string;
    /** `EventListener`'s useCapture. */
    useCapture: boolean;
    /** `EventListener`'s passive flag. */
    passive: boolean;
    /** `EventListener`'s once flag. */
    once: boolean;
    /** Script id of the handler code. */
    scriptId: Runtime.ScriptId;
    /** Line number in the script (0-based). */
    lineNumber: number;
    /** Column number in the script (0-based). */
    columnNumber: number;
    /** Event handler function value. */
    handler?: Runtime.RemoteObject;
    /** Event original handler function value. */
    originalHandler?: Runtime.RemoteObject;
    /** Node the listener is added to (if any). */
    backendNodeId?: DOM.BackendNodeId;
  }

}

export namespace DOMSnapshot {

  /** A Node in the DOM tree. */
  export interface DOMNode {
    /** `Node`'s nodeType. */
    nodeType: number;
    /** `Node`'s nodeName. */
    nodeName: string;
    /** `Node`'s nodeValue. */
    nodeValue: string;
    /** Only set for textarea elements, contains the text value. */
    textValue?: string;
    /** Only set for input elements, contains the input's associated text value. */
    inputValue?: string;
    /** Only set for radio and checkbox input elements, indicates if the element has been checked */
    inputChecked?: boolean;
    /** Only set for option elements, indicates if the element has been selected */
    optionSelected?: boolean;
    /** `Node`'s id, corresponds to DOM.Node.backendNodeId. */
    backendNodeId: DOM.BackendNodeId;
    /**
     * The indexes of the node's child nodes in the `domNodes` array returned by `getSnapshot`, if
     * any.
     */
    childNodeIndexes?: number[];
    /** Attributes of an `Element` node. */
    attributes?: DOMSnapshot.NameValue[];
    /**
     * Indexes of pseudo elements associated with this node in the `domNodes` array returned by
     * `getSnapshot`, if any.
     */
    pseudoElementIndexes?: number[];
    /**
     * The index of the node's related layout tree node in the `layoutTreeNodes` array returned by
     * `getSnapshot`, if any.
     */
    layoutNodeIndex?: number;
    /** Document URL that `Document` or `FrameOwner` node points to. */
    documentURL?: string;
    /** Base URL that `Document` or `FrameOwner` node uses for URL completion. */
    baseURL?: string;
    /** Only set for documents, contains the document's content language. */
    contentLanguage?: string;
    /** Only set for documents, contains the document's character set encoding. */
    documentEncoding?: string;
    /** `DocumentType` node's publicId. */
    publicId?: string;
    /** `DocumentType` node's systemId. */
    systemId?: string;
    /** Frame ID for frame owner elements and also for the document node. */
    frameId?: Page.FrameId;
    /**
     * The index of a frame owner element's content document in the `domNodes` array returned by
     * `getSnapshot`, if any.
     */
    contentDocumentIndex?: number;
    /** Type of a pseudo element node. */
    pseudoType?: DOM.PseudoType;
    /** Shadow root type. */
    shadowRootType?: DOM.ShadowRootType;
    /**
     * Whether this DOM node responds to mouse clicks. This includes nodes that have had click
     * event listeners attached via JavaScript as well as anchor tags that naturally navigate when
     * clicked.
     */
    isClickable?: boolean;
    /** Details of the node's event listeners, if any. */
    eventListeners?: DOMDebugger.EventListener[];
    /** The selected url for nodes with a srcset attribute. */
    currentSourceURL?: string;
    /** The url of the script (if any) that generates this node. */
    originURL?: string;
    /** Scroll offsets, set when this node is a Document. */
    scrollOffsetX?: number;
    scrollOffsetY?: number;
  }

  /**
   * Details of post layout rendered text positions. The exact layout should not be regarded as
   * stable and may change between versions.
   */
  export interface InlineTextBox {
    /** The bounding box in document coordinates. Note that scroll offset of the document is ignored. */
    boundingBox: DOM.Rect;
    /**
     * The starting index in characters, for this post layout textbox substring. Characters that
     * would be represented as a surrogate pair in UTF-16 have length 2.
     */
    startCharacterIndex: number;
    /**
     * The number of characters in this post layout textbox substring. Characters that would be
     * represented as a surrogate pair in UTF-16 have length 2.
     */
    numCharacters: number;
  }

  /** Details of an element in the DOM tree with a LayoutObject. */
  export interface LayoutTreeNode {
    /** The index of the related DOM node in the `domNodes` array returned by `getSnapshot`. */
    domNodeIndex: number;
    /** The bounding box in document coordinates. Note that scroll offset of the document is ignored. */
    boundingBox: DOM.Rect;
    /** Contents of the LayoutText, if any. */
    layoutText?: string;
    /** The post-layout inline text nodes, if any. */
    inlineTextNodes?: DOMSnapshot.InlineTextBox[];
    /** Index into the `computedStyles` array returned by `getSnapshot`. */
    styleIndex?: number;
    /**
     * Global paint order index, which is determined by the stacking order of the nodes. Nodes
     * that are painted together will have the same index. Only provided if includePaintOrder in
     * getSnapshot was true.
     */
    paintOrder?: number;
    /** Set to true to indicate the element begins a new stacking context. */
    isStackingContext?: boolean;
  }

  /** A subset of the full ComputedStyle as defined by the request whitelist. */
  export interface ComputedStyle {
    /** Name/value pairs of computed style properties. */
    properties: DOMSnapshot.NameValue[];
  }

  /** A name/value pair. */
  export interface NameValue {
    /** Attribute/property name. */
    name: string;
    /** Attribute/property value. */
    value: string;
  }

  /** Index of the string in the strings table. */
  export type StringIndex = number;

  /** Index of the string in the strings table. */
  export type ArrayOfStrings = DOMSnapshot.StringIndex[];

  /** Data that is only present on rare nodes. */
  export interface RareStringData {
    index: number[];
    value: DOMSnapshot.StringIndex[];
  }

  export interface RareBooleanData {
    index: number[];
  }

  export interface RareIntegerData {
    index: number[];
    value: number[];
  }

  export type Rectangle = number[];

  /** Document snapshot. */
  export interface DocumentSnapshot {
    /** Document URL that `Document` or `FrameOwner` node points to. */
    documentURL: DOMSnapshot.StringIndex;
    /** Document title. */
    title: DOMSnapshot.StringIndex;
    /** Base URL that `Document` or `FrameOwner` node uses for URL completion. */
    baseURL: DOMSnapshot.StringIndex;
    /** Contains the document's content language. */
    contentLanguage: DOMSnapshot.StringIndex;
    /** Contains the document's character set encoding. */
    encodingName: DOMSnapshot.StringIndex;
    /** `DocumentType` node's publicId. */
    publicId: DOMSnapshot.StringIndex;
    /** `DocumentType` node's systemId. */
    systemId: DOMSnapshot.StringIndex;
    /** Frame ID for frame owner elements and also for the document node. */
    frameId: DOMSnapshot.StringIndex;
    /** A table with dom nodes. */
    nodes: DOMSnapshot.NodeTreeSnapshot;
    /** The nodes in the layout tree. */
    layout: DOMSnapshot.LayoutTreeSnapshot;
    /** The post-layout inline text nodes. */
    textBoxes: DOMSnapshot.TextBoxSnapshot;
    /** Horizontal scroll offset. */
    scrollOffsetX?: number;
    /** Vertical scroll offset. */
    scrollOffsetY?: number;
    /** Document content width. */
    contentWidth?: number;
    /** Document content height. */
    contentHeight?: number;
  }

  /** Table containing nodes. */
  export interface NodeTreeSnapshot {
    /** Parent node index. */
    parentIndex?: number[];
    /** `Node`'s nodeType. */
    nodeType?: number[];
    /** Type of the shadow root the `Node` is in. String values are equal to the `ShadowRootType` enum. */
    shadowRootType?: DOMSnapshot.RareStringData;
    /** `Node`'s nodeName. */
    nodeName?: DOMSnapshot.StringIndex[];
    /** `Node`'s nodeValue. */
    nodeValue?: DOMSnapshot.StringIndex[];
    /** `Node`'s id, corresponds to DOM.Node.backendNodeId. */
    backendNodeId?: DOM.BackendNodeId[];
    /** Attributes of an `Element` node. Flatten name, value pairs. */
    attributes?: DOMSnapshot.ArrayOfStrings[];
    /** Only set for textarea elements, contains the text value. */
    textValue?: DOMSnapshot.RareStringData;
    /** Only set for input elements, contains the input's associated text value. */
    inputValue?: DOMSnapshot.RareStringData;
    /** Only set for radio and checkbox input elements, indicates if the element has been checked */
    inputChecked?: DOMSnapshot.RareBooleanData;
    /** Only set for option elements, indicates if the element has been selected */
    optionSelected?: DOMSnapshot.RareBooleanData;
    /** The index of the document in the list of the snapshot documents. */
    contentDocumentIndex?: DOMSnapshot.RareIntegerData;
    /** Type of a pseudo element node. */
    pseudoType?: DOMSnapshot.RareStringData;
    /**
     * Pseudo element identifier for this node. Only present if there is a
     * valid pseudoType.
     */
    pseudoIdentifier?: DOMSnapshot.RareStringData;
    /**
     * Whether this DOM node responds to mouse clicks. This includes nodes that have had click
     * event listeners attached via JavaScript as well as anchor tags that naturally navigate when
     * clicked.
     */
    isClickable?: DOMSnapshot.RareBooleanData;
    /** The selected url for nodes with a srcset attribute. */
    currentSourceURL?: DOMSnapshot.RareStringData;
    /** The url of the script (if any) that generates this node. */
    originURL?: DOMSnapshot.RareStringData;
  }

  /** Table of details of an element in the DOM tree with a LayoutObject. */
  export interface LayoutTreeSnapshot {
    /** Index of the corresponding node in the `NodeTreeSnapshot` array returned by `captureSnapshot`. */
    nodeIndex: number[];
    /** Array of indexes specifying computed style strings, filtered according to the `computedStyles` parameter passed to `captureSnapshot`. */
    styles: DOMSnapshot.ArrayOfStrings[];
    /** The absolute position bounding box. */
    bounds: DOMSnapshot.Rectangle[];
    /** Contents of the LayoutText, if any. */
    text: DOMSnapshot.StringIndex[];
    /** Stacking context information. */
    stackingContexts: DOMSnapshot.RareBooleanData;
    /**
     * Global paint order index, which is determined by the stacking order of the nodes. Nodes
     * that are painted together will have the same index. Only provided if includePaintOrder in
     * captureSnapshot was true.
     */
    paintOrders?: number[];
    /** The offset rect of nodes. Only available when includeDOMRects is set to true */
    offsetRects?: DOMSnapshot.Rectangle[];
    /** The scroll rect of nodes. Only available when includeDOMRects is set to true */
    scrollRects?: DOMSnapshot.Rectangle[];
    /** The client rect of nodes. Only available when includeDOMRects is set to true */
    clientRects?: DOMSnapshot.Rectangle[];
    /** The list of background colors that are blended with colors of overlapping elements. */
    blendedBackgroundColors?: DOMSnapshot.StringIndex[];
    /** The list of computed text opacities. */
    textColorOpacities?: number[];
  }

  /**
   * Table of details of the post layout rendered text positions. The exact layout should not be regarded as
   * stable and may change between versions.
   */
  export interface TextBoxSnapshot {
    /** Index of the layout tree node that owns this box collection. */
    layoutIndex: number[];
    /** The absolute position bounding box. */
    bounds: DOMSnapshot.Rectangle[];
    /**
     * The starting index in characters, for this post layout textbox substring. Characters that
     * would be represented as a surrogate pair in UTF-16 have length 2.
     */
    start: number[];
    /**
     * The number of characters in this post layout textbox substring. Characters that would be
     * represented as a surrogate pair in UTF-16 have length 2.
     */
    length: number[];
  }

}

export namespace DOMStorage {

  export type SerializedStorageKey = string;

  /** DOM Storage identifier. */
  export interface StorageId {
    /** Security origin for the storage. */
    securityOrigin?: string;
    /** Represents a key by which DOM Storage keys its CachedStorageAreas */
    storageKey?: DOMStorage.SerializedStorageKey;
    /** Whether the storage is local storage (not session storage). */
    isLocalStorage: boolean;
  }

  /** DOM Storage item. */
  export type Item = string[];

}

export namespace Emulation {

  export interface SafeAreaInsets {
    /** Overrides safe-area-inset-top. */
    top?: number;
    /** Overrides safe-area-max-inset-top. */
    topMax?: number;
    /** Overrides safe-area-inset-left. */
    left?: number;
    /** Overrides safe-area-max-inset-left. */
    leftMax?: number;
    /** Overrides safe-area-inset-bottom. */
    bottom?: number;
    /** Overrides safe-area-max-inset-bottom. */
    bottomMax?: number;
    /** Overrides safe-area-inset-right. */
    right?: number;
    /** Overrides safe-area-max-inset-right. */
    rightMax?: number;
  }

  /** Screen orientation. */
  export interface ScreenOrientation {
    /** Orientation type. */
    type: "portraitPrimary" | "portraitSecondary" | "landscapePrimary" | "landscapeSecondary";
    /** Orientation angle. */
    angle: number;
  }

  export interface DisplayFeature {
    /** Orientation of a display feature in relation to screen */
    orientation: "vertical" | "horizontal";
    /**
     * The offset from the screen origin in either the x (for vertical
     * orientation) or y (for horizontal orientation) direction.
     */
    offset: number;
    /**
     * A display feature may mask content such that it is not physically
     * displayed - this length along with the offset describes this area.
     * A display feature that only splits content will have a 0 mask_length.
     */
    maskLength: number;
  }

  export interface DevicePosture {
    /** Current posture of the device */
    type: "continuous" | "folded";
  }

  export interface MediaFeature {
    name: string;
    value: string;
  }

  /**
   * advance: If the scheduler runs out of immediate work, the virtual time base may fast forward to
   * allow the next delayed task (if any) to run; pause: The virtual time base may not advance;
   * pauseIfNetworkFetchesPending: The virtual time base may not advance if there are any pending
   * resource fetches.
   */
  export type VirtualTimePolicy = "advance" | "pause" | "pauseIfNetworkFetchesPending";

  /** Used to specify User Agent Client Hints to emulate. See https://wicg.github.io/ua-client-hints */
  export interface UserAgentBrandVersion {
    brand: string;
    version: string;
  }

  /**
   * Used to specify User Agent Client Hints to emulate. See https://wicg.github.io/ua-client-hints
   * Missing optional values will be filled in by the target with what it would normally use.
   */
  export interface UserAgentMetadata {
    /** Brands appearing in Sec-CH-UA. */
    brands?: Emulation.UserAgentBrandVersion[];
    /** Brands appearing in Sec-CH-UA-Full-Version-List. */
    fullVersionList?: Emulation.UserAgentBrandVersion[];
    fullVersion?: string;
    platform: string;
    platformVersion: string;
    architecture: string;
    model: string;
    mobile: boolean;
    bitness?: string;
    wow64?: boolean;
    /**
     * Used to specify User Agent form-factor values.
     * See https://wicg.github.io/ua-client-hints/#sec-ch-ua-form-factors
     */
    formFactors?: string[];
  }

  /**
   * Used to specify sensor types to emulate.
   * See https://w3c.github.io/sensors/#automation for more information.
   */
  export type SensorType = "absolute-orientation" | "accelerometer" | "ambient-light" | "gravity" | "gyroscope" | "linear-acceleration" | "magnetometer" | "relative-orientation";

  export interface SensorMetadata {
    available?: boolean;
    minimumFrequency?: number;
    maximumFrequency?: number;
  }

  export interface SensorReadingSingle {
    value: number;
  }

  export interface SensorReadingXYZ {
    x: number;
    y: number;
    z: number;
  }

  export interface SensorReadingQuaternion {
    x: number;
    y: number;
    z: number;
    w: number;
  }

  export interface SensorReading {
    single?: Emulation.SensorReadingSingle;
    xyz?: Emulation.SensorReadingXYZ;
    quaternion?: Emulation.SensorReadingQuaternion;
  }

  export type PressureSource = "cpu";

  export type PressureState = "nominal" | "fair" | "serious" | "critical";

  export interface PressureMetadata {
    available?: boolean;
  }

  export interface WorkAreaInsets {
    /** Work area top inset in pixels. Default is 0; */
    top?: number;
    /** Work area left inset in pixels. Default is 0; */
    left?: number;
    /** Work area bottom inset in pixels. Default is 0; */
    bottom?: number;
    /** Work area right inset in pixels. Default is 0; */
    right?: number;
  }

  export type ScreenId = string;

  /**
   * Screen information similar to the one returned by window.getScreenDetails() method,
   * see https://w3c.github.io/window-management/#screendetailed.
   */
  export interface ScreenInfo {
    /** Offset of the left edge of the screen. */
    left: number;
    /** Offset of the top edge of the screen. */
    top: number;
    /** Width of the screen. */
    width: number;
    /** Height of the screen. */
    height: number;
    /** Offset of the left edge of the available screen area. */
    availLeft: number;
    /** Offset of the top edge of the available screen area. */
    availTop: number;
    /** Width of the available screen area. */
    availWidth: number;
    /** Height of the available screen area. */
    availHeight: number;
    /** Specifies the screen's device pixel ratio. */
    devicePixelRatio: number;
    /** Specifies the screen's orientation. */
    orientation: Emulation.ScreenOrientation;
    /** Specifies the screen's color depth in bits. */
    colorDepth: number;
    /** Indicates whether the device has multiple screens. */
    isExtended: boolean;
    /** Indicates whether the screen is internal to the device or external, attached to the device. */
    isInternal: boolean;
    /** Indicates whether the screen is set as the the operating system primary screen. */
    isPrimary: boolean;
    /** Specifies the descriptive label for the screen. */
    label: string;
    /** Specifies the unique identifier of the screen. */
    id: Emulation.ScreenId;
  }

  /** Enum of image types that can be disabled. */
  export type DisabledImageType = "avif" | "jxl" | "webp";

}

export namespace EventBreakpoints {
  // (no types)
}

export namespace Extensions {

  /** Storage areas. */
  export type StorageArea = "session" | "local" | "sync" | "managed";

  /** Detailed information about an extension. */
  export interface ExtensionInfo {
    /** Extension id. */
    id: string;
    /** Extension name. */
    name: string;
    /** Extension version. */
    version: string;
    /** The path from which the extension was loaded. */
    path: string;
    /** Extension enabled status. */
    enabled: boolean;
  }

}

export namespace FedCm {

  /**
   * Whether this is a sign-up or sign-in action for this account, i.e.
   * whether this account has ever been used to sign in to this RP before.
   */
  export type LoginState = "SignIn" | "SignUp";

  /** The types of FedCM dialogs. */
  export type DialogType = "AccountChooser" | "AutoReauthn" | "ConfirmIdpLogin" | "Error";

  /** The buttons on the FedCM dialog. */
  export type DialogButton = "ConfirmIdpLoginContinue" | "ErrorGotIt" | "ErrorMoreDetails";

  /** The URLs that each account has */
  export type AccountUrlType = "TermsOfService" | "PrivacyPolicy";

  /** Corresponds to IdentityRequestAccount */
  export interface Account {
    accountId: string;
    email: string;
    name: string;
    givenName: string;
    pictureUrl: string;
    idpConfigUrl: string;
    idpLoginUrl: string;
    loginState: FedCm.LoginState;
    /** These two are only set if the loginState is signUp */
    termsOfServiceUrl?: string;
    privacyPolicyUrl?: string;
  }

}

export namespace Fetch {

  /**
   * Unique request identifier.
   * Note that this does not identify individual HTTP requests that are part of
   * a network request.
   */
  export type RequestId = string;

  /**
   * Stages of the request to handle. Request will intercept before the request is
   * sent. Response will intercept after the response is received (but before response
   * body is received).
   */
  export type RequestStage = "Request" | "Response";

  export interface RequestPattern {
    /**
     * Wildcards (`'*'` -> zero or more, `'?'` -> exactly one) are allowed. Escape character is
     * backslash. Omitting is equivalent to `"*"`.
     */
    urlPattern?: string;
    /** If set, only requests for matching resource types will be intercepted. */
    resourceType?: Network.ResourceType;
    /** Stage at which to begin intercepting requests. Default is Request. */
    requestStage?: Fetch.RequestStage;
  }

  /** Response HTTP header entry */
  export interface HeaderEntry {
    name: string;
    value: string;
  }

  /** Authorization challenge for HTTP status code 401 or 407. */
  export interface AuthChallenge {
    /** Source of the authentication challenge. */
    source?: "Server" | "Proxy";
    /** Origin of the challenger. */
    origin: string;
    /** The authentication scheme used, such as basic or digest */
    scheme: string;
    /** The realm of the challenge. May be empty. */
    realm: string;
  }

  /** Response to an AuthChallenge. */
  export interface AuthChallengeResponse {
    /**
     * The decision on what to do in response to the authorization challenge.  Default means
     * deferring to the default behavior of the net stack, which will likely either the Cancel
     * authentication or display a popup dialog box.
     */
    response: "Default" | "CancelAuth" | "ProvideCredentials";
    /**
     * The username to provide, possibly empty. Should only be set if response is
     * ProvideCredentials.
     */
    username?: string;
    /**
     * The password to provide, possibly empty. Should only be set if response is
     * ProvideCredentials.
     */
    password?: string;
  }

}

export namespace FileSystem {

  export interface File {
    name: string;
    /** Timestamp */
    lastModified: Network.TimeSinceEpoch;
    /** Size in bytes */
    size: number;
    type: string;
  }

  export interface Directory {
    name: string;
    nestedDirectories: string[];
    /** Files that are directly nested under this directory. */
    nestedFiles: FileSystem.File[];
  }

  export interface BucketFileSystemLocator {
    /** Storage key */
    storageKey: Storage.SerializedStorageKey;
    /** Bucket name. Not passing a `bucketName` will retrieve the default Bucket. (https://developer.mozilla.org/en-US/docs/Web/API/Storage_API#storage_buckets) */
    bucketName?: string;
    /** Path to the directory using each path component as an array item. */
    pathComponents: string[];
  }

}

export namespace HeadlessExperimental {

  /** Encoding options for a screenshot. */
  export interface ScreenshotParams {
    /** Image compression format (defaults to png). */
    format?: "jpeg" | "png" | "webp";
    /** Compression quality from range [0..100] (jpeg and webp only). */
    quality?: number;
    /** Optimize image encoding for speed, not for resulting size (defaults to false) */
    optimizeForSpeed?: boolean;
  }

}

export namespace HeapProfiler {

  /** Heap snapshot object id. */
  export type HeapSnapshotObjectId = string;

  /** Sampling Heap Profile node. Holds callsite information, allocation statistics and child nodes. */
  export interface SamplingHeapProfileNode {
    /** Function location. */
    callFrame: Runtime.CallFrame;
    /** Allocations size in bytes for the node excluding children. */
    selfSize: number;
    /** Node id. Ids are unique across all profiles collected between startSampling and stopSampling. */
    id: number;
    /** Child nodes. */
    children: HeapProfiler.SamplingHeapProfileNode[];
  }

  /** A single sample from a sampling profile. */
  export interface SamplingHeapProfileSample {
    /** Allocation size in bytes attributed to the sample. */
    size: number;
    /** Id of the corresponding profile tree node. */
    nodeId: number;
    /**
     * Time-ordered sample ordinal number. It is unique across all profiles retrieved
     * between startSampling and stopSampling.
     */
    ordinal: number;
  }

  /** Sampling profile. */
  export interface SamplingHeapProfile {
    head: HeapProfiler.SamplingHeapProfileNode;
    samples: HeapProfiler.SamplingHeapProfileSample[];
  }

}

export namespace IndexedDB {

  /** Database with an array of object stores. */
  export interface DatabaseWithObjectStores {
    /** Database name. */
    name: string;
    /**
     * Database version (type is not 'integer', as the standard
     * requires the version number to be 'unsigned long long')
     */
    version: number;
    /** Object stores in this database. */
    objectStores: IndexedDB.ObjectStore[];
  }

  /** Object store. */
  export interface ObjectStore {
    /** Object store name. */
    name: string;
    /** Object store key path. */
    keyPath: IndexedDB.KeyPath;
    /** If true, object store has auto increment flag set. */
    autoIncrement: boolean;
    /** Indexes in this object store. */
    indexes: IndexedDB.ObjectStoreIndex[];
  }

  /** Object store index. */
  export interface ObjectStoreIndex {
    /** Index name. */
    name: string;
    /** Index key path. */
    keyPath: IndexedDB.KeyPath;
    /** If true, index is unique. */
    unique: boolean;
    /** If true, index allows multiple entries for a key. */
    multiEntry: boolean;
  }

  /** Key. */
  export interface Key {
    /** Key type. */
    type: "number" | "string" | "date" | "array";
    /** Number value. */
    number?: number;
    /** String value. */
    string?: string;
    /** Date value. */
    date?: number;
    /** Array value. */
    array?: IndexedDB.Key[];
  }

  /** Key range. */
  export interface KeyRange {
    /** Lower bound. */
    lower?: IndexedDB.Key;
    /** Upper bound. */
    upper?: IndexedDB.Key;
    /** If true lower bound is open. */
    lowerOpen: boolean;
    /** If true upper bound is open. */
    upperOpen: boolean;
  }

  /** Data entry. */
  export interface DataEntry {
    /** Key object. */
    key: Runtime.RemoteObject;
    /** Primary key object. */
    primaryKey: Runtime.RemoteObject;
    /** Value object. */
    value: Runtime.RemoteObject;
  }

  /** Key path. */
  export interface KeyPath {
    /** Key path type. */
    type: "null" | "string" | "array";
    /** String value. */
    string?: string;
    /** Array value. */
    array?: string[];
  }

}

export namespace Input {

  export interface TouchPoint {
    /** X coordinate of the event relative to the main frame's viewport in CSS pixels. */
    x: number;
    /**
     * Y coordinate of the event relative to the main frame's viewport in CSS pixels. 0 refers to
     * the top of the viewport and Y increases as it proceeds towards the bottom of the viewport.
     */
    y: number;
    /** X radius of the touch area (default: 1.0). */
    radiusX?: number;
    /** Y radius of the touch area (default: 1.0). */
    radiusY?: number;
    /** Rotation angle (default: 0.0). */
    rotationAngle?: number;
    /** Force (default: 1.0). */
    force?: number;
    /** The normalized tangential pressure, which has a range of [-1,1] (default: 0). */
    tangentialPressure?: number;
    /** The plane angle between the Y-Z plane and the plane containing both the stylus axis and the Y axis, in degrees of the range [-90,90], a positive tiltX is to the right (default: 0) */
    tiltX?: number;
    /** The plane angle between the X-Z plane and the plane containing both the stylus axis and the X axis, in degrees of the range [-90,90], a positive tiltY is towards the user (default: 0). */
    tiltY?: number;
    /** The clockwise rotation of a pen stylus around its own major axis, in degrees in the range [0,359] (default: 0). */
    twist?: number;
    /** Identifier used to track touch sources between events, must be unique within an event. */
    id?: number;
  }

  export type GestureSourceType = "default" | "touch" | "mouse";

  export type MouseButton = "none" | "left" | "middle" | "right" | "back" | "forward";

  /** UTC time in seconds, counted from January 1, 1970. */
  export type TimeSinceEpoch = number;

  export interface DragDataItem {
    /** Mime type of the dragged data. */
    mimeType: string;
    /**
     * Depending of the value of `mimeType`, it contains the dragged link,
     * text, HTML markup or any other data.
     */
    data: string;
    /** Title associated with a link. Only valid when `mimeType` == "text/uri-list". */
    title?: string;
    /**
     * Stores the base URL for the contained markup. Only valid when `mimeType`
     * == "text/html".
     */
    baseURL?: string;
  }

  export interface DragData {
    items: Input.DragDataItem[];
    /** List of filenames that should be included when dropping */
    files?: string[];
    /** Bit field representing allowed drag operations. Copy = 1, Link = 2, Move = 16 */
    dragOperationsMask: number;
  }

}

export namespace Inspector {
  // (no types)
}

export namespace IO {

  /**
   * This is either obtained from another method or specified as `blob:<uuid>` where
   * `<uuid>` is an UUID of a Blob.
   */
  export type StreamHandle = string;

}

export namespace LayerTree {

  /** Unique Layer identifier. */
  export type LayerId = string;

  /** Unique snapshot identifier. */
  export type SnapshotId = string;

  /** Rectangle where scrolling happens on the main thread. */
  export interface ScrollRect {
    /** Rectangle itself. */
    rect: DOM.Rect;
    /** Reason for rectangle to force scrolling on the main thread */
    type: "RepaintsOnScroll" | "TouchEventHandler" | "WheelEventHandler";
  }

  /** Sticky position constraints. */
  export interface StickyPositionConstraint {
    /** Layout rectangle of the sticky element before being shifted */
    stickyBoxRect: DOM.Rect;
    /** Layout rectangle of the containing block of the sticky element */
    containingBlockRect: DOM.Rect;
    /** The nearest sticky layer that shifts the sticky box */
    nearestLayerShiftingStickyBox?: LayerTree.LayerId;
    /** The nearest sticky layer that shifts the containing block */
    nearestLayerShiftingContainingBlock?: LayerTree.LayerId;
  }

  /** Serialized fragment of layer picture along with its offset within the layer. */
  export interface PictureTile {
    /** Offset from owning layer left boundary */
    x: number;
    /** Offset from owning layer top boundary */
    y: number;
    /** Base64-encoded snapshot data. (Encoded as a base64 string when passed over JSON) */
    picture: string;
  }

  /** Information about a compositing layer. */
  export interface Layer {
    /** The unique id for this layer. */
    layerId: LayerTree.LayerId;
    /** The id of parent (not present for root). */
    parentLayerId?: LayerTree.LayerId;
    /** The backend id for the node associated with this layer. */
    backendNodeId?: DOM.BackendNodeId;
    /** Offset from parent layer, X coordinate. */
    offsetX: number;
    /** Offset from parent layer, Y coordinate. */
    offsetY: number;
    /** Layer width. */
    width: number;
    /** Layer height. */
    height: number;
    /** Transformation matrix for layer, default is identity matrix */
    transform?: number[];
    /** Transform anchor point X, absent if no transform specified */
    anchorX?: number;
    /** Transform anchor point Y, absent if no transform specified */
    anchorY?: number;
    /** Transform anchor point Z, absent if no transform specified */
    anchorZ?: number;
    /** Indicates how many time this layer has painted. */
    paintCount: number;
    /**
     * Indicates whether this layer hosts any content, rather than being used for
     * transform/scrolling purposes only.
     */
    drawsContent: boolean;
    /** Set if layer is not visible. */
    invisible?: boolean;
    /** Rectangles scrolling on main thread only. */
    scrollRects?: LayerTree.ScrollRect[];
    /** Sticky position constraint information */
    stickyPositionConstraint?: LayerTree.StickyPositionConstraint;
  }

  /** Array of timings, one per paint step. */
  export type PaintProfile = number[];

}

export namespace Log {

  /** Log entry. */
  export interface LogEntry {
    /** Log entry source. */
    source: "xml" | "javascript" | "network" | "storage" | "appcache" | "rendering" | "security" | "deprecation" | "worker" | "violation" | "intervention" | "recommendation" | "other";
    /** Log entry severity. */
    level: "verbose" | "info" | "warning" | "error";
    /** Logged text. */
    text: string;
    category?: "cors";
    /** Timestamp when this entry was added. */
    timestamp: Runtime.Timestamp;
    /** URL of the resource if known. */
    url?: string;
    /** Line number in the resource. */
    lineNumber?: number;
    /** JavaScript stack trace. */
    stackTrace?: Runtime.StackTrace;
    /** Identifier of the network request associated with this entry. */
    networkRequestId?: Network.RequestId;
    /** Identifier of the worker associated with this entry. */
    workerId?: string;
    /** Call arguments. */
    args?: Runtime.RemoteObject[];
  }

  /** Violation configuration setting. */
  export interface ViolationSetting {
    /** Violation type. */
    name: "longTask" | "longLayout" | "blockedEvent" | "blockedParser" | "discouragedAPIUse" | "handler" | "recurringHandler";
    /** Time threshold to trigger upon. */
    threshold: number;
  }

}

export namespace Media {

  /** Players will get an ID that is unique within the agent context. */
  export type PlayerId = string;

  export type Timestamp = number;

  /**
   * Have one type per entry in MediaLogRecord::Type
   * Corresponds to kMessage
   */
  export interface PlayerMessage {
    /**
     * Keep in sync with MediaLogMessageLevel
     * We are currently keeping the message level 'error' separate from the
     * PlayerError type because right now they represent different things,
     * this one being a DVLOG(ERROR) style log message that gets printed
     * based on what log level is selected in the UI, and the other is a
     * representation of a media::PipelineStatus object. Soon however we're
     * going to be moving away from using PipelineStatus for errors and
     * introducing a new error type which should hopefully let us integrate
     * the error log level into the PlayerError type.
     */
    level: "error" | "warning" | "info" | "debug";
    message: string;
  }

  /** Corresponds to kMediaPropertyChange */
  export interface PlayerProperty {
    name: string;
    value: string;
  }

  /** Corresponds to kMediaEventTriggered */
  export interface PlayerEvent {
    timestamp: Media.Timestamp;
    value: string;
  }

  /**
   * Represents logged source line numbers reported in an error.
   * NOTE: file and line are from chromium c++ implementation code, not js.
   */
  export interface PlayerErrorSourceLocation {
    file: string;
    line: number;
  }

  /** Corresponds to kMediaError */
  export interface PlayerError {
    errorType: string;
    /**
     * Code is the numeric enum entry for a specific set of error codes, such
     * as PipelineStatusCodes in media/base/pipeline_status.h
     */
    code: number;
    /** A trace of where this error was caused / where it passed through. */
    stack: Media.PlayerErrorSourceLocation[];
    /**
     * Errors potentially have a root cause error, ie, a DecoderError might be
     * caused by an WindowsError
     */
    cause: Media.PlayerError[];
    /** Extra data attached to an error, such as an HRESULT, Video Codec, etc. */
    data: Record<string, unknown>;
  }

  export interface Player {
    playerId: Media.PlayerId;
    domNodeId?: DOM.BackendNodeId;
  }

}

export namespace Memory {

  /** Memory pressure level. */
  export type PressureLevel = "moderate" | "critical";

  /** Heap profile sample. */
  export interface SamplingProfileNode {
    /** Size of the sampled allocation. */
    size: number;
    /** Total bytes attributed to this sample. */
    total: number;
    /** Execution stack at the point of allocation. */
    stack: string[];
  }

  /** Array of heap profile samples. */
  export interface SamplingProfile {
    samples: Memory.SamplingProfileNode[];
    modules: Memory.Module[];
  }

  /** Executable module information */
  export interface Module {
    /** Name of the module. */
    name: string;
    /** UUID of the module. */
    uuid: string;
    /**
     * Base address where the module is loaded into memory. Encoded as a decimal
     * or hexadecimal (0x prefixed) string.
     */
    baseAddress: string;
    /** Size of the module in bytes. */
    size: number;
  }

  /** DOM object counter data. */
  export interface DOMCounter {
    /**
     * Object name. Note: object names should be presumed volatile and clients should not expect
     * the returned names to be consistent across runs.
     */
    name: string;
    /** Object count. */
    count: number;
  }

}

export namespace Network {

  /** Resource type as it was perceived by the rendering engine. */
  export type ResourceType = "Document" | "Stylesheet" | "Image" | "Media" | "Font" | "Script" | "TextTrack" | "XHR" | "Fetch" | "Prefetch" | "EventSource" | "WebSocket" | "Manifest" | "SignedExchange" | "Ping" | "CSPViolationReport" | "Preflight" | "FedCM" | "Other";

  /** Unique loader identifier. */
  export type LoaderId = string;

  /**
   * Unique network request identifier.
   * Note that this does not identify individual HTTP requests that are part of
   * a network request.
   */
  export type RequestId = string;

  /** Unique intercepted request identifier. */
  export type InterceptionId = string;

  /** Network level fetch failure reason. */
  export type ErrorReason = "Failed" | "Aborted" | "TimedOut" | "AccessDenied" | "ConnectionClosed" | "ConnectionReset" | "ConnectionRefused" | "ConnectionAborted" | "ConnectionFailed" | "NameNotResolved" | "InternetDisconnected" | "AddressUnreachable" | "BlockedByClient" | "BlockedByResponse";

  /** UTC time in seconds, counted from January 1, 1970. */
  export type TimeSinceEpoch = number;

  /** Monotonically increasing time in seconds since an arbitrary point in the past. */
  export type MonotonicTime = number;

  /** Request / response headers as keys / values of JSON object. */
  export type Headers = Record<string, unknown>;

  /** The underlying connection technology that the browser is supposedly using. */
  export type ConnectionType = "none" | "cellular2g" | "cellular3g" | "cellular4g" | "bluetooth" | "ethernet" | "wifi" | "wimax" | "other";

  /**
   * Represents the cookie's 'SameSite' status:
   * https://tools.ietf.org/html/draft-west-first-party-cookies
   */
  export type CookieSameSite = "Strict" | "Lax" | "None";

  /**
   * Represents the cookie's 'Priority' status:
   * https://tools.ietf.org/html/draft-west-cookie-priority-00
   */
  export type CookiePriority = "Low" | "Medium" | "High";

  /**
   * Represents the source scheme of the origin that originally set the cookie.
   * A value of "Unset" allows protocol clients to emulate legacy cookie scope for the scheme.
   * This is a temporary ability and it will be removed in the future.
   */
  export type CookieSourceScheme = "Unset" | "NonSecure" | "Secure";

  /** Timing information for the request. */
  export interface ResourceTiming {
    /**
     * Timing's requestTime is a baseline in seconds, while the other numbers are ticks in
     * milliseconds relatively to this requestTime.
     */
    requestTime: number;
    /** Started resolving proxy. */
    proxyStart: number;
    /** Finished resolving proxy. */
    proxyEnd: number;
    /** Started DNS address resolve. */
    dnsStart: number;
    /** Finished DNS address resolve. */
    dnsEnd: number;
    /** Started connecting to the remote host. */
    connectStart: number;
    /** Connected to the remote host. */
    connectEnd: number;
    /** Started SSL handshake. */
    sslStart: number;
    /** Finished SSL handshake. */
    sslEnd: number;
    /** Started running ServiceWorker. */
    workerStart: number;
    /** Finished Starting ServiceWorker. */
    workerReady: number;
    /** Started fetch event. */
    workerFetchStart: number;
    /** Settled fetch event respondWith promise. */
    workerRespondWithSettled: number;
    /** Started ServiceWorker static routing source evaluation. */
    workerRouterEvaluationStart?: number;
    /** Started cache lookup when the source was evaluated to `cache`. */
    workerCacheLookupStart?: number;
    /** Started sending request. */
    sendStart: number;
    /** Finished sending request. */
    sendEnd: number;
    /** Time the server started pushing request. */
    pushStart: number;
    /** Time the server finished pushing request. */
    pushEnd: number;
    /** Started receiving response headers. */
    receiveHeadersStart: number;
    /** Finished receiving response headers. */
    receiveHeadersEnd: number;
  }

  /** Loading priority of a resource request. */
  export type ResourcePriority = "VeryLow" | "Low" | "Medium" | "High" | "VeryHigh";

  /** The render-blocking behavior of a resource request. */
  export type RenderBlockingBehavior = "Blocking" | "InBodyParserBlocking" | "NonBlocking" | "NonBlockingDynamic" | "PotentiallyBlocking";

  /** Post data entry for HTTP request */
  export interface PostDataEntry {
    bytes?: string;
  }

  /** HTTP request data. */
  export interface Request {
    /** Request URL (without fragment). */
    url: string;
    /** Fragment of the requested URL starting with hash, if present. */
    urlFragment?: string;
    /** HTTP request method. */
    method: string;
    /** HTTP request headers. */
    headers: Network.Headers;
    /**
     * HTTP POST request data.
     * Use postDataEntries instead.
     */
    postData?: string;
    /** True when the request has POST data. Note that postData might still be omitted when this flag is true when the data is too long. */
    hasPostData?: boolean;
    /** Request body elements (post data broken into individual entries). */
    postDataEntries?: Network.PostDataEntry[];
    /** The mixed content type of the request. */
    mixedContentType?: Security.MixedContentType;
    /** Priority of the resource request at the time request is sent. */
    initialPriority: Network.ResourcePriority;
    /** The referrer policy of the request, as defined in https://www.w3.org/TR/referrer-policy/ */
    referrerPolicy: "unsafe-url" | "no-referrer-when-downgrade" | "no-referrer" | "origin" | "origin-when-cross-origin" | "same-origin" | "strict-origin" | "strict-origin-when-cross-origin";
    /** Whether is loaded via link preload. */
    isLinkPreload?: boolean;
    /**
     * Set for requests when the TrustToken API is used. Contains the parameters
     * passed by the developer (e.g. via "fetch") as understood by the backend.
     */
    trustTokenParams?: Network.TrustTokenParams;
    /**
     * True if this resource request is considered to be the 'same site' as the
     * request corresponding to the main frame.
     */
    isSameSite?: boolean;
    /** True when the resource request is ad-related. */
    isAdRelated?: boolean;
  }

  /** Details of a signed certificate timestamp (SCT). */
  export interface SignedCertificateTimestamp {
    /** Validation status. */
    status: string;
    /** Origin. */
    origin: string;
    /** Log name / description. */
    logDescription: string;
    /** Log ID. */
    logId: string;
    /**
     * Issuance date. Unlike TimeSinceEpoch, this contains the number of
     * milliseconds since January 1, 1970, UTC, not the number of seconds.
     */
    timestamp: number;
    /** Hash algorithm. */
    hashAlgorithm: string;
    /** Signature algorithm. */
    signatureAlgorithm: string;
    /** Signature data. */
    signatureData: string;
  }

  /** Security details about a request. */
  export interface SecurityDetails {
    /** Protocol name (e.g. "TLS 1.2" or "QUIC"). */
    protocol: string;
    /** Key Exchange used by the connection, or the empty string if not applicable. */
    keyExchange: string;
    /** (EC)DH group used by the connection, if applicable. */
    keyExchangeGroup?: string;
    /** Cipher name. */
    cipher: string;
    /** TLS MAC. Note that AEAD ciphers do not have separate MACs. */
    mac?: string;
    /** Certificate ID value. */
    certificateId: Security.CertificateId;
    /** Certificate subject name. */
    subjectName: string;
    /** Subject Alternative Name (SAN) DNS names and IP addresses. */
    sanList: string[];
    /** Name of the issuing CA. */
    issuer: string;
    /** Certificate valid from date. */
    validFrom: Network.TimeSinceEpoch;
    /** Certificate valid to (expiration) date */
    validTo: Network.TimeSinceEpoch;
    /** List of signed certificate timestamps (SCTs). */
    signedCertificateTimestampList: Network.SignedCertificateTimestamp[];
    /** Whether the request complied with Certificate Transparency policy */
    certificateTransparencyCompliance: Network.CertificateTransparencyCompliance;
    /**
     * The signature algorithm used by the server in the TLS server signature,
     * represented as a TLS SignatureScheme code point. Omitted if not
     * applicable or not known.
     */
    serverSignatureAlgorithm?: number;
    /** Whether the connection used Encrypted ClientHello */
    encryptedClientHello: boolean;
  }

  /** Whether the request complied with Certificate Transparency policy. */
  export type CertificateTransparencyCompliance = "unknown" | "not-compliant" | "compliant";

  /** The reason why request was blocked. */
  export type BlockedReason = "other" | "csp" | "mixed-content" | "origin" | "inspector" | "integrity" | "subresource-filter" | "content-type" | "coep-frame-resource-needs-coep-header" | "coop-sandboxed-iframe-cannot-navigate-to-coop-page" | "corp-not-same-origin" | "corp-not-same-origin-after-defaulted-to-same-origin-by-coep" | "corp-not-same-origin-after-defaulted-to-same-origin-by-dip" | "corp-not-same-origin-after-defaulted-to-same-origin-by-coep-and-dip" | "corp-not-same-site" | "sri-message-signature-mismatch";

  /** The reason why request was blocked. */
  export type CorsError = "DisallowedByMode" | "InvalidResponse" | "WildcardOriginNotAllowed" | "MissingAllowOriginHeader" | "MultipleAllowOriginValues" | "InvalidAllowOriginValue" | "AllowOriginMismatch" | "InvalidAllowCredentials" | "CorsDisabledScheme" | "PreflightInvalidStatus" | "PreflightDisallowedRedirect" | "PreflightWildcardOriginNotAllowed" | "PreflightMissingAllowOriginHeader" | "PreflightMultipleAllowOriginValues" | "PreflightInvalidAllowOriginValue" | "PreflightAllowOriginMismatch" | "PreflightInvalidAllowCredentials" | "PreflightMissingAllowExternal" | "PreflightInvalidAllowExternal" | "InvalidAllowMethodsPreflightResponse" | "InvalidAllowHeadersPreflightResponse" | "MethodDisallowedByPreflightResponse" | "HeaderDisallowedByPreflightResponse" | "RedirectContainsCredentials" | "InsecureLocalNetwork" | "InvalidLocalNetworkAccess" | "NoCorsRedirectModeNotFollow" | "LocalNetworkAccessPermissionDenied";

  export interface CorsErrorStatus {
    corsError: Network.CorsError;
    failedParameter: string;
  }

  /** Source of serviceworker response. */
  export type ServiceWorkerResponseSource = "cache-storage" | "http-cache" | "fallback-code" | "network";

  /**
   * Determines what type of Trust Token operation is executed and
   * depending on the type, some additional parameters. The values
   * are specified in third_party/blink/renderer/core/fetch/trust_token.idl.
   */
  export interface TrustTokenParams {
    operation: Network.TrustTokenOperationType;
    /**
     * Only set for "token-redemption" operation and determine whether
     * to request a fresh SRR or use a still valid cached SRR.
     */
    refreshPolicy: "UseCached" | "Refresh";
    /**
     * Origins of issuers from whom to request tokens or redemption
     * records.
     */
    issuers?: string[];
  }

  export type TrustTokenOperationType = "Issuance" | "Redemption" | "Signing";

  /** The reason why Chrome uses a specific transport protocol for HTTP semantics. */
  export type AlternateProtocolUsage = "alternativeJobWonWithoutRace" | "alternativeJobWonRace" | "mainJobWonRace" | "mappingMissing" | "broken" | "dnsAlpnH3JobWonWithoutRace" | "dnsAlpnH3JobWonRace" | "unspecifiedReason";

  /** Source of service worker router. */
  export type ServiceWorkerRouterSource = "network" | "cache" | "fetch-event" | "race-network-and-fetch-handler" | "race-network-and-cache";

  export interface ServiceWorkerRouterInfo {
    /**
     * ID of the rule matched. If there is a matched rule, this field will
     * be set, otherwiser no value will be set.
     */
    ruleIdMatched?: number;
    /**
     * The router source of the matched rule. If there is a matched rule, this
     * field will be set, otherwise no value will be set.
     */
    matchedSourceType?: Network.ServiceWorkerRouterSource;
    /** The actual router source used. */
    actualSourceType?: Network.ServiceWorkerRouterSource;
  }

  /** HTTP response data. */
  export interface Response {
    /** Response URL. This URL can be different from CachedResource.url in case of redirect. */
    url: string;
    /** HTTP response status code. */
    status: number;
    /** HTTP response status text. */
    statusText: string;
    /** HTTP response headers. */
    headers: Network.Headers;
    /** HTTP response headers text. This has been replaced by the headers in Network.responseReceivedExtraInfo. */
    headersText?: string;
    /** Resource mimeType as determined by the browser. */
    mimeType: string;
    /** Resource charset as determined by the browser (if applicable). */
    charset: string;
    /** Refined HTTP request headers that were actually transmitted over the network. */
    requestHeaders?: Network.Headers;
    /** HTTP request headers text. This has been replaced by the headers in Network.requestWillBeSentExtraInfo. */
    requestHeadersText?: string;
    /** Specifies whether physical connection was actually reused for this request. */
    connectionReused: boolean;
    /** Physical connection id that was actually used for this request. */
    connectionId: number;
    /** Remote IP address. */
    remoteIPAddress?: string;
    /** Remote port. */
    remotePort?: number;
    /** Specifies that the request was served from the disk cache. */
    fromDiskCache?: boolean;
    /** Specifies that the request was served from the ServiceWorker. */
    fromServiceWorker?: boolean;
    /** Specifies that the request was served from the prefetch cache. */
    fromPrefetchCache?: boolean;
    /** Specifies that the request was served from the prefetch cache. */
    fromEarlyHints?: boolean;
    /**
     * Information about how ServiceWorker Static Router API was used. If this
     * field is set with `matchedSourceType` field, a matching rule is found.
     * If this field is set without `matchedSource`, no matching rule is found.
     * Otherwise, the API is not used.
     */
    serviceWorkerRouterInfo?: Network.ServiceWorkerRouterInfo;
    /** Total number of bytes received for this request so far. */
    encodedDataLength: number;
    /** Timing information for the given request. */
    timing?: Network.ResourceTiming;
    /** Response source of response from ServiceWorker. */
    serviceWorkerResponseSource?: Network.ServiceWorkerResponseSource;
    /** The time at which the returned response was generated. */
    responseTime?: Network.TimeSinceEpoch;
    /** Cache Storage Cache Name. */
    cacheStorageCacheName?: string;
    /** Protocol used to fetch this request. */
    protocol?: string;
    /** The reason why Chrome uses a specific transport protocol for HTTP semantics. */
    alternateProtocolUsage?: Network.AlternateProtocolUsage;
    /** Security state of the request resource. */
    securityState: Security.SecurityState;
    /** Security details for the request. */
    securityDetails?: Network.SecurityDetails;
  }

  /** WebSocket request data. */
  export interface WebSocketRequest {
    /** HTTP request headers. */
    headers: Network.Headers;
  }

  /** WebSocket response data. */
  export interface WebSocketResponse {
    /** HTTP response status code. */
    status: number;
    /** HTTP response status text. */
    statusText: string;
    /** HTTP response headers. */
    headers: Network.Headers;
    /** HTTP response headers text. */
    headersText?: string;
    /** HTTP request headers. */
    requestHeaders?: Network.Headers;
    /** HTTP request headers text. */
    requestHeadersText?: string;
  }

  /** WebSocket message data. This represents an entire WebSocket message, not just a fragmented frame as the name suggests. */
  export interface WebSocketFrame {
    /** WebSocket message opcode. */
    opcode: number;
    /** WebSocket message mask. */
    mask: boolean;
    /**
     * WebSocket message payload data.
     * If the opcode is 1, this is a text message and payloadData is a UTF-8 string.
     * If the opcode isn't 1, then payloadData is a base64 encoded string representing binary data.
     */
    payloadData: string;
  }

  /** Information about the cached resource. */
  export interface CachedResource {
    /** Resource URL. This is the url of the original network request. */
    url: string;
    /** Type of this resource. */
    type: Network.ResourceType;
    /** Cached response data. */
    response?: Network.Response;
    /** Cached response body size. */
    bodySize: number;
  }

  /** Information about the request initiator. */
  export interface Initiator {
    /** Type of this initiator. */
    type: "parser" | "script" | "preload" | "SignedExchange" | "preflight" | "FedCM" | "other";
    /**
     * Initiator JavaScript stack trace, set for Script only.
     * Requires the Debugger domain to be enabled.
     */
    stack?: Runtime.StackTrace;
    /** Initiator URL, set for Parser type or for Script type (when script is importing module) or for SignedExchange type. */
    url?: string;
    /**
     * Initiator line number, set for Parser type or for Script type (when script is importing
     * module) (0-based).
     */
    lineNumber?: number;
    /**
     * Initiator column number, set for Parser type or for Script type (when script is importing
     * module) (0-based).
     */
    columnNumber?: number;
    /** Set if another request triggered this request (e.g. preflight). */
    requestId?: Network.RequestId;
  }

  /**
   * cookiePartitionKey object
   * The representation of the components of the key that are created by the cookiePartitionKey class contained in net/cookies/cookie_partition_key.h.
   */
  export interface CookiePartitionKey {
    /**
     * The site of the top-level URL the browser was visiting at the start
     * of the request to the endpoint that set the cookie.
     */
    topLevelSite: string;
    /** Indicates if the cookie has any ancestors that are cross-site to the topLevelSite. */
    hasCrossSiteAncestor: boolean;
  }

  /** Cookie object */
  export interface Cookie {
    /** Cookie name. */
    name: string;
    /** Cookie value. */
    value: string;
    /** Cookie domain. */
    domain: string;
    /** Cookie path. */
    path: string;
    /**
     * Cookie expiration date as the number of seconds since the UNIX epoch.
     * The value is set to -1 if the expiry date is not set.
     * The value can be null for values that cannot be represented in
     * JSON (±Inf).
     */
    expires: number;
    /** Cookie size. */
    size: number;
    /** True if cookie is http-only. */
    httpOnly: boolean;
    /** True if cookie is secure. */
    secure: boolean;
    /** True in case of session cookie. */
    session: boolean;
    /** Cookie SameSite type. */
    sameSite?: Network.CookieSameSite;
    /** Cookie Priority */
    priority: Network.CookiePriority;
    /** Cookie source scheme type. */
    sourceScheme: Network.CookieSourceScheme;
    /**
     * Cookie source port. Valid values are {-1, [1, 65535]}, -1 indicates an unspecified port.
     * An unspecified port value allows protocol clients to emulate legacy cookie scope for the port.
     * This is a temporary ability and it will be removed in the future.
     */
    sourcePort: number;
    /** Cookie partition key. */
    partitionKey?: Network.CookiePartitionKey;
    /** True if cookie partition key is opaque. */
    partitionKeyOpaque?: boolean;
  }

  /** Types of reasons why a cookie may not be stored from a response. */
  export type SetCookieBlockedReason = "SecureOnly" | "SameSiteStrict" | "SameSiteLax" | "SameSiteUnspecifiedTreatedAsLax" | "SameSiteNoneInsecure" | "UserPreferences" | "ThirdPartyPhaseout" | "ThirdPartyBlockedInFirstPartySet" | "SyntaxError" | "SchemeNotSupported" | "OverwriteSecure" | "InvalidDomain" | "InvalidPrefix" | "UnknownError" | "SchemefulSameSiteStrict" | "SchemefulSameSiteLax" | "SchemefulSameSiteUnspecifiedTreatedAsLax" | "NameValuePairExceedsMaxSize" | "DisallowedCharacter" | "NoCookieContent";

  /** Types of reasons why a cookie may not be sent with a request. */
  export type CookieBlockedReason = "SecureOnly" | "NotOnPath" | "DomainMismatch" | "SameSiteStrict" | "SameSiteLax" | "SameSiteUnspecifiedTreatedAsLax" | "SameSiteNoneInsecure" | "UserPreferences" | "ThirdPartyPhaseout" | "ThirdPartyBlockedInFirstPartySet" | "UnknownError" | "SchemefulSameSiteStrict" | "SchemefulSameSiteLax" | "SchemefulSameSiteUnspecifiedTreatedAsLax" | "NameValuePairExceedsMaxSize" | "PortMismatch" | "SchemeMismatch" | "AnonymousContext";

  /** Types of reasons why a cookie should have been blocked by 3PCD but is exempted for the request. */
  export type CookieExemptionReason = "None" | "UserSetting" | "TPCDMetadata" | "TPCDDeprecationTrial" | "TopLevelTPCDDeprecationTrial" | "TPCDHeuristics" | "EnterprisePolicy" | "StorageAccess" | "TopLevelStorageAccess" | "Scheme" | "SameSiteNoneCookiesInSandbox";

  /** A cookie which was not stored from a response with the corresponding reason. */
  export interface BlockedSetCookieWithReason {
    /** The reason(s) this cookie was blocked. */
    blockedReasons: Network.SetCookieBlockedReason[];
    /**
     * The string representing this individual cookie as it would appear in the header.
     * This is not the entire "cookie" or "set-cookie" header which could have multiple cookies.
     */
    cookieLine: string;
    /**
     * The cookie object which represents the cookie which was not stored. It is optional because
     * sometimes complete cookie information is not available, such as in the case of parsing
     * errors.
     */
    cookie?: Network.Cookie;
  }

  /**
   * A cookie should have been blocked by 3PCD but is exempted and stored from a response with the
   * corresponding reason. A cookie could only have at most one exemption reason.
   */
  export interface ExemptedSetCookieWithReason {
    /** The reason the cookie was exempted. */
    exemptionReason: Network.CookieExemptionReason;
    /** The string representing this individual cookie as it would appear in the header. */
    cookieLine: string;
    /** The cookie object representing the cookie. */
    cookie: Network.Cookie;
  }

  /**
   * A cookie associated with the request which may or may not be sent with it.
   * Includes the cookies itself and reasons for blocking or exemption.
   */
  export interface AssociatedCookie {
    /** The cookie object representing the cookie which was not sent. */
    cookie: Network.Cookie;
    /** The reason(s) the cookie was blocked. If empty means the cookie is included. */
    blockedReasons: Network.CookieBlockedReason[];
    /**
     * The reason the cookie should have been blocked by 3PCD but is exempted. A cookie could
     * only have at most one exemption reason.
     */
    exemptionReason?: Network.CookieExemptionReason;
  }

  /** Cookie parameter object */
  export interface CookieParam {
    /** Cookie name. */
    name: string;
    /** Cookie value. */
    value: string;
    /**
     * The request-URI to associate with the setting of the cookie. This value can affect the
     * default domain, path, source port, and source scheme values of the created cookie.
     */
    url?: string;
    /** Cookie domain. */
    domain?: string;
    /** Cookie path. */
    path?: string;
    /** True if cookie is secure. */
    secure?: boolean;
    /** True if cookie is http-only. */
    httpOnly?: boolean;
    /** Cookie SameSite type. */
    sameSite?: Network.CookieSameSite;
    /** Cookie expiration date, session cookie if not set */
    expires?: Network.TimeSinceEpoch;
    /** Cookie Priority. */
    priority?: Network.CookiePriority;
    /** Cookie source scheme type. */
    sourceScheme?: Network.CookieSourceScheme;
    /**
     * Cookie source port. Valid values are {-1, [1, 65535]}, -1 indicates an unspecified port.
     * An unspecified port value allows protocol clients to emulate legacy cookie scope for the port.
     * This is a temporary ability and it will be removed in the future.
     */
    sourcePort?: number;
    /** Cookie partition key. If not set, the cookie will be set as not partitioned. */
    partitionKey?: Network.CookiePartitionKey;
  }

  /** Authorization challenge for HTTP status code 401 or 407. */
  export interface AuthChallenge {
    /** Source of the authentication challenge. */
    source?: "Server" | "Proxy";
    /** Origin of the challenger. */
    origin: string;
    /** The authentication scheme used, such as basic or digest */
    scheme: string;
    /** The realm of the challenge. May be empty. */
    realm: string;
  }

  /** Response to an AuthChallenge. */
  export interface AuthChallengeResponse {
    /**
     * The decision on what to do in response to the authorization challenge.  Default means
     * deferring to the default behavior of the net stack, which will likely either the Cancel
     * authentication or display a popup dialog box.
     */
    response: "Default" | "CancelAuth" | "ProvideCredentials";
    /**
     * The username to provide, possibly empty. Should only be set if response is
     * ProvideCredentials.
     */
    username?: string;
    /**
     * The password to provide, possibly empty. Should only be set if response is
     * ProvideCredentials.
     */
    password?: string;
  }

  /**
   * Stages of the interception to begin intercepting. Request will intercept before the request is
   * sent. Response will intercept after the response is received.
   */
  export type InterceptionStage = "Request" | "HeadersReceived";

  /** Request pattern for interception. */
  export interface RequestPattern {
    /**
     * Wildcards (`'*'` -> zero or more, `'?'` -> exactly one) are allowed. Escape character is
     * backslash. Omitting is equivalent to `"*"`.
     */
    urlPattern?: string;
    /** If set, only requests for matching resource types will be intercepted. */
    resourceType?: Network.ResourceType;
    /** Stage at which to begin intercepting requests. Default is Request. */
    interceptionStage?: Network.InterceptionStage;
  }

  /**
   * Information about a signed exchange signature.
   * https://wicg.github.io/webpackage/draft-yasskin-httpbis-origin-signed-exchanges-impl.html#rfc.section.3.1
   */
  export interface SignedExchangeSignature {
    /** Signed exchange signature label. */
    label: string;
    /** The hex string of signed exchange signature. */
    signature: string;
    /** Signed exchange signature integrity. */
    integrity: string;
    /** Signed exchange signature cert Url. */
    certUrl?: string;
    /** The hex string of signed exchange signature cert sha256. */
    certSha256?: string;
    /** Signed exchange signature validity Url. */
    validityUrl: string;
    /** Signed exchange signature date. */
    date: number;
    /** Signed exchange signature expires. */
    expires: number;
    /** The encoded certificates. */
    certificates?: string[];
  }

  /**
   * Information about a signed exchange header.
   * https://wicg.github.io/webpackage/draft-yasskin-httpbis-origin-signed-exchanges-impl.html#cbor-representation
   */
  export interface SignedExchangeHeader {
    /** Signed exchange request URL. */
    requestUrl: string;
    /** Signed exchange response code. */
    responseCode: number;
    /** Signed exchange response headers. */
    responseHeaders: Network.Headers;
    /** Signed exchange response signature. */
    signatures: Network.SignedExchangeSignature[];
    /** Signed exchange header integrity hash in the form of `sha256-<base64-hash-value>`. */
    headerIntegrity: string;
  }

  /** Field type for a signed exchange related error. */
  export type SignedExchangeErrorField = "signatureSig" | "signatureIntegrity" | "signatureCertUrl" | "signatureCertSha256" | "signatureValidityUrl" | "signatureTimestamps";

  /** Information about a signed exchange response. */
  export interface SignedExchangeError {
    /** Error message. */
    message: string;
    /** The index of the signature which caused the error. */
    signatureIndex?: number;
    /** The field which caused the error. */
    errorField?: Network.SignedExchangeErrorField;
  }

  /** Information about a signed exchange response. */
  export interface SignedExchangeInfo {
    /** The outer response of signed HTTP exchange which was received from network. */
    outerResponse: Network.Response;
    /**
     * Whether network response for the signed exchange was accompanied by
     * extra headers.
     */
    hasExtraInfo: boolean;
    /** Information about the signed exchange header. */
    header?: Network.SignedExchangeHeader;
    /** Security details for the signed exchange header. */
    securityDetails?: Network.SecurityDetails;
    /** Errors occurred while handling the signed exchange. */
    errors?: Network.SignedExchangeError[];
  }

  /** List of content encodings supported by the backend. */
  export type ContentEncoding = "deflate" | "gzip" | "br" | "zstd";

  export interface NetworkConditions {
    /**
     * Only matching requests will be affected by these conditions. Patterns use the URLPattern constructor string
     * syntax (https://urlpattern.spec.whatwg.org/) and must be absolute. If the pattern is empty, all requests are
     * matched (including p2p connections).
     */
    urlPattern: string;
    /** Minimum latency from request sent to response headers received (ms). */
    latency: number;
    /** Maximal aggregated download throughput (bytes/sec). -1 disables download throttling. */
    downloadThroughput: number;
    /** Maximal aggregated upload throughput (bytes/sec).  -1 disables upload throttling. */
    uploadThroughput: number;
    /** Connection type if known. */
    connectionType?: Network.ConnectionType;
    /** WebRTC packet loss (percent, 0-100). 0 disables packet loss emulation, 100 drops all the packets. */
    packetLoss?: number;
    /** WebRTC packet queue length (packet). 0 removes any queue length limitations. */
    packetQueueLength?: number;
    /** WebRTC packetReordering feature. */
    packetReordering?: boolean;
    /** True to emulate internet disconnection. */
    offline?: boolean;
  }

  export interface BlockPattern {
    /**
     * URL pattern to match. Patterns use the URLPattern constructor string syntax
     * (https://urlpattern.spec.whatwg.org/) and must be absolute. Example: `*://*:*\/*.css`.
     */
    urlPattern: string;
    /**
     * Whether or not to block the pattern. If false, a matching request will not be blocked even if it matches a later
     * `BlockPattern`.
     */
    block: boolean;
  }

  export type DirectSocketDnsQueryType = "ipv4" | "ipv6";

  export interface DirectTCPSocketOptions {
    /** TCP_NODELAY option */
    noDelay: boolean;
    /** Expected to be unsigned integer. */
    keepAliveDelay?: number;
    /** Expected to be unsigned integer. */
    sendBufferSize?: number;
    /** Expected to be unsigned integer. */
    receiveBufferSize?: number;
    dnsQueryType?: Network.DirectSocketDnsQueryType;
  }

  export interface DirectUDPSocketOptions {
    remoteAddr?: string;
    /** Unsigned int 16. */
    remotePort?: number;
    localAddr?: string;
    /** Unsigned int 16. */
    localPort?: number;
    dnsQueryType?: Network.DirectSocketDnsQueryType;
    /** Expected to be unsigned integer. */
    sendBufferSize?: number;
    /** Expected to be unsigned integer. */
    receiveBufferSize?: number;
    multicastLoopback?: boolean;
    /** Unsigned int 8. */
    multicastTimeToLive?: number;
    multicastAllowAddressSharing?: boolean;
  }

  export interface DirectUDPMessage {
    data: string;
    /** Null for connected mode. */
    remoteAddr?: string;
    /**
     * Null for connected mode.
     * Expected to be unsigned integer.
     */
    remotePort?: number;
  }

  export type LocalNetworkAccessRequestPolicy = "Allow" | "BlockFromInsecureToMorePrivate" | "WarnFromInsecureToMorePrivate" | "PermissionBlock" | "PermissionWarn";

  export type IPAddressSpace = "Loopback" | "Local" | "Public" | "Unknown";

  export interface ConnectTiming {
    /**
     * Timing's requestTime is a baseline in seconds, while the other numbers are ticks in
     * milliseconds relatively to this requestTime. Matches ResourceTiming's requestTime for
     * the same request (but not for redirected requests).
     */
    requestTime: number;
  }

  export interface ClientSecurityState {
    initiatorIsSecureContext: boolean;
    initiatorIPAddressSpace: Network.IPAddressSpace;
    localNetworkAccessRequestPolicy: Network.LocalNetworkAccessRequestPolicy;
  }

  /**
   * Identifies the script on the stack that caused a resource or element to be
   * labeled as an ad. For resources, this indicates the context that triggered
   * the fetch. For elements, this indicates the context that caused the element
   * to be appended to the DOM.
   */
  export interface AdScriptIdentifier {
    /** The script's V8 identifier. */
    scriptId: Runtime.ScriptId;
    /** V8's debugging ID for the v8::Context. */
    debuggerId: Runtime.UniqueDebuggerId;
    /** The script's url (or generated name based on id if inline script). */
    name: string;
  }

  /**
   * Encapsulates the script ancestry and the root script filter list rule that
   * caused the resource or element to be labeled as an ad.
   */
  export interface AdAncestry {
    /**
     * A chain of `AdScriptIdentifier`s representing the ancestry of an ad
     * script that led to the creation of a resource or element. The chain is
     * ordered from the script itself (lowest level) up to its root ancestor
     * that was flagged by a filter list.
     */
    ancestryChain: Network.AdScriptIdentifier[];
    /**
     * The filter list rule that caused the root (last) script in
     * `ancestryChain` to be tagged as an ad.
     */
    rootScriptFilterlistRule?: string;
  }

  /**
   * Represents the provenance of an ad resource or element. Only one of
   * `filterlistRule` or `adScriptAncestry` can be set. If `filterlistRule`
   * is provided, the resource URL directly matches a filter list rule. If
   * `adScriptAncestry` is provided, an ad script initiated the resource fetch or
   * appended the element to the DOM. If neither is provided, the entity is
   * known to be an ad, but provenance tracking information is unavailable.
   */
  export interface AdProvenance {
    /** The filterlist rule that matched, if any. */
    filterlistRule?: string;
    /** The script ancestry that created the ad, if any. */
    adScriptAncestry?: Network.AdAncestry;
  }

  export type CrossOriginOpenerPolicyValue = "SameOrigin" | "SameOriginAllowPopups" | "RestrictProperties" | "UnsafeNone" | "SameOriginPlusCoep" | "RestrictPropertiesPlusCoep" | "NoopenerAllowPopups";

  export interface CrossOriginOpenerPolicyStatus {
    value: Network.CrossOriginOpenerPolicyValue;
    reportOnlyValue: Network.CrossOriginOpenerPolicyValue;
    reportingEndpoint?: string;
    reportOnlyReportingEndpoint?: string;
  }

  export type CrossOriginEmbedderPolicyValue = "None" | "Credentialless" | "RequireCorp";

  export interface CrossOriginEmbedderPolicyStatus {
    value: Network.CrossOriginEmbedderPolicyValue;
    reportOnlyValue: Network.CrossOriginEmbedderPolicyValue;
    reportingEndpoint?: string;
    reportOnlyReportingEndpoint?: string;
  }

  export type ContentSecurityPolicySource = "HTTP" | "Meta";

  export interface ContentSecurityPolicyStatus {
    effectiveDirectives: string;
    isEnforced: boolean;
    source: Network.ContentSecurityPolicySource;
  }

  export interface SecurityIsolationStatus {
    coop?: Network.CrossOriginOpenerPolicyStatus;
    coep?: Network.CrossOriginEmbedderPolicyStatus;
    csp?: Network.ContentSecurityPolicyStatus[];
  }

  /** The status of a Reporting API report. */
  export type ReportStatus = "Queued" | "Pending" | "MarkedForRemoval" | "Success";

  export type ReportId = string;

  /** An object representing a report generated by the Reporting API. */
  export interface ReportingApiReport {
    id: Network.ReportId;
    /** The URL of the document that triggered the report. */
    initiatorUrl: string;
    /** The name of the endpoint group that should be used to deliver the report. */
    destination: string;
    /** The type of the report (specifies the set of data that is contained in the report body). */
    type: string;
    /** When the report was generated. */
    timestamp: Network.TimeSinceEpoch;
    /** How many uploads deep the related request was. */
    depth: number;
    /** The number of delivery attempts made so far, not including an active attempt. */
    completedAttempts: number;
    body: Record<string, unknown>;
    status: Network.ReportStatus;
  }

  export interface ReportingApiEndpoint {
    /** The URL of the endpoint to which reports may be delivered. */
    url: string;
    /** Name of the endpoint group. */
    groupName: string;
  }

  /** Unique identifier for a device bound session. */
  export interface DeviceBoundSessionKey {
    /** The site the session is set up for. */
    site: string;
    /** The id of the session. */
    id: string;
  }

  /** How a device bound session was used during a request. */
  export interface DeviceBoundSessionWithUsage {
    /** The key for the session. */
    sessionKey: Network.DeviceBoundSessionKey;
    /** How the session was used (or not used). */
    usage: "NotInScope" | "InScopeRefreshNotYetNeeded" | "InScopeRefreshNotAllowed" | "ProactiveRefreshNotPossible" | "ProactiveRefreshAttempted" | "Deferred";
  }

  /** A device bound session's cookie craving. */
  export interface DeviceBoundSessionCookieCraving {
    /** The name of the craving. */
    name: string;
    /** The domain of the craving. */
    domain: string;
    /** The path of the craving. */
    path: string;
    /** The `Secure` attribute of the craving attributes. */
    secure: boolean;
    /** The `HttpOnly` attribute of the craving attributes. */
    httpOnly: boolean;
    /** The `SameSite` attribute of the craving attributes. */
    sameSite?: Network.CookieSameSite;
  }

  /** A device bound session's inclusion URL rule. */
  export interface DeviceBoundSessionUrlRule {
    /** See comments on `net::device_bound_sessions::SessionInclusionRules::UrlRule::rule_type`. */
    ruleType: "Exclude" | "Include";
    /** See comments on `net::device_bound_sessions::SessionInclusionRules::UrlRule::host_pattern`. */
    hostPattern: string;
    /** See comments on `net::device_bound_sessions::SessionInclusionRules::UrlRule::path_prefix`. */
    pathPrefix: string;
  }

  /** A device bound session's inclusion rules. */
  export interface DeviceBoundSessionInclusionRules {
    /** See comments on `net::device_bound_sessions::SessionInclusionRules::origin_`. */
    origin: string;
    /**
     * Whether the whole site is included. See comments on
     * `net::device_bound_sessions::SessionInclusionRules::include_site_` for more
     * details; this boolean is true if that value is populated.
     */
    includeSite: boolean;
    /** See comments on `net::device_bound_sessions::SessionInclusionRules::url_rules_`. */
    urlRules: Network.DeviceBoundSessionUrlRule[];
  }

  /** A device bound session. */
  export interface DeviceBoundSession {
    /** The site and session ID of the session. */
    key: Network.DeviceBoundSessionKey;
    /** See comments on `net::device_bound_sessions::Session::refresh_url_`. */
    refreshUrl: string;
    /** See comments on `net::device_bound_sessions::Session::inclusion_rules_`. */
    inclusionRules: Network.DeviceBoundSessionInclusionRules;
    /** See comments on `net::device_bound_sessions::Session::cookie_cravings_`. */
    cookieCravings: Network.DeviceBoundSessionCookieCraving[];
    /** See comments on `net::device_bound_sessions::Session::expiry_date_`. */
    expiryDate: Network.TimeSinceEpoch;
    /** See comments on `net::device_bound_sessions::Session::cached_challenge__`. */
    cachedChallenge?: string;
    /** See comments on `net::device_bound_sessions::Session::allowed_refresh_initiators_`. */
    allowedRefreshInitiators: string[];
  }

  /** A unique identifier for a device bound session event. */
  export type DeviceBoundSessionEventId = string;

  /** A fetch result for a device bound session creation or refresh. */
  export type DeviceBoundSessionFetchResult = "Success" | "KeyError" | "SigningError" | "ServerRequestedTermination" | "InvalidSessionId" | "InvalidChallenge" | "TooManyChallenges" | "InvalidFetcherUrl" | "InvalidRefreshUrl" | "TransientHttpError" | "ScopeOriginSameSiteMismatch" | "RefreshUrlSameSiteMismatch" | "MismatchedSessionId" | "MissingScope" | "NoCredentials" | "SubdomainRegistrationWellKnownUnavailable" | "SubdomainRegistrationUnauthorized" | "SubdomainRegistrationWellKnownMalformed" | "SessionProviderWellKnownUnavailable" | "RelyingPartyWellKnownUnavailable" | "FederatedKeyThumbprintMismatch" | "InvalidFederatedSessionUrl" | "InvalidFederatedKey" | "TooManyRelyingOriginLabels" | "BoundCookieSetForbidden" | "NetError" | "ProxyError" | "EmptySessionConfig" | "InvalidCredentialsConfig" | "InvalidCredentialsType" | "InvalidCredentialsEmptyName" | "InvalidCredentialsCookie" | "PersistentHttpError" | "RegistrationAttemptedChallenge" | "InvalidScopeOrigin" | "ScopeOriginContainsPath" | "RefreshInitiatorNotString" | "RefreshInitiatorInvalidHostPattern" | "InvalidScopeSpecification" | "MissingScopeSpecificationType" | "EmptyScopeSpecificationDomain" | "EmptyScopeSpecificationPath" | "InvalidScopeSpecificationType" | "InvalidScopeIncludeSite" | "MissingScopeIncludeSite" | "FederatedNotAuthorizedByProvider" | "FederatedNotAuthorizedByRelyingParty" | "SessionProviderWellKnownMalformed" | "SessionProviderWellKnownHasProviderOrigin" | "RelyingPartyWellKnownMalformed" | "RelyingPartyWellKnownHasRelyingOrigins" | "InvalidFederatedSessionProviderSessionMissing" | "InvalidFederatedSessionWrongProviderOrigin" | "InvalidCredentialsCookieCreationTime" | "InvalidCredentialsCookieName" | "InvalidCredentialsCookieParsing" | "InvalidCredentialsCookieUnpermittedAttribute" | "InvalidCredentialsCookieInvalidDomain" | "InvalidCredentialsCookiePrefix" | "InvalidScopeRulePath" | "InvalidScopeRuleHostPattern" | "ScopeRuleOriginScopedHostPatternMismatch" | "ScopeRuleSiteScopedHostPatternMismatch" | "SigningQuotaExceeded" | "InvalidConfigJson" | "InvalidFederatedSessionProviderFailedToRestoreKey" | "FailedToUnwrapKey" | "SessionDeletedDuringRefresh";

  /** Details about a failed device bound session network request. */
  export interface DeviceBoundSessionFailedRequest {
    /** The failed request URL. */
    requestUrl: string;
    /** The net error of the response if it was not OK. */
    netError?: string;
    /**
     * The response code if the net error was OK and the response code was not
     * 200.
     */
    responseError?: number;
    /**
     * The body of the response if the net error was OK, the response code was
     * not 200, and the response body was not empty.
     */
    responseErrorBody?: string;
  }

  /** Session event details specific to creation. */
  export interface CreationEventDetails {
    /** The result of the fetch attempt. */
    fetchResult: Network.DeviceBoundSessionFetchResult;
    /**
     * The session if there was a newly created session. This is populated for
     * all successful creation events.
     */
    newSession?: Network.DeviceBoundSession;
    /**
     * Details about a failed device bound session network request if there was
     * one.
     */
    failedRequest?: Network.DeviceBoundSessionFailedRequest;
  }

  /** Session event details specific to refresh. */
  export interface RefreshEventDetails {
    /** The result of a refresh. */
    refreshResult: "Refreshed" | "RefreshedAsWaiter" | "InitializedService" | "Unreachable" | "ServerError" | "RefreshQuotaExceeded" | "FatalError" | "SigningQuotaExceeded";
    /** If there was a fetch attempt, the result of that. */
    fetchResult?: Network.DeviceBoundSessionFetchResult;
    /**
     * The session display if there was a newly created session. This is populated
     * for any refresh event that modifies the session config.
     */
    newSession?: Network.DeviceBoundSession;
    /** See comments on `net::device_bound_sessions::RefreshEventResult::was_fully_proactive_refresh`. */
    wasFullyProactiveRefresh: boolean;
    /**
     * Details about a failed device bound session network request if there was
     * one.
     */
    failedRequest?: Network.DeviceBoundSessionFailedRequest;
  }

  /** Session event details specific to termination. */
  export interface TerminationEventDetails {
    /** The reason for a session being deleted. */
    deletionReason: "Expired" | "FailedToRestoreKey" | "FailedToUnwrapKey" | "StoragePartitionCleared" | "ClearBrowsingData" | "ServerRequested" | "InvalidSessionParams" | "RefreshFatalError" | "DevTools";
  }

  /** Session event details specific to challenges. */
  export interface ChallengeEventDetails {
    /** The result of a challenge. */
    challengeResult: "Success" | "NoSessionId" | "NoSessionMatch" | "CantSetBoundCookie";
    /** The challenge set. */
    challenge: string;
  }

  /** An object providing the result of a network resource load. */
  export interface LoadNetworkResourcePageResult {
    success: boolean;
    /** Optional values used for error reporting. */
    netError?: number;
    netErrorName?: string;
    httpStatusCode?: number;
    /** If successful, one of the following two fields holds the result. */
    stream?: IO.StreamHandle;
    /** Response headers. */
    headers?: Network.Headers;
  }

  /**
   * An options object that may be extended later to better support CORS,
   * CORB and streaming.
   */
  export interface LoadNetworkResourceOptions {
    disableCache: boolean;
    includeCredentials: boolean;
  }

}

export namespace Overlay {

  /** Configuration data for drawing the source order of an elements children. */
  export interface SourceOrderConfig {
    /** the color to outline the given element in. */
    parentOutlineColor: DOM.RGBA;
    /** the color to outline the child elements in. */
    childOutlineColor: DOM.RGBA;
  }

  /** Configuration data for the highlighting of Grid elements. */
  export interface GridHighlightConfig {
    /** Whether the extension lines from grid cells to the rulers should be shown (default: false). */
    showGridExtensionLines?: boolean;
    /** Show Positive line number labels (default: false). */
    showPositiveLineNumbers?: boolean;
    /** Show Negative line number labels (default: false). */
    showNegativeLineNumbers?: boolean;
    /** Show area name labels (default: false). */
    showAreaNames?: boolean;
    /** Show line name labels (default: false). */
    showLineNames?: boolean;
    /** Show track size labels (default: false). */
    showTrackSizes?: boolean;
    /** The grid container border highlight color (default: transparent). */
    gridBorderColor?: DOM.RGBA;
    /** The cell border color (default: transparent). Deprecated, please use rowLineColor and columnLineColor instead. */
    cellBorderColor?: DOM.RGBA;
    /** The row line color (default: transparent). */
    rowLineColor?: DOM.RGBA;
    /** The column line color (default: transparent). */
    columnLineColor?: DOM.RGBA;
    /** Whether the grid border is dashed (default: false). */
    gridBorderDash?: boolean;
    /** Whether the cell border is dashed (default: false). Deprecated, please us rowLineDash and columnLineDash instead. */
    cellBorderDash?: boolean;
    /** Whether row lines are dashed (default: false). */
    rowLineDash?: boolean;
    /** Whether column lines are dashed (default: false). */
    columnLineDash?: boolean;
    /** The row gap highlight fill color (default: transparent). */
    rowGapColor?: DOM.RGBA;
    /** The row gap hatching fill color (default: transparent). */
    rowHatchColor?: DOM.RGBA;
    /** The column gap highlight fill color (default: transparent). */
    columnGapColor?: DOM.RGBA;
    /** The column gap hatching fill color (default: transparent). */
    columnHatchColor?: DOM.RGBA;
    /** The named grid areas border color (Default: transparent). */
    areaBorderColor?: DOM.RGBA;
    /** The grid container background color (Default: transparent). */
    gridBackgroundColor?: DOM.RGBA;
  }

  /** Configuration data for the highlighting of Flex container elements. */
  export interface FlexContainerHighlightConfig {
    /** The style of the container border */
    containerBorder?: Overlay.LineStyle;
    /** The style of the separator between lines */
    lineSeparator?: Overlay.LineStyle;
    /** The style of the separator between items */
    itemSeparator?: Overlay.LineStyle;
    /** Style of content-distribution space on the main axis (justify-content). */
    mainDistributedSpace?: Overlay.BoxStyle;
    /** Style of content-distribution space on the cross axis (align-content). */
    crossDistributedSpace?: Overlay.BoxStyle;
    /** Style of empty space caused by row gaps (gap/row-gap). */
    rowGapSpace?: Overlay.BoxStyle;
    /** Style of empty space caused by columns gaps (gap/column-gap). */
    columnGapSpace?: Overlay.BoxStyle;
    /** Style of the self-alignment line (align-items). */
    crossAlignment?: Overlay.LineStyle;
  }

  /** Configuration data for the highlighting of Flex item elements. */
  export interface FlexItemHighlightConfig {
    /** Style of the box representing the item's base size */
    baseSizeBox?: Overlay.BoxStyle;
    /** Style of the border around the box representing the item's base size */
    baseSizeBorder?: Overlay.LineStyle;
    /** Style of the arrow representing if the item grew or shrank */
    flexibilityArrow?: Overlay.LineStyle;
  }

  /** Style information for drawing a line. */
  export interface LineStyle {
    /** The color of the line (default: transparent) */
    color?: DOM.RGBA;
    /** The line pattern (default: solid) */
    pattern?: "dashed" | "dotted";
  }

  /** Style information for drawing a box. */
  export interface BoxStyle {
    /** The background color for the box (default: transparent) */
    fillColor?: DOM.RGBA;
    /** The hatching color for the box (default: transparent) */
    hatchColor?: DOM.RGBA;
  }

  export type ContrastAlgorithm = "aa" | "aaa" | "apca";

  /** Configuration data for the highlighting of page elements. */
  export interface HighlightConfig {
    /** Whether the node info tooltip should be shown (default: false). */
    showInfo?: boolean;
    /** Whether the node styles in the tooltip (default: false). */
    showStyles?: boolean;
    /** Whether the rulers should be shown (default: false). */
    showRulers?: boolean;
    /** Whether the a11y info should be shown (default: true). */
    showAccessibilityInfo?: boolean;
    /** Whether the extension lines from node to the rulers should be shown (default: false). */
    showExtensionLines?: boolean;
    /** The content box highlight fill color (default: transparent). */
    contentColor?: DOM.RGBA;
    /** The padding highlight fill color (default: transparent). */
    paddingColor?: DOM.RGBA;
    /** The border highlight fill color (default: transparent). */
    borderColor?: DOM.RGBA;
    /** The margin highlight fill color (default: transparent). */
    marginColor?: DOM.RGBA;
    /** The event target element highlight fill color (default: transparent). */
    eventTargetColor?: DOM.RGBA;
    /** The shape outside fill color (default: transparent). */
    shapeColor?: DOM.RGBA;
    /** The shape margin fill color (default: transparent). */
    shapeMarginColor?: DOM.RGBA;
    /** The grid layout color (default: transparent). */
    cssGridColor?: DOM.RGBA;
    /** The color format used to format color styles (default: hex). */
    colorFormat?: Overlay.ColorFormat;
    /** The grid layout highlight configuration (default: all transparent). */
    gridHighlightConfig?: Overlay.GridHighlightConfig;
    /** The flex container highlight configuration (default: all transparent). */
    flexContainerHighlightConfig?: Overlay.FlexContainerHighlightConfig;
    /** The flex item highlight configuration (default: all transparent). */
    flexItemHighlightConfig?: Overlay.FlexItemHighlightConfig;
    /** The contrast algorithm to use for the contrast ratio (default: aa). */
    contrastAlgorithm?: Overlay.ContrastAlgorithm;
    /** The container query container highlight configuration (default: all transparent). */
    containerQueryContainerHighlightConfig?: Overlay.ContainerQueryContainerHighlightConfig;
  }

  export type ColorFormat = "rgb" | "hsl" | "hwb" | "hex";

  /** Configurations for Persistent Grid Highlight */
  export interface GridNodeHighlightConfig {
    /** A descriptor for the highlight appearance. */
    gridHighlightConfig: Overlay.GridHighlightConfig;
    /** Identifier of the node to highlight. */
    nodeId: DOM.NodeId;
  }

  export interface FlexNodeHighlightConfig {
    /** A descriptor for the highlight appearance of flex containers. */
    flexContainerHighlightConfig: Overlay.FlexContainerHighlightConfig;
    /** Identifier of the node to highlight. */
    nodeId: DOM.NodeId;
  }

  export interface ScrollSnapContainerHighlightConfig {
    /** The style of the snapport border (default: transparent) */
    snapportBorder?: Overlay.LineStyle;
    /** The style of the snap area border (default: transparent) */
    snapAreaBorder?: Overlay.LineStyle;
    /** The margin highlight fill color (default: transparent). */
    scrollMarginColor?: DOM.RGBA;
    /** The padding highlight fill color (default: transparent). */
    scrollPaddingColor?: DOM.RGBA;
  }

  export interface ScrollSnapHighlightConfig {
    /** A descriptor for the highlight appearance of scroll snap containers. */
    scrollSnapContainerHighlightConfig: Overlay.ScrollSnapContainerHighlightConfig;
    /** Identifier of the node to highlight. */
    nodeId: DOM.NodeId;
  }

  /** Configuration for dual screen hinge */
  export interface HingeConfig {
    /** A rectangle represent hinge */
    rect: DOM.Rect;
    /** The content box highlight fill color (default: a dark color). */
    contentColor?: DOM.RGBA;
    /** The content box highlight outline color (default: transparent). */
    outlineColor?: DOM.RGBA;
  }

  /** Configuration for Window Controls Overlay */
  export interface WindowControlsOverlayConfig {
    /** Whether the title bar CSS should be shown when emulating the Window Controls Overlay. */
    showCSS: boolean;
    /** Selected platforms to show the overlay. */
    selectedPlatform: string;
    /** The theme color defined in app manifest. */
    themeColor: string;
  }

  export interface ContainerQueryHighlightConfig {
    /** A descriptor for the highlight appearance of container query containers. */
    containerQueryContainerHighlightConfig: Overlay.ContainerQueryContainerHighlightConfig;
    /** Identifier of the container node to highlight. */
    nodeId: DOM.NodeId;
  }

  export interface ContainerQueryContainerHighlightConfig {
    /** The style of the container border. */
    containerBorder?: Overlay.LineStyle;
    /** The style of the descendants' borders. */
    descendantBorder?: Overlay.LineStyle;
  }

  export interface IsolatedElementHighlightConfig {
    /** A descriptor for the highlight appearance of an element in isolation mode. */
    isolationModeHighlightConfig: Overlay.IsolationModeHighlightConfig;
    /** Identifier of the isolated element to highlight. */
    nodeId: DOM.NodeId;
  }

  export interface IsolationModeHighlightConfig {
    /** The fill color of the resizers (default: transparent). */
    resizerColor?: DOM.RGBA;
    /** The fill color for resizer handles (default: transparent). */
    resizerHandleColor?: DOM.RGBA;
    /** The fill color for the mask covering non-isolated elements (default: transparent). */
    maskColor?: DOM.RGBA;
  }

  export type InspectMode = "searchForNode" | "searchForUAShadowDOM" | "captureAreaScreenshot" | "none";

  export interface InspectedElementAnchorConfig {
    /** Identifier of the node to highlight. */
    nodeId?: DOM.NodeId;
    /** Identifier of the backend node to highlight. */
    backendNodeId?: DOM.BackendNodeId;
  }

}

export namespace Page {

  /** Unique frame identifier. */
  export type FrameId = string;

  /** Indicates whether a frame has been identified as an ad. */
  export type AdFrameType = "none" | "child" | "root";

  export type AdFrameExplanation = "ParentIsAd" | "CreatedByAdScript" | "MatchedBlockingRule";

  /** Indicates whether a frame has been identified as an ad and why. */
  export interface AdFrameStatus {
    adFrameType: Page.AdFrameType;
    explanations?: Page.AdFrameExplanation[];
  }

  /** Indicates whether the frame is a secure context and why it is the case. */
  export type SecureContextType = "Secure" | "SecureLocalhost" | "InsecureScheme" | "InsecureAncestor";

  /** Indicates whether the frame is cross-origin isolated and why it is the case. */
  export type CrossOriginIsolatedContextType = "Isolated" | "NotIsolated" | "NotIsolatedFeatureDisabled";

  export type GatedAPIFeatures = "SharedArrayBuffers" | "SharedArrayBuffersTransferAllowed" | "PerformanceMeasureMemory" | "PerformanceProfile";

  /**
   * All Permissions Policy features. This enum should match the one defined
   * in services/network/public/cpp/permissions_policy/permissions_policy_features.json5.
   * LINT.IfChange(PermissionsPolicyFeature)
   */
  export type PermissionsPolicyFeature = "accelerometer" | "all-screens-capture" | "ambient-light-sensor" | "aria-notify" | "attribution-reporting" | "autofill" | "autoplay" | "bluetooth" | "browsing-topics" | "camera" | "captured-surface-control" | "ch-dpr" | "ch-device-memory" | "ch-downlink" | "ch-ect" | "ch-prefers-color-scheme" | "ch-prefers-reduced-motion" | "ch-prefers-reduced-transparency" | "ch-rtt" | "ch-save-data" | "ch-ua" | "ch-ua-arch" | "ch-ua-bitness" | "ch-ua-high-entropy-values" | "ch-ua-platform" | "ch-ua-model" | "ch-ua-mobile" | "ch-ua-form-factors" | "ch-ua-full-version" | "ch-ua-full-version-list" | "ch-ua-platform-version" | "ch-ua-wow64" | "ch-viewport-height" | "ch-viewport-width" | "ch-width" | "clipboard-read" | "clipboard-write" | "compute-pressure" | "controlled-frame" | "cross-origin-isolated" | "deferred-fetch" | "deferred-fetch-minimal" | "device-attributes" | "digital-credentials-create" | "digital-credentials-get" | "direct-sockets" | "direct-sockets-multicast" | "direct-sockets-private" | "display-capture" | "document-domain" | "encrypted-media" | "execution-while-out-of-viewport" | "execution-while-not-rendered" | "focus-without-user-activation" | "fullscreen" | "frobulate" | "gamepad" | "geolocation" | "gyroscope" | "hid" | "identity-credentials-get" | "idle-detection" | "interest-cohort" | "join-ad-interest-group" | "keyboard-map" | "language-detector" | "language-model" | "local-fonts" | "local-network" | "local-network-access" | "loopback-network" | "magnetometer" | "manual-text" | "media-playback-while-not-visible" | "microphone" | "midi" | "on-device-speech-recognition" | "otp-credentials" | "payment" | "picture-in-picture" | "private-aggregation" | "private-state-token-issuance" | "private-state-token-redemption" | "publickey-credentials-create" | "publickey-credentials-get" | "record-ad-auction-events" | "rewriter" | "run-ad-auction" | "screen-wake-lock" | "serial" | "shared-storage" | "shared-storage-select-url" | "smart-card" | "speaker-selection" | "storage-access" | "sub-apps" | "summarizer" | "sync-xhr" | "translator" | "unload" | "usb" | "usb-unrestricted" | "vertical-scroll" | "web-app-installation" | "web-printing" | "web-share" | "window-management" | "writer" | "xr-spatial-tracking";

  /** Reason for a permissions policy feature to be disabled. */
  export type PermissionsPolicyBlockReason = "Header" | "IframeAttribute" | "InFencedFrameTree" | "InIsolatedApp";

  export interface PermissionsPolicyBlockLocator {
    frameId: Page.FrameId;
    blockReason: Page.PermissionsPolicyBlockReason;
  }

  export interface PermissionsPolicyFeatureState {
    feature: Page.PermissionsPolicyFeature;
    allowed: boolean;
    locator?: Page.PermissionsPolicyBlockLocator;
  }

  /**
   * Origin Trial(https://www.chromium.org/blink/origin-trials) support.
   * Status for an Origin Trial token.
   */
  export type OriginTrialTokenStatus = "Success" | "NotSupported" | "Insecure" | "Expired" | "WrongOrigin" | "InvalidSignature" | "Malformed" | "WrongVersion" | "FeatureDisabled" | "TokenDisabled" | "FeatureDisabledForUser" | "UnknownTrial";

  /** Status for an Origin Trial. */
  export type OriginTrialStatus = "Enabled" | "ValidTokenNotProvided" | "OSNotSupported" | "TrialNotAllowed";

  export type OriginTrialUsageRestriction = "None" | "Subset";

  export interface OriginTrialToken {
    origin: string;
    matchSubDomains: boolean;
    trialName: string;
    expiryTime: Network.TimeSinceEpoch;
    isThirdParty: boolean;
    usageRestriction: Page.OriginTrialUsageRestriction;
  }

  export interface OriginTrialTokenWithStatus {
    rawTokenText: string;
    /**
     * `parsedToken` is present only when the token is extractable and
     * parsable.
     */
    parsedToken?: Page.OriginTrialToken;
    status: Page.OriginTrialTokenStatus;
  }

  export interface OriginTrial {
    trialName: string;
    status: Page.OriginTrialStatus;
    tokensWithStatus: Page.OriginTrialTokenWithStatus[];
  }

  /** Additional information about the frame document's security origin. */
  export interface SecurityOriginDetails {
    /**
     * Indicates whether the frame document's security origin is one
     * of the local hostnames (e.g. "localhost") or IP addresses (IPv4
     * 127.0.0.0/8 or IPv6 ::1).
     */
    isLocalhost: boolean;
  }

  /** Information about the Frame on the page. */
  export interface Frame {
    /** Frame unique identifier. */
    id: Page.FrameId;
    /** Parent frame identifier. */
    parentId?: Page.FrameId;
    /** Identifier of the loader associated with this frame. */
    loaderId: Network.LoaderId;
    /** Frame's name as specified in the tag. */
    name?: string;
    /** Frame document's URL without fragment. */
    url: string;
    /** Frame document's URL fragment including the '#'. */
    urlFragment?: string;
    /**
     * Frame document's registered domain, taking the public suffixes list into account.
     * Extracted from the Frame's url.
     * Example URLs: http://www.google.com/file.html -> "google.com"
     *               http://a.b.co.uk/file.html      -> "b.co.uk"
     */
    domainAndRegistry: string;
    /** Frame document's security origin. */
    securityOrigin: string;
    /** Additional details about the frame document's security origin. */
    securityOriginDetails?: Page.SecurityOriginDetails;
    /** Frame document's mimeType as determined by the browser. */
    mimeType: string;
    /** If the frame failed to load, this contains the URL that could not be loaded. Note that unlike url above, this URL may contain a fragment. */
    unreachableUrl?: string;
    /** Indicates whether this frame was tagged as an ad and why. */
    adFrameStatus?: Page.AdFrameStatus;
    /** Indicates whether the main document is a secure context and explains why that is the case. */
    secureContextType: Page.SecureContextType;
    /** Indicates whether this is a cross origin isolated context. */
    crossOriginIsolatedContextType: Page.CrossOriginIsolatedContextType;
    /** Indicated which gated APIs / features are available. */
    gatedAPIFeatures: Page.GatedAPIFeatures[];
  }

  /** Information about the Resource on the page. */
  export interface FrameResource {
    /** Resource URL. */
    url: string;
    /** Type of this resource. */
    type: Network.ResourceType;
    /** Resource mimeType as determined by the browser. */
    mimeType: string;
    /** last-modified timestamp as reported by server. */
    lastModified?: Network.TimeSinceEpoch;
    /** Resource content size. */
    contentSize?: number;
    /** True if the resource failed to load. */
    failed?: boolean;
    /** True if the resource was canceled during loading. */
    canceled?: boolean;
  }

  /** Information about the Frame hierarchy along with their cached resources. */
  export interface FrameResourceTree {
    /** Frame information for this tree item. */
    frame: Page.Frame;
    /** Child frames. */
    childFrames?: Page.FrameResourceTree[];
    /** Information about frame resources. */
    resources: Page.FrameResource[];
  }

  /** Information about the Frame hierarchy. */
  export interface FrameTree {
    /** Frame information for this tree item. */
    frame: Page.Frame;
    /** Child frames. */
    childFrames?: Page.FrameTree[];
  }

  /** Unique script identifier. */
  export type ScriptIdentifier = string;

  /** Transition type. */
  export type TransitionType = "link" | "typed" | "address_bar" | "auto_bookmark" | "auto_subframe" | "manual_subframe" | "generated" | "auto_toplevel" | "form_submit" | "reload" | "keyword" | "keyword_generated" | "other";

  /** Navigation history entry. */
  export interface NavigationEntry {
    /** Unique id of the navigation history entry. */
    id: number;
    /** URL of the navigation history entry. */
    url: string;
    /** URL that the user typed in the url bar. */
    userTypedURL: string;
    /** Title of the navigation history entry. */
    title: string;
    /** Transition type. */
    transitionType: Page.TransitionType;
  }

  /** Screencast frame metadata. */
  export interface ScreencastFrameMetadata {
    /** Top offset in DIP. */
    offsetTop: number;
    /** Page scale factor. */
    pageScaleFactor: number;
    /** Device screen width in DIP. */
    deviceWidth: number;
    /** Device screen height in DIP. */
    deviceHeight: number;
    /** Position of horizontal scroll in CSS pixels. */
    scrollOffsetX: number;
    /** Position of vertical scroll in CSS pixels. */
    scrollOffsetY: number;
    /** Frame swap timestamp. */
    timestamp?: Network.TimeSinceEpoch;
  }

  /** Javascript dialog type. */
  export type DialogType = "alert" | "confirm" | "prompt" | "beforeunload";

  /** Error while paring app manifest. */
  export interface AppManifestError {
    /** Error message. */
    message: string;
    /** If critical, this is a non-recoverable parse error. */
    critical: number;
    /** Error line. */
    line: number;
    /** Error column. */
    column: number;
  }

  /** Parsed app manifest properties. */
  export interface AppManifestParsedProperties {
    /** Computed scope value */
    scope: string;
  }

  /** Layout viewport position and dimensions. */
  export interface LayoutViewport {
    /** Horizontal offset relative to the document (CSS pixels). */
    pageX: number;
    /** Vertical offset relative to the document (CSS pixels). */
    pageY: number;
    /** Width (CSS pixels), excludes scrollbar if present. */
    clientWidth: number;
    /** Height (CSS pixels), excludes scrollbar if present. */
    clientHeight: number;
  }

  /** Visual viewport position, dimensions, and scale. */
  export interface VisualViewport {
    /** Horizontal offset relative to the layout viewport (CSS pixels). */
    offsetX: number;
    /** Vertical offset relative to the layout viewport (CSS pixels). */
    offsetY: number;
    /** Horizontal offset relative to the document (CSS pixels). */
    pageX: number;
    /** Vertical offset relative to the document (CSS pixels). */
    pageY: number;
    /** Width (CSS pixels), excludes scrollbar if present. */
    clientWidth: number;
    /** Height (CSS pixels), excludes scrollbar if present. */
    clientHeight: number;
    /** Scale relative to the ideal viewport (size at width=device-width). */
    scale: number;
    /** Page zoom factor (CSS to device independent pixels ratio). */
    zoom?: number;
  }

  /** Viewport for capturing screenshot. */
  export interface Viewport {
    /** X offset in device independent pixels (dip). */
    x: number;
    /** Y offset in device independent pixels (dip). */
    y: number;
    /** Rectangle width in device independent pixels (dip). */
    width: number;
    /** Rectangle height in device independent pixels (dip). */
    height: number;
    /** Page scale factor. */
    scale: number;
  }

  /** Generic font families collection. */
  export interface FontFamilies {
    /** The standard font-family. */
    standard?: string;
    /** The fixed font-family. */
    fixed?: string;
    /** The serif font-family. */
    serif?: string;
    /** The sansSerif font-family. */
    sansSerif?: string;
    /** The cursive font-family. */
    cursive?: string;
    /** The fantasy font-family. */
    fantasy?: string;
    /** The math font-family. */
    math?: string;
  }

  /** Font families collection for a script. */
  export interface ScriptFontFamilies {
    /** Name of the script which these font families are defined for. */
    script: string;
    /** Generic font families collection for the script. */
    fontFamilies: Page.FontFamilies;
  }

  /** Default font sizes. */
  export interface FontSizes {
    /** Default standard font size. */
    standard?: number;
    /** Default fixed font size. */
    fixed?: number;
  }

  export type ClientNavigationReason = "anchorClick" | "formSubmissionGet" | "formSubmissionPost" | "httpHeaderRefresh" | "initialFrameNavigation" | "metaTagRefresh" | "other" | "pageBlockInterstitial" | "reload" | "scriptInitiated";

  export type ClientNavigationDisposition = "currentTab" | "newTab" | "newWindow" | "download";

  export interface InstallabilityErrorArgument {
    /** Argument name (e.g. name:'minimum-icon-size-in-pixels'). */
    name: string;
    /** Argument value (e.g. value:'64'). */
    value: string;
  }

  /** The installability error */
  export interface InstallabilityError {
    /** The error id (e.g. 'manifest-missing-suitable-icon'). */
    errorId: string;
    /** The list of error arguments (e.g. {name:'minimum-icon-size-in-pixels', value:'64'}). */
    errorArguments: Page.InstallabilityErrorArgument[];
  }

  /** The referring-policy used for the navigation. */
  export type ReferrerPolicy = "noReferrer" | "noReferrerWhenDowngrade" | "origin" | "originWhenCrossOrigin" | "sameOrigin" | "strictOrigin" | "strictOriginWhenCrossOrigin" | "unsafeUrl";

  /** Per-script compilation cache parameters for `Page.produceCompilationCache` */
  export interface CompilationCacheParams {
    /** The URL of the script to produce a compilation cache entry for. */
    url: string;
    /**
     * A hint to the backend whether eager compilation is recommended.
     * (the actual compilation mode used is upon backend discretion).
     */
    eager?: boolean;
  }

  export interface FileFilter {
    name?: string;
    accepts?: string[];
  }

  export interface FileHandler {
    action: string;
    name: string;
    icons?: Page.ImageResource[];
    /** Mimic a map, name is the key, accepts is the value. */
    accepts?: Page.FileFilter[];
    /**
     * Won't repeat the enums, using string for easy comparison. Same as the
     * other enums below.
     */
    launchType: string;
  }

  /** The image definition used in both icon and screenshot. */
  export interface ImageResource {
    /**
     * The src field in the definition, but changing to url in favor of
     * consistency.
     */
    url: string;
    sizes?: string;
    type?: string;
  }

  export interface LaunchHandler {
    clientMode: string;
  }

  export interface ProtocolHandler {
    protocol: string;
    url: string;
  }

  export interface RelatedApplication {
    id?: string;
    url: string;
  }

  export interface ScopeExtension {
    /**
     * Instead of using tuple, this field always returns the serialized string
     * for easy understanding and comparison.
     */
    origin: string;
    hasOriginWildcard: boolean;
  }

  export interface Screenshot {
    image: Page.ImageResource;
    formFactor: string;
    label?: string;
  }

  export interface ShareTarget {
    action: string;
    method: string;
    enctype: string;
    /** Embed the ShareTargetParams */
    title?: string;
    text?: string;
    url?: string;
    files?: Page.FileFilter[];
  }

  export interface Shortcut {
    name: string;
    url: string;
  }

  export interface WebAppManifest {
    backgroundColor?: string;
    /** The extra description provided by the manifest. */
    description?: string;
    dir?: string;
    display?: string;
    /** The overrided display mode controlled by the user. */
    displayOverrides?: string[];
    /** The handlers to open files. */
    fileHandlers?: Page.FileHandler[];
    icons?: Page.ImageResource[];
    id?: string;
    lang?: string;
    /**
     * TODO(crbug.com/1231886): This field is non-standard and part of a Chrome
     * experiment. See:
     * https://github.com/WICG/web-app-launch/blob/main/launch_handler.md
     */
    launchHandler?: Page.LaunchHandler;
    name?: string;
    orientation?: string;
    preferRelatedApplications?: boolean;
    /** The handlers to open protocols. */
    protocolHandlers?: Page.ProtocolHandler[];
    relatedApplications?: Page.RelatedApplication[];
    scope?: string;
    /**
     * Non-standard, see
     * https://github.com/WICG/manifest-incubations/blob/gh-pages/scope_extensions-explainer.md
     */
    scopeExtensions?: Page.ScopeExtension[];
    /** The screenshots used by chromium. */
    screenshots?: Page.Screenshot[];
    shareTarget?: Page.ShareTarget;
    shortName?: string;
    shortcuts?: Page.Shortcut[];
    startUrl?: string;
    themeColor?: string;
  }

  /** The type of a frameNavigated event. */
  export type NavigationType = "Navigation" | "BackForwardCacheRestore";

  /** List of not restored reasons for back-forward cache. */
  export type BackForwardCacheNotRestoredReason = "NotPrimaryMainFrame" | "BackForwardCacheDisabled" | "RelatedActiveContentsExist" | "HTTPStatusNotOK" | "SchemeNotHTTPOrHTTPS" | "Loading" | "WasGrantedMediaAccess" | "DisableForRenderFrameHostCalled" | "DomainNotAllowed" | "HTTPMethodNotGET" | "SubframeIsNavigating" | "Timeout" | "CacheLimit" | "JavaScriptExecution" | "RendererProcessKilled" | "RendererProcessCrashed" | "SchedulerTrackedFeatureUsed" | "ConflictingBrowsingInstance" | "CacheFlushed" | "ServiceWorkerVersionActivation" | "SessionRestored" | "ServiceWorkerPostMessage" | "EnteredBackForwardCacheBeforeServiceWorkerHostAdded" | "RenderFrameHostReused_SameSite" | "RenderFrameHostReused_CrossSite" | "ServiceWorkerClaim" | "IgnoreEventAndEvict" | "HaveInnerContents" | "TimeoutPuttingInCache" | "BackForwardCacheDisabledByLowMemory" | "BackForwardCacheDisabledByCommandLine" | "NetworkRequestDatapipeDrainedAsBytesConsumer" | "NetworkRequestRedirected" | "NetworkRequestTimeout" | "NetworkExceedsBufferLimit" | "NavigationCancelledWhileRestoring" | "NotMostRecentNavigationEntry" | "BackForwardCacheDisabledForPrerender" | "UserAgentOverrideDiffers" | "ForegroundCacheLimit" | "ForwardCacheDisabled" | "BrowsingInstanceNotSwapped" | "BackForwardCacheDisabledForDelegate" | "UnloadHandlerExistsInMainFrame" | "UnloadHandlerExistsInSubFrame" | "ServiceWorkerUnregistration" | "CacheControlNoStore" | "CacheControlNoStoreCookieModified" | "CacheControlNoStoreHTTPOnlyCookieModified" | "NoResponseHead" | "Unknown" | "ActivationNavigationsDisallowedForBug1234857" | "ErrorDocument" | "FencedFramesEmbedder" | "CookieDisabled" | "HTTPAuthRequired" | "CookieFlushed" | "BroadcastChannelOnMessage" | "WebViewSettingsChanged" | "WebViewJavaScriptObjectChanged" | "WebViewMessageListenerInjected" | "WebViewSafeBrowsingAllowlistChanged" | "WebViewDocumentStartJavascriptChanged" | "WebSocket" | "WebTransport" | "WebRTC" | "MainResourceHasCacheControlNoStore" | "MainResourceHasCacheControlNoCache" | "SubresourceHasCacheControlNoStore" | "SubresourceHasCacheControlNoCache" | "ContainsPlugins" | "DocumentLoaded" | "OutstandingNetworkRequestOthers" | "RequestedMIDIPermission" | "RequestedAudioCapturePermission" | "RequestedVideoCapturePermission" | "RequestedBackForwardCacheBlockedSensors" | "RequestedBackgroundWorkPermission" | "BroadcastChannel" | "WebXR" | "SharedWorker" | "SharedWorkerMessage" | "SharedWorkerWithNoActiveClient" | "WebLocks" | "WebLocksContention" | "WebHID" | "WebBluetooth" | "WebShare" | "RequestedStorageAccessGrant" | "WebNfc" | "OutstandingNetworkRequestFetch" | "OutstandingNetworkRequestXHR" | "AppBanner" | "Printing" | "WebDatabase" | "PictureInPicture" | "SpeechRecognizer" | "IdleManager" | "PaymentManager" | "SpeechSynthesis" | "KeyboardLock" | "WebOTPService" | "OutstandingNetworkRequestDirectSocket" | "InjectedJavascript" | "InjectedStyleSheet" | "KeepaliveRequest" | "IndexedDBEvent" | "Dummy" | "JsNetworkRequestReceivedCacheControlNoStoreResource" | "WebRTCUsedWithCCNS" | "WebTransportUsedWithCCNS" | "WebSocketUsedWithCCNS" | "SmartCard" | "LiveMediaStreamTrack" | "UnloadHandler" | "ParserAborted" | "ContentSecurityHandler" | "ContentWebAuthenticationAPI" | "ContentFileChooser" | "ContentSerial" | "ContentFileSystemAccess" | "ContentMediaDevicesDispatcherHost" | "ContentWebBluetooth" | "ContentWebUSB" | "ContentMediaSessionService" | "ContentScreenReader" | "ContentDiscarded" | "EmbedderPopupBlockerTabHelper" | "EmbedderSafeBrowsingTriggeredPopupBlocker" | "EmbedderSafeBrowsingThreatDetails" | "EmbedderAppBannerManager" | "EmbedderDomDistillerViewerSource" | "EmbedderDomDistillerSelfDeletingRequestDelegate" | "EmbedderOomInterventionTabHelper" | "EmbedderOfflinePage" | "EmbedderChromePasswordManagerClientBindCredentialManager" | "EmbedderPermissionRequestManager" | "EmbedderModalDialog" | "EmbedderExtensions" | "EmbedderExtensionMessaging" | "EmbedderExtensionMessagingForOpenPort" | "EmbedderExtensionSentMessageToCachedFrame" | "RequestedByWebViewClient" | "PostMessageByWebViewClient" | "CacheControlNoStoreDeviceBoundSessionTerminated" | "CacheLimitPrunedOnModerateMemoryPressure" | "CacheLimitPrunedOnCriticalMemoryPressure";

  /** Types of not restored reasons for back-forward cache. */
  export type BackForwardCacheNotRestoredReasonType = "SupportPending" | "PageSupportNeeded" | "Circumstantial";

  export interface BackForwardCacheBlockingDetails {
    /** Url of the file where blockage happened. Optional because of tests. */
    url?: string;
    /** Function name where blockage happened. Optional because of anonymous functions and tests. */
    "function"?: string;
    /** Line number in the script (0-based). */
    lineNumber: number;
    /** Column number in the script (0-based). */
    columnNumber: number;
  }

  export interface BackForwardCacheNotRestoredExplanation {
    /** Type of the reason */
    type: Page.BackForwardCacheNotRestoredReasonType;
    /** Not restored reason */
    reason: Page.BackForwardCacheNotRestoredReason;
    /**
     * Context associated with the reason. The meaning of this context is
     * dependent on the reason:
     * - EmbedderExtensionSentMessageToCachedFrame: the extension ID.
     */
    context?: string;
    details?: Page.BackForwardCacheBlockingDetails[];
  }

  export interface BackForwardCacheNotRestoredExplanationTree {
    /** URL of each frame */
    url: string;
    /** Not restored reasons of each frame */
    explanations: Page.BackForwardCacheNotRestoredExplanation[];
    /** Array of children frame */
    children: Page.BackForwardCacheNotRestoredExplanationTree[];
  }

}

export namespace Performance {

  /** Run-time execution metric. */
  export interface Metric {
    /** Metric name. */
    name: string;
    /** Metric value. */
    value: number;
  }

}

export namespace PerformanceTimeline {

  /** See https://github.com/WICG/LargestContentfulPaint and largest_contentful_paint.idl */
  export interface LargestContentfulPaint {
    renderTime: Network.TimeSinceEpoch;
    loadTime: Network.TimeSinceEpoch;
    /** The number of pixels being painted. */
    size: number;
    /** The id attribute of the element, if available. */
    elementId?: string;
    /** The URL of the image (may be trimmed). */
    url?: string;
    nodeId?: DOM.BackendNodeId;
  }

  export interface LayoutShiftAttribution {
    previousRect: DOM.Rect;
    currentRect: DOM.Rect;
    nodeId?: DOM.BackendNodeId;
  }

  /** See https://wicg.github.io/layout-instability/#sec-layout-shift and layout_shift.idl */
  export interface LayoutShift {
    /** Score increment produced by this event. */
    value: number;
    hadRecentInput: boolean;
    lastInputTime: Network.TimeSinceEpoch;
    sources: PerformanceTimeline.LayoutShiftAttribution[];
  }

  export interface TimelineEvent {
    /** Identifies the frame that this event is related to. Empty for non-frame targets. */
    frameId: Page.FrameId;
    /**
     * The event type, as specified in https://w3c.github.io/performance-timeline/#dom-performanceentry-entrytype
     * This determines which of the optional "details" fields is present.
     */
    type: string;
    /** Name may be empty depending on the type. */
    name: string;
    /** Time in seconds since Epoch, monotonically increasing within document lifetime. */
    time: Network.TimeSinceEpoch;
    /** Event duration, if applicable. */
    duration?: number;
    lcpDetails?: PerformanceTimeline.LargestContentfulPaint;
    layoutShiftDetails?: PerformanceTimeline.LayoutShift;
  }

}

export namespace Preload {

  /** Unique id */
  export type RuleSetId = string;

  /** Corresponds to SpeculationRuleSet */
  export interface RuleSet {
    id: Preload.RuleSetId;
    /** Identifies a document which the rule set is associated with. */
    loaderId: Network.LoaderId;
    /**
     * Source text of JSON representing the rule set. If it comes from
     * `<script>` tag, it is the textContent of the node. Note that it is
     * a JSON for valid case.
     *
     * See also:
     * - https://wicg.github.io/nav-speculation/speculation-rules.html
     * - https://github.com/WICG/nav-speculation/blob/main/triggers.md
     */
    sourceText: string;
    /**
     * A speculation rule set is either added through an inline
     * `<script>` tag or through an external resource via the
     * 'Speculation-Rules' HTTP header. For the first case, we include
     * the BackendNodeId of the relevant `<script>` tag. For the second
     * case, we include the external URL where the rule set was loaded
     * from, and also RequestId if Network domain is enabled.
     *
     * See also:
     * - https://wicg.github.io/nav-speculation/speculation-rules.html#speculation-rules-script
     * - https://wicg.github.io/nav-speculation/speculation-rules.html#speculation-rules-header
     */
    backendNodeId?: DOM.BackendNodeId;
    url?: string;
    requestId?: Network.RequestId;
    /**
     * Error information
     * `errorMessage` is null iff `errorType` is null.
     */
    errorType?: Preload.RuleSetErrorType;
    /** TODO(https://crbug.com/1425354): Replace this property with structured error. */
    errorMessage?: string;
    /**
     * For more details, see:
     * https://github.com/WICG/nav-speculation/blob/main/speculation-rules-tags.md
     */
    tag?: string;
  }

  export type RuleSetErrorType = "SourceIsNotJsonObject" | "InvalidRulesSkipped" | "InvalidRulesetLevelTag";

  /**
   * The type of preloading attempted. It corresponds to
   * mojom::SpeculationAction (although PrefetchWithSubresources is omitted as it
   * isn't being used by clients).
   */
  export type SpeculationAction = "Prefetch" | "Prerender" | "PrerenderUntilScript";

  /**
   * Corresponds to mojom::SpeculationTargetHint.
   * See https://github.com/WICG/nav-speculation/blob/main/triggers.md#window-name-targeting-hints
   */
  export type SpeculationTargetHint = "Blank" | "Self";

  /**
   * A key that identifies a preloading attempt.
   *
   * The url used is the url specified by the trigger (i.e. the initial URL), and
   * not the final url that is navigated to. For example, prerendering allows
   * same-origin main frame navigations during the attempt, but the attempt is
   * still keyed with the initial URL.
   */
  export interface PreloadingAttemptKey {
    loaderId: Network.LoaderId;
    action: Preload.SpeculationAction;
    url: string;
    formSubmission?: boolean;
    targetHint?: Preload.SpeculationTargetHint;
  }

  /**
   * Lists sources for a preloading attempt, specifically the ids of rule sets
   * that had a speculation rule that triggered the attempt, and the
   * BackendNodeIds of <a href> or <area href> elements that triggered the
   * attempt (in the case of attempts triggered by a document rule). It is
   * possible for multiple rule sets and links to trigger a single attempt.
   */
  export interface PreloadingAttemptSource {
    key: Preload.PreloadingAttemptKey;
    ruleSetIds: Preload.RuleSetId[];
    nodeIds: DOM.BackendNodeId[];
  }

  /**
   * Chrome manages different types of preloads together using a
   * concept of preloading pipeline. For example, if a site uses a
   * SpeculationRules for prerender, Chrome first starts a prefetch and
   * then upgrades it to prerender.
   *
   * CDP events for them are emitted separately but they share
   * `PreloadPipelineId`.
   */
  export type PreloadPipelineId = string;

  /** List of FinalStatus reasons for Prerender2. */
  export type PrerenderFinalStatus = "Activated" | "Destroyed" | "LowEndDevice" | "InvalidSchemeRedirect" | "InvalidSchemeNavigation" | "NavigationRequestBlockedByCsp" | "MojoBinderPolicy" | "RendererProcessCrashed" | "RendererProcessKilled" | "Download" | "TriggerDestroyed" | "NavigationNotCommitted" | "NavigationBadHttpStatus" | "ClientCertRequested" | "NavigationRequestNetworkError" | "CancelAllHostsForTesting" | "DidFailLoad" | "Stop" | "SslCertificateError" | "LoginAuthRequested" | "UaChangeRequiresReload" | "BlockedByClient" | "AudioOutputDeviceRequested" | "MixedContent" | "TriggerBackgrounded" | "MemoryLimitExceeded" | "DataSaverEnabled" | "TriggerUrlHasEffectiveUrl" | "ActivatedBeforeStarted" | "InactivePageRestriction" | "StartFailed" | "TimeoutBackgrounded" | "CrossSiteRedirectInInitialNavigation" | "CrossSiteNavigationInInitialNavigation" | "SameSiteCrossOriginRedirectNotOptInInInitialNavigation" | "SameSiteCrossOriginNavigationNotOptInInInitialNavigation" | "ActivationNavigationParameterMismatch" | "ActivatedInBackground" | "EmbedderHostDisallowed" | "ActivationNavigationDestroyedBeforeSuccess" | "TabClosedByUserGesture" | "TabClosedWithoutUserGesture" | "PrimaryMainFrameRendererProcessCrashed" | "PrimaryMainFrameRendererProcessKilled" | "ActivationFramePolicyNotCompatible" | "PreloadingDisabled" | "BatterySaverEnabled" | "ActivatedDuringMainFrameNavigation" | "PreloadingUnsupportedByWebContents" | "CrossSiteRedirectInMainFrameNavigation" | "CrossSiteNavigationInMainFrameNavigation" | "SameSiteCrossOriginRedirectNotOptInInMainFrameNavigation" | "SameSiteCrossOriginNavigationNotOptInInMainFrameNavigation" | "MemoryPressureOnTrigger" | "MemoryPressureAfterTriggered" | "PrerenderingDisabledByDevTools" | "SpeculationRuleRemoved" | "ActivatedWithAuxiliaryBrowsingContexts" | "MaxNumOfRunningEagerPrerendersExceeded" | "MaxNumOfRunningNonEagerPrerendersExceeded" | "MaxNumOfRunningEmbedderPrerendersExceeded" | "PrerenderingUrlHasEffectiveUrl" | "RedirectedPrerenderingUrlHasEffectiveUrl" | "ActivationUrlHasEffectiveUrl" | "JavaScriptInterfaceAdded" | "JavaScriptInterfaceRemoved" | "AllPrerenderingCanceled" | "WindowClosed" | "SlowNetwork" | "OtherPrerenderedPageActivated" | "V8OptimizerDisabled" | "PrerenderFailedDuringPrefetch" | "BrowsingDataRemoved" | "PrerenderHostReused" | "FormSubmitWhenPrerendering";

  /**
   * Preloading status values, see also PreloadingTriggeringOutcome. This
   * status is shared by prefetchStatusUpdated and prerenderStatusUpdated.
   */
  export type PreloadingStatus = "Pending" | "Running" | "Ready" | "Success" | "Failure" | "NotSupported";

  /**
   * TODO(https://crbug.com/1384419): revisit the list of PrefetchStatus and
   * filter out the ones that aren't necessary to the developers.
   */
  export type PrefetchStatus = "PrefetchAllowed" | "PrefetchFailedIneligibleRedirect" | "PrefetchFailedInvalidRedirect" | "PrefetchFailedMIMENotSupported" | "PrefetchFailedNetError" | "PrefetchFailedNon2XX" | "PrefetchEvictedAfterBrowsingDataRemoved" | "PrefetchEvictedAfterCandidateRemoved" | "PrefetchEvictedForNewerPrefetch" | "PrefetchHeldback" | "PrefetchIneligibleRetryAfter" | "PrefetchIsPrivacyDecoy" | "PrefetchIsStale" | "PrefetchNotEligibleBrowserContextOffTheRecord" | "PrefetchNotEligibleDataSaverEnabled" | "PrefetchNotEligibleExistingProxy" | "PrefetchNotEligibleHostIsNonUnique" | "PrefetchNotEligibleNonDefaultStoragePartition" | "PrefetchNotEligibleSameSiteCrossOriginPrefetchRequiredProxy" | "PrefetchNotEligibleSchemeIsNotHttps" | "PrefetchNotEligibleUserHasCookies" | "PrefetchNotEligibleUserHasServiceWorker" | "PrefetchNotEligibleUserHasServiceWorkerNoFetchHandler" | "PrefetchNotEligibleRedirectFromServiceWorker" | "PrefetchNotEligibleRedirectToServiceWorker" | "PrefetchNotEligibleBatterySaverEnabled" | "PrefetchNotEligiblePreloadingDisabled" | "PrefetchNotFinishedInTime" | "PrefetchNotStarted" | "PrefetchNotUsedCookiesChanged" | "PrefetchProxyNotAvailable" | "PrefetchResponseUsed" | "PrefetchSuccessfulButNotUsed" | "PrefetchNotUsedProbeFailed";

  /** Information of headers to be displayed when the header mismatch occurred. */
  export interface PrerenderMismatchedHeaders {
    headerName: string;
    initialValue?: string;
    activationValue?: string;
  }

}

export namespace Profiler {

  /** Profile node. Holds callsite information, execution statistics and child nodes. */
  export interface ProfileNode {
    /** Unique id of the node. */
    id: number;
    /** Function location. */
    callFrame: Runtime.CallFrame;
    /** Number of samples where this node was on top of the call stack. */
    hitCount?: number;
    /** Child node ids. */
    children?: number[];
    /**
     * The reason of being not optimized. The function may be deoptimized or marked as don't
     * optimize.
     */
    deoptReason?: string;
    /** An array of source position ticks. */
    positionTicks?: Profiler.PositionTickInfo[];
  }

  /** Profile. */
  export interface Profile {
    /** The list of profile nodes. First item is the root node. */
    nodes: Profiler.ProfileNode[];
    /** Profiling start timestamp in microseconds. */
    startTime: number;
    /** Profiling end timestamp in microseconds. */
    endTime: number;
    /** Ids of samples top nodes. */
    samples?: number[];
    /**
     * Time intervals between adjacent samples in microseconds. The first delta is relative to the
     * profile startTime.
     */
    timeDeltas?: number[];
  }

  /** Specifies a number of samples attributed to a certain source position. */
  export interface PositionTickInfo {
    /** Source line number (1-based). */
    line: number;
    /** Number of samples attributed to the source line. */
    ticks: number;
  }

  /** Coverage data for a source range. */
  export interface CoverageRange {
    /** JavaScript script source offset for the range start. */
    startOffset: number;
    /** JavaScript script source offset for the range end. */
    endOffset: number;
    /** Collected execution count of the source range. */
    count: number;
  }

  /** Coverage data for a JavaScript function. */
  export interface FunctionCoverage {
    /** JavaScript function name. */
    functionName: string;
    /** Source ranges inside the function with coverage data. */
    ranges: Profiler.CoverageRange[];
    /** Whether coverage data for this function has block granularity. */
    isBlockCoverage: boolean;
  }

  /** Coverage data for a JavaScript script. */
  export interface ScriptCoverage {
    /** JavaScript script id. */
    scriptId: Runtime.ScriptId;
    /** JavaScript script name or url. */
    url: string;
    /** Functions contained in the script that has coverage data. */
    functions: Profiler.FunctionCoverage[];
  }

}

export namespace PWA {

  /**
   * The following types are the replica of
   * https://crsrc.org/c/chrome/browser/web_applications/proto/web_app_os_integration_state.proto;drc=9910d3be894c8f142c977ba1023f30a656bc13fc;l=67
   */
  export interface FileHandlerAccept {
    /**
     * New name of the mimetype according to
     * https://www.iana.org/assignments/media-types/media-types.xhtml
     */
    mediaType: string;
    fileExtensions: string[];
  }

  export interface FileHandler {
    action: string;
    accepts: PWA.FileHandlerAccept[];
    displayName: string;
  }

  /** If user prefers opening the app in browser or an app window. */
  export type DisplayMode = "standalone" | "browser";

}

export namespace Runtime {

  /** Unique script identifier. */
  export type ScriptId = string;

  /** Represents options for serialization. Overrides `generatePreview` and `returnByValue`. */
  export interface SerializationOptions {
    serialization: "deep" | "json" | "idOnly";
    /** Deep serialization depth. Default is full depth. Respected only in `deep` serialization mode. */
    maxDepth?: number;
    /**
     * Embedder-specific parameters. For example if connected to V8 in Chrome these control DOM
     * serialization via `maxNodeDepth: integer` and `includeShadowTree: "none" | "open" | "all"`.
     * Values can be only of type string or integer.
     */
    additionalParameters?: Record<string, unknown>;
  }

  /** Represents deep serialized value. */
  export interface DeepSerializedValue {
    type: "undefined" | "null" | "string" | "number" | "boolean" | "bigint" | "regexp" | "date" | "symbol" | "array" | "object" | "function" | "map" | "set" | "weakmap" | "weakset" | "error" | "proxy" | "promise" | "typedarray" | "arraybuffer" | "node" | "window" | "generator";
    value?: unknown;
    objectId?: string;
    /**
     * Set if value reference met more then once during serialization. In such
     * case, value is provided only to one of the serialized values. Unique
     * per value in the scope of one CDP call.
     */
    weakLocalObjectReference?: number;
  }

  /** Unique object identifier. */
  export type RemoteObjectId = string;

  /**
   * Primitive value which cannot be JSON-stringified. Includes values `-0`, `NaN`, `Infinity`,
   * `-Infinity`, and bigint literals.
   */
  export type UnserializableValue = string;

  /** Mirror object referencing original JavaScript object. */
  export interface RemoteObject {
    /** Object type. */
    type: "object" | "function" | "undefined" | "string" | "number" | "boolean" | "symbol" | "bigint";
    /**
     * Object subtype hint. Specified for `object` type values only.
     * NOTE: If you change anything here, make sure to also update
     * `subtype` in `ObjectPreview` and `PropertyPreview` below.
     */
    subtype?: "array" | "null" | "node" | "regexp" | "date" | "map" | "set" | "weakmap" | "weakset" | "iterator" | "generator" | "error" | "proxy" | "promise" | "typedarray" | "arraybuffer" | "dataview" | "webassemblymemory" | "wasmvalue" | "trustedtype";
    /** Object class (constructor) name. Specified for `object` type values only. */
    className?: string;
    /** Remote object value in case of primitive values or JSON values (if it was requested). */
    value?: unknown;
    /**
     * Primitive value which can not be JSON-stringified does not have `value`, but gets this
     * property.
     */
    unserializableValue?: Runtime.UnserializableValue;
    /** String representation of the object. */
    description?: string;
    /** Deep serialized value. */
    deepSerializedValue?: Runtime.DeepSerializedValue;
    /** Unique object identifier (for non-primitive values). */
    objectId?: Runtime.RemoteObjectId;
    /** Preview containing abbreviated property values. Specified for `object` type values only. */
    preview?: Runtime.ObjectPreview;
    customPreview?: Runtime.CustomPreview;
  }

  export interface CustomPreview {
    /**
     * The JSON-stringified result of formatter.header(object, config) call.
     * It contains json ML array that represents RemoteObject.
     */
    header: string;
    /**
     * If formatter returns true as a result of formatter.hasBody call then bodyGetterId will
     * contain RemoteObjectId for the function that returns result of formatter.body(object, config) call.
     * The result value is json ML array.
     */
    bodyGetterId?: Runtime.RemoteObjectId;
  }

  /** Object containing abbreviated remote object value. */
  export interface ObjectPreview {
    /** Object type. */
    type: "object" | "function" | "undefined" | "string" | "number" | "boolean" | "symbol" | "bigint";
    /** Object subtype hint. Specified for `object` type values only. */
    subtype?: "array" | "null" | "node" | "regexp" | "date" | "map" | "set" | "weakmap" | "weakset" | "iterator" | "generator" | "error" | "proxy" | "promise" | "typedarray" | "arraybuffer" | "dataview" | "webassemblymemory" | "wasmvalue" | "trustedtype";
    /** String representation of the object. */
    description?: string;
    /** True iff some of the properties or entries of the original object did not fit. */
    overflow: boolean;
    /** List of the properties. */
    properties: Runtime.PropertyPreview[];
    /** List of the entries. Specified for `map` and `set` subtype values only. */
    entries?: Runtime.EntryPreview[];
  }

  export interface PropertyPreview {
    /** Property name. */
    name: string;
    /** Object type. Accessor means that the property itself is an accessor property. */
    type: "object" | "function" | "undefined" | "string" | "number" | "boolean" | "symbol" | "accessor" | "bigint";
    /** User-friendly property value string. */
    value?: string;
    /** Nested value preview. */
    valuePreview?: Runtime.ObjectPreview;
    /** Object subtype hint. Specified for `object` type values only. */
    subtype?: "array" | "null" | "node" | "regexp" | "date" | "map" | "set" | "weakmap" | "weakset" | "iterator" | "generator" | "error" | "proxy" | "promise" | "typedarray" | "arraybuffer" | "dataview" | "webassemblymemory" | "wasmvalue" | "trustedtype";
  }

  export interface EntryPreview {
    /** Preview of the key. Specified for map-like collection entries. */
    key?: Runtime.ObjectPreview;
    /** Preview of the value. */
    value: Runtime.ObjectPreview;
  }

  /** Object property descriptor. */
  export interface PropertyDescriptor {
    /** Property name or symbol description. */
    name: string;
    /** The value associated with the property. */
    value?: Runtime.RemoteObject;
    /** True if the value associated with the property may be changed (data descriptors only). */
    writable?: boolean;
    /**
     * A function which serves as a getter for the property, or `undefined` if there is no getter
     * (accessor descriptors only).
     */
    get?: Runtime.RemoteObject;
    /**
     * A function which serves as a setter for the property, or `undefined` if there is no setter
     * (accessor descriptors only).
     */
    set?: Runtime.RemoteObject;
    /**
     * True if the type of this property descriptor may be changed and if the property may be
     * deleted from the corresponding object.
     */
    configurable: boolean;
    /**
     * True if this property shows up during enumeration of the properties on the corresponding
     * object.
     */
    enumerable: boolean;
    /** True if the result was thrown during the evaluation. */
    wasThrown?: boolean;
    /** True if the property is owned for the object. */
    isOwn?: boolean;
    /** Property symbol object, if the property is of the `symbol` type. */
    symbol?: Runtime.RemoteObject;
  }

  /** Object internal property descriptor. This property isn't normally visible in JavaScript code. */
  export interface InternalPropertyDescriptor {
    /** Conventional property name. */
    name: string;
    /** The value associated with the property. */
    value?: Runtime.RemoteObject;
  }

  /** Object private field descriptor. */
  export interface PrivatePropertyDescriptor {
    /** Private property name. */
    name: string;
    /** The value associated with the private property. */
    value?: Runtime.RemoteObject;
    /**
     * A function which serves as a getter for the private property,
     * or `undefined` if there is no getter (accessor descriptors only).
     */
    get?: Runtime.RemoteObject;
    /**
     * A function which serves as a setter for the private property,
     * or `undefined` if there is no setter (accessor descriptors only).
     */
    set?: Runtime.RemoteObject;
  }

  /**
   * Represents function call argument. Either remote object id `objectId`, primitive `value`,
   * unserializable primitive value or neither of (for undefined) them should be specified.
   */
  export interface CallArgument {
    /** Primitive value or serializable javascript object. */
    value?: unknown;
    /** Primitive value which can not be JSON-stringified. */
    unserializableValue?: Runtime.UnserializableValue;
    /** Remote object handle. */
    objectId?: Runtime.RemoteObjectId;
  }

  /** Id of an execution context. */
  export type ExecutionContextId = number;

  /** Description of an isolated world. */
  export interface ExecutionContextDescription {
    /**
     * Unique id of the execution context. It can be used to specify in which execution context
     * script evaluation should be performed.
     */
    id: Runtime.ExecutionContextId;
    /** Execution context origin. */
    origin: string;
    /** Human readable name describing given context. */
    name: string;
    /**
     * A system-unique execution context identifier. Unlike the id, this is unique across
     * multiple processes, so can be reliably used to identify specific context while backend
     * performs a cross-process navigation.
     */
    uniqueId: string;
    /** Embedder-specific auxiliary data likely matching {isDefault: boolean, type: 'default'|'isolated'|'worker', frameId: string} */
    auxData?: Record<string, unknown>;
  }

  /**
   * Detailed information about exception (or error) that was thrown during script compilation or
   * execution.
   */
  export interface ExceptionDetails {
    /** Exception id. */
    exceptionId: number;
    /** Exception text, which should be used together with exception object when available. */
    text: string;
    /** Line number of the exception location (0-based). */
    lineNumber: number;
    /** Column number of the exception location (0-based). */
    columnNumber: number;
    /** Script ID of the exception location. */
    scriptId?: Runtime.ScriptId;
    /** URL of the exception location, to be used when the script was not reported. */
    url?: string;
    /** JavaScript stack trace if available. */
    stackTrace?: Runtime.StackTrace;
    /** Exception object if available. */
    exception?: Runtime.RemoteObject;
    /** Identifier of the context where exception happened. */
    executionContextId?: Runtime.ExecutionContextId;
    /**
     * Dictionary with entries of meta data that the client associated
     * with this exception, such as information about associated network
     * requests, etc.
     */
    exceptionMetaData?: Record<string, unknown>;
  }

  /** Number of milliseconds since epoch. */
  export type Timestamp = number;

  /** Number of milliseconds. */
  export type TimeDelta = number;

  /** Stack entry for runtime errors and assertions. */
  export interface CallFrame {
    /** JavaScript function name. */
    functionName: string;
    /** JavaScript script id. */
    scriptId: Runtime.ScriptId;
    /** JavaScript script name or url. */
    url: string;
    /** JavaScript script line number (0-based). */
    lineNumber: number;
    /** JavaScript script column number (0-based). */
    columnNumber: number;
  }

  /** Call frames for assertions or error messages. */
  export interface StackTrace {
    /**
     * String label of this stack trace. For async traces this may be a name of the function that
     * initiated the async call.
     */
    description?: string;
    /** JavaScript function name. */
    callFrames: Runtime.CallFrame[];
    /** Asynchronous JavaScript stack trace that preceded this stack, if available. */
    parent?: Runtime.StackTrace;
    /** Asynchronous JavaScript stack trace that preceded this stack, if available. */
    parentId?: Runtime.StackTraceId;
  }

  /** Unique identifier of current debugger. */
  export type UniqueDebuggerId = string;

  /**
   * If `debuggerId` is set stack trace comes from another debugger and can be resolved there. This
   * allows to track cross-debugger calls. See `Runtime.StackTrace` and `Debugger.paused` for usages.
   */
  export interface StackTraceId {
    id: string;
    debuggerId?: Runtime.UniqueDebuggerId;
  }

}

export namespace Schema {

  /** Description of the protocol domain. */
  export interface Domain {
    /** Domain name. */
    name: string;
    /** Domain version. */
    version: string;
  }

}

export namespace Security {

  /** An internal certificate ID value. */
  export type CertificateId = number;

  /**
   * A description of mixed content (HTTP resources on HTTPS pages), as defined by
   * https://www.w3.org/TR/mixed-content/#categories
   */
  export type MixedContentType = "blockable" | "optionally-blockable" | "none";

  /** The security level of a page or resource. */
  export type SecurityState = "unknown" | "neutral" | "insecure" | "secure" | "info" | "insecure-broken";

  /** Details about the security state of the page certificate. */
  export interface CertificateSecurityState {
    /** Protocol name (e.g. "TLS 1.2" or "QUIC"). */
    protocol: string;
    /** Key Exchange used by the connection, or the empty string if not applicable. */
    keyExchange: string;
    /** (EC)DH group used by the connection, if applicable. */
    keyExchangeGroup?: string;
    /** Cipher name. */
    cipher: string;
    /** TLS MAC. Note that AEAD ciphers do not have separate MACs. */
    mac?: string;
    /** Page certificate. */
    certificate: string[];
    /** Certificate subject name. */
    subjectName: string;
    /** Name of the issuing CA. */
    issuer: string;
    /** Certificate valid from date. */
    validFrom: Network.TimeSinceEpoch;
    /** Certificate valid to (expiration) date */
    validTo: Network.TimeSinceEpoch;
    /** The highest priority network error code, if the certificate has an error. */
    certificateNetworkError?: string;
    /** True if the certificate uses a weak signature algorithm. */
    certificateHasWeakSignature: boolean;
    /** True if the certificate has a SHA1 signature in the chain. */
    certificateHasSha1Signature: boolean;
    /** True if modern SSL */
    modernSSL: boolean;
    /** True if the connection is using an obsolete SSL protocol. */
    obsoleteSslProtocol: boolean;
    /** True if the connection is using an obsolete SSL key exchange. */
    obsoleteSslKeyExchange: boolean;
    /** True if the connection is using an obsolete SSL cipher. */
    obsoleteSslCipher: boolean;
    /** True if the connection is using an obsolete SSL signature. */
    obsoleteSslSignature: boolean;
  }

  export type SafetyTipStatus = "badReputation" | "lookalike";

  export interface SafetyTipInfo {
    /** Describes whether the page triggers any safety tips or reputation warnings. Default is unknown. */
    safetyTipStatus: Security.SafetyTipStatus;
    /** The URL the safety tip suggested ("Did you mean?"). Only filled in for lookalike matches. */
    safeUrl?: string;
  }

  /** Security state information about the page. */
  export interface VisibleSecurityState {
    /** The security level of the page. */
    securityState: Security.SecurityState;
    /** Security state details about the page certificate. */
    certificateSecurityState?: Security.CertificateSecurityState;
    /** The type of Safety Tip triggered on the page. Note that this field will be set even if the Safety Tip UI was not actually shown. */
    safetyTipInfo?: Security.SafetyTipInfo;
    /** Array of security state issues ids. */
    securityStateIssueIds: string[];
  }

  /** An explanation of an factor contributing to the security state. */
  export interface SecurityStateExplanation {
    /** Security state representing the severity of the factor being explained. */
    securityState: Security.SecurityState;
    /** Title describing the type of factor. */
    title: string;
    /** Short phrase describing the type of factor. */
    summary: string;
    /** Full text explanation of the factor. */
    description: string;
    /** The type of mixed content described by the explanation. */
    mixedContentType: Security.MixedContentType;
    /** Page certificate. */
    certificate: string[];
    /** Recommendations to fix any issues. */
    recommendations?: string[];
  }

  /** Information about insecure content on the page. */
  export interface InsecureContentStatus {
    /** Always false. */
    ranMixedContent: boolean;
    /** Always false. */
    displayedMixedContent: boolean;
    /** Always false. */
    containedMixedForm: boolean;
    /** Always false. */
    ranContentWithCertErrors: boolean;
    /** Always false. */
    displayedContentWithCertErrors: boolean;
    /** Always set to unknown. */
    ranInsecureContentStyle: Security.SecurityState;
    /** Always set to unknown. */
    displayedInsecureContentStyle: Security.SecurityState;
  }

  /**
   * The action to take when a certificate error occurs. continue will continue processing the
   * request and cancel will cancel the request.
   */
  export type CertificateErrorAction = "continue" | "cancel";

}

export namespace ServiceWorker {

  export type RegistrationID = string;

  /** ServiceWorker registration. */
  export interface ServiceWorkerRegistration {
    registrationId: ServiceWorker.RegistrationID;
    scopeURL: string;
    isDeleted: boolean;
  }

  export type ServiceWorkerVersionRunningStatus = "stopped" | "starting" | "running" | "stopping";

  export type ServiceWorkerVersionStatus = "new" | "installing" | "installed" | "activating" | "activated" | "redundant";

  /** ServiceWorker version. */
  export interface ServiceWorkerVersion {
    versionId: string;
    registrationId: ServiceWorker.RegistrationID;
    scriptURL: string;
    runningStatus: ServiceWorker.ServiceWorkerVersionRunningStatus;
    status: ServiceWorker.ServiceWorkerVersionStatus;
    /** The Last-Modified header value of the main script. */
    scriptLastModified?: number;
    /**
     * The time at which the response headers of the main script were received from the server.
     * For cached script it is the last time the cache entry was validated.
     */
    scriptResponseTime?: number;
    controlledClients?: Target.TargetID[];
    targetId?: Target.TargetID;
    routerRules?: string;
  }

  /** ServiceWorker error message. */
  export interface ServiceWorkerErrorMessage {
    errorMessage: string;
    registrationId: ServiceWorker.RegistrationID;
    versionId: string;
    sourceURL: string;
    lineNumber: number;
    columnNumber: number;
  }

}

export namespace SmartCardEmulation {

  /**
   * Indicates the PC/SC error code.
   *
   * This maps to:
   * PC/SC Lite: https://pcsclite.apdu.fr/api/group__ErrorCodes.html
   * Microsoft: https://learn.microsoft.com/en-us/windows/win32/secauthn/authentication-return-values
   */
  export type ResultCode = "success" | "removed-card" | "reset-card" | "unpowered-card" | "unresponsive-card" | "unsupported-card" | "reader-unavailable" | "sharing-violation" | "not-transacted" | "no-smartcard" | "proto-mismatch" | "system-cancelled" | "not-ready" | "cancelled" | "insufficient-buffer" | "invalid-handle" | "invalid-parameter" | "invalid-value" | "no-memory" | "timeout" | "unknown-reader" | "unsupported-feature" | "no-readers-available" | "service-stopped" | "no-service" | "comm-error" | "internal-error" | "server-too-busy" | "unexpected" | "shutdown" | "unknown-card" | "unknown";

  /** Maps to the |SCARD_SHARE_*| values. */
  export type ShareMode = "shared" | "exclusive" | "direct";

  /** Indicates what the reader should do with the card. */
  export type Disposition = "leave-card" | "reset-card" | "unpower-card" | "eject-card";

  /** Maps to |SCARD_*| connection state values. */
  export type ConnectionState = "absent" | "present" | "swallowed" | "powered" | "negotiable" | "specific";

  /** Maps to the |SCARD_STATE_*| flags. */
  export interface ReaderStateFlags {
    unaware?: boolean;
    ignore?: boolean;
    changed?: boolean;
    unknown?: boolean;
    unavailable?: boolean;
    empty?: boolean;
    present?: boolean;
    exclusive?: boolean;
    inuse?: boolean;
    mute?: boolean;
    unpowered?: boolean;
  }

  /** Maps to the |SCARD_PROTOCOL_*| flags. */
  export interface ProtocolSet {
    t0?: boolean;
    t1?: boolean;
    raw?: boolean;
  }

  /** Maps to the |SCARD_PROTOCOL_*| values. */
  export type Protocol = "t0" | "t1" | "raw";

  export interface ReaderStateIn {
    reader: string;
    currentState: SmartCardEmulation.ReaderStateFlags;
    currentInsertionCount: number;
  }

  export interface ReaderStateOut {
    reader: string;
    eventState: SmartCardEmulation.ReaderStateFlags;
    eventCount: number;
    atr: string;
  }

}

export namespace Storage {

  export type SerializedStorageKey = string;

  /** Enum of possible storage types. */
  export type StorageType = "cookies" | "file_systems" | "indexeddb" | "local_storage" | "shader_cache" | "websql" | "service_workers" | "cache_storage" | "interest_groups" | "shared_storage" | "storage_buckets" | "all" | "other";

  /** Usage for a storage type. */
  export interface UsageForType {
    /** Name of storage type. */
    storageType: Storage.StorageType;
    /** Storage usage (bytes). */
    usage: number;
  }

  /**
   * Pair of issuer origin and number of available (signed, but not used) Trust
   * Tokens from that issuer.
   */
  export interface TrustTokens {
    issuerOrigin: string;
    count: number;
  }

  /** Protected audience interest group auction identifier. */
  export type InterestGroupAuctionId = string;

  /** Enum of interest group access types. */
  export type InterestGroupAccessType = "join" | "leave" | "update" | "loaded" | "bid" | "win" | "additionalBid" | "additionalBidWin" | "topLevelBid" | "topLevelAdditionalBid" | "clear";

  /** Enum of auction events. */
  export type InterestGroupAuctionEventType = "started" | "configResolved";

  /** Enum of network fetches auctions can do. */
  export type InterestGroupAuctionFetchType = "bidderJs" | "bidderWasm" | "sellerJs" | "bidderTrustedSignals" | "sellerTrustedSignals";

  /** Enum of shared storage access scopes. */
  export type SharedStorageAccessScope = "window" | "sharedStorageWorklet" | "protectedAudienceWorklet" | "header";

  /** Enum of shared storage access methods. */
  export type SharedStorageAccessMethod = "addModule" | "createWorklet" | "selectURL" | "run" | "batchUpdate" | "set" | "append" | "delete" | "clear" | "get" | "keys" | "values" | "entries" | "length" | "remainingBudget";

  /** Struct for a single key-value pair in an origin's shared storage. */
  export interface SharedStorageEntry {
    key: string;
    value: string;
  }

  /** Details for an origin's shared storage. */
  export interface SharedStorageMetadata {
    /** Time when the origin's shared storage was last created. */
    creationTime: Network.TimeSinceEpoch;
    /** Number of key-value pairs stored in origin's shared storage. */
    length: number;
    /** Current amount of bits of entropy remaining in the navigation budget. */
    remainingBudget: number;
    /**
     * Total number of bytes stored as key-value pairs in origin's shared
     * storage.
     */
    bytesUsed: number;
  }

  /**
   * Represents a dictionary object passed in as privateAggregationConfig to
   * run or selectURL.
   */
  export interface SharedStoragePrivateAggregationConfig {
    /** The chosen aggregation service deployment. */
    aggregationCoordinatorOrigin?: string;
    /** The context ID provided. */
    contextId?: string;
    /** Configures the maximum size allowed for filtering IDs. */
    filteringIdMaxBytes: number;
    /** The limit on the number of contributions in the final report. */
    maxContributions?: number;
  }

  /** Pair of reporting metadata details for a candidate URL for `selectURL()`. */
  export interface SharedStorageReportingMetadata {
    eventType: string;
    reportingUrl: string;
  }

  /** Bundles a candidate URL with its reporting metadata. */
  export interface SharedStorageUrlWithMetadata {
    /** Spec of candidate URL. */
    url: string;
    /** Any associated reporting metadata. */
    reportingMetadata: Storage.SharedStorageReportingMetadata[];
  }

  /**
   * Bundles the parameters for shared storage access events whose
   * presence/absence can vary according to SharedStorageAccessType.
   */
  export interface SharedStorageAccessParams {
    /**
     * Spec of the module script URL.
     * Present only for SharedStorageAccessMethods: addModule and
     * createWorklet.
     */
    scriptSourceUrl?: string;
    /**
     * String denoting "context-origin", "script-origin", or a custom
     * origin to be used as the worklet's data origin.
     * Present only for SharedStorageAccessMethod: createWorklet.
     */
    dataOrigin?: string;
    /**
     * Name of the registered operation to be run.
     * Present only for SharedStorageAccessMethods: run and selectURL.
     */
    operationName?: string;
    /**
     * ID of the operation call.
     * Present only for SharedStorageAccessMethods: run and selectURL.
     */
    operationId?: string;
    /**
     * Whether or not to keep the worket alive for future run or selectURL
     * calls.
     * Present only for SharedStorageAccessMethods: run and selectURL.
     */
    keepAlive?: boolean;
    /**
     * Configures the private aggregation options.
     * Present only for SharedStorageAccessMethods: run and selectURL.
     */
    privateAggregationConfig?: Storage.SharedStoragePrivateAggregationConfig;
    /**
     * The operation's serialized data in bytes (converted to a string).
     * Present only for SharedStorageAccessMethods: run and selectURL.
     * TODO(crbug.com/401011862): Consider updating this parameter to binary.
     */
    serializedData?: string;
    /**
     * Array of candidate URLs' specs, along with any associated metadata.
     * Present only for SharedStorageAccessMethod: selectURL.
     */
    urlsWithMetadata?: Storage.SharedStorageUrlWithMetadata[];
    /**
     * Spec of the URN:UUID generated for a selectURL call.
     * Present only for SharedStorageAccessMethod: selectURL.
     */
    urnUuid?: string;
    /**
     * Key for a specific entry in an origin's shared storage.
     * Present only for SharedStorageAccessMethods: set, append, delete, and
     * get.
     */
    key?: string;
    /**
     * Value for a specific entry in an origin's shared storage.
     * Present only for SharedStorageAccessMethods: set and append.
     */
    value?: string;
    /**
     * Whether or not to set an entry for a key if that key is already present.
     * Present only for SharedStorageAccessMethod: set.
     */
    ignoreIfPresent?: boolean;
    /**
     * A number denoting the (0-based) order of the worklet's
     * creation relative to all other shared storage worklets created by
     * documents using the current storage partition.
     * Present only for SharedStorageAccessMethods: addModule, createWorklet.
     */
    workletOrdinal?: number;
    /**
     * Hex representation of the DevTools token used as the TargetID for the
     * associated shared storage worklet.
     * Present only for SharedStorageAccessMethods: addModule, createWorklet,
     * run, selectURL, and any other SharedStorageAccessMethod when the
     * SharedStorageAccessScope is sharedStorageWorklet.
     */
    workletTargetId?: Target.TargetID;
    /**
     * Name of the lock to be acquired, if present.
     * Optionally present only for SharedStorageAccessMethods: batchUpdate,
     * set, append, delete, and clear.
     */
    withLock?: string;
    /**
     * If the method has been called as part of a batchUpdate, then this
     * number identifies the batch to which it belongs.
     * Optionally present only for SharedStorageAccessMethods:
     * batchUpdate (required), set, append, delete, and clear.
     */
    batchUpdateId?: string;
    /**
     * Number of modifier methods sent in batch.
     * Present only for SharedStorageAccessMethod: batchUpdate.
     */
    batchSize?: number;
  }

  export type StorageBucketsDurability = "relaxed" | "strict";

  export interface StorageBucket {
    storageKey: Storage.SerializedStorageKey;
    /** If not specified, it is the default bucket of the storageKey. */
    name?: string;
  }

  export interface StorageBucketInfo {
    bucket: Storage.StorageBucket;
    id: string;
    expiration: Network.TimeSinceEpoch;
    /** Storage quota (bytes). */
    quota: number;
    persistent: boolean;
    durability: Storage.StorageBucketsDurability;
  }

  /** A single Related Website Set object. */
  export interface RelatedWebsiteSet {
    /** The primary site of this set, along with the ccTLDs if there is any. */
    primarySites: string[];
    /** The associated sites of this set, along with the ccTLDs if there is any. */
    associatedSites: string[];
    /** The service sites of this set, along with the ccTLDs if there is any. */
    serviceSites: string[];
  }

}

export namespace SystemInfo {

  /** Describes a single graphics processor (GPU). */
  export interface GPUDevice {
    /** PCI ID of the GPU vendor, if available; 0 otherwise. */
    vendorId: number;
    /** PCI ID of the GPU device, if available; 0 otherwise. */
    deviceId: number;
    /** Sub sys ID of the GPU, only available on Windows. */
    subSysId?: number;
    /** Revision of the GPU, only available on Windows. */
    revision?: number;
    /** String description of the GPU vendor, if the PCI ID is not available. */
    vendorString: string;
    /** String description of the GPU device, if the PCI ID is not available. */
    deviceString: string;
    /** String description of the GPU driver vendor. */
    driverVendor: string;
    /** String description of the GPU driver version. */
    driverVersion: string;
  }

  /** Describes the width and height dimensions of an entity. */
  export interface Size {
    /** Width in pixels. */
    width: number;
    /** Height in pixels. */
    height: number;
  }

  /**
   * Describes a supported video decoding profile with its associated minimum and
   * maximum resolutions.
   */
  export interface VideoDecodeAcceleratorCapability {
    /** Video codec profile that is supported, e.g. VP9 Profile 2. */
    profile: string;
    /** Maximum video dimensions in pixels supported for this |profile|. */
    maxResolution: SystemInfo.Size;
    /** Minimum video dimensions in pixels supported for this |profile|. */
    minResolution: SystemInfo.Size;
  }

  /**
   * Describes a supported video encoding profile with its associated maximum
   * resolution and maximum framerate.
   */
  export interface VideoEncodeAcceleratorCapability {
    /** Video codec profile that is supported, e.g H264 Main. */
    profile: string;
    /** Maximum video dimensions in pixels supported for this |profile|. */
    maxResolution: SystemInfo.Size;
    /**
     * Maximum encoding framerate in frames per second supported for this
     * |profile|, as fraction's numerator and denominator, e.g. 24/1 fps,
     * 24000/1001 fps, etc.
     */
    maxFramerateNumerator: number;
    maxFramerateDenominator: number;
  }

  /** YUV subsampling type of the pixels of a given image. */
  export type SubsamplingFormat = "yuv420" | "yuv422" | "yuv444";

  /** Image format of a given image. */
  export type ImageType = "jpeg" | "webp" | "unknown";

  /** Provides information about the GPU(s) on the system. */
  export interface GPUInfo {
    /** The graphics devices on the system. Element 0 is the primary GPU. */
    devices: SystemInfo.GPUDevice[];
    /** An optional dictionary of additional GPU related attributes. */
    auxAttributes?: Record<string, unknown>;
    /** An optional dictionary of graphics features and their status. */
    featureStatus?: Record<string, unknown>;
    /** An optional array of GPU driver bug workarounds. */
    driverBugWorkarounds: string[];
    /** Supported accelerated video decoding capabilities. */
    videoDecoding: SystemInfo.VideoDecodeAcceleratorCapability[];
    /** Supported accelerated video encoding capabilities. */
    videoEncoding: SystemInfo.VideoEncodeAcceleratorCapability[];
  }

  /** Represents process info. */
  export interface ProcessInfo {
    /** Specifies process type. */
    type: string;
    /** Specifies process id. */
    id: number;
    /**
     * Specifies cumulative CPU usage in seconds across all threads of the
     * process since the process start.
     */
    cpuTime: number;
  }

}

export namespace Target {

  export type TargetID = string;

  /** Unique identifier of attached debugging session. */
  export type SessionID = string;

  export interface TargetInfo {
    targetId: Target.TargetID;
    /** List of types: https://source.chromium.org/chromium/chromium/src/+/main:content/browser/devtools/devtools_agent_host_impl.cc?ss=chromium&q=f:devtools%20-f:out%20%22::kTypeTab%5B%5D%22 */
    type: string;
    title: string;
    url: string;
    /** Whether the target has an attached client. */
    attached: boolean;
    /** Id of the parent target, if any. For example, "iframe" target may have a "page" parent. */
    parentId?: Target.TargetID;
    /** Opener target Id */
    openerId?: Target.TargetID;
    /** Whether the target has access to the originating window. */
    canAccessOpener: boolean;
    /** Frame id of originating window (is only set if target has an opener). */
    openerFrameId?: Page.FrameId;
    /**
     * Id of the parent frame, present for "iframe" and "worker" targets. For nested workers,
     * this is the "ancestor" frame that created the first worker in the nested chain.
     */
    parentFrameId?: Page.FrameId;
    browserContextId?: Browser.BrowserContextID;
    /**
     * Provides additional details for specific target types. For example, for
     * the type of "page", this may be set to "prerender".
     */
    subtype?: string;
  }

  /** A filter used by target query/discovery/auto-attach operations. */
  export interface FilterEntry {
    /** If set, causes exclusion of matching targets from the list. */
    exclude?: boolean;
    /** If not present, matches any type. */
    type?: string;
  }

  /**
   * The entries in TargetFilter are matched sequentially against targets and
   * the first entry that matches determines if the target is included or not,
   * depending on the value of `exclude` field in the entry.
   * If filter is not specified, the one assumed is
   * [{type: "browser", exclude: true}, {type: "tab", exclude: true}, {}]
   * (i.e. include everything but `browser` and `tab`).
   */
  export type TargetFilter = Target.FilterEntry[];

  export interface RemoteLocation {
    host: string;
    port: number;
  }

  /** The state of the target window. */
  export type WindowState = "normal" | "minimized" | "maximized" | "fullscreen";

}

export namespace Tethering {
  // (no types)
}

export namespace Tracing {

  /** Configuration for memory dump. Used only when "memory-infra" category is enabled. */
  export type MemoryDumpConfig = Record<string, unknown>;

  export interface TraceConfig {
    /** Controls how the trace buffer stores data. The default is `recordUntilFull`. */
    recordMode?: "recordUntilFull" | "recordContinuously" | "recordAsMuchAsPossible" | "echoToConsole";
    /**
     * Size of the trace buffer in kilobytes. If not specified or zero is passed, a default value
     * of 200 MB would be used.
     */
    traceBufferSizeInKb?: number;
    /** Turns on JavaScript stack sampling. */
    enableSampling?: boolean;
    /** Turns on system tracing. */
    enableSystrace?: boolean;
    /** Turns on argument filter. */
    enableArgumentFilter?: boolean;
    /** Included category filters. */
    includedCategories?: string[];
    /** Excluded category filters. */
    excludedCategories?: string[];
    /** Configuration to synthesize the delays in tracing. */
    syntheticDelays?: string[];
    /** Configuration for memory dump triggers. Used only when "memory-infra" category is enabled. */
    memoryDumpConfig?: Tracing.MemoryDumpConfig;
  }

  /**
   * Data format of a trace. Can be either the legacy JSON format or the
   * protocol buffer format. Note that the JSON format will be deprecated soon.
   */
  export type StreamFormat = "json" | "proto";

  /** Compression type to use for traces returned via streams. */
  export type StreamCompression = "none" | "gzip";

  /**
   * Details exposed when memory request explicitly declared.
   * Keep consistent with memory_dump_request_args.h and
   * memory_instrumentation.mojom
   */
  export type MemoryDumpLevelOfDetail = "background" | "light" | "detailed";

  /**
   * Backend type to use for tracing. `chrome` uses the Chrome-integrated
   * tracing service and is supported on all platforms. `system` is only
   * supported on Chrome OS and uses the Perfetto system tracing service.
   * `auto` chooses `system` when the perfettoConfig provided to Tracing.start
   * specifies at least one non-Chrome data source; otherwise uses `chrome`.
   */
  export type TracingBackend = "auto" | "chrome" | "system";

}

export namespace WebAudio {

  /** An unique ID for a graph object (AudioContext, AudioNode, AudioParam) in Web Audio API */
  export type GraphObjectId = string;

  /** Enum of BaseAudioContext types */
  export type ContextType = "realtime" | "offline";

  /** Enum of AudioContextState from the spec */
  export type ContextState = "suspended" | "running" | "closed" | "interrupted";

  /** Enum of AudioNode types */
  export type NodeType = string;

  /** Enum of AudioNode::ChannelCountMode from the spec */
  export type ChannelCountMode = "clamped-max" | "explicit" | "max";

  /** Enum of AudioNode::ChannelInterpretation from the spec */
  export type ChannelInterpretation = "discrete" | "speakers";

  /** Enum of AudioParam types */
  export type ParamType = string;

  /** Enum of AudioParam::AutomationRate from the spec */
  export type AutomationRate = "a-rate" | "k-rate";

  /** Fields in AudioContext that change in real-time. */
  export interface ContextRealtimeData {
    /** The current context time in second in BaseAudioContext. */
    currentTime: number;
    /**
     * The time spent on rendering graph divided by render quantum duration,
     * and multiplied by 100. 100 means the audio renderer reached the full
     * capacity and glitch may occur.
     */
    renderCapacity: number;
    /** A running mean of callback interval. */
    callbackIntervalMean: number;
    /** A running variance of callback interval. */
    callbackIntervalVariance: number;
  }

  /** Protocol object for BaseAudioContext */
  export interface BaseAudioContext {
    contextId: WebAudio.GraphObjectId;
    contextType: WebAudio.ContextType;
    contextState: WebAudio.ContextState;
    realtimeData?: WebAudio.ContextRealtimeData;
    /** Platform-dependent callback buffer size. */
    callbackBufferSize: number;
    /** Number of output channels supported by audio hardware in use. */
    maxOutputChannelCount: number;
    /** Context sample rate. */
    sampleRate: number;
  }

  /** Protocol object for AudioListener */
  export interface AudioListener {
    listenerId: WebAudio.GraphObjectId;
    contextId: WebAudio.GraphObjectId;
  }

  /** Protocol object for AudioNode */
  export interface AudioNode {
    nodeId: WebAudio.GraphObjectId;
    contextId: WebAudio.GraphObjectId;
    nodeType: WebAudio.NodeType;
    numberOfInputs: number;
    numberOfOutputs: number;
    channelCount: number;
    channelCountMode: WebAudio.ChannelCountMode;
    channelInterpretation: WebAudio.ChannelInterpretation;
  }

  /** Protocol object for AudioParam */
  export interface AudioParam {
    paramId: WebAudio.GraphObjectId;
    nodeId: WebAudio.GraphObjectId;
    contextId: WebAudio.GraphObjectId;
    paramType: WebAudio.ParamType;
    rate: WebAudio.AutomationRate;
    defaultValue: number;
    minValue: number;
    maxValue: number;
  }

}

export namespace WebAuthn {

  export type AuthenticatorId = string;

  export type AuthenticatorProtocol = "u2f" | "ctap2";

  export type Ctap2Version = "ctap2_0" | "ctap2_1" | "ctap2_2";

  export type AuthenticatorTransport = "usb" | "nfc" | "ble" | "cable" | "internal";

  export interface VirtualAuthenticatorOptions {
    protocol: WebAuthn.AuthenticatorProtocol;
    /** Defaults to ctap2_0. Ignored if |protocol| == u2f. */
    ctap2Version?: WebAuthn.Ctap2Version;
    transport: WebAuthn.AuthenticatorTransport;
    /** Defaults to false. */
    hasResidentKey?: boolean;
    /** Defaults to false. */
    hasUserVerification?: boolean;
    /**
     * If set to true, the authenticator will support the largeBlob extension.
     * https://w3c.github.io/webauthn#largeBlob
     * Defaults to false.
     */
    hasLargeBlob?: boolean;
    /**
     * If set to true, the authenticator will support the credBlob extension.
     * https://fidoalliance.org/specs/fido-v2.1-rd-20201208/fido-client-to-authenticator-protocol-v2.1-rd-20201208.html#sctn-credBlob-extension
     * Defaults to false.
     */
    hasCredBlob?: boolean;
    /**
     * If set to true, the authenticator will support the minPinLength extension.
     * https://fidoalliance.org/specs/fido-v2.1-ps-20210615/fido-client-to-authenticator-protocol-v2.1-ps-20210615.html#sctn-minpinlength-extension
     * Defaults to false.
     */
    hasMinPinLength?: boolean;
    /**
     * If set to true, the authenticator will support the prf extension.
     * https://w3c.github.io/webauthn/#prf-extension
     * Defaults to false.
     */
    hasPrf?: boolean;
    /**
     * If set to true, the authenticator will support the hmac-secret extension.
     * https://fidoalliance.org/specs/fido-v2.1-ps-20210615/fido-client-to-authenticator-protocol-v2.1-ps-20210615.html#sctn-hmac-secret-extension
     * Defaults to false.
     */
    hasHmacSecret?: boolean;
    /**
     * If set to true, the authenticator will support the hmac-secret-mc extension.
     * https://fidoalliance.org/specs/fido-v2.2-rd-20241003/fido-client-to-authenticator-protocol-v2.2-rd-20241003.html#sctn-hmac-secret-make-cred-extension
     * Defaults to false.
     */
    hasHmacSecretMc?: boolean;
    /**
     * If set to true, tests of user presence will succeed immediately.
     * Otherwise, they will not be resolved. Defaults to true.
     */
    automaticPresenceSimulation?: boolean;
    /**
     * Sets whether User Verification succeeds or fails for an authenticator.
     * Defaults to false.
     */
    isUserVerified?: boolean;
    /**
     * Credentials created by this authenticator will have the backup
     * eligibility (BE) flag set to this value. Defaults to false.
     * https://w3c.github.io/webauthn/#sctn-credential-backup
     */
    defaultBackupEligibility?: boolean;
    /**
     * Credentials created by this authenticator will have the backup state
     * (BS) flag set to this value. Defaults to false.
     * https://w3c.github.io/webauthn/#sctn-credential-backup
     */
    defaultBackupState?: boolean;
  }

  export interface Credential {
    credentialId: string;
    isResidentCredential: boolean;
    /**
     * Relying Party ID the credential is scoped to. Must be set when adding a
     * credential.
     */
    rpId?: string;
    /** The ECDSA P-256 private key in PKCS#8 format. (Encoded as a base64 string when passed over JSON) */
    privateKey: string;
    /**
     * An opaque byte sequence with a maximum size of 64 bytes mapping the
     * credential to a specific user. (Encoded as a base64 string when passed over JSON)
     */
    userHandle?: string;
    /**
     * Signature counter. This is incremented by one for each successful
     * assertion.
     * See https://w3c.github.io/webauthn/#signature-counter
     */
    signCount: number;
    /**
     * The large blob associated with the credential.
     * See https://w3c.github.io/webauthn/#sctn-large-blob-extension (Encoded as a base64 string when passed over JSON)
     */
    largeBlob?: string;
    /**
     * Assertions returned by this credential will have the backup eligibility
     * (BE) flag set to this value. Defaults to the authenticator's
     * defaultBackupEligibility value.
     */
    backupEligibility?: boolean;
    /**
     * Assertions returned by this credential will have the backup state (BS)
     * flag set to this value. Defaults to the authenticator's
     * defaultBackupState value.
     */
    backupState?: boolean;
    /**
     * The credential's user.name property. Equivalent to empty if not set.
     * https://w3c.github.io/webauthn/#dom-publickeycredentialentity-name
     */
    userName?: string;
    /**
     * The credential's user.displayName property. Equivalent to empty if
     * not set.
     * https://w3c.github.io/webauthn/#dom-publickeycredentialuserentity-displayname
     */
    userDisplayName?: string;
  }

}

export namespace WebMCP {

  /** Tool annotations */
  export interface Annotation {
    /** A hint indicating that the tool does not modify any state. */
    readOnly?: boolean;
    /** If the declarative tool was declared with the autosubmit attribute. */
    autosubmit?: boolean;
  }

  /** Represents the status of a tool invocation. */
  export type InvocationStatus = "Completed" | "Canceled" | "Error";

  /** Definition of a tool that can be invoked. */
  export interface Tool {
    /** Tool name. */
    name: string;
    /** Tool description. */
    description: string;
    /** Schema for the tool's input parameters. */
    inputSchema?: Record<string, unknown>;
    /** Optional annotations for the tool. */
    annotations?: WebMCP.Annotation;
    /** Frame identifier associated with the tool registration. */
    frameId: Page.FrameId;
    /** Optional node ID for declarative tools. */
    backendNodeId?: DOM.BackendNodeId;
    /** The stack trace at the time of the registration. */
    stackTrace?: Runtime.StackTrace;
  }

}

export namespace Accessibility {
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface GetPartialAXTreeParams {
    /** Identifier of the node to get the partial accessibility tree for. */
    nodeId?: DOM.NodeId;
    /** Identifier of the backend node to get the partial accessibility tree for. */
    backendNodeId?: DOM.BackendNodeId;
    /** JavaScript object id of the node wrapper to get the partial accessibility tree for. */
    objectId?: Runtime.RemoteObjectId;
    /** Whether to fetch this node's ancestors, siblings and children. Defaults to true. */
    fetchRelatives?: boolean;
  }
  export interface GetPartialAXTreeReturn {
    /**
     * The `Accessibility.AXNode` for this DOM node, if it exists, plus its ancestors, siblings and
     * children, if requested.
     */
    nodes: Accessibility.AXNode[];
  }
  export interface GetFullAXTreeParams {
    /**
     * The maximum depth at which descendants of the root node should be retrieved.
     * If omitted, the full tree is returned.
     */
    depth?: number;
    /**
     * The frame for whose document the AX tree should be retrieved.
     * If omitted, the root frame is used.
     */
    frameId?: Page.FrameId;
  }
  export interface GetFullAXTreeReturn {
    nodes: Accessibility.AXNode[];
  }
  export interface GetRootAXNodeParams {
    /**
     * The frame in whose document the node resides.
     * If omitted, the root frame is used.
     */
    frameId?: Page.FrameId;
  }
  export interface GetRootAXNodeReturn {
    node: Accessibility.AXNode;
  }
  export interface GetAXNodeAndAncestorsParams {
    /** Identifier of the node to get. */
    nodeId?: DOM.NodeId;
    /** Identifier of the backend node to get. */
    backendNodeId?: DOM.BackendNodeId;
    /** JavaScript object id of the node wrapper to get. */
    objectId?: Runtime.RemoteObjectId;
  }
  export interface GetAXNodeAndAncestorsReturn {
    nodes: Accessibility.AXNode[];
  }
  export interface GetChildAXNodesParams {
    id: Accessibility.AXNodeId;
    /**
     * The frame in whose document the node resides.
     * If omitted, the root frame is used.
     */
    frameId?: Page.FrameId;
  }
  export interface GetChildAXNodesReturn {
    nodes: Accessibility.AXNode[];
  }
  export interface QueryAXTreeParams {
    /** Identifier of the node for the root to query. */
    nodeId?: DOM.NodeId;
    /** Identifier of the backend node for the root to query. */
    backendNodeId?: DOM.BackendNodeId;
    /** JavaScript object id of the node wrapper for the root to query. */
    objectId?: Runtime.RemoteObjectId;
    /** Find nodes with this computed name. */
    accessibleName?: string;
    /** Find nodes with this computed role. */
    role?: string;
  }
  export interface QueryAXTreeReturn {
    /**
     * A list of `Accessibility.AXNode` matching the specified attributes,
     * including nodes that are ignored for accessibility.
     */
    nodes: Accessibility.AXNode[];
  }
}

export namespace Animation {
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface GetCurrentTimeParams {
    /** Id of animation. */
    id: string;
  }
  export interface GetCurrentTimeReturn {
    /** Current time of the page. */
    currentTime: number;
  }
  export interface GetPlaybackRateParams {}
  export interface GetPlaybackRateReturn {
    /** Playback rate for animations on page. */
    playbackRate: number;
  }
  export interface ReleaseAnimationsParams {
    /** List of animation ids to seek. */
    animations: string[];
  }
  export type ReleaseAnimationsReturn = void;
  export interface ResolveAnimationParams {
    /** Animation id. */
    animationId: string;
  }
  export interface ResolveAnimationReturn {
    /** Corresponding remote object. */
    remoteObject: Runtime.RemoteObject;
  }
  export interface SeekAnimationsParams {
    /** List of animation ids to seek. */
    animations: string[];
    /** Set the current time of each animation. */
    currentTime: number;
  }
  export type SeekAnimationsReturn = void;
  export interface SetPausedParams {
    /** Animations to set the pause state of. */
    animations: string[];
    /** Paused state to set to. */
    paused: boolean;
  }
  export type SetPausedReturn = void;
  export interface SetPlaybackRateParams {
    /** Playback rate for animations on page */
    playbackRate: number;
  }
  export type SetPlaybackRateReturn = void;
  export interface SetTimingParams {
    /** Animation id. */
    animationId: string;
    /** Duration of the animation. */
    duration: number;
    /** Delay of the animation. */
    delay: number;
  }
  export type SetTimingReturn = void;
}

export namespace Audits {
  export interface GetEncodedResponseParams {
    /** Identifier of the network request to get content for. */
    requestId: Network.RequestId;
    /** The encoding to use. */
    encoding: "webp" | "jpeg" | "png";
    /** The quality of the encoding (0-1). (defaults to 1) */
    quality?: number;
    /** Whether to only return the size information (defaults to false). */
    sizeOnly?: boolean;
  }
  export interface GetEncodedResponseReturn {
    /** The encoded body as a base64 string. Omitted if sizeOnly is true. (Encoded as a base64 string when passed over JSON) */
    body?: string;
    /** Size before re-encoding. */
    originalSize: number;
    /** Size after re-encoding. */
    encodedSize: number;
  }
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface CheckFormsIssuesParams {}
  export interface CheckFormsIssuesReturn {
    formIssues: Audits.GenericIssueDetails[];
  }
}

export namespace Autofill {
  export interface TriggerParams {
    /** Identifies a field that serves as an anchor for autofill. */
    fieldId: DOM.BackendNodeId;
    /** Identifies the frame that field belongs to. */
    frameId?: Page.FrameId;
    /** Credit card information to fill out the form. Credit card data is not saved.  Mutually exclusive with `address`. */
    card?: Autofill.CreditCard;
    /** Address to fill out the form. Address data is not saved. Mutually exclusive with `card`. */
    address?: Autofill.Address;
  }
  export type TriggerReturn = void;
  export interface SetAddressesParams {
    addresses: Autofill.Address[];
  }
  export type SetAddressesReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
}

export namespace BackgroundService {
  export interface StartObservingParams {
    service: BackgroundService.ServiceName;
  }
  export type StartObservingReturn = void;
  export interface StopObservingParams {
    service: BackgroundService.ServiceName;
  }
  export type StopObservingReturn = void;
  export interface SetRecordingParams {
    shouldRecord: boolean;
    service: BackgroundService.ServiceName;
  }
  export type SetRecordingReturn = void;
  export interface ClearEventsParams {
    service: BackgroundService.ServiceName;
  }
  export type ClearEventsReturn = void;
}

export namespace BluetoothEmulation {
  export interface EnableParams {
    /** State of the simulated central. */
    state: BluetoothEmulation.CentralState;
    /** If the simulated central supports low-energy. */
    leSupported: boolean;
  }
  export type EnableReturn = void;
  export interface SetSimulatedCentralStateParams {
    /** State of the simulated central. */
    state: BluetoothEmulation.CentralState;
  }
  export type SetSimulatedCentralStateReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface SimulatePreconnectedPeripheralParams {
    address: string;
    name: string;
    manufacturerData: BluetoothEmulation.ManufacturerData[];
    knownServiceUuids: string[];
  }
  export type SimulatePreconnectedPeripheralReturn = void;
  export interface SimulateAdvertisementParams {
    entry: BluetoothEmulation.ScanEntry;
  }
  export type SimulateAdvertisementReturn = void;
  export interface SimulateGATTOperationResponseParams {
    address: string;
    type: BluetoothEmulation.GATTOperationType;
    code: number;
  }
  export type SimulateGATTOperationResponseReturn = void;
  export interface SimulateCharacteristicOperationResponseParams {
    characteristicId: string;
    type: BluetoothEmulation.CharacteristicOperationType;
    code: number;
    data?: string;
  }
  export type SimulateCharacteristicOperationResponseReturn = void;
  export interface SimulateDescriptorOperationResponseParams {
    descriptorId: string;
    type: BluetoothEmulation.DescriptorOperationType;
    code: number;
    data?: string;
  }
  export type SimulateDescriptorOperationResponseReturn = void;
  export interface AddServiceParams {
    address: string;
    serviceUuid: string;
  }
  export interface AddServiceReturn {
    /** An identifier that uniquely represents this service. */
    serviceId: string;
  }
  export interface RemoveServiceParams {
    serviceId: string;
  }
  export type RemoveServiceReturn = void;
  export interface AddCharacteristicParams {
    serviceId: string;
    characteristicUuid: string;
    properties: BluetoothEmulation.CharacteristicProperties;
  }
  export interface AddCharacteristicReturn {
    /** An identifier that uniquely represents this characteristic. */
    characteristicId: string;
  }
  export interface RemoveCharacteristicParams {
    characteristicId: string;
  }
  export type RemoveCharacteristicReturn = void;
  export interface AddDescriptorParams {
    characteristicId: string;
    descriptorUuid: string;
  }
  export interface AddDescriptorReturn {
    /** An identifier that uniquely represents this descriptor. */
    descriptorId: string;
  }
  export interface RemoveDescriptorParams {
    descriptorId: string;
  }
  export type RemoveDescriptorReturn = void;
  export interface SimulateGATTDisconnectionParams {
    address: string;
  }
  export type SimulateGATTDisconnectionReturn = void;
}

export namespace Browser {
  export interface SetPermissionParams {
    /** Descriptor of permission to override. */
    permission: Browser.PermissionDescriptor;
    /** Setting of the permission. */
    setting: Browser.PermissionSetting;
    /** Embedding origin the permission applies to, all origins if not specified. */
    origin?: string;
    /**
     * Embedded origin the permission applies to. It is ignored unless the embedding origin is
     * present and valid. If the embedding origin is provided but the embedded origin isn't, the
     * embedding origin is used as the embedded origin.
     */
    embeddedOrigin?: string;
    /** Context to override. When omitted, default browser context is used. */
    browserContextId?: Browser.BrowserContextID;
  }
  export type SetPermissionReturn = void;
  export interface GrantPermissionsParams {
    permissions: Browser.PermissionType[];
    /** Origin the permission applies to, all origins if not specified. */
    origin?: string;
    /** BrowserContext to override permissions. When omitted, default browser context is used. */
    browserContextId?: Browser.BrowserContextID;
  }
  export type GrantPermissionsReturn = void;
  export interface ResetPermissionsParams {
    /** BrowserContext to reset permissions. When omitted, default browser context is used. */
    browserContextId?: Browser.BrowserContextID;
  }
  export type ResetPermissionsReturn = void;
  export interface SetDownloadBehaviorParams {
    /**
     * Whether to allow all or deny all download requests, or use default Chrome behavior if
     * available (otherwise deny). |allowAndName| allows download and names files according to
     * their download guids.
     */
    behavior: "deny" | "allow" | "allowAndName" | "default";
    /** BrowserContext to set download behavior. When omitted, default browser context is used. */
    browserContextId?: Browser.BrowserContextID;
    /**
     * The default path to save downloaded files to. This is required if behavior is set to 'allow'
     * or 'allowAndName'.
     */
    downloadPath?: string;
    /** Whether to emit download events (defaults to false). */
    eventsEnabled?: boolean;
  }
  export type SetDownloadBehaviorReturn = void;
  export interface CancelDownloadParams {
    /** Global unique identifier of the download. */
    guid: string;
    /** BrowserContext to perform the action in. When omitted, default browser context is used. */
    browserContextId?: Browser.BrowserContextID;
  }
  export type CancelDownloadReturn = void;
  export interface CloseParams {}
  export type CloseReturn = void;
  export interface CrashParams {}
  export type CrashReturn = void;
  export interface CrashGpuProcessParams {}
  export type CrashGpuProcessReturn = void;
  export interface GetVersionParams {}
  export interface GetVersionReturn {
    /** Protocol version. */
    protocolVersion: string;
    /** Product name. */
    product: string;
    /** Product revision. */
    revision: string;
    /** User-Agent. */
    userAgent: string;
    /** V8 version. */
    jsVersion: string;
  }
  export interface GetBrowserCommandLineParams {}
  export interface GetBrowserCommandLineReturn {
    /** Commandline parameters */
    arguments: string[];
  }
  export interface GetHistogramsParams {
    /**
     * Requested substring in name. Only histograms which have query as a
     * substring in their name are extracted. An empty or absent query returns
     * all histograms.
     */
    query?: string;
    /** If true, retrieve delta since last delta call. */
    delta?: boolean;
  }
  export interface GetHistogramsReturn {
    /** Histograms. */
    histograms: Browser.Histogram[];
  }
  export interface GetHistogramParams {
    /** Requested histogram name. */
    name: string;
    /** If true, retrieve delta since last delta call. */
    delta?: boolean;
  }
  export interface GetHistogramReturn {
    /** Histogram. */
    histogram: Browser.Histogram;
  }
  export interface GetWindowBoundsParams {
    /** Browser window id. */
    windowId: Browser.WindowID;
  }
  export interface GetWindowBoundsReturn {
    /**
     * Bounds information of the window. When window state is 'minimized', the restored window
     * position and size are returned.
     */
    bounds: Browser.Bounds;
  }
  export interface GetWindowForTargetParams {
    /** Devtools agent host id. If called as a part of the session, associated targetId is used. */
    targetId?: Target.TargetID;
  }
  export interface GetWindowForTargetReturn {
    /** Browser window id. */
    windowId: Browser.WindowID;
    /**
     * Bounds information of the window. When window state is 'minimized', the restored window
     * position and size are returned.
     */
    bounds: Browser.Bounds;
  }
  export interface SetWindowBoundsParams {
    /** Browser window id. */
    windowId: Browser.WindowID;
    /**
     * New window bounds. The 'minimized', 'maximized' and 'fullscreen' states cannot be combined
     * with 'left', 'top', 'width' or 'height'. Leaves unspecified fields unchanged.
     */
    bounds: Browser.Bounds;
  }
  export type SetWindowBoundsReturn = void;
  export interface SetContentsSizeParams {
    /** Browser window id. */
    windowId: Browser.WindowID;
    /**
     * The window contents width in DIP. Assumes current width if omitted.
     * Must be specified if 'height' is omitted.
     */
    width?: number;
    /**
     * The window contents height in DIP. Assumes current height if omitted.
     * Must be specified if 'width' is omitted.
     */
    height?: number;
  }
  export type SetContentsSizeReturn = void;
  export interface SetDockTileParams {
    badgeLabel?: string;
    /** Png encoded image. (Encoded as a base64 string when passed over JSON) */
    image?: string;
  }
  export type SetDockTileReturn = void;
  export interface ExecuteBrowserCommandParams {
    commandId: Browser.BrowserCommandId;
  }
  export type ExecuteBrowserCommandReturn = void;
  export interface AddPrivacySandboxEnrollmentOverrideParams {
    url: string;
  }
  export type AddPrivacySandboxEnrollmentOverrideReturn = void;
  export interface AddPrivacySandboxCoordinatorKeyConfigParams {
    api: Browser.PrivacySandboxAPI;
    coordinatorOrigin: string;
    keyConfig: string;
    /**
     * BrowserContext to perform the action in. When omitted, default browser
     * context is used.
     */
    browserContextId?: Browser.BrowserContextID;
  }
  export type AddPrivacySandboxCoordinatorKeyConfigReturn = void;
}

export namespace CacheStorage {
  export interface DeleteCacheParams {
    /** Id of cache for deletion. */
    cacheId: CacheStorage.CacheId;
  }
  export type DeleteCacheReturn = void;
  export interface DeleteEntryParams {
    /** Id of cache where the entry will be deleted. */
    cacheId: CacheStorage.CacheId;
    /** URL spec of the request. */
    request: string;
  }
  export type DeleteEntryReturn = void;
  export interface RequestCacheNamesParams {
    /**
     * At least and at most one of securityOrigin, storageKey, storageBucket must be specified.
     * Security origin.
     */
    securityOrigin?: string;
    /** Storage key. */
    storageKey?: string;
    /** Storage bucket. If not specified, it uses the default bucket. */
    storageBucket?: Storage.StorageBucket;
  }
  export interface RequestCacheNamesReturn {
    /** Caches for the security origin. */
    caches: CacheStorage.Cache[];
  }
  export interface RequestCachedResponseParams {
    /** Id of cache that contains the entry. */
    cacheId: CacheStorage.CacheId;
    /** URL spec of the request. */
    requestURL: string;
    /** headers of the request. */
    requestHeaders: CacheStorage.Header[];
  }
  export interface RequestCachedResponseReturn {
    /** Response read from the cache. */
    response: CacheStorage.CachedResponse;
  }
  export interface RequestEntriesParams {
    /** ID of cache to get entries from. */
    cacheId: CacheStorage.CacheId;
    /** Number of records to skip. */
    skipCount?: number;
    /** Number of records to fetch. */
    pageSize?: number;
    /** If present, only return the entries containing this substring in the path */
    pathFilter?: string;
  }
  export interface RequestEntriesReturn {
    /** Array of object store data entries. */
    cacheDataEntries: CacheStorage.DataEntry[];
    /**
     * Count of returned entries from this storage. If pathFilter is empty, it
     * is the count of all entries from this storage.
     */
    returnCount: number;
  }
}

export namespace Cast {
  export interface EnableParams {
    presentationUrl?: string;
  }
  export type EnableReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface SetSinkToUseParams {
    sinkName: string;
  }
  export type SetSinkToUseReturn = void;
  export interface StartDesktopMirroringParams {
    sinkName: string;
  }
  export type StartDesktopMirroringReturn = void;
  export interface StartTabMirroringParams {
    sinkName: string;
  }
  export type StartTabMirroringReturn = void;
  export interface StopCastingParams {
    sinkName: string;
  }
  export type StopCastingReturn = void;
}

export namespace Console {
  export interface ClearMessagesParams {}
  export type ClearMessagesReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
}

export namespace CrashReportContext {
  export interface GetEntriesParams {}
  export interface GetEntriesReturn {
    entries: CrashReportContext.CrashReportContextEntry[];
  }
}

export namespace CSS {
  export interface AddRuleParams {
    /** The css style sheet identifier where a new rule should be inserted. */
    styleSheetId: DOM.StyleSheetId;
    /** The text of a new rule. */
    ruleText: string;
    /** Text position of a new rule in the target style sheet. */
    location: CSS.SourceRange;
    /**
     * NodeId for the DOM node in whose context custom property declarations for registered properties should be
     * validated. If omitted, declarations in the new rule text can only be validated statically, which may produce
     * incorrect results if the declaration contains a var() for example.
     */
    nodeForPropertySyntaxValidation?: DOM.NodeId;
  }
  export interface AddRuleReturn {
    /** The newly created rule. */
    rule: CSS.CSSRule;
  }
  export interface CollectClassNamesParams {
    styleSheetId: DOM.StyleSheetId;
  }
  export interface CollectClassNamesReturn {
    /** Class name list. */
    classNames: string[];
  }
  export interface CreateStyleSheetParams {
    /** Identifier of the frame where "via-inspector" stylesheet should be created. */
    frameId: Page.FrameId;
    /**
     * If true, creates a new stylesheet for every call. If false,
     * returns a stylesheet previously created by a call with force=false
     * for the frame's document if it exists or creates a new stylesheet
     * (default: false).
     */
    force?: boolean;
  }
  export interface CreateStyleSheetReturn {
    /** Identifier of the created "via-inspector" stylesheet. */
    styleSheetId: DOM.StyleSheetId;
  }
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface ForcePseudoStateParams {
    /** The element id for which to force the pseudo state. */
    nodeId: DOM.NodeId;
    /** Element pseudo classes to force when computing the element's style. */
    forcedPseudoClasses: string[];
  }
  export type ForcePseudoStateReturn = void;
  export interface ForceStartingStyleParams {
    /** The element id for which to force the starting-style state. */
    nodeId: DOM.NodeId;
    /** Boolean indicating if this is on or off. */
    forced: boolean;
  }
  export type ForceStartingStyleReturn = void;
  export interface GetBackgroundColorsParams {
    /** Id of the node to get background colors for. */
    nodeId: DOM.NodeId;
  }
  export interface GetBackgroundColorsReturn {
    /**
     * The range of background colors behind this element, if it contains any visible text. If no
     * visible text is present, this will be undefined. In the case of a flat background color,
     * this will consist of simply that color. In the case of a gradient, this will consist of each
     * of the color stops. For anything more complicated, this will be an empty array. Images will
     * be ignored (as if the image had failed to load).
     */
    backgroundColors?: string[];
    /** The computed font size for this node, as a CSS computed value string (e.g. '12px'). */
    computedFontSize?: string;
    /**
     * The computed font weight for this node, as a CSS computed value string (e.g. 'normal' or
     * '100').
     */
    computedFontWeight?: string;
  }
  export interface GetComputedStyleForNodeParams {
    nodeId: DOM.NodeId;
  }
  export interface GetComputedStyleForNodeReturn {
    /** Computed style for the specified DOM node. */
    computedStyle: CSS.CSSComputedStyleProperty[];
    /**
     * A list of non-standard "extra fields" which blink stores alongside each
     * computed style.
     */
    extraFields: CSS.ComputedStyleExtraFields;
  }
  export interface ResolveValuesParams {
    /** Cascade-dependent keywords (revert/revert-layer) do not work. */
    values: string[];
    /** Id of the node in whose context the expression is evaluated */
    nodeId: DOM.NodeId;
    /** Only longhands and custom property names are accepted. */
    propertyName?: string;
    /**
     * Pseudo element type, only works for pseudo elements that generate
     * elements in the tree, such as ::before and ::after.
     */
    pseudoType?: DOM.PseudoType;
    /** Pseudo element custom ident. */
    pseudoIdentifier?: string;
  }
  export interface ResolveValuesReturn {
    results: string[];
  }
  export interface GetLonghandPropertiesParams {
    shorthandName: string;
    value: string;
  }
  export interface GetLonghandPropertiesReturn {
    longhandProperties: CSS.CSSProperty[];
  }
  export interface GetInlineStylesForNodeParams {
    nodeId: DOM.NodeId;
  }
  export interface GetInlineStylesForNodeReturn {
    /** Inline style for the specified DOM node. */
    inlineStyle?: CSS.CSSStyle;
    /** Attribute-defined element style (e.g. resulting from "width=20 height=100%"). */
    attributesStyle?: CSS.CSSStyle;
  }
  export interface GetAnimatedStylesForNodeParams {
    nodeId: DOM.NodeId;
  }
  export interface GetAnimatedStylesForNodeReturn {
    /** Styles coming from animations. */
    animationStyles?: CSS.CSSAnimationStyle[];
    /** Style coming from transitions. */
    transitionsStyle?: CSS.CSSStyle;
    /**
     * Inherited style entries for animationsStyle and transitionsStyle from
     * the inheritance chain of the element.
     */
    inherited?: CSS.InheritedAnimatedStyleEntry[];
  }
  export interface GetMatchedStylesForNodeParams {
    nodeId: DOM.NodeId;
  }
  export interface GetMatchedStylesForNodeReturn {
    /** Inline style for the specified DOM node. */
    inlineStyle?: CSS.CSSStyle;
    /** Attribute-defined element style (e.g. resulting from "width=20 height=100%"). */
    attributesStyle?: CSS.CSSStyle;
    /** CSS rules matching this node, from all applicable stylesheets. */
    matchedCSSRules?: CSS.RuleMatch[];
    /** Pseudo style matches for this node. */
    pseudoElements?: CSS.PseudoElementMatches[];
    /** A chain of inherited styles (from the immediate node parent up to the DOM tree root). */
    inherited?: CSS.InheritedStyleEntry[];
    /** A chain of inherited pseudo element styles (from the immediate node parent up to the DOM tree root). */
    inheritedPseudoElements?: CSS.InheritedPseudoElementMatches[];
    /** A list of CSS keyframed animations matching this node. */
    cssKeyframesRules?: CSS.CSSKeyframesRule[];
    /** A list of CSS @position-try rules matching this node, based on the position-try-fallbacks property. */
    cssPositionTryRules?: CSS.CSSPositionTryRule[];
    /**
     * Index of the active fallback in the applied position-try-fallback property,
     * will not be set if there is no active position-try fallback.
     */
    activePositionFallbackIndex?: number;
    /** A list of CSS at-property rules matching this node. */
    cssPropertyRules?: CSS.CSSPropertyRule[];
    /** A list of CSS property registrations matching this node. */
    cssPropertyRegistrations?: CSS.CSSPropertyRegistration[];
    /** A list of simple @rules matching this node or its pseudo-elements. */
    cssAtRules?: CSS.CSSAtRule[];
    /** Id of the first parent element that does not have display: contents. */
    parentLayoutNodeId?: DOM.NodeId;
    /** A list of CSS at-function rules referenced by styles of this node. */
    cssFunctionRules?: CSS.CSSFunctionRule[];
  }
  export interface GetEnvironmentVariablesParams {}
  export interface GetEnvironmentVariablesReturn {
    environmentVariables: Record<string, unknown>;
  }
  export interface GetMediaQueriesParams {}
  export interface GetMediaQueriesReturn {
    medias: CSS.CSSMedia[];
  }
  export interface GetPlatformFontsForNodeParams {
    nodeId: DOM.NodeId;
  }
  export interface GetPlatformFontsForNodeReturn {
    /** Usage statistics for every employed platform font. */
    fonts: CSS.PlatformFontUsage[];
  }
  export interface GetStyleSheetTextParams {
    styleSheetId: DOM.StyleSheetId;
  }
  export interface GetStyleSheetTextReturn {
    /** The stylesheet text. */
    text: string;
  }
  export interface GetLayersForNodeParams {
    nodeId: DOM.NodeId;
  }
  export interface GetLayersForNodeReturn {
    rootLayer: CSS.CSSLayerData;
  }
  export interface GetLocationForSelectorParams {
    styleSheetId: DOM.StyleSheetId;
    selectorText: string;
  }
  export interface GetLocationForSelectorReturn {
    ranges: CSS.SourceRange[];
  }
  export interface TrackComputedStyleUpdatesForNodeParams {
    nodeId?: DOM.NodeId;
  }
  export type TrackComputedStyleUpdatesForNodeReturn = void;
  export interface TrackComputedStyleUpdatesParams {
    propertiesToTrack: CSS.CSSComputedStyleProperty[];
  }
  export type TrackComputedStyleUpdatesReturn = void;
  export interface TakeComputedStyleUpdatesParams {}
  export interface TakeComputedStyleUpdatesReturn {
    /** The list of node Ids that have their tracked computed styles updated. */
    nodeIds: DOM.NodeId[];
  }
  export interface SetEffectivePropertyValueForNodeParams {
    /** The element id for which to set property. */
    nodeId: DOM.NodeId;
    propertyName: string;
    value: string;
  }
  export type SetEffectivePropertyValueForNodeReturn = void;
  export interface SetPropertyRulePropertyNameParams {
    styleSheetId: DOM.StyleSheetId;
    range: CSS.SourceRange;
    propertyName: string;
  }
  export interface SetPropertyRulePropertyNameReturn {
    /** The resulting key text after modification. */
    propertyName: CSS.Value;
  }
  export interface SetKeyframeKeyParams {
    styleSheetId: DOM.StyleSheetId;
    range: CSS.SourceRange;
    keyText: string;
  }
  export interface SetKeyframeKeyReturn {
    /** The resulting key text after modification. */
    keyText: CSS.Value;
  }
  export interface SetMediaTextParams {
    styleSheetId: DOM.StyleSheetId;
    range: CSS.SourceRange;
    text: string;
  }
  export interface SetMediaTextReturn {
    /** The resulting CSS media rule after modification. */
    media: CSS.CSSMedia;
  }
  export interface SetContainerQueryTextParams {
    styleSheetId: DOM.StyleSheetId;
    range: CSS.SourceRange;
    text: string;
  }
  export interface SetContainerQueryTextReturn {
    /** The resulting CSS container query rule after modification. */
    containerQuery: CSS.CSSContainerQuery;
  }
  export interface SetSupportsTextParams {
    styleSheetId: DOM.StyleSheetId;
    range: CSS.SourceRange;
    text: string;
  }
  export interface SetSupportsTextReturn {
    /** The resulting CSS Supports rule after modification. */
    supports: CSS.CSSSupports;
  }
  export interface SetNavigationTextParams {
    styleSheetId: DOM.StyleSheetId;
    range: CSS.SourceRange;
    text: string;
  }
  export interface SetNavigationTextReturn {
    /** The resulting CSS Navigation rule after modification. */
    navigation: CSS.CSSNavigation;
  }
  export interface SetScopeTextParams {
    styleSheetId: DOM.StyleSheetId;
    range: CSS.SourceRange;
    text: string;
  }
  export interface SetScopeTextReturn {
    /** The resulting CSS Scope rule after modification. */
    scope: CSS.CSSScope;
  }
  export interface SetRuleSelectorParams {
    styleSheetId: DOM.StyleSheetId;
    range: CSS.SourceRange;
    selector: string;
  }
  export interface SetRuleSelectorReturn {
    /** The resulting selector list after modification. */
    selectorList: CSS.SelectorList;
  }
  export interface SetStyleSheetTextParams {
    styleSheetId: DOM.StyleSheetId;
    text: string;
  }
  export interface SetStyleSheetTextReturn {
    /** URL of source map associated with script (if any). */
    sourceMapURL?: string;
  }
  export interface SetStyleTextsParams {
    edits: CSS.StyleDeclarationEdit[];
    /**
     * NodeId for the DOM node in whose context custom property declarations for registered properties should be
     * validated. If omitted, declarations in the new rule text can only be validated statically, which may produce
     * incorrect results if the declaration contains a var() for example.
     */
    nodeForPropertySyntaxValidation?: DOM.NodeId;
  }
  export interface SetStyleTextsReturn {
    /** The resulting styles after modification. */
    styles: CSS.CSSStyle[];
  }
  export interface StartRuleUsageTrackingParams {}
  export type StartRuleUsageTrackingReturn = void;
  export interface StopRuleUsageTrackingParams {}
  export interface StopRuleUsageTrackingReturn {
    ruleUsage: CSS.RuleUsage[];
  }
  export interface TakeCoverageDeltaParams {}
  export interface TakeCoverageDeltaReturn {
    coverage: CSS.RuleUsage[];
    /** Monotonically increasing time, in seconds. */
    timestamp: number;
  }
  export interface SetLocalFontsEnabledParams {
    /** Whether rendering of local fonts is enabled. */
    enabled: boolean;
  }
  export type SetLocalFontsEnabledReturn = void;
}

export namespace Debugger {
  export interface ContinueToLocationParams {
    /** Location to continue to. */
    location: Debugger.Location;
    targetCallFrames?: "any" | "current";
  }
  export type ContinueToLocationReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {
    /**
     * The maximum size in bytes of collected scripts (not referenced by other heap objects)
     * the debugger can hold. Puts no limit if parameter is omitted.
     */
    maxScriptsCacheSize?: number;
  }
  export interface EnableReturn {
    /** Unique identifier of the debugger. */
    debuggerId: Runtime.UniqueDebuggerId;
  }
  export interface EvaluateOnCallFrameParams {
    /** Call frame identifier to evaluate on. */
    callFrameId: Debugger.CallFrameId;
    /** Expression to evaluate. */
    expression: string;
    /**
     * String object group name to put result into (allows rapid releasing resulting object handles
     * using `releaseObjectGroup`).
     */
    objectGroup?: string;
    /**
     * Specifies whether command line API should be available to the evaluated expression, defaults
     * to false.
     */
    includeCommandLineAPI?: boolean;
    /**
     * In silent mode exceptions thrown during evaluation are not reported and do not pause
     * execution. Overrides `setPauseOnException` state.
     */
    silent?: boolean;
    /** Whether the result is expected to be a JSON object that should be sent by value. */
    returnByValue?: boolean;
    /** Whether preview should be generated for the result. */
    generatePreview?: boolean;
    /** Whether to throw an exception if side effect cannot be ruled out during evaluation. */
    throwOnSideEffect?: boolean;
    /** Terminate execution after timing out (number of milliseconds). */
    timeout?: Runtime.TimeDelta;
  }
  export interface EvaluateOnCallFrameReturn {
    /** Object wrapper for the evaluation result. */
    result: Runtime.RemoteObject;
    /** Exception details. */
    exceptionDetails?: Runtime.ExceptionDetails;
  }
  export interface GetPossibleBreakpointsParams {
    /** Start of range to search possible breakpoint locations in. */
    start: Debugger.Location;
    /**
     * End of range to search possible breakpoint locations in (excluding). When not specified, end
     * of scripts is used as end of range.
     */
    end?: Debugger.Location;
    /** Only consider locations which are in the same (non-nested) function as start. */
    restrictToFunction?: boolean;
  }
  export interface GetPossibleBreakpointsReturn {
    /** List of the possible breakpoint locations. */
    locations: Debugger.BreakLocation[];
  }
  export interface GetScriptSourceParams {
    /** Id of the script to get source for. */
    scriptId: Runtime.ScriptId;
  }
  export interface GetScriptSourceReturn {
    /** Script source (empty in case of Wasm bytecode). */
    scriptSource: string;
    /** Wasm bytecode. (Encoded as a base64 string when passed over JSON) */
    bytecode?: string;
  }
  export interface DisassembleWasmModuleParams {
    /** Id of the script to disassemble */
    scriptId: Runtime.ScriptId;
  }
  export interface DisassembleWasmModuleReturn {
    /**
     * For large modules, return a stream from which additional chunks of
     * disassembly can be read successively.
     */
    streamId?: string;
    /** The total number of lines in the disassembly text. */
    totalNumberOfLines: number;
    /**
     * The offsets of all function bodies, in the format [start1, end1,
     * start2, end2, ...] where all ends are exclusive.
     */
    functionBodyOffsets: number[];
    /** The first chunk of disassembly. */
    chunk: Debugger.WasmDisassemblyChunk;
  }
  export interface NextWasmDisassemblyChunkParams {
    streamId: string;
  }
  export interface NextWasmDisassemblyChunkReturn {
    /** The next chunk of disassembly. */
    chunk: Debugger.WasmDisassemblyChunk;
  }
  export interface GetWasmBytecodeParams {
    /** Id of the Wasm script to get source for. */
    scriptId: Runtime.ScriptId;
  }
  export interface GetWasmBytecodeReturn {
    /** Script source. (Encoded as a base64 string when passed over JSON) */
    bytecode: string;
  }
  export interface GetStackTraceParams {
    stackTraceId: Runtime.StackTraceId;
  }
  export interface GetStackTraceReturn {
    stackTrace: Runtime.StackTrace;
  }
  export interface PauseParams {}
  export type PauseReturn = void;
  export interface PauseOnAsyncCallParams {
    /** Debugger will pause when async call with given stack trace is started. */
    parentStackTraceId: Runtime.StackTraceId;
  }
  export type PauseOnAsyncCallReturn = void;
  export interface RemoveBreakpointParams {
    breakpointId: Debugger.BreakpointId;
  }
  export type RemoveBreakpointReturn = void;
  export interface RestartFrameParams {
    /** Call frame identifier to evaluate on. */
    callFrameId: Debugger.CallFrameId;
    /**
     * The `mode` parameter must be present and set to 'StepInto', otherwise
     * `restartFrame` will error out.
     */
    mode?: "StepInto";
  }
  export interface RestartFrameReturn {
    /** New stack trace. */
    callFrames: Debugger.CallFrame[];
    /** Async stack trace, if any. */
    asyncStackTrace?: Runtime.StackTrace;
    /** Async stack trace, if any. */
    asyncStackTraceId?: Runtime.StackTraceId;
  }
  export interface ResumeParams {
    /**
     * Set to true to terminate execution upon resuming execution. In contrast
     * to Runtime.terminateExecution, this will allows to execute further
     * JavaScript (i.e. via evaluation) until execution of the paused code
     * is actually resumed, at which point termination is triggered.
     * If execution is currently not paused, this parameter has no effect.
     */
    terminateOnResume?: boolean;
  }
  export type ResumeReturn = void;
  export interface SearchInContentParams {
    /** Id of the script to search in. */
    scriptId: Runtime.ScriptId;
    /** String to search for. */
    query: string;
    /** If true, search is case sensitive. */
    caseSensitive?: boolean;
    /** If true, treats string parameter as regex. */
    isRegex?: boolean;
  }
  export interface SearchInContentReturn {
    /** List of search matches. */
    result: Debugger.SearchMatch[];
  }
  export interface SetAsyncCallStackDepthParams {
    /**
     * Maximum depth of async call stacks. Setting to `0` will effectively disable collecting async
     * call stacks (default).
     */
    maxDepth: number;
  }
  export type SetAsyncCallStackDepthReturn = void;
  export interface SetBlackboxExecutionContextsParams {
    /** Array of execution context unique ids for the debugger to ignore. */
    uniqueIds: string[];
  }
  export type SetBlackboxExecutionContextsReturn = void;
  export interface SetBlackboxPatternsParams {
    /** Array of regexps that will be used to check script url for blackbox state. */
    patterns: string[];
    /** If true, also ignore scripts with no source url. */
    skipAnonymous?: boolean;
  }
  export type SetBlackboxPatternsReturn = void;
  export interface SetBlackboxedRangesParams {
    /** Id of the script. */
    scriptId: Runtime.ScriptId;
    positions: Debugger.ScriptPosition[];
  }
  export type SetBlackboxedRangesReturn = void;
  export interface SetBreakpointParams {
    /** Location to set breakpoint in. */
    location: Debugger.Location;
    /**
     * Expression to use as a breakpoint condition. When specified, debugger will only stop on the
     * breakpoint if this expression evaluates to true.
     */
    condition?: string;
  }
  export interface SetBreakpointReturn {
    /** Id of the created breakpoint for further reference. */
    breakpointId: Debugger.BreakpointId;
    /** Location this breakpoint resolved into. */
    actualLocation: Debugger.Location;
  }
  export interface SetInstrumentationBreakpointParams {
    /** Instrumentation name. */
    instrumentation: "beforeScriptExecution" | "beforeScriptWithSourceMapExecution";
  }
  export interface SetInstrumentationBreakpointReturn {
    /** Id of the created breakpoint for further reference. */
    breakpointId: Debugger.BreakpointId;
  }
  export interface SetBreakpointByUrlParams {
    /** Line number to set breakpoint at. */
    lineNumber: number;
    /** URL of the resources to set breakpoint on. */
    url?: string;
    /**
     * Regex pattern for the URLs of the resources to set breakpoints on. Either `url` or
     * `urlRegex` must be specified.
     */
    urlRegex?: string;
    /** Script hash of the resources to set breakpoint on. */
    scriptHash?: string;
    /** Offset in the line to set breakpoint at. */
    columnNumber?: number;
    /**
     * Expression to use as a breakpoint condition. When specified, debugger will only stop on the
     * breakpoint if this expression evaluates to true.
     */
    condition?: string;
  }
  export interface SetBreakpointByUrlReturn {
    /** Id of the created breakpoint for further reference. */
    breakpointId: Debugger.BreakpointId;
    /** List of the locations this breakpoint resolved into upon addition. */
    locations: Debugger.Location[];
  }
  export interface SetBreakpointOnFunctionCallParams {
    /** Function object id. */
    objectId: Runtime.RemoteObjectId;
    /**
     * Expression to use as a breakpoint condition. When specified, debugger will
     * stop on the breakpoint if this expression evaluates to true.
     */
    condition?: string;
  }
  export interface SetBreakpointOnFunctionCallReturn {
    /** Id of the created breakpoint for further reference. */
    breakpointId: Debugger.BreakpointId;
  }
  export interface SetBreakpointsActiveParams {
    /** New value for breakpoints active state. */
    active: boolean;
  }
  export type SetBreakpointsActiveReturn = void;
  export interface SetPauseOnExceptionsParams {
    /** Pause on exceptions mode. */
    state: "none" | "caught" | "uncaught" | "all";
  }
  export type SetPauseOnExceptionsReturn = void;
  export interface SetReturnValueParams {
    /** New return value. */
    newValue: Runtime.CallArgument;
  }
  export type SetReturnValueReturn = void;
  export interface SetScriptSourceParams {
    /** Id of the script to edit. */
    scriptId: Runtime.ScriptId;
    /** New content of the script. */
    scriptSource: string;
    /**
     * If true the change will not actually be applied. Dry run may be used to get result
     * description without actually modifying the code.
     */
    dryRun?: boolean;
    /**
     * If true, then `scriptSource` is allowed to change the function on top of the stack
     * as long as the top-most stack frame is the only activation of that function.
     */
    allowTopFrameEditing?: boolean;
  }
  export interface SetScriptSourceReturn {
    /** New stack trace in case editing has happened while VM was stopped. */
    callFrames?: Debugger.CallFrame[];
    /** Whether current call stack  was modified after applying the changes. */
    stackChanged?: boolean;
    /** Async stack trace, if any. */
    asyncStackTrace?: Runtime.StackTrace;
    /** Async stack trace, if any. */
    asyncStackTraceId?: Runtime.StackTraceId;
    /**
     * Whether the operation was successful or not. Only `Ok` denotes a
     * successful live edit while the other enum variants denote why
     * the live edit failed.
     */
    status: "Ok" | "CompileError" | "BlockedByActiveGenerator" | "BlockedByActiveFunction" | "BlockedByTopLevelEsModuleChange";
    /** Exception details if any. Only present when `status` is `CompileError`. */
    exceptionDetails?: Runtime.ExceptionDetails;
  }
  export interface SetSkipAllPausesParams {
    /** New value for skip pauses state. */
    skip: boolean;
  }
  export type SetSkipAllPausesReturn = void;
  export interface SetVariableValueParams {
    /**
     * 0-based number of scope as was listed in scope chain. Only 'local', 'closure' and 'catch'
     * scope types are allowed. Other scopes could be manipulated manually.
     */
    scopeNumber: number;
    /** Variable name. */
    variableName: string;
    /** New variable value. */
    newValue: Runtime.CallArgument;
    /** Id of callframe that holds variable. */
    callFrameId: Debugger.CallFrameId;
  }
  export type SetVariableValueReturn = void;
  export interface StepIntoParams {
    /**
     * Debugger will pause on the execution of the first async task which was scheduled
     * before next pause.
     */
    breakOnAsyncCall?: boolean;
    /** The skipList specifies location ranges that should be skipped on step into. */
    skipList?: Debugger.LocationRange[];
  }
  export type StepIntoReturn = void;
  export interface StepOutParams {}
  export type StepOutReturn = void;
  export interface StepOverParams {
    /** The skipList specifies location ranges that should be skipped on step over. */
    skipList?: Debugger.LocationRange[];
  }
  export type StepOverReturn = void;
}

export namespace DeviceAccess {
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface SelectPromptParams {
    id: DeviceAccess.RequestId;
    deviceId: DeviceAccess.DeviceId;
  }
  export type SelectPromptReturn = void;
  export interface CancelPromptParams {
    id: DeviceAccess.RequestId;
  }
  export type CancelPromptReturn = void;
}

export namespace DeviceOrientation {
  export interface ClearDeviceOrientationOverrideParams {}
  export type ClearDeviceOrientationOverrideReturn = void;
  export interface SetDeviceOrientationOverrideParams {
    /** Mock alpha */
    alpha: number;
    /** Mock beta */
    beta: number;
    /** Mock gamma */
    gamma: number;
  }
  export type SetDeviceOrientationOverrideReturn = void;
}

export namespace DOM {
  export interface CollectClassNamesFromSubtreeParams {
    /** Id of the node to collect class names. */
    nodeId: DOM.NodeId;
  }
  export interface CollectClassNamesFromSubtreeReturn {
    /** Class name list. */
    classNames: string[];
  }
  export interface CopyToParams {
    /** Id of the node to copy. */
    nodeId: DOM.NodeId;
    /** Id of the element to drop the copy into. */
    targetNodeId: DOM.NodeId;
    /**
     * Drop the copy before this node (if absent, the copy becomes the last child of
     * `targetNodeId`).
     */
    insertBeforeNodeId?: DOM.NodeId;
  }
  export interface CopyToReturn {
    /** Id of the node clone. */
    nodeId: DOM.NodeId;
  }
  export interface DescribeNodeParams {
    /** Identifier of the node. */
    nodeId?: DOM.NodeId;
    /** Identifier of the backend node. */
    backendNodeId?: DOM.BackendNodeId;
    /** JavaScript object id of the node wrapper. */
    objectId?: Runtime.RemoteObjectId;
    /**
     * The maximum depth at which children should be retrieved, defaults to 1. Use -1 for the
     * entire subtree or provide an integer larger than 0.
     */
    depth?: number;
    /**
     * Whether or not iframes and shadow roots should be traversed when returning the subtree
     * (default is false).
     */
    pierce?: boolean;
  }
  export interface DescribeNodeReturn {
    /** Node description. */
    node: DOM.Node;
  }
  export interface ScrollIntoViewIfNeededParams {
    /** Identifier of the node. */
    nodeId?: DOM.NodeId;
    /** Identifier of the backend node. */
    backendNodeId?: DOM.BackendNodeId;
    /** JavaScript object id of the node wrapper. */
    objectId?: Runtime.RemoteObjectId;
    /**
     * The rect to be scrolled into view, relative to the node's border box, in CSS pixels.
     * When omitted, center of the node will be used, similar to Element.scrollIntoView.
     */
    rect?: DOM.Rect;
  }
  export type ScrollIntoViewIfNeededReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface DiscardSearchResultsParams {
    /** Unique search session identifier. */
    searchId: string;
  }
  export type DiscardSearchResultsReturn = void;
  export interface EnableParams {
    /** Whether to include whitespaces in the children array of returned Nodes. */
    includeWhitespace?: "none" | "all";
  }
  export type EnableReturn = void;
  export interface FocusParams {
    /** Identifier of the node. */
    nodeId?: DOM.NodeId;
    /** Identifier of the backend node. */
    backendNodeId?: DOM.BackendNodeId;
    /** JavaScript object id of the node wrapper. */
    objectId?: Runtime.RemoteObjectId;
  }
  export type FocusReturn = void;
  export interface GetAttributesParams {
    /** Id of the node to retrieve attributes for. */
    nodeId: DOM.NodeId;
  }
  export interface GetAttributesReturn {
    /** An interleaved array of node attribute names and values. */
    attributes: string[];
  }
  export interface GetBoxModelParams {
    /** Identifier of the node. */
    nodeId?: DOM.NodeId;
    /** Identifier of the backend node. */
    backendNodeId?: DOM.BackendNodeId;
    /** JavaScript object id of the node wrapper. */
    objectId?: Runtime.RemoteObjectId;
  }
  export interface GetBoxModelReturn {
    /** Box model for the node. */
    model: DOM.BoxModel;
  }
  export interface GetContentQuadsParams {
    /** Identifier of the node. */
    nodeId?: DOM.NodeId;
    /** Identifier of the backend node. */
    backendNodeId?: DOM.BackendNodeId;
    /** JavaScript object id of the node wrapper. */
    objectId?: Runtime.RemoteObjectId;
  }
  export interface GetContentQuadsReturn {
    /** Quads that describe node layout relative to viewport. */
    quads: DOM.Quad[];
  }
  export interface GetDocumentParams {
    /**
     * The maximum depth at which children should be retrieved, defaults to 1. Use -1 for the
     * entire subtree or provide an integer larger than 0.
     */
    depth?: number;
    /**
     * Whether or not iframes and shadow roots should be traversed when returning the subtree
     * (default is false).
     */
    pierce?: boolean;
  }
  export interface GetDocumentReturn {
    /** Resulting node. */
    root: DOM.Node;
  }
  export interface GetFlattenedDocumentParams {
    /**
     * The maximum depth at which children should be retrieved, defaults to 1. Use -1 for the
     * entire subtree or provide an integer larger than 0.
     */
    depth?: number;
    /**
     * Whether or not iframes and shadow roots should be traversed when returning the subtree
     * (default is false).
     */
    pierce?: boolean;
  }
  export interface GetFlattenedDocumentReturn {
    /** Resulting node. */
    nodes: DOM.Node[];
  }
  export interface GetNodesForSubtreeByStyleParams {
    /** Node ID pointing to the root of a subtree. */
    nodeId: DOM.NodeId;
    /** The style to filter nodes by (includes nodes if any of properties matches). */
    computedStyles: DOM.CSSComputedStyleProperty[];
    /**
     * Whether or not iframes and shadow roots in the same target should be traversed when returning the
     * results (default is false).
     */
    pierce?: boolean;
  }
  export interface GetNodesForSubtreeByStyleReturn {
    /** Resulting nodes. */
    nodeIds: DOM.NodeId[];
  }
  export interface GetNodeForLocationParams {
    /** X coordinate. */
    x: number;
    /** Y coordinate. */
    y: number;
    /** False to skip to the nearest non-UA shadow root ancestor (default: false). */
    includeUserAgentShadowDOM?: boolean;
    /** Whether to ignore pointer-events: none on elements and hit test them. */
    ignorePointerEventsNone?: boolean;
  }
  export interface GetNodeForLocationReturn {
    /** Resulting node. */
    backendNodeId: DOM.BackendNodeId;
    /** Frame this node belongs to. */
    frameId: Page.FrameId;
    /** Id of the node at given coordinates, only when enabled and requested document. */
    nodeId?: DOM.NodeId;
  }
  export interface GetOuterHTMLParams {
    /** Identifier of the node. */
    nodeId?: DOM.NodeId;
    /** Identifier of the backend node. */
    backendNodeId?: DOM.BackendNodeId;
    /** JavaScript object id of the node wrapper. */
    objectId?: Runtime.RemoteObjectId;
    /** Include all shadow roots. Equals to false if not specified. */
    includeShadowDOM?: boolean;
  }
  export interface GetOuterHTMLReturn {
    /** Outer HTML markup. */
    outerHTML: string;
  }
  export interface GetRelayoutBoundaryParams {
    /** Id of the node. */
    nodeId: DOM.NodeId;
  }
  export interface GetRelayoutBoundaryReturn {
    /** Relayout boundary node id for the given node. */
    nodeId: DOM.NodeId;
  }
  export interface GetSearchResultsParams {
    /** Unique search session identifier. */
    searchId: string;
    /** Start index of the search result to be returned. */
    fromIndex: number;
    /** End index of the search result to be returned. */
    toIndex: number;
  }
  export interface GetSearchResultsReturn {
    /** Ids of the search result nodes. */
    nodeIds: DOM.NodeId[];
  }
  export interface MarkUndoableStateParams {}
  export type MarkUndoableStateReturn = void;
  export interface MoveToParams {
    /** Id of the node to move. */
    nodeId: DOM.NodeId;
    /** Id of the element to drop the moved node into. */
    targetNodeId: DOM.NodeId;
    /**
     * Drop node before this one (if absent, the moved node becomes the last child of
     * `targetNodeId`).
     */
    insertBeforeNodeId?: DOM.NodeId;
  }
  export interface MoveToReturn {
    /** New id of the moved node. */
    nodeId: DOM.NodeId;
  }
  export interface PerformSearchParams {
    /** Plain text or query selector or XPath search query. */
    query: string;
    /** True to search in user agent shadow DOM. */
    includeUserAgentShadowDOM?: boolean;
  }
  export interface PerformSearchReturn {
    /** Unique search session identifier. */
    searchId: string;
    /** Number of search results. */
    resultCount: number;
  }
  export interface PushNodeByPathToFrontendParams {
    /** Path to node in the proprietary format. */
    path: string;
  }
  export interface PushNodeByPathToFrontendReturn {
    /** Id of the node for given path. */
    nodeId: DOM.NodeId;
  }
  export interface PushNodesByBackendIdsToFrontendParams {
    /** The array of backend node ids. */
    backendNodeIds: DOM.BackendNodeId[];
  }
  export interface PushNodesByBackendIdsToFrontendReturn {
    /**
     * The array of ids of pushed nodes that correspond to the backend ids specified in
     * backendNodeIds.
     */
    nodeIds: DOM.NodeId[];
  }
  export interface QuerySelectorParams {
    /** Id of the node to query upon. */
    nodeId: DOM.NodeId;
    /** Selector string. */
    selector: string;
  }
  export interface QuerySelectorReturn {
    /** Query selector result. */
    nodeId: DOM.NodeId;
  }
  export interface QuerySelectorAllParams {
    /** Id of the node to query upon. */
    nodeId: DOM.NodeId;
    /** Selector string. */
    selector: string;
  }
  export interface QuerySelectorAllReturn {
    /** Query selector result. */
    nodeIds: DOM.NodeId[];
  }
  export interface GetTopLayerElementsParams {}
  export interface GetTopLayerElementsReturn {
    /** NodeIds of top layer elements */
    nodeIds: DOM.NodeId[];
  }
  export interface GetElementByRelationParams {
    /** Id of the node from which to query the relation. */
    nodeId: DOM.NodeId;
    /** Type of relation to get. */
    relation: "PopoverTarget" | "InterestTarget" | "CommandFor";
  }
  export interface GetElementByRelationReturn {
    /** NodeId of the element matching the queried relation. */
    nodeId: DOM.NodeId;
  }
  export interface RedoParams {}
  export type RedoReturn = void;
  export interface RemoveAttributeParams {
    /** Id of the element to remove attribute from. */
    nodeId: DOM.NodeId;
    /** Name of the attribute to remove. */
    name: string;
  }
  export type RemoveAttributeReturn = void;
  export interface RemoveNodeParams {
    /** Id of the node to remove. */
    nodeId: DOM.NodeId;
  }
  export type RemoveNodeReturn = void;
  export interface RequestChildNodesParams {
    /** Id of the node to get children for. */
    nodeId: DOM.NodeId;
    /**
     * The maximum depth at which children should be retrieved, defaults to 1. Use -1 for the
     * entire subtree or provide an integer larger than 0.
     */
    depth?: number;
    /**
     * Whether or not iframes and shadow roots should be traversed when returning the sub-tree
     * (default is false).
     */
    pierce?: boolean;
  }
  export type RequestChildNodesReturn = void;
  export interface RequestNodeParams {
    /** JavaScript object id to convert into node. */
    objectId: Runtime.RemoteObjectId;
  }
  export interface RequestNodeReturn {
    /** Node id for given object. */
    nodeId: DOM.NodeId;
  }
  export interface ResolveNodeParams {
    /** Id of the node to resolve. */
    nodeId?: DOM.NodeId;
    /** Backend identifier of the node to resolve. */
    backendNodeId?: DOM.BackendNodeId;
    /** Symbolic group name that can be used to release multiple objects. */
    objectGroup?: string;
    /** Execution context in which to resolve the node. */
    executionContextId?: Runtime.ExecutionContextId;
  }
  export interface ResolveNodeReturn {
    /** JavaScript object wrapper for given node. */
    object: Runtime.RemoteObject;
  }
  export interface SetAttributeValueParams {
    /** Id of the element to set attribute for. */
    nodeId: DOM.NodeId;
    /** Attribute name. */
    name: string;
    /** Attribute value. */
    value: string;
  }
  export type SetAttributeValueReturn = void;
  export interface SetAttributesAsTextParams {
    /** Id of the element to set attributes for. */
    nodeId: DOM.NodeId;
    /** Text with a number of attributes. Will parse this text using HTML parser. */
    text: string;
    /**
     * Attribute name to replace with new attributes derived from text in case text parsed
     * successfully.
     */
    name?: string;
  }
  export type SetAttributesAsTextReturn = void;
  export interface SetFileInputFilesParams {
    /** Array of file paths to set. */
    files: string[];
    /** Identifier of the node. */
    nodeId?: DOM.NodeId;
    /** Identifier of the backend node. */
    backendNodeId?: DOM.BackendNodeId;
    /** JavaScript object id of the node wrapper. */
    objectId?: Runtime.RemoteObjectId;
  }
  export type SetFileInputFilesReturn = void;
  export interface SetNodeStackTracesEnabledParams {
    /** Enable or disable. */
    enable: boolean;
  }
  export type SetNodeStackTracesEnabledReturn = void;
  export interface GetNodeStackTracesParams {
    /** Id of the node to get stack traces for. */
    nodeId: DOM.NodeId;
  }
  export interface GetNodeStackTracesReturn {
    /** Creation stack trace, if available. */
    creation?: Runtime.StackTrace;
  }
  export interface GetFileInfoParams {
    /** JavaScript object id of the node wrapper. */
    objectId: Runtime.RemoteObjectId;
  }
  export interface GetFileInfoReturn {
    path: string;
  }
  export interface GetDetachedDomNodesParams {}
  export interface GetDetachedDomNodesReturn {
    /** The list of detached nodes */
    detachedNodes: DOM.DetachedElementInfo[];
  }
  export interface SetInspectedNodeParams {
    /** DOM node id to be accessible by means of $x command line API. */
    nodeId: DOM.NodeId;
  }
  export type SetInspectedNodeReturn = void;
  export interface SetNodeNameParams {
    /** Id of the node to set name for. */
    nodeId: DOM.NodeId;
    /** New node's name. */
    name: string;
  }
  export interface SetNodeNameReturn {
    /** New node's id. */
    nodeId: DOM.NodeId;
  }
  export interface SetNodeValueParams {
    /** Id of the node to set value for. */
    nodeId: DOM.NodeId;
    /** New node's value. */
    value: string;
  }
  export type SetNodeValueReturn = void;
  export interface SetOuterHTMLParams {
    /** Id of the node to set markup for. */
    nodeId: DOM.NodeId;
    /** Outer HTML markup to set. */
    outerHTML: string;
  }
  export type SetOuterHTMLReturn = void;
  export interface UndoParams {}
  export type UndoReturn = void;
  export interface GetFrameOwnerParams {
    frameId: Page.FrameId;
  }
  export interface GetFrameOwnerReturn {
    /** Resulting node. */
    backendNodeId: DOM.BackendNodeId;
    /** Id of the node at given coordinates, only when enabled and requested document. */
    nodeId?: DOM.NodeId;
  }
  export interface GetContainerForNodeParams {
    nodeId: DOM.NodeId;
    containerName?: string;
    physicalAxes?: DOM.PhysicalAxes;
    logicalAxes?: DOM.LogicalAxes;
    queriesScrollState?: boolean;
    queriesAnchored?: boolean;
  }
  export interface GetContainerForNodeReturn {
    /** The container node for the given node, or null if not found. */
    nodeId?: DOM.NodeId;
  }
  export interface GetQueryingDescendantsForContainerParams {
    /** Id of the container node to find querying descendants from. */
    nodeId: DOM.NodeId;
  }
  export interface GetQueryingDescendantsForContainerReturn {
    /** Descendant nodes with container queries against the given container. */
    nodeIds: DOM.NodeId[];
  }
  export interface GetAnchorElementParams {
    /** Id of the positioned element from which to find the anchor. */
    nodeId: DOM.NodeId;
    /**
     * An optional anchor specifier, as defined in
     * https://www.w3.org/TR/css-anchor-position-1/#anchor-specifier.
     * If not provided, it will return the implicit anchor element for
     * the given positioned element.
     */
    anchorSpecifier?: string;
  }
  export interface GetAnchorElementReturn {
    /** The anchor element of the given anchor query. */
    nodeId: DOM.NodeId;
  }
  export interface ForceShowPopoverParams {
    /** Id of the popover HTMLElement */
    nodeId: DOM.NodeId;
    /**
     * If true, opens the popover and keeps it open. If false, closes the
     * popover if it was previously force-opened.
     */
    enable: boolean;
  }
  export interface ForceShowPopoverReturn {
    /** List of popovers that were closed in order to respect popover stacking order. */
    nodeIds: DOM.NodeId[];
  }
}

export namespace DOMDebugger {
  export interface GetEventListenersParams {
    /** Identifier of the object to return listeners for. */
    objectId: Runtime.RemoteObjectId;
    /**
     * The maximum depth at which Node children should be retrieved, defaults to 1. Use -1 for the
     * entire subtree or provide an integer larger than 0.
     */
    depth?: number;
    /**
     * Whether or not iframes and shadow roots should be traversed when returning the subtree
     * (default is false). Reports listeners for all contexts if pierce is enabled.
     */
    pierce?: boolean;
  }
  export interface GetEventListenersReturn {
    /** Array of relevant listeners. */
    listeners: DOMDebugger.EventListener[];
  }
  export interface RemoveDOMBreakpointParams {
    /** Identifier of the node to remove breakpoint from. */
    nodeId: DOM.NodeId;
    /** Type of the breakpoint to remove. */
    type: DOMDebugger.DOMBreakpointType;
  }
  export type RemoveDOMBreakpointReturn = void;
  export interface RemoveEventListenerBreakpointParams {
    /** Event name. */
    eventName: string;
    /** EventTarget interface name. */
    targetName?: string;
  }
  export type RemoveEventListenerBreakpointReturn = void;
  export interface RemoveXHRBreakpointParams {
    /** Resource URL substring. */
    url: string;
  }
  export type RemoveXHRBreakpointReturn = void;
  export interface SetBreakOnCSPViolationParams {
    /** CSP Violations to stop upon. */
    violationTypes: DOMDebugger.CSPViolationType[];
  }
  export type SetBreakOnCSPViolationReturn = void;
  export interface SetDOMBreakpointParams {
    /** Identifier of the node to set breakpoint on. */
    nodeId: DOM.NodeId;
    /** Type of the operation to stop upon. */
    type: DOMDebugger.DOMBreakpointType;
  }
  export type SetDOMBreakpointReturn = void;
  export interface SetEventListenerBreakpointParams {
    /** DOM Event name to stop on (any DOM event will do). */
    eventName: string;
    /**
     * EventTarget interface name to stop on. If equal to `"*"` or not provided, will stop on any
     * EventTarget.
     */
    targetName?: string;
  }
  export type SetEventListenerBreakpointReturn = void;
  export interface SetXHRBreakpointParams {
    /** Resource URL substring. All XHRs having this substring in the URL will get stopped upon. */
    url: string;
  }
  export type SetXHRBreakpointReturn = void;
}

export namespace DOMSnapshot {
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface GetSnapshotParams {
    /** Whitelist of computed styles to return. */
    computedStyleWhitelist: string[];
    /** Whether or not to retrieve details of DOM listeners (default false). */
    includeEventListeners?: boolean;
    /** Whether to determine and include the paint order index of LayoutTreeNodes (default false). */
    includePaintOrder?: boolean;
    /** Whether to include UA shadow tree in the snapshot (default false). */
    includeUserAgentShadowTree?: boolean;
  }
  export interface GetSnapshotReturn {
    /** The nodes in the DOM tree. The DOMNode at index 0 corresponds to the root document. */
    domNodes: DOMSnapshot.DOMNode[];
    /** The nodes in the layout tree. */
    layoutTreeNodes: DOMSnapshot.LayoutTreeNode[];
    /** Whitelisted ComputedStyle properties for each node in the layout tree. */
    computedStyles: DOMSnapshot.ComputedStyle[];
  }
  export interface CaptureSnapshotParams {
    /** Whitelist of computed styles to return. */
    computedStyles: string[];
    /** Whether to include layout object paint orders into the snapshot. */
    includePaintOrder?: boolean;
    /** Whether to include DOM rectangles (offsetRects, clientRects, scrollRects) into the snapshot */
    includeDOMRects?: boolean;
    /**
     * Whether to include blended background colors in the snapshot (default: false).
     * Blended background color is achieved by blending background colors of all elements
     * that overlap with the current element.
     */
    includeBlendedBackgroundColors?: boolean;
    /**
     * Whether to include text color opacity in the snapshot (default: false).
     * An element might have the opacity property set that affects the text color of the element.
     * The final text color opacity is computed based on the opacity of all overlapping elements.
     */
    includeTextColorOpacities?: boolean;
  }
  export interface CaptureSnapshotReturn {
    /** The nodes in the DOM tree. The DOMNode at index 0 corresponds to the root document. */
    documents: DOMSnapshot.DocumentSnapshot[];
    /** Shared string table that all string properties refer to with indexes. */
    strings: string[];
  }
}

export namespace DOMStorage {
  export interface ClearParams {
    storageId: DOMStorage.StorageId;
  }
  export type ClearReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface GetDOMStorageItemsParams {
    storageId: DOMStorage.StorageId;
  }
  export interface GetDOMStorageItemsReturn {
    entries: DOMStorage.Item[];
  }
  export interface RemoveDOMStorageItemParams {
    storageId: DOMStorage.StorageId;
    key: string;
  }
  export type RemoveDOMStorageItemReturn = void;
  export interface SetDOMStorageItemParams {
    storageId: DOMStorage.StorageId;
    key: string;
    value: string;
  }
  export type SetDOMStorageItemReturn = void;
}

export namespace Emulation {
  export interface CanEmulateParams {}
  export interface CanEmulateReturn {
    /** True if emulation is supported. */
    result: boolean;
  }
  export interface ClearDeviceMetricsOverrideParams {}
  export type ClearDeviceMetricsOverrideReturn = void;
  export interface ClearGeolocationOverrideParams {}
  export type ClearGeolocationOverrideReturn = void;
  export interface ResetPageScaleFactorParams {}
  export type ResetPageScaleFactorReturn = void;
  export interface SetFocusEmulationEnabledParams {
    /** Whether to enable to disable focus emulation. */
    enabled: boolean;
  }
  export type SetFocusEmulationEnabledReturn = void;
  export interface SetAutoDarkModeOverrideParams {
    /**
     * Whether to enable or disable automatic dark mode.
     * If not specified, any existing override will be cleared.
     */
    enabled?: boolean;
  }
  export type SetAutoDarkModeOverrideReturn = void;
  export interface SetCPUThrottlingRateParams {
    /** Throttling rate as a slowdown factor (1 is no throttle, 2 is 2x slowdown, etc). */
    rate: number;
  }
  export type SetCPUThrottlingRateReturn = void;
  export interface SetDefaultBackgroundColorOverrideParams {
    /**
     * RGBA of the default background color. If not specified, any existing override will be
     * cleared.
     */
    color?: DOM.RGBA;
  }
  export type SetDefaultBackgroundColorOverrideReturn = void;
  export interface SetSafeAreaInsetsOverrideParams {
    insets: Emulation.SafeAreaInsets;
  }
  export type SetSafeAreaInsetsOverrideReturn = void;
  export interface SetDeviceMetricsOverrideParams {
    /** Overriding width value in pixels (minimum 0, maximum 10000000). 0 disables the override. */
    width: number;
    /** Overriding height value in pixels (minimum 0, maximum 10000000). 0 disables the override. */
    height: number;
    /** Overriding device scale factor value. 0 disables the override. */
    deviceScaleFactor: number;
    /**
     * Whether to emulate mobile device. This includes viewport meta tag, overlay scrollbars, text
     * autosizing and more.
     */
    mobile: boolean;
    /** Scale to apply to resulting view image. */
    scale?: number;
    /** Overriding screen width value in pixels (minimum 0, maximum 10000000). */
    screenWidth?: number;
    /** Overriding screen height value in pixels (minimum 0, maximum 10000000). */
    screenHeight?: number;
    /** Overriding view X position on screen in pixels (minimum 0, maximum 10000000). */
    positionX?: number;
    /** Overriding view Y position on screen in pixels (minimum 0, maximum 10000000). */
    positionY?: number;
    /** Do not set visible view size, rely upon explicit setVisibleSize call. */
    dontSetVisibleSize?: boolean;
    /** Screen orientation override. */
    screenOrientation?: Emulation.ScreenOrientation;
    /**
     * If set, the visible area of the page will be overridden to this viewport. This viewport
     * change is not observed by the page, e.g. viewport-relative elements do not change positions.
     */
    viewport?: Page.Viewport;
    /**
     * If set, the display feature of a multi-segment screen. If not set, multi-segment support
     * is turned-off.
     * Deprecated, use Emulation.setDisplayFeaturesOverride.
     */
    displayFeature?: Emulation.DisplayFeature;
    /**
     * If set, the posture of a foldable device. If not set the posture is set
     * to continuous.
     * Deprecated, use Emulation.setDevicePostureOverride.
     */
    devicePosture?: Emulation.DevicePosture;
    /** Scrollbar type. Default: `default`. */
    scrollbarType?: "overlay" | "default";
    /**
     * If set to true, enables screen orientation lock emulation, which
     * intercepts screen.orientation.lock() calls from the page and reports
     * orientation changes via screenOrientationLockChanged events. This is
     * useful for emulating mobile device orientation lock behavior in
     * responsive design mode.
     */
    screenOrientationLockEmulation?: boolean;
  }
  export type SetDeviceMetricsOverrideReturn = void;
  export interface SetDevicePostureOverrideParams {
    posture: Emulation.DevicePosture;
  }
  export type SetDevicePostureOverrideReturn = void;
  export interface ClearDevicePostureOverrideParams {}
  export type ClearDevicePostureOverrideReturn = void;
  export interface SetDisplayFeaturesOverrideParams {
    features: Emulation.DisplayFeature[];
  }
  export type SetDisplayFeaturesOverrideReturn = void;
  export interface ClearDisplayFeaturesOverrideParams {}
  export type ClearDisplayFeaturesOverrideReturn = void;
  export interface SetScrollbarsHiddenParams {
    /** Whether scrollbars should be always hidden. */
    hidden: boolean;
  }
  export type SetScrollbarsHiddenReturn = void;
  export interface SetDocumentCookieDisabledParams {
    /** Whether document.coookie API should be disabled. */
    disabled: boolean;
  }
  export type SetDocumentCookieDisabledReturn = void;
  export interface SetEmitTouchEventsForMouseParams {
    /** Whether touch emulation based on mouse input should be enabled. */
    enabled: boolean;
    /** Touch/gesture events configuration. Default: current platform. */
    configuration?: "mobile" | "desktop";
  }
  export type SetEmitTouchEventsForMouseReturn = void;
  export interface SetEmulatedMediaParams {
    /** Media type to emulate. Empty string disables the override. */
    media?: string;
    /** Media features to emulate. */
    features?: Emulation.MediaFeature[];
  }
  export type SetEmulatedMediaReturn = void;
  export interface SetEmulatedVisionDeficiencyParams {
    /**
     * Vision deficiency to emulate. Order: best-effort emulations come first, followed by any
     * physiologically accurate emulations for medically recognized color vision deficiencies.
     */
    type: "none" | "blurredVision" | "reducedContrast" | "achromatopsia" | "deuteranopia" | "protanopia" | "tritanopia";
  }
  export type SetEmulatedVisionDeficiencyReturn = void;
  export interface SetEmulatedOSTextScaleParams {
    scale?: number;
  }
  export type SetEmulatedOSTextScaleReturn = void;
  export interface SetGeolocationOverrideParams {
    /** Mock latitude */
    latitude?: number;
    /** Mock longitude */
    longitude?: number;
    /** Mock accuracy */
    accuracy?: number;
    /** Mock altitude */
    altitude?: number;
    /** Mock altitudeAccuracy */
    altitudeAccuracy?: number;
    /** Mock heading */
    heading?: number;
    /** Mock speed */
    speed?: number;
  }
  export type SetGeolocationOverrideReturn = void;
  export interface GetOverriddenSensorInformationParams {
    type: Emulation.SensorType;
  }
  export interface GetOverriddenSensorInformationReturn {
    requestedSamplingFrequency: number;
  }
  export interface SetSensorOverrideEnabledParams {
    enabled: boolean;
    type: Emulation.SensorType;
    metadata?: Emulation.SensorMetadata;
  }
  export type SetSensorOverrideEnabledReturn = void;
  export interface SetSensorOverrideReadingsParams {
    type: Emulation.SensorType;
    reading: Emulation.SensorReading;
  }
  export type SetSensorOverrideReadingsReturn = void;
  export interface SetPressureSourceOverrideEnabledParams {
    enabled: boolean;
    source: Emulation.PressureSource;
    metadata?: Emulation.PressureMetadata;
  }
  export type SetPressureSourceOverrideEnabledReturn = void;
  export interface SetPressureStateOverrideParams {
    source: Emulation.PressureSource;
    state: Emulation.PressureState;
  }
  export type SetPressureStateOverrideReturn = void;
  export interface SetPressureDataOverrideParams {
    source: Emulation.PressureSource;
    state: Emulation.PressureState;
    ownContributionEstimate?: number;
  }
  export type SetPressureDataOverrideReturn = void;
  export interface SetIdleOverrideParams {
    /** Mock isUserActive */
    isUserActive: boolean;
    /** Mock isScreenUnlocked */
    isScreenUnlocked: boolean;
  }
  export type SetIdleOverrideReturn = void;
  export interface ClearIdleOverrideParams {}
  export type ClearIdleOverrideReturn = void;
  export interface SetNavigatorOverridesParams {
    /** The platform navigator.platform should return. */
    platform: string;
  }
  export type SetNavigatorOverridesReturn = void;
  export interface SetPageScaleFactorParams {
    /** Page scale factor. */
    pageScaleFactor: number;
  }
  export type SetPageScaleFactorReturn = void;
  export interface SetScriptExecutionDisabledParams {
    /** Whether script execution should be disabled in the page. */
    value: boolean;
  }
  export type SetScriptExecutionDisabledReturn = void;
  export interface SetTouchEmulationEnabledParams {
    /** Whether the touch event emulation should be enabled. */
    enabled: boolean;
    /** Maximum touch points supported. Defaults to one. */
    maxTouchPoints?: number;
  }
  export type SetTouchEmulationEnabledReturn = void;
  export interface SetVirtualTimePolicyParams {
    policy: Emulation.VirtualTimePolicy;
    /**
     * If set, after this many virtual milliseconds have elapsed virtual time will be paused and a
     * virtualTimeBudgetExpired event is sent.
     */
    budget?: number;
    /**
     * If set this specifies the maximum number of tasks that can be run before virtual is forced
     * forwards to prevent deadlock.
     */
    maxVirtualTimeTaskStarvationCount?: number;
    /** If set, base::Time::Now will be overridden to initially return this value. */
    initialVirtualTime?: Network.TimeSinceEpoch;
  }
  export interface SetVirtualTimePolicyReturn {
    /** Absolute timestamp at which virtual time was first enabled (up time in milliseconds). */
    virtualTimeTicksBase: number;
  }
  export interface SetLocaleOverrideParams {
    /**
     * ICU style C locale (e.g. "en_US"). If not specified or empty, disables the override and
     * restores default host system locale.
     */
    locale?: string;
  }
  export type SetLocaleOverrideReturn = void;
  export interface SetTimezoneOverrideParams {
    /**
     * The timezone identifier. List of supported timezones:
     * https://source.chromium.org/chromium/chromium/deps/icu.git/+/faee8bc70570192d82d2978a71e2a615788597d1:source/data/misc/metaZones.txt
     * If empty, disables the override and restores default host system timezone.
     */
    timezoneId: string;
  }
  export type SetTimezoneOverrideReturn = void;
  export interface SetVisibleSizeParams {
    /** Frame width (DIP). */
    width: number;
    /** Frame height (DIP). */
    height: number;
  }
  export type SetVisibleSizeReturn = void;
  export interface SetDisabledImageTypesParams {
    /** Image types to disable. */
    imageTypes: Emulation.DisabledImageType[];
  }
  export type SetDisabledImageTypesReturn = void;
  export interface SetDataSaverOverrideParams {
    /** Override value. Omitting the parameter disables the override. */
    dataSaverEnabled?: boolean;
  }
  export type SetDataSaverOverrideReturn = void;
  export interface SetHardwareConcurrencyOverrideParams {
    /** Hardware concurrency to report */
    hardwareConcurrency: number;
  }
  export type SetHardwareConcurrencyOverrideReturn = void;
  export interface SetUserAgentOverrideParams {
    /** User agent to use. */
    userAgent: string;
    /** Browser language to emulate. */
    acceptLanguage?: string;
    /** The platform navigator.platform should return. */
    platform?: string;
    /** To be sent in Sec-CH-UA-* headers and returned in navigator.userAgentData */
    userAgentMetadata?: Emulation.UserAgentMetadata;
  }
  export type SetUserAgentOverrideReturn = void;
  export interface SetAutomationOverrideParams {
    /** Whether the override should be enabled. */
    enabled: boolean;
  }
  export type SetAutomationOverrideReturn = void;
  export interface SetSmallViewportHeightDifferenceOverrideParams {
    /**
     * This will cause an element of size 100svh to be `difference` pixels smaller than an element
     * of size 100lvh.
     */
    difference: number;
  }
  export type SetSmallViewportHeightDifferenceOverrideReturn = void;
  export interface GetScreenInfosParams {}
  export interface GetScreenInfosReturn {
    screenInfos: Emulation.ScreenInfo[];
  }
  export interface AddScreenParams {
    /** Offset of the left edge of the screen in pixels. */
    left: number;
    /** Offset of the top edge of the screen in pixels. */
    top: number;
    /** The width of the screen in pixels. */
    width: number;
    /** The height of the screen in pixels. */
    height: number;
    /** Specifies the screen's work area. Default is entire screen. */
    workAreaInsets?: Emulation.WorkAreaInsets;
    /** Specifies the screen's device pixel ratio. Default is 1. */
    devicePixelRatio?: number;
    /** Specifies the screen's rotation angle. Available values are 0, 90, 180 and 270. Default is 0. */
    rotation?: number;
    /** Specifies the screen's color depth in bits. Default is 24. */
    colorDepth?: number;
    /** Specifies the descriptive label for the screen. Default is none. */
    label?: string;
    /** Indicates whether the screen is internal to the device or external, attached to the device. Default is false. */
    isInternal?: boolean;
  }
  export interface AddScreenReturn {
    screenInfo: Emulation.ScreenInfo;
  }
  export interface UpdateScreenParams {
    /** Target screen identifier. */
    screenId: Emulation.ScreenId;
    /** Offset of the left edge of the screen in pixels. */
    left?: number;
    /** Offset of the top edge of the screen in pixels. */
    top?: number;
    /** The width of the screen in pixels. */
    width?: number;
    /** The height of the screen in pixels. */
    height?: number;
    /** Specifies the screen's work area. */
    workAreaInsets?: Emulation.WorkAreaInsets;
    /** Specifies the screen's device pixel ratio. */
    devicePixelRatio?: number;
    /** Specifies the screen's rotation angle. Available values are 0, 90, 180 and 270. */
    rotation?: number;
    /** Specifies the screen's color depth in bits. */
    colorDepth?: number;
    /** Specifies the descriptive label for the screen. */
    label?: string;
    /** Indicates whether the screen is internal to the device or external, attached to the device. Default is false. */
    isInternal?: boolean;
  }
  export interface UpdateScreenReturn {
    screenInfo: Emulation.ScreenInfo;
  }
  export interface RemoveScreenParams {
    screenId: Emulation.ScreenId;
  }
  export type RemoveScreenReturn = void;
  export interface SetPrimaryScreenParams {
    screenId: Emulation.ScreenId;
  }
  export type SetPrimaryScreenReturn = void;
}

export namespace EventBreakpoints {
  export interface SetInstrumentationBreakpointParams {
    /** Instrumentation name to stop on. */
    eventName: string;
  }
  export type SetInstrumentationBreakpointReturn = void;
  export interface RemoveInstrumentationBreakpointParams {
    /** Instrumentation name to stop on. */
    eventName: string;
  }
  export type RemoveInstrumentationBreakpointReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
}

export namespace Extensions {
  export interface TriggerActionParams {
    /** Extension id. */
    id: string;
    /** A tab target ID to trigger the default extension action on. */
    targetId: string;
  }
  export type TriggerActionReturn = void;
  export interface LoadUnpackedParams {
    /** Absolute file path. */
    path: string;
    /** Enable the extension in incognito */
    enableInIncognito?: boolean;
  }
  export interface LoadUnpackedReturn {
    /** Extension id. */
    id: string;
  }
  export interface GetExtensionsParams {}
  export interface GetExtensionsReturn {
    extensions: Extensions.ExtensionInfo[];
  }
  export interface UninstallParams {
    /** Extension id. */
    id: string;
  }
  export type UninstallReturn = void;
  export interface GetStorageItemsParams {
    /** ID of extension. */
    id: string;
    /** StorageArea to retrieve data from. */
    storageArea: Extensions.StorageArea;
    /** Keys to retrieve. */
    keys?: string[];
  }
  export interface GetStorageItemsReturn {
    data: Record<string, unknown>;
  }
  export interface RemoveStorageItemsParams {
    /** ID of extension. */
    id: string;
    /** StorageArea to remove data from. */
    storageArea: Extensions.StorageArea;
    /** Keys to remove. */
    keys: string[];
  }
  export type RemoveStorageItemsReturn = void;
  export interface ClearStorageItemsParams {
    /** ID of extension. */
    id: string;
    /** StorageArea to remove data from. */
    storageArea: Extensions.StorageArea;
  }
  export type ClearStorageItemsReturn = void;
  export interface SetStorageItemsParams {
    /** ID of extension. */
    id: string;
    /** StorageArea to set data in. */
    storageArea: Extensions.StorageArea;
    /** Values to set. */
    values: Record<string, unknown>;
  }
  export type SetStorageItemsReturn = void;
}

export namespace FedCm {
  export interface EnableParams {
    /**
     * Allows callers to disable the promise rejection delay that would
     * normally happen, if this is unimportant to what's being tested.
     * (step 4 of https://fedidcg.github.io/FedCM/#browser-api-rp-sign-in)
     */
    disableRejectionDelay?: boolean;
  }
  export type EnableReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface SelectAccountParams {
    dialogId: string;
    accountIndex: number;
  }
  export type SelectAccountReturn = void;
  export interface ClickDialogButtonParams {
    dialogId: string;
    dialogButton: FedCm.DialogButton;
  }
  export type ClickDialogButtonReturn = void;
  export interface OpenUrlParams {
    dialogId: string;
    accountIndex: number;
    accountUrlType: FedCm.AccountUrlType;
  }
  export type OpenUrlReturn = void;
  export interface DismissDialogParams {
    dialogId: string;
    triggerCooldown?: boolean;
  }
  export type DismissDialogReturn = void;
  export interface ResetCooldownParams {}
  export type ResetCooldownReturn = void;
}

export namespace Fetch {
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {
    /**
     * If specified, only requests matching any of these patterns will produce
     * fetchRequested event and will be paused until clients response. If not set,
     * all requests will be affected.
     */
    patterns?: Fetch.RequestPattern[];
    /**
     * If true, authRequired events will be issued and requests will be paused
     * expecting a call to continueWithAuth.
     */
    handleAuthRequests?: boolean;
  }
  export type EnableReturn = void;
  export interface FailRequestParams {
    /** An id the client received in requestPaused event. */
    requestId: Fetch.RequestId;
    /** Causes the request to fail with the given reason. */
    errorReason: Network.ErrorReason;
  }
  export type FailRequestReturn = void;
  export interface FulfillRequestParams {
    /** An id the client received in requestPaused event. */
    requestId: Fetch.RequestId;
    /** An HTTP response code. */
    responseCode: number;
    /** Response headers. */
    responseHeaders?: Fetch.HeaderEntry[];
    /**
     * Alternative way of specifying response headers as a \0-separated
     * series of name: value pairs. Prefer the above method unless you
     * need to represent some non-UTF8 values that can't be transmitted
     * over the protocol as text. (Encoded as a base64 string when passed over JSON)
     */
    binaryResponseHeaders?: string;
    /**
     * A response body. If absent, original response body will be used if
     * the request is intercepted at the response stage and empty body
     * will be used if the request is intercepted at the request stage. (Encoded as a base64 string when passed over JSON)
     */
    body?: string;
    /**
     * A textual representation of responseCode.
     * If absent, a standard phrase matching responseCode is used.
     */
    responsePhrase?: string;
  }
  export type FulfillRequestReturn = void;
  export interface ContinueRequestParams {
    /** An id the client received in requestPaused event. */
    requestId: Fetch.RequestId;
    /** If set, the request url will be modified in a way that's not observable by page. */
    url?: string;
    /** If set, the request method is overridden. */
    method?: string;
    /** If set, overrides the post data in the request. (Encoded as a base64 string when passed over JSON) */
    postData?: string;
    /**
     * If set, overrides the request headers. Note that the overrides do not
     * extend to subsequent redirect hops, if a redirect happens. Another override
     * may be applied to a different request produced by a redirect.
     */
    headers?: Fetch.HeaderEntry[];
    /** If set, overrides response interception behavior for this request. */
    interceptResponse?: boolean;
  }
  export type ContinueRequestReturn = void;
  export interface ContinueWithAuthParams {
    /** An id the client received in authRequired event. */
    requestId: Fetch.RequestId;
    /** Response to  with an authChallenge. */
    authChallengeResponse: Fetch.AuthChallengeResponse;
  }
  export type ContinueWithAuthReturn = void;
  export interface ContinueResponseParams {
    /** An id the client received in requestPaused event. */
    requestId: Fetch.RequestId;
    /** An HTTP response code. If absent, original response code will be used. */
    responseCode?: number;
    /**
     * A textual representation of responseCode.
     * If absent, a standard phrase matching responseCode is used.
     */
    responsePhrase?: string;
    /** Response headers. If absent, original response headers will be used. */
    responseHeaders?: Fetch.HeaderEntry[];
    /**
     * Alternative way of specifying response headers as a \0-separated
     * series of name: value pairs. Prefer the above method unless you
     * need to represent some non-UTF8 values that can't be transmitted
     * over the protocol as text. (Encoded as a base64 string when passed over JSON)
     */
    binaryResponseHeaders?: string;
  }
  export type ContinueResponseReturn = void;
  export interface GetResponseBodyParams {
    /** Identifier for the intercepted request to get body for. */
    requestId: Fetch.RequestId;
  }
  export interface GetResponseBodyReturn {
    /** Response body. */
    body: string;
    /** True, if content was sent as base64. */
    base64Encoded: boolean;
  }
  export interface TakeResponseBodyAsStreamParams {
    requestId: Fetch.RequestId;
  }
  export interface TakeResponseBodyAsStreamReturn {
    stream: IO.StreamHandle;
  }
}

export namespace FileSystem {
  export interface GetDirectoryParams {
    bucketFileSystemLocator: FileSystem.BucketFileSystemLocator;
  }
  export interface GetDirectoryReturn {
    /** Returns the directory object at the path. */
    directory: FileSystem.Directory;
  }
}

export namespace HeadlessExperimental {
  export interface BeginFrameParams {
    /**
     * Timestamp of this BeginFrame in Renderer TimeTicks (milliseconds of uptime). If not set,
     * the current time will be used.
     */
    frameTimeTicks?: number;
    /**
     * The interval between BeginFrames that is reported to the compositor, in milliseconds.
     * Defaults to a 60 frames/second interval, i.e. about 16.666 milliseconds.
     */
    interval?: number;
    /**
     * Whether updates should not be committed and drawn onto the display. False by default. If
     * true, only side effects of the BeginFrame will be run, such as layout and animations, but
     * any visual updates may not be visible on the display or in screenshots.
     */
    noDisplayUpdates?: boolean;
    /**
     * If set, a screenshot of the frame will be captured and returned in the response. Otherwise,
     * no screenshot will be captured. Note that capturing a screenshot can fail, for example,
     * during renderer initialization. In such a case, no screenshot data will be returned.
     */
    screenshot?: HeadlessExperimental.ScreenshotParams;
  }
  export interface BeginFrameReturn {
    /**
     * Whether the BeginFrame resulted in damage and, thus, a new frame was committed to the
     * display. Reported for diagnostic uses, may be removed in the future.
     */
    hasDamage: boolean;
    /** Base64-encoded image data of the screenshot, if one was requested and successfully taken. (Encoded as a base64 string when passed over JSON) */
    screenshotData?: string;
  }
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
}

export namespace HeapProfiler {
  export interface AddInspectedHeapObjectParams {
    /** Heap snapshot object id to be accessible by means of $x command line API. */
    heapObjectId: HeapProfiler.HeapSnapshotObjectId;
  }
  export type AddInspectedHeapObjectReturn = void;
  export interface CollectGarbageParams {}
  export type CollectGarbageReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface GetHeapObjectIdParams {
    /** Identifier of the object to get heap object id for. */
    objectId: Runtime.RemoteObjectId;
  }
  export interface GetHeapObjectIdReturn {
    /** Id of the heap snapshot object corresponding to the passed remote object id. */
    heapSnapshotObjectId: HeapProfiler.HeapSnapshotObjectId;
  }
  export interface GetObjectByHeapObjectIdParams {
    objectId: HeapProfiler.HeapSnapshotObjectId;
    /** Symbolic group name that can be used to release multiple objects. */
    objectGroup?: string;
  }
  export interface GetObjectByHeapObjectIdReturn {
    /** Evaluation result. */
    result: Runtime.RemoteObject;
  }
  export interface GetSamplingProfileParams {}
  export interface GetSamplingProfileReturn {
    /** Return the sampling profile being collected. */
    profile: HeapProfiler.SamplingHeapProfile;
  }
  export interface StartSamplingParams {
    /**
     * Average sample interval in bytes. Poisson distribution is used for the intervals. The
     * default value is 32768 bytes.
     */
    samplingInterval?: number;
    /** Maximum stack depth. The default value is 128. */
    stackDepth?: number;
    /**
     * By default, the sampling heap profiler reports only objects which are
     * still alive when the profile is returned via getSamplingProfile or
     * stopSampling, which is useful for determining what functions contribute
     * the most to steady-state memory usage. This flag instructs the sampling
     * heap profiler to also include information about objects discarded by
     * major GC, which will show which functions cause large temporary memory
     * usage or long GC pauses.
     */
    includeObjectsCollectedByMajorGC?: boolean;
    /**
     * By default, the sampling heap profiler reports only objects which are
     * still alive when the profile is returned via getSamplingProfile or
     * stopSampling, which is useful for determining what functions contribute
     * the most to steady-state memory usage. This flag instructs the sampling
     * heap profiler to also include information about objects discarded by
     * minor GC, which is useful when tuning a latency-sensitive application
     * for minimal GC activity.
     */
    includeObjectsCollectedByMinorGC?: boolean;
  }
  export type StartSamplingReturn = void;
  export interface StartTrackingHeapObjectsParams {
    trackAllocations?: boolean;
  }
  export type StartTrackingHeapObjectsReturn = void;
  export interface StopSamplingParams {}
  export interface StopSamplingReturn {
    /** Recorded sampling heap profile. */
    profile: HeapProfiler.SamplingHeapProfile;
  }
  export interface StopTrackingHeapObjectsParams {
    /**
     * If true 'reportHeapSnapshotProgress' events will be generated while snapshot is being taken
     * when the tracking is stopped.
     */
    reportProgress?: boolean;
    /** Deprecated in favor of `exposeInternals`. */
    treatGlobalObjectsAsRoots?: boolean;
    /** If true, numerical values are included in the snapshot */
    captureNumericValue?: boolean;
    /** If true, exposes internals of the snapshot. */
    exposeInternals?: boolean;
  }
  export type StopTrackingHeapObjectsReturn = void;
  export interface TakeHeapSnapshotParams {
    /** If true 'reportHeapSnapshotProgress' events will be generated while snapshot is being taken. */
    reportProgress?: boolean;
    /**
     * If true, a raw snapshot without artificial roots will be generated.
     * Deprecated in favor of `exposeInternals`.
     */
    treatGlobalObjectsAsRoots?: boolean;
    /** If true, numerical values are included in the snapshot */
    captureNumericValue?: boolean;
    /** If true, exposes internals of the snapshot. */
    exposeInternals?: boolean;
  }
  export type TakeHeapSnapshotReturn = void;
}

export namespace IndexedDB {
  export interface ClearObjectStoreParams {
    /**
     * At least and at most one of securityOrigin, storageKey, or storageBucket must be specified.
     * Security origin.
     */
    securityOrigin?: string;
    /** Storage key. */
    storageKey?: string;
    /** Storage bucket. If not specified, it uses the default bucket. */
    storageBucket?: Storage.StorageBucket;
    /** Database name. */
    databaseName: string;
    /** Object store name. */
    objectStoreName: string;
  }
  export type ClearObjectStoreReturn = void;
  export interface DeleteDatabaseParams {
    /**
     * At least and at most one of securityOrigin, storageKey, or storageBucket must be specified.
     * Security origin.
     */
    securityOrigin?: string;
    /** Storage key. */
    storageKey?: string;
    /** Storage bucket. If not specified, it uses the default bucket. */
    storageBucket?: Storage.StorageBucket;
    /** Database name. */
    databaseName: string;
  }
  export type DeleteDatabaseReturn = void;
  export interface DeleteObjectStoreEntriesParams {
    /**
     * At least and at most one of securityOrigin, storageKey, or storageBucket must be specified.
     * Security origin.
     */
    securityOrigin?: string;
    /** Storage key. */
    storageKey?: string;
    /** Storage bucket. If not specified, it uses the default bucket. */
    storageBucket?: Storage.StorageBucket;
    databaseName: string;
    objectStoreName: string;
    /** Range of entry keys to delete */
    keyRange: IndexedDB.KeyRange;
  }
  export type DeleteObjectStoreEntriesReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface RequestDataParams {
    /**
     * At least and at most one of securityOrigin, storageKey, or storageBucket must be specified.
     * Security origin.
     */
    securityOrigin?: string;
    /** Storage key. */
    storageKey?: string;
    /** Storage bucket. If not specified, it uses the default bucket. */
    storageBucket?: Storage.StorageBucket;
    /** Database name. */
    databaseName: string;
    /** Object store name. */
    objectStoreName: string;
    /** Index name. If not specified, it performs an object store data request. */
    indexName?: string;
    /** Number of records to skip. */
    skipCount: number;
    /** Number of records to fetch. */
    pageSize: number;
    /** Key range. */
    keyRange?: IndexedDB.KeyRange;
  }
  export interface RequestDataReturn {
    /** Array of object store data entries. */
    objectStoreDataEntries: IndexedDB.DataEntry[];
    /** If true, there are more entries to fetch in the given range. */
    hasMore: boolean;
  }
  export interface GetMetadataParams {
    /**
     * At least and at most one of securityOrigin, storageKey, or storageBucket must be specified.
     * Security origin.
     */
    securityOrigin?: string;
    /** Storage key. */
    storageKey?: string;
    /** Storage bucket. If not specified, it uses the default bucket. */
    storageBucket?: Storage.StorageBucket;
    /** Database name. */
    databaseName: string;
    /** Object store name. */
    objectStoreName: string;
  }
  export interface GetMetadataReturn {
    /** the entries count */
    entriesCount: number;
    /**
     * the current value of key generator, to become the next inserted
     * key into the object store. Valid if objectStore.autoIncrement
     * is true.
     */
    keyGeneratorValue: number;
  }
  export interface RequestDatabaseParams {
    /**
     * At least and at most one of securityOrigin, storageKey, or storageBucket must be specified.
     * Security origin.
     */
    securityOrigin?: string;
    /** Storage key. */
    storageKey?: string;
    /** Storage bucket. If not specified, it uses the default bucket. */
    storageBucket?: Storage.StorageBucket;
    /** Database name. */
    databaseName: string;
  }
  export interface RequestDatabaseReturn {
    /** Database with an array of object stores. */
    databaseWithObjectStores: IndexedDB.DatabaseWithObjectStores;
  }
  export interface RequestDatabaseNamesParams {
    /**
     * At least and at most one of securityOrigin, storageKey, or storageBucket must be specified.
     * Security origin.
     */
    securityOrigin?: string;
    /** Storage key. */
    storageKey?: string;
    /** Storage bucket. If not specified, it uses the default bucket. */
    storageBucket?: Storage.StorageBucket;
  }
  export interface RequestDatabaseNamesReturn {
    /** Database names for origin. */
    databaseNames: string[];
  }
}

export namespace Input {
  export interface DispatchDragEventParams {
    /** Type of the drag event. */
    type: "dragEnter" | "dragOver" | "drop" | "dragCancel";
    /** X coordinate of the event relative to the main frame's viewport in CSS pixels. */
    x: number;
    /**
     * Y coordinate of the event relative to the main frame's viewport in CSS pixels. 0 refers to
     * the top of the viewport and Y increases as it proceeds towards the bottom of the viewport.
     */
    y: number;
    data: Input.DragData;
    /**
     * Bit field representing pressed modifier keys. Alt=1, Ctrl=2, Meta/Command=4, Shift=8
     * (default: 0).
     */
    modifiers?: number;
  }
  export type DispatchDragEventReturn = void;
  export interface DispatchKeyEventParams {
    /** Type of the key event. */
    type: "keyDown" | "keyUp" | "rawKeyDown" | "char";
    /**
     * Bit field representing pressed modifier keys. Alt=1, Ctrl=2, Meta/Command=4, Shift=8
     * (default: 0).
     */
    modifiers?: number;
    /** Time at which the event occurred. */
    timestamp?: Input.TimeSinceEpoch;
    /**
     * Text as generated by processing a virtual key code with a keyboard layout. Not needed for
     * for `keyUp` and `rawKeyDown` events (default: "")
     */
    text?: string;
    /**
     * Text that would have been generated by the keyboard if no modifiers were pressed (except for
     * shift). Useful for shortcut (accelerator) key handling (default: "").
     */
    unmodifiedText?: string;
    /** Unique key identifier (e.g., 'U+0041') (default: ""). */
    keyIdentifier?: string;
    /** Unique DOM defined string value for each physical key (e.g., 'KeyA') (default: ""). */
    code?: string;
    /**
     * Unique DOM defined string value describing the meaning of the key in the context of active
     * modifiers, keyboard layout, etc (e.g., 'AltGr') (default: "").
     */
    key?: string;
    /** Windows virtual key code (default: 0). */
    windowsVirtualKeyCode?: number;
    /** Native virtual key code (default: 0). */
    nativeVirtualKeyCode?: number;
    /** Whether the event was generated from auto repeat (default: false). */
    autoRepeat?: boolean;
    /** Whether the event was generated from the keypad (default: false). */
    isKeypad?: boolean;
    /** Whether the event was a system key event (default: false). */
    isSystemKey?: boolean;
    /**
     * Whether the event was from the left or right side of the keyboard. 1=Left, 2=Right (default:
     * 0).
     */
    location?: number;
    /**
     * Editing commands to send with the key event (e.g., 'selectAll') (default: []).
     * These are related to but not equal the command names used in `document.execCommand` and NSStandardKeyBindingResponding.
     * See https://source.chromium.org/chromium/chromium/src/+/main:third_party/blink/renderer/core/editing/commands/editor_command_names.h for valid command names.
     */
    commands?: string[];
  }
  export type DispatchKeyEventReturn = void;
  export interface InsertTextParams {
    /** The text to insert. */
    text: string;
  }
  export type InsertTextReturn = void;
  export interface ImeSetCompositionParams {
    /** The text to insert */
    text: string;
    /** selection start */
    selectionStart: number;
    /** selection end */
    selectionEnd: number;
    /** replacement start */
    replacementStart?: number;
    /** replacement end */
    replacementEnd?: number;
  }
  export type ImeSetCompositionReturn = void;
  export interface DispatchMouseEventParams {
    /** Type of the mouse event. */
    type: "mousePressed" | "mouseReleased" | "mouseMoved" | "mouseWheel";
    /** X coordinate of the event relative to the main frame's viewport in CSS pixels. */
    x: number;
    /**
     * Y coordinate of the event relative to the main frame's viewport in CSS pixels. 0 refers to
     * the top of the viewport and Y increases as it proceeds towards the bottom of the viewport.
     */
    y: number;
    /**
     * Bit field representing pressed modifier keys. Alt=1, Ctrl=2, Meta/Command=4, Shift=8
     * (default: 0).
     */
    modifiers?: number;
    /** Time at which the event occurred. */
    timestamp?: Input.TimeSinceEpoch;
    /** Mouse button (default: "none"). */
    button?: Input.MouseButton;
    /**
     * A number indicating which buttons are pressed on the mouse when a mouse event is triggered.
     * Left=1, Right=2, Middle=4, Back=8, Forward=16, None=0.
     */
    buttons?: number;
    /** Number of times the mouse button was clicked (default: 0). */
    clickCount?: number;
    /** The normalized pressure, which has a range of [0,1] (default: 0). */
    force?: number;
    /** The normalized tangential pressure, which has a range of [-1,1] (default: 0). */
    tangentialPressure?: number;
    /** The plane angle between the Y-Z plane and the plane containing both the stylus axis and the Y axis, in degrees of the range [-90,90], a positive tiltX is to the right (default: 0). */
    tiltX?: number;
    /** The plane angle between the X-Z plane and the plane containing both the stylus axis and the X axis, in degrees of the range [-90,90], a positive tiltY is towards the user (default: 0). */
    tiltY?: number;
    /** The clockwise rotation of a pen stylus around its own major axis, in degrees in the range [0,359] (default: 0). */
    twist?: number;
    /** X delta in CSS pixels for mouse wheel event (default: 0). */
    deltaX?: number;
    /** Y delta in CSS pixels for mouse wheel event (default: 0). */
    deltaY?: number;
    /** Pointer type (default: "mouse"). */
    pointerType?: "mouse" | "pen";
  }
  export type DispatchMouseEventReturn = void;
  export interface DispatchTouchEventParams {
    /**
     * Type of the touch event. TouchEnd and TouchCancel must not contain any touch points, while
     * TouchStart and TouchMove must contains at least one.
     */
    type: "touchStart" | "touchEnd" | "touchMove" | "touchCancel";
    /**
     * Active touch points on the touch device. One event per any changed point (compared to
     * previous touch event in a sequence) is generated, emulating pressing/moving/releasing points
     * one by one.
     */
    touchPoints: Input.TouchPoint[];
    /**
     * Bit field representing pressed modifier keys. Alt=1, Ctrl=2, Meta/Command=4, Shift=8
     * (default: 0).
     */
    modifiers?: number;
    /** Time at which the event occurred. */
    timestamp?: Input.TimeSinceEpoch;
  }
  export type DispatchTouchEventReturn = void;
  export interface CancelDraggingParams {}
  export type CancelDraggingReturn = void;
  export interface EmulateTouchFromMouseEventParams {
    /** Type of the mouse event. */
    type: "mousePressed" | "mouseReleased" | "mouseMoved" | "mouseWheel";
    /** X coordinate of the mouse pointer in DIP. */
    x: number;
    /** Y coordinate of the mouse pointer in DIP. */
    y: number;
    /** Mouse button. Only "none", "left", "right" are supported. */
    button: Input.MouseButton;
    /** Time at which the event occurred (default: current time). */
    timestamp?: Input.TimeSinceEpoch;
    /** X delta in DIP for mouse wheel event (default: 0). */
    deltaX?: number;
    /** Y delta in DIP for mouse wheel event (default: 0). */
    deltaY?: number;
    /**
     * Bit field representing pressed modifier keys. Alt=1, Ctrl=2, Meta/Command=4, Shift=8
     * (default: 0).
     */
    modifiers?: number;
    /** Number of times the mouse button was clicked (default: 0). */
    clickCount?: number;
  }
  export type EmulateTouchFromMouseEventReturn = void;
  export interface SetIgnoreInputEventsParams {
    /** Ignores input events processing when set to true. */
    ignore: boolean;
  }
  export type SetIgnoreInputEventsReturn = void;
  export interface SetInterceptDragsParams {
    enabled: boolean;
  }
  export type SetInterceptDragsReturn = void;
  export interface SynthesizePinchGestureParams {
    /** X coordinate of the start of the gesture in CSS pixels. */
    x: number;
    /** Y coordinate of the start of the gesture in CSS pixels. */
    y: number;
    /** Relative scale factor after zooming (>1.0 zooms in, <1.0 zooms out). */
    scaleFactor: number;
    /** Relative pointer speed in pixels per second (default: 800). */
    relativeSpeed?: number;
    /**
     * Which type of input events to be generated (default: 'default', which queries the platform
     * for the preferred input type).
     */
    gestureSourceType?: Input.GestureSourceType;
  }
  export type SynthesizePinchGestureReturn = void;
  export interface SynthesizeScrollGestureParams {
    /** X coordinate of the start of the gesture in CSS pixels. */
    x: number;
    /** Y coordinate of the start of the gesture in CSS pixels. */
    y: number;
    /** The distance to scroll along the X axis (positive to scroll left). */
    xDistance?: number;
    /** The distance to scroll along the Y axis (positive to scroll up). */
    yDistance?: number;
    /**
     * The number of additional pixels to scroll back along the X axis, in addition to the given
     * distance.
     */
    xOverscroll?: number;
    /**
     * The number of additional pixels to scroll back along the Y axis, in addition to the given
     * distance.
     */
    yOverscroll?: number;
    /** Prevent fling (default: true). */
    preventFling?: boolean;
    /** Swipe speed in pixels per second (default: 800). */
    speed?: number;
    /**
     * Which type of input events to be generated (default: 'default', which queries the platform
     * for the preferred input type).
     */
    gestureSourceType?: Input.GestureSourceType;
    /** The number of times to repeat the gesture (default: 0). */
    repeatCount?: number;
    /** The number of milliseconds delay between each repeat. (default: 250). */
    repeatDelayMs?: number;
    /** The name of the interaction markers to generate, if not empty (default: ""). */
    interactionMarkerName?: string;
  }
  export type SynthesizeScrollGestureReturn = void;
  export interface SynthesizeTapGestureParams {
    /** X coordinate of the start of the gesture in CSS pixels. */
    x: number;
    /** Y coordinate of the start of the gesture in CSS pixels. */
    y: number;
    /** Duration between touchdown and touchup events in ms (default: 50). */
    duration?: number;
    /** Number of times to perform the tap (e.g. 2 for double tap, default: 1). */
    tapCount?: number;
    /**
     * Which type of input events to be generated (default: 'default', which queries the platform
     * for the preferred input type).
     */
    gestureSourceType?: Input.GestureSourceType;
  }
  export type SynthesizeTapGestureReturn = void;
}

export namespace Inspector {
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
}

export namespace IO {
  export interface CloseParams {
    /** Handle of the stream to close. */
    handle: IO.StreamHandle;
  }
  export type CloseReturn = void;
  export interface ReadParams {
    /** Handle of the stream to read. */
    handle: IO.StreamHandle;
    /**
     * Seek to the specified offset before reading (if not specified, proceed with offset
     * following the last read). Some types of streams may only support sequential reads.
     */
    offset?: number;
    /** Maximum number of bytes to read (left upon the agent discretion if not specified). */
    size?: number;
  }
  export interface ReadReturn {
    /** Set if the data is base64-encoded */
    base64Encoded?: boolean;
    /** Data that were read. */
    data: string;
    /** Set if the end-of-file condition occurred while reading. */
    eof: boolean;
  }
  export interface ResolveBlobParams {
    /** Object id of a Blob object wrapper. */
    objectId: Runtime.RemoteObjectId;
  }
  export interface ResolveBlobReturn {
    /** UUID of the specified Blob. */
    uuid: string;
  }
}

export namespace LayerTree {
  export interface CompositingReasonsParams {
    /** The id of the layer for which we want to get the reasons it was composited. */
    layerId: LayerTree.LayerId;
  }
  export interface CompositingReasonsReturn {
    /** A list of strings specifying reasons for the given layer to become composited. */
    compositingReasons: string[];
    /** A list of strings specifying reason IDs for the given layer to become composited. */
    compositingReasonIds: string[];
  }
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface LoadSnapshotParams {
    /** An array of tiles composing the snapshot. */
    tiles: LayerTree.PictureTile[];
  }
  export interface LoadSnapshotReturn {
    /** The id of the snapshot. */
    snapshotId: LayerTree.SnapshotId;
  }
  export interface MakeSnapshotParams {
    /** The id of the layer. */
    layerId: LayerTree.LayerId;
  }
  export interface MakeSnapshotReturn {
    /** The id of the layer snapshot. */
    snapshotId: LayerTree.SnapshotId;
  }
  export interface ProfileSnapshotParams {
    /** The id of the layer snapshot. */
    snapshotId: LayerTree.SnapshotId;
    /** The maximum number of times to replay the snapshot (1, if not specified). */
    minRepeatCount?: number;
    /** The minimum duration (in seconds) to replay the snapshot. */
    minDuration?: number;
    /** The clip rectangle to apply when replaying the snapshot. */
    clipRect?: DOM.Rect;
  }
  export interface ProfileSnapshotReturn {
    /** The array of paint profiles, one per run. */
    timings: LayerTree.PaintProfile[];
  }
  export interface ReleaseSnapshotParams {
    /** The id of the layer snapshot. */
    snapshotId: LayerTree.SnapshotId;
  }
  export type ReleaseSnapshotReturn = void;
  export interface ReplaySnapshotParams {
    /** The id of the layer snapshot. */
    snapshotId: LayerTree.SnapshotId;
    /** The first step to replay from (replay from the very start if not specified). */
    fromStep?: number;
    /** The last step to replay to (replay till the end if not specified). */
    toStep?: number;
    /** The scale to apply while replaying (defaults to 1). */
    scale?: number;
  }
  export interface ReplaySnapshotReturn {
    /** A data: URL for resulting image. */
    dataURL: string;
  }
  export interface SnapshotCommandLogParams {
    /** The id of the layer snapshot. */
    snapshotId: LayerTree.SnapshotId;
  }
  export interface SnapshotCommandLogReturn {
    /** The array of canvas function calls. */
    commandLog: Record<string, unknown>[];
  }
}

export namespace Log {
  export interface ClearParams {}
  export type ClearReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface StartViolationsReportParams {
    /** Configuration for violations. */
    config: Log.ViolationSetting[];
  }
  export type StartViolationsReportReturn = void;
  export interface StopViolationsReportParams {}
  export type StopViolationsReportReturn = void;
}

export namespace Media {
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
}

export namespace Memory {
  export interface GetDOMCountersParams {}
  export interface GetDOMCountersReturn {
    documents: number;
    nodes: number;
    jsEventListeners: number;
  }
  export interface GetDOMCountersForLeakDetectionParams {}
  export interface GetDOMCountersForLeakDetectionReturn {
    /** DOM object counters. */
    counters: Memory.DOMCounter[];
  }
  export interface PrepareForLeakDetectionParams {}
  export type PrepareForLeakDetectionReturn = void;
  export interface ForciblyPurgeJavaScriptMemoryParams {}
  export type ForciblyPurgeJavaScriptMemoryReturn = void;
  export interface SetPressureNotificationsSuppressedParams {
    /** If true, memory pressure notifications will be suppressed. */
    suppressed: boolean;
  }
  export type SetPressureNotificationsSuppressedReturn = void;
  export interface SimulatePressureNotificationParams {
    /** Memory pressure level of the notification. */
    level: Memory.PressureLevel;
  }
  export type SimulatePressureNotificationReturn = void;
  export interface StartSamplingParams {
    /** Average number of bytes between samples. */
    samplingInterval?: number;
    /** Do not randomize intervals between samples. */
    suppressRandomness?: boolean;
  }
  export type StartSamplingReturn = void;
  export interface StopSamplingParams {}
  export type StopSamplingReturn = void;
  export interface GetAllTimeSamplingProfileParams {}
  export interface GetAllTimeSamplingProfileReturn {
    profile: Memory.SamplingProfile;
  }
  export interface GetBrowserSamplingProfileParams {}
  export interface GetBrowserSamplingProfileReturn {
    profile: Memory.SamplingProfile;
  }
  export interface GetSamplingProfileParams {}
  export interface GetSamplingProfileReturn {
    profile: Memory.SamplingProfile;
  }
}

export namespace Network {
  export interface SetAcceptedEncodingsParams {
    /** List of accepted content encodings. */
    encodings: Network.ContentEncoding[];
  }
  export type SetAcceptedEncodingsReturn = void;
  export interface ClearAcceptedEncodingsOverrideParams {}
  export type ClearAcceptedEncodingsOverrideReturn = void;
  export interface CanClearBrowserCacheParams {}
  export interface CanClearBrowserCacheReturn {
    /** True if browser cache can be cleared. */
    result: boolean;
  }
  export interface CanClearBrowserCookiesParams {}
  export interface CanClearBrowserCookiesReturn {
    /** True if browser cookies can be cleared. */
    result: boolean;
  }
  export interface CanEmulateNetworkConditionsParams {}
  export interface CanEmulateNetworkConditionsReturn {
    /** True if emulation of network conditions is supported. */
    result: boolean;
  }
  export interface ClearBrowserCacheParams {}
  export type ClearBrowserCacheReturn = void;
  export interface ClearBrowserCookiesParams {}
  export type ClearBrowserCookiesReturn = void;
  export interface ContinueInterceptedRequestParams {
    interceptionId: Network.InterceptionId;
    /**
     * If set this causes the request to fail with the given reason. Passing `Aborted` for requests
     * marked with `isNavigationRequest` also cancels the navigation. Must not be set in response
     * to an authChallenge.
     */
    errorReason?: Network.ErrorReason;
    /**
     * If set the requests completes using with the provided base64 encoded raw response, including
     * HTTP status line and headers etc... Must not be set in response to an authChallenge. (Encoded as a base64 string when passed over JSON)
     */
    rawResponse?: string;
    /**
     * If set the request url will be modified in a way that's not observable by page. Must not be
     * set in response to an authChallenge.
     */
    url?: string;
    /**
     * If set this allows the request method to be overridden. Must not be set in response to an
     * authChallenge.
     */
    method?: string;
    /** If set this allows postData to be set. Must not be set in response to an authChallenge. */
    postData?: string;
    /**
     * If set this allows the request headers to be changed. Must not be set in response to an
     * authChallenge.
     */
    headers?: Network.Headers;
    /** Response to a requestIntercepted with an authChallenge. Must not be set otherwise. */
    authChallengeResponse?: Network.AuthChallengeResponse;
  }
  export type ContinueInterceptedRequestReturn = void;
  export interface DeleteCookiesParams {
    /** Name of the cookies to remove. */
    name: string;
    /**
     * If specified, deletes all the cookies with the given name where domain and path match
     * provided URL.
     */
    url?: string;
    /** If specified, deletes only cookies with the exact domain. */
    domain?: string;
    /** If specified, deletes only cookies with the exact path. */
    path?: string;
    /**
     * If specified, deletes only cookies with the the given name and partitionKey where
     * all partition key attributes match the cookie partition key attribute.
     */
    partitionKey?: Network.CookiePartitionKey;
  }
  export type DeleteCookiesReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EmulateNetworkConditionsParams {
    /** True to emulate internet disconnection. */
    offline: boolean;
    /** Minimum latency from request sent to response headers received (ms). */
    latency: number;
    /** Maximal aggregated download throughput (bytes/sec). -1 disables download throttling. */
    downloadThroughput: number;
    /** Maximal aggregated upload throughput (bytes/sec).  -1 disables upload throttling. */
    uploadThroughput: number;
    /** Connection type if known. */
    connectionType?: Network.ConnectionType;
    /** WebRTC packet loss (percent, 0-100). 0 disables packet loss emulation, 100 drops all the packets. */
    packetLoss?: number;
    /** WebRTC packet queue length (packet). 0 removes any queue length limitations. */
    packetQueueLength?: number;
    /** WebRTC packetReordering feature. */
    packetReordering?: boolean;
  }
  export type EmulateNetworkConditionsReturn = void;
  export interface EmulateNetworkConditionsByRuleParams {
    /**
     * True to emulate internet disconnection. Deprecated, use the offline property in matchedNetworkConditions
     * or emulateOfflineServiceWorker instead.
     */
    offline?: boolean;
    /** True to emulate offline service worker. */
    emulateOfflineServiceWorker?: boolean;
    /**
     * Configure conditions for matching requests. If multiple entries match a request, the first entry wins.  Global
     * conditions can be configured by leaving the urlPattern for the conditions empty. These global conditions are
     * also applied for throttling of p2p connections.
     */
    matchedNetworkConditions: Network.NetworkConditions[];
  }
  export interface EmulateNetworkConditionsByRuleReturn {
    /**
     * An id for each entry in matchedNetworkConditions. The id will be included in the requestWillBeSentExtraInfo for
     * requests affected by a rule.
     */
    ruleIds: string[];
  }
  export interface OverrideNetworkStateParams {
    /** True to emulate internet disconnection. */
    offline: boolean;
    /** Minimum latency from request sent to response headers received (ms). */
    latency: number;
    /** Maximal aggregated download throughput (bytes/sec). -1 disables download throttling. */
    downloadThroughput: number;
    /** Maximal aggregated upload throughput (bytes/sec).  -1 disables upload throttling. */
    uploadThroughput: number;
    /** Connection type if known. */
    connectionType?: Network.ConnectionType;
  }
  export type OverrideNetworkStateReturn = void;
  export interface EnableParams {
    /**
     * Buffer size in bytes to use when preserving network payloads (XHRs, etc).
     * This is the maximum number of bytes that will be collected by this
     * DevTools session.
     */
    maxTotalBufferSize?: number;
    /** Per-resource buffer size in bytes to use when preserving network payloads (XHRs, etc). */
    maxResourceBufferSize?: number;
    /** Longest post body size (in bytes) that would be included in requestWillBeSent notification */
    maxPostDataSize?: number;
    /** Whether DirectSocket chunk send/receive events should be reported. */
    reportDirectSocketTraffic?: boolean;
    /**
     * Enable storing response bodies outside of renderer, so that these survive
     * a cross-process navigation. Requires maxTotalBufferSize to be set.
     * Currently defaults to false. This field is being deprecated in favor of the dedicated
     * configureDurableMessages command, due to the possibility of deadlocks when awaiting
     * Network.enable before issuing Runtime.runIfWaitingForDebugger.
     */
    enableDurableMessages?: boolean;
  }
  export type EnableReturn = void;
  export interface ConfigureDurableMessagesParams {
    /** Buffer size in bytes to use when preserving network payloads (XHRs, etc). */
    maxTotalBufferSize?: number;
    /** Per-resource buffer size in bytes to use when preserving network payloads (XHRs, etc). */
    maxResourceBufferSize?: number;
  }
  export type ConfigureDurableMessagesReturn = void;
  export interface GetAllCookiesParams {}
  export interface GetAllCookiesReturn {
    /** Array of cookie objects. */
    cookies: Network.Cookie[];
  }
  export interface GetCertificateParams {
    /** Origin to get certificate for. */
    origin: string;
  }
  export interface GetCertificateReturn {
    tableNames: string[];
  }
  export interface GetCookiesParams {
    /**
     * The list of URLs for which applicable cookies will be fetched.
     * If not specified, it's assumed to be set to the list containing
     * the URLs of the page and all of its subframes.
     */
    urls?: string[];
  }
  export interface GetCookiesReturn {
    /** Array of cookie objects. */
    cookies: Network.Cookie[];
  }
  export interface GetResponseBodyParams {
    /** Identifier of the network request to get content for. */
    requestId: Network.RequestId;
  }
  export interface GetResponseBodyReturn {
    /** Response body. */
    body: string;
    /** True, if content was sent as base64. */
    base64Encoded: boolean;
  }
  export interface GetRequestPostDataParams {
    /** Identifier of the network request to get content for. */
    requestId: Network.RequestId;
  }
  export interface GetRequestPostDataReturn {
    /** Request body string, omitting files from multipart requests */
    postData: string;
    /** True, if content was sent as base64. */
    base64Encoded: boolean;
  }
  export interface GetResponseBodyForInterceptionParams {
    /** Identifier for the intercepted request to get body for. */
    interceptionId: Network.InterceptionId;
  }
  export interface GetResponseBodyForInterceptionReturn {
    /** Response body. */
    body: string;
    /** True, if content was sent as base64. */
    base64Encoded: boolean;
  }
  export interface TakeResponseBodyForInterceptionAsStreamParams {
    interceptionId: Network.InterceptionId;
  }
  export interface TakeResponseBodyForInterceptionAsStreamReturn {
    stream: IO.StreamHandle;
  }
  export interface ReplayXHRParams {
    /** Identifier of XHR to replay. */
    requestId: Network.RequestId;
  }
  export type ReplayXHRReturn = void;
  export interface SearchInResponseBodyParams {
    /** Identifier of the network response to search. */
    requestId: Network.RequestId;
    /** String to search for. */
    query: string;
    /** If true, search is case sensitive. */
    caseSensitive?: boolean;
    /** If true, treats string parameter as regex. */
    isRegex?: boolean;
  }
  export interface SearchInResponseBodyReturn {
    /** List of search matches. */
    result: Debugger.SearchMatch[];
  }
  export interface SetBlockedURLsParams {
    /**
     * Patterns to match in the order in which they are given. These patterns
     * also take precedence over any wildcard patterns defined in `urls`.
     */
    urlPatterns?: Network.BlockPattern[];
    /** URL patterns to block. Wildcards ('*') are allowed. */
    urls?: string[];
  }
  export type SetBlockedURLsReturn = void;
  export interface SetBypassServiceWorkerParams {
    /** Bypass service worker and load from network. */
    bypass: boolean;
  }
  export type SetBypassServiceWorkerReturn = void;
  export interface SetCacheDisabledParams {
    /** Cache disabled state. */
    cacheDisabled: boolean;
  }
  export type SetCacheDisabledReturn = void;
  export interface SetCookieParams {
    /** Cookie name. */
    name: string;
    /** Cookie value. */
    value: string;
    /**
     * The request-URI to associate with the setting of the cookie. This value can affect the
     * default domain, path, source port, and source scheme values of the created cookie.
     */
    url?: string;
    /** Cookie domain. */
    domain?: string;
    /** Cookie path. */
    path?: string;
    /** True if cookie is secure. */
    secure?: boolean;
    /** True if cookie is http-only. */
    httpOnly?: boolean;
    /** Cookie SameSite type. */
    sameSite?: Network.CookieSameSite;
    /** Cookie expiration date, session cookie if not set */
    expires?: Network.TimeSinceEpoch;
    /** Cookie Priority type. */
    priority?: Network.CookiePriority;
    /** Cookie source scheme type. */
    sourceScheme?: Network.CookieSourceScheme;
    /**
     * Cookie source port. Valid values are {-1, [1, 65535]}, -1 indicates an unspecified port.
     * An unspecified port value allows protocol clients to emulate legacy cookie scope for the port.
     * This is a temporary ability and it will be removed in the future.
     */
    sourcePort?: number;
    /** Cookie partition key. If not set, the cookie will be set as not partitioned. */
    partitionKey?: Network.CookiePartitionKey;
  }
  export interface SetCookieReturn {
    /** Always set to true. If an error occurs, the response indicates protocol error. */
    success: boolean;
  }
  export interface SetCookiesParams {
    /** Cookies to be set. */
    cookies: Network.CookieParam[];
  }
  export type SetCookiesReturn = void;
  export interface SetExtraHTTPHeadersParams {
    /** Map with extra HTTP headers. */
    headers: Network.Headers;
  }
  export type SetExtraHTTPHeadersReturn = void;
  export interface SetAttachDebugStackParams {
    /** Whether to attach a page script stack for debugging purpose. */
    enabled: boolean;
  }
  export type SetAttachDebugStackReturn = void;
  export interface SetRequestInterceptionParams {
    /**
     * Requests matching any of these patterns will be forwarded and wait for the corresponding
     * continueInterceptedRequest call.
     */
    patterns: Network.RequestPattern[];
  }
  export type SetRequestInterceptionReturn = void;
  export interface StreamResourceContentParams {
    /** Identifier of the request to stream. */
    requestId: Network.RequestId;
  }
  export interface StreamResourceContentReturn {
    /** Data that has been buffered until streaming is enabled. (Encoded as a base64 string when passed over JSON) */
    bufferedData: string;
  }
  export interface GetSecurityIsolationStatusParams {
    /** If no frameId is provided, the status of the target is provided. */
    frameId?: Page.FrameId;
  }
  export interface GetSecurityIsolationStatusReturn {
    status: Network.SecurityIsolationStatus;
  }
  export interface EnableReportingApiParams {
    /** Whether to enable or disable events for the Reporting API */
    enable: boolean;
  }
  export type EnableReportingApiReturn = void;
  export interface EnableDeviceBoundSessionsParams {
    /** Whether to enable or disable events. */
    enable: boolean;
  }
  export type EnableDeviceBoundSessionsReturn = void;
  export interface DeleteDeviceBoundSessionParams {
    key: Network.DeviceBoundSessionKey;
  }
  export type DeleteDeviceBoundSessionReturn = void;
  export interface FetchSchemefulSiteParams {
    /** The URL origin. */
    origin: string;
  }
  export interface FetchSchemefulSiteReturn {
    /** The corresponding schemeful site. */
    schemefulSite: string;
  }
  export interface LoadNetworkResourceParams {
    /**
     * Frame id to get the resource for. Mandatory for frame targets, and
     * should be omitted for worker targets.
     */
    frameId?: Page.FrameId;
    /** URL of the resource to get content for. */
    url: string;
    /** Options for the request. */
    options: Network.LoadNetworkResourceOptions;
  }
  export interface LoadNetworkResourceReturn {
    resource: Network.LoadNetworkResourcePageResult;
  }
  export interface SetCookieControlsParams {
    /** Whether 3pc restriction is enabled. */
    enableThirdPartyCookieRestriction: boolean;
  }
  export type SetCookieControlsReturn = void;
}

export namespace Overlay {
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface GetHighlightObjectForTestParams {
    /** Id of the node to get highlight object for. */
    nodeId: DOM.NodeId;
    /** Whether to include distance info. */
    includeDistance?: boolean;
    /** Whether to include style info. */
    includeStyle?: boolean;
    /** The color format to get config with (default: hex). */
    colorFormat?: Overlay.ColorFormat;
    /** Whether to show accessibility info (default: true). */
    showAccessibilityInfo?: boolean;
  }
  export interface GetHighlightObjectForTestReturn {
    /** Highlight data for the node. */
    highlight: Record<string, unknown>;
  }
  export interface GetGridHighlightObjectsForTestParams {
    /** Ids of the node to get highlight object for. */
    nodeIds: DOM.NodeId[];
  }
  export interface GetGridHighlightObjectsForTestReturn {
    /** Grid Highlight data for the node ids provided. */
    highlights: Record<string, unknown>;
  }
  export interface GetSourceOrderHighlightObjectForTestParams {
    /** Id of the node to highlight. */
    nodeId: DOM.NodeId;
  }
  export interface GetSourceOrderHighlightObjectForTestReturn {
    /** Source order highlight data for the node id provided. */
    highlight: Record<string, unknown>;
  }
  export interface HideHighlightParams {}
  export type HideHighlightReturn = void;
  export interface HighlightFrameParams {
    /** Identifier of the frame to highlight. */
    frameId: Page.FrameId;
    /** The content box highlight fill color (default: transparent). */
    contentColor?: DOM.RGBA;
    /** The content box highlight outline color (default: transparent). */
    contentOutlineColor?: DOM.RGBA;
  }
  export type HighlightFrameReturn = void;
  export interface HighlightNodeParams {
    /** A descriptor for the highlight appearance. */
    highlightConfig: Overlay.HighlightConfig;
    /** Identifier of the node to highlight. */
    nodeId?: DOM.NodeId;
    /** Identifier of the backend node to highlight. */
    backendNodeId?: DOM.BackendNodeId;
    /** JavaScript object id of the node to be highlighted. */
    objectId?: Runtime.RemoteObjectId;
    /** Selectors to highlight relevant nodes. */
    selector?: string;
  }
  export type HighlightNodeReturn = void;
  export interface HighlightQuadParams {
    /** Quad to highlight */
    quad: DOM.Quad;
    /** The highlight fill color (default: transparent). */
    color?: DOM.RGBA;
    /** The highlight outline color (default: transparent). */
    outlineColor?: DOM.RGBA;
  }
  export type HighlightQuadReturn = void;
  export interface HighlightRectParams {
    /** X coordinate */
    x: number;
    /** Y coordinate */
    y: number;
    /** Rectangle width */
    width: number;
    /** Rectangle height */
    height: number;
    /** The highlight fill color (default: transparent). */
    color?: DOM.RGBA;
    /** The highlight outline color (default: transparent). */
    outlineColor?: DOM.RGBA;
  }
  export type HighlightRectReturn = void;
  export interface HighlightSourceOrderParams {
    /** A descriptor for the appearance of the overlay drawing. */
    sourceOrderConfig: Overlay.SourceOrderConfig;
    /** Identifier of the node to highlight. */
    nodeId?: DOM.NodeId;
    /** Identifier of the backend node to highlight. */
    backendNodeId?: DOM.BackendNodeId;
    /** JavaScript object id of the node to be highlighted. */
    objectId?: Runtime.RemoteObjectId;
  }
  export type HighlightSourceOrderReturn = void;
  export interface SetInspectModeParams {
    /** Set an inspection mode. */
    mode: Overlay.InspectMode;
    /**
     * A descriptor for the highlight appearance of hovered-over nodes. May be omitted if `enabled
     * == false`.
     */
    highlightConfig?: Overlay.HighlightConfig;
  }
  export type SetInspectModeReturn = void;
  export interface SetShowAdHighlightsParams {
    /** True for showing ad highlights */
    show: boolean;
  }
  export type SetShowAdHighlightsReturn = void;
  export interface SetPausedInDebuggerMessageParams {
    /** The message to display, also triggers resume and step over controls. */
    message?: string;
  }
  export type SetPausedInDebuggerMessageReturn = void;
  export interface SetShowDebugBordersParams {
    /** True for showing debug borders */
    show: boolean;
  }
  export type SetShowDebugBordersReturn = void;
  export interface SetShowFPSCounterParams {
    /** True for showing the FPS counter */
    show: boolean;
  }
  export type SetShowFPSCounterReturn = void;
  export interface SetShowGridOverlaysParams {
    /** An array of node identifiers and descriptors for the highlight appearance. */
    gridNodeHighlightConfigs: Overlay.GridNodeHighlightConfig[];
  }
  export type SetShowGridOverlaysReturn = void;
  export interface SetShowFlexOverlaysParams {
    /** An array of node identifiers and descriptors for the highlight appearance. */
    flexNodeHighlightConfigs: Overlay.FlexNodeHighlightConfig[];
  }
  export type SetShowFlexOverlaysReturn = void;
  export interface SetShowScrollSnapOverlaysParams {
    /** An array of node identifiers and descriptors for the highlight appearance. */
    scrollSnapHighlightConfigs: Overlay.ScrollSnapHighlightConfig[];
  }
  export type SetShowScrollSnapOverlaysReturn = void;
  export interface SetShowContainerQueryOverlaysParams {
    /** An array of node identifiers and descriptors for the highlight appearance. */
    containerQueryHighlightConfigs: Overlay.ContainerQueryHighlightConfig[];
  }
  export type SetShowContainerQueryOverlaysReturn = void;
  export interface SetShowInspectedElementAnchorParams {
    /** Node identifier for which to show an anchor for. */
    inspectedElementAnchorConfig: Overlay.InspectedElementAnchorConfig;
  }
  export type SetShowInspectedElementAnchorReturn = void;
  export interface SetShowPaintRectsParams {
    /** True for showing paint rectangles */
    result: boolean;
  }
  export type SetShowPaintRectsReturn = void;
  export interface SetShowLayoutShiftRegionsParams {
    /** True for showing layout shift regions */
    result: boolean;
  }
  export type SetShowLayoutShiftRegionsReturn = void;
  export interface SetShowScrollBottleneckRectsParams {
    /** True for showing scroll bottleneck rects */
    show: boolean;
  }
  export type SetShowScrollBottleneckRectsReturn = void;
  export interface SetShowHitTestBordersParams {
    /** True for showing hit-test borders */
    show: boolean;
  }
  export type SetShowHitTestBordersReturn = void;
  export interface SetShowWebVitalsParams {
    show: boolean;
  }
  export type SetShowWebVitalsReturn = void;
  export interface SetShowViewportSizeOnResizeParams {
    /** Whether to paint size or not. */
    show: boolean;
  }
  export type SetShowViewportSizeOnResizeReturn = void;
  export interface SetShowHingeParams {
    /** hinge data, null means hideHinge */
    hingeConfig?: Overlay.HingeConfig;
  }
  export type SetShowHingeReturn = void;
  export interface SetShowIsolatedElementsParams {
    /** An array of node identifiers and descriptors for the highlight appearance. */
    isolatedElementHighlightConfigs: Overlay.IsolatedElementHighlightConfig[];
  }
  export type SetShowIsolatedElementsReturn = void;
  export interface SetShowWindowControlsOverlayParams {
    /** Window Controls Overlay data, null means hide Window Controls Overlay */
    windowControlsOverlayConfig?: Overlay.WindowControlsOverlayConfig;
  }
  export type SetShowWindowControlsOverlayReturn = void;
}

export namespace Page {
  export interface AddScriptToEvaluateOnLoadParams {
    scriptSource: string;
  }
  export interface AddScriptToEvaluateOnLoadReturn {
    /** Identifier of the added script. */
    identifier: Page.ScriptIdentifier;
  }
  export interface AddScriptToEvaluateOnNewDocumentParams {
    source: string;
    /**
     * If specified, creates an isolated world with the given name and evaluates given script in it.
     * This world name will be used as the ExecutionContextDescription::name when the corresponding
     * event is emitted.
     */
    worldName?: string;
    /**
     * Specifies whether command line API should be available to the script, defaults
     * to false.
     */
    includeCommandLineAPI?: boolean;
    /**
     * If true, runs the script immediately on existing execution contexts or worlds.
     * Default: false.
     */
    runImmediately?: boolean;
  }
  export interface AddScriptToEvaluateOnNewDocumentReturn {
    /** Identifier of the added script. */
    identifier: Page.ScriptIdentifier;
  }
  export interface BringToFrontParams {}
  export type BringToFrontReturn = void;
  export interface CaptureScreenshotParams {
    /** Image compression format (defaults to png). */
    format?: "jpeg" | "png" | "webp";
    /** Compression quality from range [0..100] (jpeg only). */
    quality?: number;
    /** Capture the screenshot of a given region only. */
    clip?: Page.Viewport;
    /** Capture the screenshot from the surface, rather than the view. Defaults to true. */
    fromSurface?: boolean;
    /** Capture the screenshot beyond the viewport. Defaults to false. */
    captureBeyondViewport?: boolean;
    /** Optimize image encoding for speed, not for resulting size (defaults to false) */
    optimizeForSpeed?: boolean;
  }
  export interface CaptureScreenshotReturn {
    /** Base64-encoded image data. (Encoded as a base64 string when passed over JSON) */
    data: string;
  }
  export interface CaptureSnapshotParams {
    /** Format (defaults to mhtml). */
    format?: "mhtml";
  }
  export interface CaptureSnapshotReturn {
    /** Serialized page data. */
    data: string;
  }
  export interface CreateIsolatedWorldParams {
    /** Id of the frame in which the isolated world should be created. */
    frameId: Page.FrameId;
    /** An optional name which is reported in the Execution Context. */
    worldName?: string;
    /**
     * Whether or not universal access should be granted to the isolated world. This is a powerful
     * option, use with caution.
     */
    grantUniveralAccess?: boolean;
  }
  export interface CreateIsolatedWorldReturn {
    /** Execution context of the isolated world. */
    executionContextId: Runtime.ExecutionContextId;
  }
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {
    /**
     * If true, the `Page.fileChooserOpened` event will be emitted regardless of the state set by
     * `Page.setInterceptFileChooserDialog` command (default: false).
     */
    enableFileChooserOpenedEvent?: boolean;
  }
  export type EnableReturn = void;
  export interface GetAppManifestParams {
    manifestId?: string;
  }
  export interface GetAppManifestReturn {
    /** Manifest location. */
    url: string;
    errors: Page.AppManifestError[];
    /** Manifest content. */
    data?: string;
    /** Parsed manifest properties. Deprecated, use manifest instead. */
    parsed?: Page.AppManifestParsedProperties;
    manifest: Page.WebAppManifest;
  }
  export interface GetInstallabilityErrorsParams {}
  export interface GetInstallabilityErrorsReturn {
    installabilityErrors: Page.InstallabilityError[];
  }
  export interface GetManifestIconsParams {}
  export interface GetManifestIconsReturn {
    primaryIcon?: string;
  }
  export interface GetAppIdParams {}
  export interface GetAppIdReturn {
    /** App id, either from manifest's id attribute or computed from start_url */
    appId?: string;
    /** Recommendation for manifest's id attribute to match current id computed from start_url */
    recommendedId?: string;
  }
  export interface GetAdScriptAncestryParams {
    frameId: Page.FrameId;
  }
  export interface GetAdScriptAncestryReturn {
    /**
     * The ancestry chain of ad script identifiers leading to this frame's
     * creation, along with the root script's filterlist rule. The ancestry
     * chain is ordered from the most immediate script (in the frame creation
     * stack) to more distant ancestors (that created the immediately preceding
     * script). Only sent if frame is labelled as an ad and ids are available.
     */
    adScriptAncestry?: Network.AdAncestry;
  }
  export interface GetFrameTreeParams {}
  export interface GetFrameTreeReturn {
    /** Present frame tree structure. */
    frameTree: Page.FrameTree;
  }
  export interface GetLayoutMetricsParams {}
  export interface GetLayoutMetricsReturn {
    /** Deprecated metrics relating to the layout viewport. Is in device pixels. Use `cssLayoutViewport` instead. */
    layoutViewport: Page.LayoutViewport;
    /** Deprecated metrics relating to the visual viewport. Is in device pixels. Use `cssVisualViewport` instead. */
    visualViewport: Page.VisualViewport;
    /** Deprecated size of scrollable area. Is in DP. Use `cssContentSize` instead. */
    contentSize: DOM.Rect;
    /** Metrics relating to the layout viewport in CSS pixels. */
    cssLayoutViewport: Page.LayoutViewport;
    /** Metrics relating to the visual viewport in CSS pixels. */
    cssVisualViewport: Page.VisualViewport;
    /** Size of scrollable area in CSS pixels. */
    cssContentSize: DOM.Rect;
  }
  export interface GetNavigationHistoryParams {}
  export interface GetNavigationHistoryReturn {
    /** Index of the current navigation history entry. */
    currentIndex: number;
    /** Array of navigation history entries. */
    entries: Page.NavigationEntry[];
  }
  export interface ResetNavigationHistoryParams {}
  export type ResetNavigationHistoryReturn = void;
  export interface GetResourceContentParams {
    /** Frame id to get resource for. */
    frameId: Page.FrameId;
    /** URL of the resource to get content for. */
    url: string;
  }
  export interface GetResourceContentReturn {
    /** Resource content. */
    content: string;
    /** True, if content was served as base64. */
    base64Encoded: boolean;
  }
  export interface GetResourceTreeParams {}
  export interface GetResourceTreeReturn {
    /** Present frame / resource tree structure. */
    frameTree: Page.FrameResourceTree;
  }
  export interface HandleJavaScriptDialogParams {
    /** Whether to accept or dismiss the dialog. */
    accept: boolean;
    /**
     * The text to enter into the dialog prompt before accepting. Used only if this is a prompt
     * dialog.
     */
    promptText?: string;
  }
  export type HandleJavaScriptDialogReturn = void;
  export interface NavigateParams {
    /** URL to navigate the page to. */
    url: string;
    /** Referrer URL. */
    referrer?: string;
    /** Intended transition type. */
    transitionType?: Page.TransitionType;
    /** Frame id to navigate, if not specified navigates the top frame. */
    frameId?: Page.FrameId;
    /** Referrer-policy used for the navigation. */
    referrerPolicy?: Page.ReferrerPolicy;
  }
  export interface NavigateReturn {
    /** Frame id that has navigated (or failed to navigate) */
    frameId: Page.FrameId;
    /**
     * Loader identifier. This is omitted in case of same-document navigation,
     * as the previously committed loaderId would not change.
     */
    loaderId?: Network.LoaderId;
    /** User friendly error message, present if and only if navigation has failed. */
    errorText?: string;
    /** Whether the navigation resulted in a download. */
    isDownload?: boolean;
  }
  export interface NavigateToHistoryEntryParams {
    /** Unique id of the entry to navigate to. */
    entryId: number;
  }
  export type NavigateToHistoryEntryReturn = void;
  export interface PrintToPDFParams {
    /** Paper orientation. Defaults to false. */
    landscape?: boolean;
    /** Display header and footer. Defaults to false. */
    displayHeaderFooter?: boolean;
    /** Print background graphics. Defaults to false. */
    printBackground?: boolean;
    /** Scale of the webpage rendering. Defaults to 1. */
    scale?: number;
    /** Paper width in inches. Defaults to 8.5 inches. */
    paperWidth?: number;
    /** Paper height in inches. Defaults to 11 inches. */
    paperHeight?: number;
    /** Top margin in inches. Defaults to 1cm (~0.4 inches). */
    marginTop?: number;
    /** Bottom margin in inches. Defaults to 1cm (~0.4 inches). */
    marginBottom?: number;
    /** Left margin in inches. Defaults to 1cm (~0.4 inches). */
    marginLeft?: number;
    /** Right margin in inches. Defaults to 1cm (~0.4 inches). */
    marginRight?: number;
    /**
     * Paper ranges to print, one based, e.g., '1-5, 8, 11-13'. Pages are
     * printed in the document order, not in the order specified, and no
     * more than once.
     * Defaults to empty string, which implies the entire document is printed.
     * The page numbers are quietly capped to actual page count of the
     * document, and ranges beyond the end of the document are ignored.
     * If this results in no pages to print, an error is reported.
     * It is an error to specify a range with start greater than end.
     */
    pageRanges?: string;
    /**
     * HTML template for the print header. Should be valid HTML markup with following
     * classes used to inject printing values into them:
     * - `date`: formatted print date
     * - `title`: document title
     * - `url`: document location
     * - `pageNumber`: current page number
     * - `totalPages`: total pages in the document
     *
     * For example, `<span class=title></span>` would generate span containing the title.
     */
    headerTemplate?: string;
    /** HTML template for the print footer. Should use the same format as the `headerTemplate`. */
    footerTemplate?: string;
    /**
     * Whether or not to prefer page size as defined by css. Defaults to false,
     * in which case the content will be scaled to fit the paper size.
     */
    preferCSSPageSize?: boolean;
    /** return as stream */
    transferMode?: "ReturnAsBase64" | "ReturnAsStream";
    /** Whether or not to generate tagged (accessible) PDF. Defaults to embedder choice. */
    generateTaggedPDF?: boolean;
    /** Whether or not to embed the document outline into the PDF. */
    generateDocumentOutline?: boolean;
  }
  export interface PrintToPDFReturn {
    /** Base64-encoded pdf data. Empty if |returnAsStream| is specified. (Encoded as a base64 string when passed over JSON) */
    data: string;
    /** A handle of the stream that holds resulting PDF data. */
    stream?: IO.StreamHandle;
  }
  export interface ReloadParams {
    /** If true, browser cache is ignored (as if the user pressed Shift+refresh). */
    ignoreCache?: boolean;
    /**
     * If set, the script will be injected into all frames of the inspected page after reload.
     * Argument will be ignored if reloading dataURL origin.
     */
    scriptToEvaluateOnLoad?: string;
    /**
     * If set, an error will be thrown if the target page's main frame's
     * loader id does not match the provided id. This prevents accidentally
     * reloading an unintended target in case there's a racing navigation.
     */
    loaderId?: Network.LoaderId;
  }
  export type ReloadReturn = void;
  export interface RemoveScriptToEvaluateOnLoadParams {
    identifier: Page.ScriptIdentifier;
  }
  export type RemoveScriptToEvaluateOnLoadReturn = void;
  export interface RemoveScriptToEvaluateOnNewDocumentParams {
    identifier: Page.ScriptIdentifier;
  }
  export type RemoveScriptToEvaluateOnNewDocumentReturn = void;
  export interface ScreencastFrameAckParams {
    /** Frame number. */
    sessionId: number;
  }
  export type ScreencastFrameAckReturn = void;
  export interface SearchInResourceParams {
    /** Frame id for resource to search in. */
    frameId: Page.FrameId;
    /** URL of the resource to search in. */
    url: string;
    /** String to search for. */
    query: string;
    /** If true, search is case sensitive. */
    caseSensitive?: boolean;
    /** If true, treats string parameter as regex. */
    isRegex?: boolean;
  }
  export interface SearchInResourceReturn {
    /** List of search matches. */
    result: Debugger.SearchMatch[];
  }
  export interface SetAdBlockingEnabledParams {
    /** Whether to block ads. */
    enabled: boolean;
  }
  export type SetAdBlockingEnabledReturn = void;
  export interface SetBypassCSPParams {
    /** Whether to bypass page CSP. */
    enabled: boolean;
  }
  export type SetBypassCSPReturn = void;
  export interface GetPermissionsPolicyStateParams {
    frameId: Page.FrameId;
  }
  export interface GetPermissionsPolicyStateReturn {
    states: Page.PermissionsPolicyFeatureState[];
  }
  export interface GetOriginTrialsParams {
    frameId: Page.FrameId;
  }
  export interface GetOriginTrialsReturn {
    originTrials: Page.OriginTrial[];
  }
  export interface SetFontFamiliesParams {
    /** Specifies font families to set. If a font family is not specified, it won't be changed. */
    fontFamilies: Page.FontFamilies;
    /** Specifies font families to set for individual scripts. */
    forScripts?: Page.ScriptFontFamilies[];
  }
  export type SetFontFamiliesReturn = void;
  export interface SetFontSizesParams {
    /** Specifies font sizes to set. If a font size is not specified, it won't be changed. */
    fontSizes: Page.FontSizes;
  }
  export type SetFontSizesReturn = void;
  export interface SetDocumentContentParams {
    /** Frame id to set HTML for. */
    frameId: Page.FrameId;
    /** HTML content to set. */
    html: string;
  }
  export type SetDocumentContentReturn = void;
  export interface SetDownloadBehaviorParams {
    /**
     * Whether to allow all or deny all download requests, or use default Chrome behavior if
     * available (otherwise deny).
     */
    behavior: "deny" | "allow" | "default";
    /** The default path to save downloaded files to. This is required if behavior is set to 'allow' */
    downloadPath?: string;
  }
  export type SetDownloadBehaviorReturn = void;
  export interface SetLifecycleEventsEnabledParams {
    /** If true, starts emitting lifecycle events. */
    enabled: boolean;
  }
  export type SetLifecycleEventsEnabledReturn = void;
  export interface StartScreencastParams {
    /** Image compression format. */
    format?: "jpeg" | "png";
    /** Compression quality from range [0..100]. */
    quality?: number;
    /** Maximum screenshot width. */
    maxWidth?: number;
    /** Maximum screenshot height. */
    maxHeight?: number;
    /** Send every n-th frame. */
    everyNthFrame?: number;
  }
  export type StartScreencastReturn = void;
  export interface StopLoadingParams {}
  export type StopLoadingReturn = void;
  export interface CrashParams {}
  export type CrashReturn = void;
  export interface CloseParams {}
  export type CloseReturn = void;
  export interface SetWebLifecycleStateParams {
    /** Target lifecycle state */
    state: "frozen" | "active";
  }
  export type SetWebLifecycleStateReturn = void;
  export interface StopScreencastParams {}
  export type StopScreencastReturn = void;
  export interface ProduceCompilationCacheParams {
    scripts: Page.CompilationCacheParams[];
  }
  export type ProduceCompilationCacheReturn = void;
  export interface AddCompilationCacheParams {
    url: string;
    /** Base64-encoded data (Encoded as a base64 string when passed over JSON) */
    data: string;
  }
  export type AddCompilationCacheReturn = void;
  export interface ClearCompilationCacheParams {}
  export type ClearCompilationCacheReturn = void;
  export interface SetSPCTransactionModeParams {
    mode: "none" | "autoAccept" | "autoChooseToAuthAnotherWay" | "autoReject" | "autoOptOut";
  }
  export type SetSPCTransactionModeReturn = void;
  export interface SetRPHRegistrationModeParams {
    mode: "none" | "autoAccept" | "autoReject";
  }
  export type SetRPHRegistrationModeReturn = void;
  export interface GenerateTestReportParams {
    /** Message to be displayed in the report. */
    message: string;
    /** Specifies the endpoint group to deliver the report to. */
    group?: string;
  }
  export type GenerateTestReportReturn = void;
  export interface WaitForDebuggerParams {}
  export type WaitForDebuggerReturn = void;
  export interface SetInterceptFileChooserDialogParams {
    enabled: boolean;
    /**
     * If true, cancels the dialog by emitting relevant events (if any)
     * in addition to not showing it if the interception is enabled
     * (default: false).
     */
    cancel?: boolean;
  }
  export type SetInterceptFileChooserDialogReturn = void;
  export interface SetPrerenderingAllowedParams {
    isAllowed: boolean;
  }
  export type SetPrerenderingAllowedReturn = void;
  export interface GetAnnotatedPageContentParams {
    /** Whether to include actionable information. Defaults to true. */
    includeActionableInformation?: boolean;
  }
  export interface GetAnnotatedPageContentReturn {
    /**
     * The annotated page content as a base64 encoded protobuf.
     * The format is defined by the `AnnotatedPageContent` message in
     * components/optimization_guide/proto/features/common_quality_data.proto (Encoded as a base64 string when passed over JSON)
     */
    content: string;
  }
}

export namespace Performance {
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {
    /** Time domain to use for collecting and reporting duration metrics. */
    timeDomain?: "timeTicks" | "threadTicks";
  }
  export type EnableReturn = void;
  export interface SetTimeDomainParams {
    /** Time domain */
    timeDomain: "timeTicks" | "threadTicks";
  }
  export type SetTimeDomainReturn = void;
  export interface GetMetricsParams {}
  export interface GetMetricsReturn {
    /** Current values for run-time metrics. */
    metrics: Performance.Metric[];
  }
}

export namespace PerformanceTimeline {
  export interface EnableParams {
    /**
     * The types of event to report, as specified in
     * https://w3c.github.io/performance-timeline/#dom-performanceentry-entrytype
     * The specified filter overrides any previous filters, passing empty
     * filter disables recording.
     * Note that not all types exposed to the web platform are currently supported.
     */
    eventTypes: string[];
  }
  export type EnableReturn = void;
}

export namespace Preload {
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
}

export namespace Profiler {
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface GetBestEffortCoverageParams {}
  export interface GetBestEffortCoverageReturn {
    /** Coverage data for the current isolate. */
    result: Profiler.ScriptCoverage[];
  }
  export interface SetSamplingIntervalParams {
    /** New sampling interval in microseconds. */
    interval: number;
  }
  export type SetSamplingIntervalReturn = void;
  export interface StartParams {}
  export type StartReturn = void;
  export interface StartPreciseCoverageParams {
    /** Collect accurate call counts beyond simple 'covered' or 'not covered'. */
    callCount?: boolean;
    /** Collect block-based coverage. */
    detailed?: boolean;
    /** Allow the backend to send updates on its own initiative */
    allowTriggeredUpdates?: boolean;
  }
  export interface StartPreciseCoverageReturn {
    /** Monotonically increasing time (in seconds) when the coverage update was taken in the backend. */
    timestamp: number;
  }
  export interface StopParams {}
  export interface StopReturn {
    /** Recorded profile. */
    profile: Profiler.Profile;
  }
  export interface StopPreciseCoverageParams {}
  export type StopPreciseCoverageReturn = void;
  export interface TakePreciseCoverageParams {}
  export interface TakePreciseCoverageReturn {
    /** Coverage data for the current isolate. */
    result: Profiler.ScriptCoverage[];
    /** Monotonically increasing time (in seconds) when the coverage update was taken in the backend. */
    timestamp: number;
  }
}

export namespace PWA {
  export interface GetOsAppStateParams {
    /**
     * The id from the webapp's manifest file, commonly it's the url of the
     * site installing the webapp. See
     * https://web.dev/learn/pwa/web-app-manifest.
     */
    manifestId: string;
  }
  export interface GetOsAppStateReturn {
    badgeCount: number;
    fileHandlers: PWA.FileHandler[];
  }
  export interface InstallParams {
    manifestId: string;
    /**
     * The location of the app or bundle overriding the one derived from the
     * manifestId.
     */
    installUrlOrBundleUrl?: string;
  }
  export type InstallReturn = void;
  export interface UninstallParams {
    manifestId: string;
  }
  export type UninstallReturn = void;
  export interface LaunchParams {
    manifestId: string;
    url?: string;
  }
  export interface LaunchReturn {
    /** ID of the tab target created as a result. */
    targetId: Target.TargetID;
  }
  export interface LaunchFilesInAppParams {
    manifestId: string;
    files: string[];
  }
  export interface LaunchFilesInAppReturn {
    /** IDs of the tab targets created as the result. */
    targetIds: Target.TargetID[];
  }
  export interface OpenCurrentPageInAppParams {
    manifestId: string;
  }
  export type OpenCurrentPageInAppReturn = void;
  export interface ChangeAppUserSettingsParams {
    manifestId: string;
    /**
     * If user allows the links clicked on by the user in the app's scope, or
     * extended scope if the manifest has scope extensions and the flags
     * `DesktopPWAsLinkCapturingWithScopeExtensions` and
     * `WebAppEnableScopeExtensions` are enabled.
     *
     * Note, the API does not support resetting the linkCapturing to the
     * initial value, uninstalling and installing the web app again will reset
     * it.
     *
     * TODO(crbug.com/339453269): Setting this value on ChromeOS is not
     * supported yet.
     */
    linkCapturing?: boolean;
    displayMode?: PWA.DisplayMode;
  }
  export type ChangeAppUserSettingsReturn = void;
}

export namespace Runtime {
  export interface AwaitPromiseParams {
    /** Identifier of the promise. */
    promiseObjectId: Runtime.RemoteObjectId;
    /** Whether the result is expected to be a JSON object that should be sent by value. */
    returnByValue?: boolean;
    /** Whether preview should be generated for the result. */
    generatePreview?: boolean;
  }
  export interface AwaitPromiseReturn {
    /** Promise result. Will contain rejected value if promise was rejected. */
    result: Runtime.RemoteObject;
    /** Exception details if stack strace is available. */
    exceptionDetails?: Runtime.ExceptionDetails;
  }
  export interface CallFunctionOnParams {
    /** Declaration of the function to call. */
    functionDeclaration: string;
    /**
     * Identifier of the object to call function on. Either objectId or executionContextId should
     * be specified.
     */
    objectId?: Runtime.RemoteObjectId;
    /**
     * Call arguments. All call arguments must belong to the same JavaScript world as the target
     * object.
     */
    arguments?: Runtime.CallArgument[];
    /**
     * In silent mode exceptions thrown during evaluation are not reported and do not pause
     * execution. Overrides `setPauseOnException` state.
     */
    silent?: boolean;
    /**
     * Whether the result is expected to be a JSON object which should be sent by value.
     * Can be overriden by `serializationOptions`.
     */
    returnByValue?: boolean;
    /** Whether preview should be generated for the result. */
    generatePreview?: boolean;
    /** Whether execution should be treated as initiated by user in the UI. */
    userGesture?: boolean;
    /**
     * Whether execution should `await` for resulting value and return once awaited promise is
     * resolved.
     */
    awaitPromise?: boolean;
    /**
     * Specifies execution context which global object will be used to call function on. Either
     * executionContextId or objectId should be specified.
     */
    executionContextId?: Runtime.ExecutionContextId;
    /**
     * Symbolic group name that can be used to release multiple objects. If objectGroup is not
     * specified and objectId is, objectGroup will be inherited from object.
     */
    objectGroup?: string;
    /** Whether to throw an exception if side effect cannot be ruled out during evaluation. */
    throwOnSideEffect?: boolean;
    /**
     * An alternative way to specify the execution context to call function on.
     * Compared to contextId that may be reused across processes, this is guaranteed to be
     * system-unique, so it can be used to prevent accidental function call
     * in context different than intended (e.g. as a result of navigation across process
     * boundaries).
     * This is mutually exclusive with `executionContextId`.
     */
    uniqueContextId?: string;
    /**
     * Specifies the result serialization. If provided, overrides
     * `generatePreview` and `returnByValue`.
     */
    serializationOptions?: Runtime.SerializationOptions;
  }
  export interface CallFunctionOnReturn {
    /** Call result. */
    result: Runtime.RemoteObject;
    /** Exception details. */
    exceptionDetails?: Runtime.ExceptionDetails;
  }
  export interface CompileScriptParams {
    /** Expression to compile. */
    expression: string;
    /** Source url to be set for the script. */
    sourceURL: string;
    /** Specifies whether the compiled script should be persisted. */
    persistScript: boolean;
    /**
     * Specifies in which execution context to perform script run. If the parameter is omitted the
     * evaluation will be performed in the context of the inspected page.
     */
    executionContextId?: Runtime.ExecutionContextId;
  }
  export interface CompileScriptReturn {
    /** Id of the script. */
    scriptId?: Runtime.ScriptId;
    /** Exception details. */
    exceptionDetails?: Runtime.ExceptionDetails;
  }
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface DiscardConsoleEntriesParams {}
  export type DiscardConsoleEntriesReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface EvaluateParams {
    /** Expression to evaluate. */
    expression: string;
    /** Symbolic group name that can be used to release multiple objects. */
    objectGroup?: string;
    /** Determines whether Command Line API should be available during the evaluation. */
    includeCommandLineAPI?: boolean;
    /**
     * In silent mode exceptions thrown during evaluation are not reported and do not pause
     * execution. Overrides `setPauseOnException` state.
     */
    silent?: boolean;
    /**
     * Specifies in which execution context to perform evaluation. If the parameter is omitted the
     * evaluation will be performed in the context of the inspected page.
     * This is mutually exclusive with `uniqueContextId`, which offers an
     * alternative way to identify the execution context that is more reliable
     * in a multi-process environment.
     */
    contextId?: Runtime.ExecutionContextId;
    /** Whether the result is expected to be a JSON object that should be sent by value. */
    returnByValue?: boolean;
    /** Whether preview should be generated for the result. */
    generatePreview?: boolean;
    /** Whether execution should be treated as initiated by user in the UI. */
    userGesture?: boolean;
    /**
     * Whether execution should `await` for resulting value and return once awaited promise is
     * resolved.
     */
    awaitPromise?: boolean;
    /**
     * Whether to throw an exception if side effect cannot be ruled out during evaluation.
     * This implies `disableBreaks` below.
     */
    throwOnSideEffect?: boolean;
    /** Terminate execution after timing out (number of milliseconds). */
    timeout?: Runtime.TimeDelta;
    /** Disable breakpoints during execution. */
    disableBreaks?: boolean;
    /**
     * Setting this flag to true enables `let` re-declaration and top-level `await`.
     * Note that `let` variables can only be re-declared if they originate from
     * `replMode` themselves.
     */
    replMode?: boolean;
    /**
     * The Content Security Policy (CSP) for the target might block 'unsafe-eval'
     * which includes eval(), Function(), setTimeout() and setInterval()
     * when called with non-callable arguments. This flag bypasses CSP for this
     * evaluation and allows unsafe-eval. Defaults to true.
     */
    allowUnsafeEvalBlockedByCSP?: boolean;
    /**
     * An alternative way to specify the execution context to evaluate in.
     * Compared to contextId that may be reused across processes, this is guaranteed to be
     * system-unique, so it can be used to prevent accidental evaluation of the expression
     * in context different than intended (e.g. as a result of navigation across process
     * boundaries).
     * This is mutually exclusive with `contextId`.
     */
    uniqueContextId?: string;
    /**
     * Specifies the result serialization. If provided, overrides
     * `generatePreview` and `returnByValue`.
     */
    serializationOptions?: Runtime.SerializationOptions;
  }
  export interface EvaluateReturn {
    /** Evaluation result. */
    result: Runtime.RemoteObject;
    /** Exception details. */
    exceptionDetails?: Runtime.ExceptionDetails;
  }
  export interface GetIsolateIdParams {}
  export interface GetIsolateIdReturn {
    /** The isolate id. */
    id: string;
  }
  export interface GetHeapUsageParams {}
  export interface GetHeapUsageReturn {
    /** Used JavaScript heap size in bytes. */
    usedSize: number;
    /** Allocated JavaScript heap size in bytes. */
    totalSize: number;
    /** Used size in bytes in the embedder's garbage-collected heap. */
    embedderHeapUsedSize: number;
    /** Size in bytes of backing storage for array buffers and external strings. */
    backingStorageSize: number;
  }
  export interface GetPropertiesParams {
    /** Identifier of the object to return properties for. */
    objectId: Runtime.RemoteObjectId;
    /**
     * If true, returns properties belonging only to the element itself, not to its prototype
     * chain.
     */
    ownProperties?: boolean;
    /**
     * If true, returns accessor properties (with getter/setter) only; internal properties are not
     * returned either.
     */
    accessorPropertiesOnly?: boolean;
    /** Whether preview should be generated for the results. */
    generatePreview?: boolean;
    /** If true, returns non-indexed properties only. */
    nonIndexedPropertiesOnly?: boolean;
  }
  export interface GetPropertiesReturn {
    /** Object properties. */
    result: Runtime.PropertyDescriptor[];
    /** Internal object properties (only of the element itself). */
    internalProperties?: Runtime.InternalPropertyDescriptor[];
    /** Object private properties. */
    privateProperties?: Runtime.PrivatePropertyDescriptor[];
    /** Exception details. */
    exceptionDetails?: Runtime.ExceptionDetails;
  }
  export interface GlobalLexicalScopeNamesParams {
    /** Specifies in which execution context to lookup global scope variables. */
    executionContextId?: Runtime.ExecutionContextId;
  }
  export interface GlobalLexicalScopeNamesReturn {
    names: string[];
  }
  export interface QueryObjectsParams {
    /** Identifier of the prototype to return objects for. */
    prototypeObjectId: Runtime.RemoteObjectId;
    /** Symbolic group name that can be used to release the results. */
    objectGroup?: string;
  }
  export interface QueryObjectsReturn {
    /** Array with objects. */
    objects: Runtime.RemoteObject;
  }
  export interface ReleaseObjectParams {
    /** Identifier of the object to release. */
    objectId: Runtime.RemoteObjectId;
  }
  export type ReleaseObjectReturn = void;
  export interface ReleaseObjectGroupParams {
    /** Symbolic object group name. */
    objectGroup: string;
  }
  export type ReleaseObjectGroupReturn = void;
  export interface RunIfWaitingForDebuggerParams {}
  export type RunIfWaitingForDebuggerReturn = void;
  export interface RunScriptParams {
    /** Id of the script to run. */
    scriptId: Runtime.ScriptId;
    /**
     * Specifies in which execution context to perform script run. If the parameter is omitted the
     * evaluation will be performed in the context of the inspected page.
     */
    executionContextId?: Runtime.ExecutionContextId;
    /** Symbolic group name that can be used to release multiple objects. */
    objectGroup?: string;
    /**
     * In silent mode exceptions thrown during evaluation are not reported and do not pause
     * execution. Overrides `setPauseOnException` state.
     */
    silent?: boolean;
    /** Determines whether Command Line API should be available during the evaluation. */
    includeCommandLineAPI?: boolean;
    /** Whether the result is expected to be a JSON object which should be sent by value. */
    returnByValue?: boolean;
    /** Whether preview should be generated for the result. */
    generatePreview?: boolean;
    /**
     * Whether execution should `await` for resulting value and return once awaited promise is
     * resolved.
     */
    awaitPromise?: boolean;
  }
  export interface RunScriptReturn {
    /** Run result. */
    result: Runtime.RemoteObject;
    /** Exception details. */
    exceptionDetails?: Runtime.ExceptionDetails;
  }
  export interface SetCustomObjectFormatterEnabledParams {
    enabled: boolean;
  }
  export type SetCustomObjectFormatterEnabledReturn = void;
  export interface SetMaxCallStackSizeToCaptureParams {
    size: number;
  }
  export type SetMaxCallStackSizeToCaptureReturn = void;
  export interface TerminateExecutionParams {}
  export type TerminateExecutionReturn = void;
  export interface AddBindingParams {
    name: string;
    /**
     * If specified, the binding would only be exposed to the specified
     * execution context. If omitted and `executionContextName` is not set,
     * the binding is exposed to all execution contexts of the target.
     * This parameter is mutually exclusive with `executionContextName`.
     * Deprecated in favor of `executionContextName` due to an unclear use case
     * and bugs in implementation (crbug.com/1169639). `executionContextId` will be
     * removed in the future.
     */
    executionContextId?: Runtime.ExecutionContextId;
    /**
     * If specified, the binding is exposed to the executionContext with
     * matching name, even for contexts created after the binding is added.
     * See also `ExecutionContext.name` and `worldName` parameter to
     * `Page.addScriptToEvaluateOnNewDocument`.
     * This parameter is mutually exclusive with `executionContextId`.
     */
    executionContextName?: string;
  }
  export type AddBindingReturn = void;
  export interface RemoveBindingParams {
    name: string;
  }
  export type RemoveBindingReturn = void;
  export interface GetExceptionDetailsParams {
    /** The error object for which to resolve the exception details. */
    errorObjectId: Runtime.RemoteObjectId;
  }
  export interface GetExceptionDetailsReturn {
    exceptionDetails?: Runtime.ExceptionDetails;
  }
}

export namespace Schema {
  export interface GetDomainsParams {}
  export interface GetDomainsReturn {
    /** List of supported domains. */
    domains: Schema.Domain[];
  }
}

export namespace Security {
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface SetIgnoreCertificateErrorsParams {
    /** If true, all certificate errors will be ignored. */
    ignore: boolean;
  }
  export type SetIgnoreCertificateErrorsReturn = void;
  export interface HandleCertificateErrorParams {
    /** The ID of the event. */
    eventId: number;
    /** The action to take on the certificate error. */
    action: Security.CertificateErrorAction;
  }
  export type HandleCertificateErrorReturn = void;
  export interface SetOverrideCertificateErrorsParams {
    /** If true, certificate errors will be overridden. */
    override: boolean;
  }
  export type SetOverrideCertificateErrorsReturn = void;
}

export namespace ServiceWorker {
  export interface DeliverPushMessageParams {
    origin: string;
    registrationId: ServiceWorker.RegistrationID;
    data: string;
  }
  export type DeliverPushMessageReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface DispatchSyncEventParams {
    origin: string;
    registrationId: ServiceWorker.RegistrationID;
    tag: string;
    lastChance: boolean;
  }
  export type DispatchSyncEventReturn = void;
  export interface DispatchPeriodicSyncEventParams {
    origin: string;
    registrationId: ServiceWorker.RegistrationID;
    tag: string;
  }
  export type DispatchPeriodicSyncEventReturn = void;
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface SetForceUpdateOnPageLoadParams {
    forceUpdateOnPageLoad: boolean;
  }
  export type SetForceUpdateOnPageLoadReturn = void;
  export interface SkipWaitingParams {
    scopeURL: string;
  }
  export type SkipWaitingReturn = void;
  export interface StartWorkerParams {
    scopeURL: string;
  }
  export type StartWorkerReturn = void;
  export interface StopAllWorkersParams {}
  export type StopAllWorkersReturn = void;
  export interface StopWorkerParams {
    versionId: string;
  }
  export type StopWorkerReturn = void;
  export interface UnregisterParams {
    scopeURL: string;
  }
  export type UnregisterReturn = void;
  export interface UpdateRegistrationParams {
    scopeURL: string;
  }
  export type UpdateRegistrationReturn = void;
}

export namespace SmartCardEmulation {
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface ReportEstablishContextResultParams {
    requestId: string;
    contextId: number;
  }
  export type ReportEstablishContextResultReturn = void;
  export interface ReportReleaseContextResultParams {
    requestId: string;
  }
  export type ReportReleaseContextResultReturn = void;
  export interface ReportListReadersResultParams {
    requestId: string;
    readers: string[];
  }
  export type ReportListReadersResultReturn = void;
  export interface ReportGetStatusChangeResultParams {
    requestId: string;
    readerStates: SmartCardEmulation.ReaderStateOut[];
  }
  export type ReportGetStatusChangeResultReturn = void;
  export interface ReportBeginTransactionResultParams {
    requestId: string;
    handle: number;
  }
  export type ReportBeginTransactionResultReturn = void;
  export interface ReportPlainResultParams {
    requestId: string;
  }
  export type ReportPlainResultReturn = void;
  export interface ReportConnectResultParams {
    requestId: string;
    handle: number;
    activeProtocol?: SmartCardEmulation.Protocol;
  }
  export type ReportConnectResultReturn = void;
  export interface ReportDataResultParams {
    requestId: string;
    data: string;
  }
  export type ReportDataResultReturn = void;
  export interface ReportStatusResultParams {
    requestId: string;
    readerName: string;
    state: SmartCardEmulation.ConnectionState;
    atr: string;
    protocol?: SmartCardEmulation.Protocol;
  }
  export type ReportStatusResultReturn = void;
  export interface ReportErrorParams {
    requestId: string;
    resultCode: SmartCardEmulation.ResultCode;
  }
  export type ReportErrorReturn = void;
}

export namespace Storage {
  export interface GetStorageKeyForFrameParams {
    frameId: Page.FrameId;
  }
  export interface GetStorageKeyForFrameReturn {
    storageKey: Storage.SerializedStorageKey;
  }
  export interface GetStorageKeyParams {
    frameId?: Page.FrameId;
  }
  export interface GetStorageKeyReturn {
    storageKey: Storage.SerializedStorageKey;
  }
  export interface ClearDataForOriginParams {
    /** Security origin. */
    origin: string;
    /** Comma separated list of StorageType to clear. */
    storageTypes: string;
  }
  export type ClearDataForOriginReturn = void;
  export interface ClearDataForStorageKeyParams {
    /** Storage key. */
    storageKey: string;
    /** Comma separated list of StorageType to clear. */
    storageTypes: string;
  }
  export type ClearDataForStorageKeyReturn = void;
  export interface GetCookiesParams {
    /** Browser context to use when called on the browser endpoint. */
    browserContextId?: Browser.BrowserContextID;
  }
  export interface GetCookiesReturn {
    /** Array of cookie objects. */
    cookies: Network.Cookie[];
  }
  export interface SetCookiesParams {
    /** Cookies to be set. */
    cookies: Network.CookieParam[];
    /** Browser context to use when called on the browser endpoint. */
    browserContextId?: Browser.BrowserContextID;
  }
  export type SetCookiesReturn = void;
  export interface ClearCookiesParams {
    /** Browser context to use when called on the browser endpoint. */
    browserContextId?: Browser.BrowserContextID;
  }
  export type ClearCookiesReturn = void;
  export interface GetUsageAndQuotaParams {
    /** Security origin. */
    origin: string;
  }
  export interface GetUsageAndQuotaReturn {
    /** Storage usage (bytes). */
    usage: number;
    /** Storage quota (bytes). */
    quota: number;
    /** Whether or not the origin has an active storage quota override */
    overrideActive: boolean;
    /** Storage usage per type (bytes). */
    usageBreakdown: Storage.UsageForType[];
  }
  export interface OverrideQuotaForOriginParams {
    /** Security origin. */
    origin: string;
    /**
     * The quota size (in bytes) to override the original quota with.
     * If this is called multiple times, the overridden quota will be equal to
     * the quotaSize provided in the final call. If this is called without
     * specifying a quotaSize, the quota will be reset to the default value for
     * the specified origin. If this is called multiple times with different
     * origins, the override will be maintained for each origin until it is
     * disabled (called without a quotaSize).
     */
    quotaSize?: number;
  }
  export type OverrideQuotaForOriginReturn = void;
  export interface TrackCacheStorageForOriginParams {
    /** Security origin. */
    origin: string;
  }
  export type TrackCacheStorageForOriginReturn = void;
  export interface TrackCacheStorageForStorageKeyParams {
    /** Storage key. */
    storageKey: string;
  }
  export type TrackCacheStorageForStorageKeyReturn = void;
  export interface TrackIndexedDBForOriginParams {
    /** Security origin. */
    origin: string;
  }
  export type TrackIndexedDBForOriginReturn = void;
  export interface TrackIndexedDBForStorageKeyParams {
    /** Storage key. */
    storageKey: string;
  }
  export type TrackIndexedDBForStorageKeyReturn = void;
  export interface UntrackCacheStorageForOriginParams {
    /** Security origin. */
    origin: string;
  }
  export type UntrackCacheStorageForOriginReturn = void;
  export interface UntrackCacheStorageForStorageKeyParams {
    /** Storage key. */
    storageKey: string;
  }
  export type UntrackCacheStorageForStorageKeyReturn = void;
  export interface UntrackIndexedDBForOriginParams {
    /** Security origin. */
    origin: string;
  }
  export type UntrackIndexedDBForOriginReturn = void;
  export interface UntrackIndexedDBForStorageKeyParams {
    /** Storage key. */
    storageKey: string;
  }
  export type UntrackIndexedDBForStorageKeyReturn = void;
  export interface GetTrustTokensParams {}
  export interface GetTrustTokensReturn {
    tokens: Storage.TrustTokens[];
  }
  export interface ClearTrustTokensParams {
    issuerOrigin: string;
  }
  export interface ClearTrustTokensReturn {
    /** True if any tokens were deleted, false otherwise. */
    didDeleteTokens: boolean;
  }
  export interface GetInterestGroupDetailsParams {
    ownerOrigin: string;
    name: string;
  }
  export interface GetInterestGroupDetailsReturn {
    /**
     * This largely corresponds to:
     * https://wicg.github.io/turtledove/#dictdef-generatebidinterestgroup
     * but has absolute expirationTime instead of relative lifetimeMs and
     * also adds joiningOrigin.
     */
    details: Record<string, unknown>;
  }
  export interface SetInterestGroupTrackingParams {
    enable: boolean;
  }
  export type SetInterestGroupTrackingReturn = void;
  export interface SetInterestGroupAuctionTrackingParams {
    enable: boolean;
  }
  export type SetInterestGroupAuctionTrackingReturn = void;
  export interface GetSharedStorageMetadataParams {
    ownerOrigin: string;
  }
  export interface GetSharedStorageMetadataReturn {
    metadata: Storage.SharedStorageMetadata;
  }
  export interface GetSharedStorageEntriesParams {
    ownerOrigin: string;
  }
  export interface GetSharedStorageEntriesReturn {
    entries: Storage.SharedStorageEntry[];
  }
  export interface SetSharedStorageEntryParams {
    ownerOrigin: string;
    key: string;
    value: string;
    /**
     * If `ignoreIfPresent` is included and true, then only sets the entry if
     * `key` doesn't already exist.
     */
    ignoreIfPresent?: boolean;
  }
  export type SetSharedStorageEntryReturn = void;
  export interface DeleteSharedStorageEntryParams {
    ownerOrigin: string;
    key: string;
  }
  export type DeleteSharedStorageEntryReturn = void;
  export interface ClearSharedStorageEntriesParams {
    ownerOrigin: string;
  }
  export type ClearSharedStorageEntriesReturn = void;
  export interface ResetSharedStorageBudgetParams {
    ownerOrigin: string;
  }
  export type ResetSharedStorageBudgetReturn = void;
  export interface SetSharedStorageTrackingParams {
    enable: boolean;
  }
  export type SetSharedStorageTrackingReturn = void;
  export interface SetStorageBucketTrackingParams {
    storageKey: string;
    enable: boolean;
  }
  export type SetStorageBucketTrackingReturn = void;
  export interface DeleteStorageBucketParams {
    bucket: Storage.StorageBucket;
  }
  export type DeleteStorageBucketReturn = void;
  export interface RunBounceTrackingMitigationsParams {}
  export interface RunBounceTrackingMitigationsReturn {
    deletedSites: string[];
  }
  export interface GetRelatedWebsiteSetsParams {}
  export interface GetRelatedWebsiteSetsReturn {
    sets: Storage.RelatedWebsiteSet[];
  }
  export interface SetProtectedAudienceKAnonymityParams {
    owner: string;
    name: string;
    hashes: string[];
  }
  export type SetProtectedAudienceKAnonymityReturn = void;
}

export namespace SystemInfo {
  export interface GetInfoParams {}
  export interface GetInfoReturn {
    /** Information about the GPUs on the system. */
    gpu: SystemInfo.GPUInfo;
    /**
     * A platform-dependent description of the model of the machine. On Mac OS, this is, for
     * example, 'MacBookPro'. Will be the empty string if not supported.
     */
    modelName: string;
    /**
     * A platform-dependent description of the version of the machine. On Mac OS, this is, for
     * example, '10.1'. Will be the empty string if not supported.
     */
    modelVersion: string;
    /**
     * The command line string used to launch the browser. Will be the empty string if not
     * supported.
     */
    commandLine: string;
  }
  export interface GetFeatureStateParams {
    featureState: string;
  }
  export interface GetFeatureStateReturn {
    featureEnabled: boolean;
  }
  export interface GetProcessInfoParams {}
  export interface GetProcessInfoReturn {
    /** An array of process info blocks. */
    processInfo: SystemInfo.ProcessInfo[];
  }
}

export namespace Target {
  export interface ActivateTargetParams {
    targetId: Target.TargetID;
  }
  export type ActivateTargetReturn = void;
  export interface AttachToTargetParams {
    targetId: Target.TargetID;
    /**
     * Enables "flat" access to the session via specifying sessionId attribute in the commands.
     * We plan to make this the default, deprecate non-flattened mode,
     * and eventually retire it. See crbug.com/991325.
     */
    flatten?: boolean;
  }
  export interface AttachToTargetReturn {
    /** Id assigned to the session. */
    sessionId: Target.SessionID;
  }
  export interface AttachToBrowserTargetParams {}
  export interface AttachToBrowserTargetReturn {
    /** Id assigned to the session. */
    sessionId: Target.SessionID;
  }
  export interface CloseTargetParams {
    targetId: Target.TargetID;
  }
  export interface CloseTargetReturn {
    /** Always set to true. If an error occurs, the response indicates protocol error. */
    success: boolean;
  }
  export interface ExposeDevToolsProtocolParams {
    targetId: Target.TargetID;
    /** Binding name, 'cdp' if not specified. */
    bindingName?: string;
    /** If true, inherits the current root session's permissions (default: false). */
    inheritPermissions?: boolean;
  }
  export type ExposeDevToolsProtocolReturn = void;
  export interface CreateBrowserContextParams {
    /** If specified, disposes this context when debugging session disconnects. */
    disposeOnDetach?: boolean;
    /** Proxy server, similar to the one passed to --proxy-server */
    proxyServer?: string;
    /** Proxy bypass list, similar to the one passed to --proxy-bypass-list */
    proxyBypassList?: string;
    /**
     * An optional list of origins to grant unlimited cross-origin access to.
     * Parts of the URL other than those constituting origin are ignored.
     */
    originsWithUniversalNetworkAccess?: string[];
  }
  export interface CreateBrowserContextReturn {
    /** The id of the context created. */
    browserContextId: Browser.BrowserContextID;
  }
  export interface GetBrowserContextsParams {}
  export interface GetBrowserContextsReturn {
    /** An array of browser context ids. */
    browserContextIds: Browser.BrowserContextID[];
    /** The id of the default browser context if available. */
    defaultBrowserContextId?: Browser.BrowserContextID;
  }
  export interface CreateTargetParams {
    /** The initial URL the page will be navigated to. An empty string indicates about:blank. */
    url: string;
    /** Frame left origin in DIP (requires newWindow to be true or headless shell). */
    left?: number;
    /** Frame top origin in DIP (requires newWindow to be true or headless shell). */
    top?: number;
    /** Frame width in DIP (requires newWindow to be true or headless shell). */
    width?: number;
    /** Frame height in DIP (requires newWindow to be true or headless shell). */
    height?: number;
    /**
     * Frame window state (requires newWindow to be true or headless shell).
     * Default is normal.
     */
    windowState?: Target.WindowState;
    /** The browser context to create the page in. */
    browserContextId?: Browser.BrowserContextID;
    /**
     * Whether BeginFrames for this target will be controlled via DevTools (headless shell only,
     * not supported on MacOS yet, false by default).
     */
    enableBeginFrameControl?: boolean;
    /** Whether to create a new Window or Tab (false by default, not supported by headless shell). */
    newWindow?: boolean;
    /**
     * Whether to create the target in background or foreground (false by default, not supported
     * by headless shell).
     */
    background?: boolean;
    /** Whether to create the target of type "tab". */
    forTab?: boolean;
    /**
     * Whether to create a hidden target. The hidden target is observable via protocol, but not
     * present in the tab UI strip. Cannot be created with `forTab: true`, `newWindow: true` or
     * `background: false`. The life-time of the tab is limited to the life-time of the session.
     */
    hidden?: boolean;
    /**
     * If specified, the option is used to determine if the new target should
     * be focused or not. By default, the focus behavior depends on the
     * value of the background field. For example, background=false and focus=false
     * will result in the target tab being opened but the browser window remain
     * unchanged (if it was in the background, it will remain in the background)
     * and background=false with focus=undefined will result in the window being focused.
     * Using background: true and focus: true is not supported and will result in an error.
     */
    focus?: boolean;
  }
  export interface CreateTargetReturn {
    /** The id of the page opened. */
    targetId: Target.TargetID;
  }
  export interface DetachFromTargetParams {
    /** Session to detach. */
    sessionId?: Target.SessionID;
    /** Deprecated. */
    targetId?: Target.TargetID;
  }
  export type DetachFromTargetReturn = void;
  export interface DisposeBrowserContextParams {
    browserContextId: Browser.BrowserContextID;
  }
  export type DisposeBrowserContextReturn = void;
  export interface GetTargetInfoParams {
    targetId?: Target.TargetID;
  }
  export interface GetTargetInfoReturn {
    targetInfo: Target.TargetInfo;
  }
  export interface GetTargetsParams {
    /**
     * Only targets matching filter will be reported. If filter is not specified
     * and target discovery is currently enabled, a filter used for target discovery
     * is used for consistency.
     */
    filter?: Target.TargetFilter;
  }
  export interface GetTargetsReturn {
    /** The list of targets. */
    targetInfos: Target.TargetInfo[];
  }
  export interface SendMessageToTargetParams {
    message: string;
    /** Identifier of the session. */
    sessionId?: Target.SessionID;
    /** Deprecated. */
    targetId?: Target.TargetID;
  }
  export type SendMessageToTargetReturn = void;
  export interface SetAutoAttachParams {
    /** Whether to auto-attach to related targets. */
    autoAttach: boolean;
    /**
     * Whether to pause new targets when attaching to them. Use `Runtime.runIfWaitingForDebugger`
     * to run paused targets.
     */
    waitForDebuggerOnStart: boolean;
    /**
     * Enables "flat" access to the session via specifying sessionId attribute in the commands.
     * We plan to make this the default, deprecate non-flattened mode,
     * and eventually retire it. See crbug.com/991325.
     */
    flatten?: boolean;
    /** Only targets matching filter will be attached. */
    filter?: Target.TargetFilter;
  }
  export type SetAutoAttachReturn = void;
  export interface AutoAttachRelatedParams {
    targetId: Target.TargetID;
    /**
     * Whether to pause new targets when attaching to them. Use `Runtime.runIfWaitingForDebugger`
     * to run paused targets.
     */
    waitForDebuggerOnStart: boolean;
    /** Only targets matching filter will be attached. */
    filter?: Target.TargetFilter;
  }
  export type AutoAttachRelatedReturn = void;
  export interface SetDiscoverTargetsParams {
    /** Whether to discover available targets. */
    discover: boolean;
    /**
     * Only targets matching filter will be attached. If `discover` is false,
     * `filter` must be omitted or empty.
     */
    filter?: Target.TargetFilter;
  }
  export type SetDiscoverTargetsReturn = void;
  export interface SetRemoteLocationsParams {
    /** List of remote locations. */
    locations: Target.RemoteLocation[];
  }
  export type SetRemoteLocationsReturn = void;
  export interface GetDevToolsTargetParams {
    /** Page or tab target ID. */
    targetId: Target.TargetID;
  }
  export interface GetDevToolsTargetReturn {
    /** The targetId of DevTools page target if exists. */
    targetId?: Target.TargetID;
  }
  export interface OpenDevToolsParams {
    /** This can be the page or tab target ID. */
    targetId: Target.TargetID;
    /**
     * The id of the panel we want DevTools to open initially. Currently
     * supported panels are elements, console, network, sources, resources
     * and performance.
     */
    panelId?: string;
  }
  export interface OpenDevToolsReturn {
    /** The targetId of DevTools page target. */
    targetId: Target.TargetID;
  }
}

export namespace Tethering {
  export interface BindParams {
    /** Port number to bind. */
    port: number;
  }
  export type BindReturn = void;
  export interface UnbindParams {
    /** Port number to unbind. */
    port: number;
  }
  export type UnbindReturn = void;
}

export namespace Tracing {
  export interface EndParams {}
  export type EndReturn = void;
  export interface GetCategoriesParams {}
  export interface GetCategoriesReturn {
    /** A list of supported tracing categories. */
    categories: string[];
  }
  export interface GetTrackEventDescriptorParams {}
  export interface GetTrackEventDescriptorReturn {
    /** Base64-encoded serialized perfetto.protos.TrackEventDescriptor protobuf message. (Encoded as a base64 string when passed over JSON) */
    descriptor: string;
  }
  export interface RecordClockSyncMarkerParams {
    /** The ID of this clock sync marker */
    syncId: string;
  }
  export type RecordClockSyncMarkerReturn = void;
  export interface RequestMemoryDumpParams {
    /** Enables more deterministic results by forcing garbage collection */
    deterministic?: boolean;
    /** Specifies level of details in memory dump. Defaults to "detailed". */
    levelOfDetail?: Tracing.MemoryDumpLevelOfDetail;
  }
  export interface RequestMemoryDumpReturn {
    /** GUID of the resulting global memory dump. */
    dumpGuid: string;
    /** True iff the global memory dump succeeded. */
    success: boolean;
  }
  export interface StartParams {
    /** Category/tag filter */
    categories?: string;
    /** Tracing options */
    options?: string;
    /** If set, the agent will issue bufferUsage events at this interval, specified in milliseconds */
    bufferUsageReportingInterval?: number;
    /**
     * Whether to report trace events as series of dataCollected events or to save trace to a
     * stream (defaults to `ReportEvents`).
     */
    transferMode?: "ReportEvents" | "ReturnAsStream";
    /**
     * Trace data format to use. This only applies when using `ReturnAsStream`
     * transfer mode (defaults to `json`).
     */
    streamFormat?: Tracing.StreamFormat;
    /**
     * Compression format to use. This only applies when using `ReturnAsStream`
     * transfer mode (defaults to `none`)
     */
    streamCompression?: Tracing.StreamCompression;
    traceConfig?: Tracing.TraceConfig;
    /**
     * Base64-encoded serialized perfetto.protos.TraceConfig protobuf message
     * When specified, the parameters `categories`, `options`, `traceConfig`
     * are ignored. (Encoded as a base64 string when passed over JSON)
     */
    perfettoConfig?: string;
    /** Backend type (defaults to `auto`) */
    tracingBackend?: Tracing.TracingBackend;
  }
  export type StartReturn = void;
}

export namespace WebAudio {
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface GetRealtimeDataParams {
    contextId: WebAudio.GraphObjectId;
  }
  export interface GetRealtimeDataReturn {
    realtimeData: WebAudio.ContextRealtimeData;
  }
}

export namespace WebAuthn {
  export interface EnableParams {
    /**
     * Whether to enable the WebAuthn user interface. Enabling the UI is
     * recommended for debugging and demo purposes, as it is closer to the real
     * experience. Disabling the UI is recommended for automated testing.
     * Supported at the embedder's discretion if UI is available.
     * Defaults to false.
     */
    enableUI?: boolean;
  }
  export type EnableReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface AddVirtualAuthenticatorParams {
    options: WebAuthn.VirtualAuthenticatorOptions;
  }
  export interface AddVirtualAuthenticatorReturn {
    authenticatorId: WebAuthn.AuthenticatorId;
  }
  export interface SetResponseOverrideBitsParams {
    authenticatorId: WebAuthn.AuthenticatorId;
    /**
     * If isBogusSignature is set, overrides the signature in the authenticator response to be zero.
     * Defaults to false.
     */
    isBogusSignature?: boolean;
    /**
     * If isBadUV is set, overrides the UV bit in the flags in the authenticator response to
     * be zero. Defaults to false.
     */
    isBadUV?: boolean;
    /**
     * If isBadUP is set, overrides the UP bit in the flags in the authenticator response to
     * be zero. Defaults to false.
     */
    isBadUP?: boolean;
  }
  export type SetResponseOverrideBitsReturn = void;
  export interface RemoveVirtualAuthenticatorParams {
    authenticatorId: WebAuthn.AuthenticatorId;
  }
  export type RemoveVirtualAuthenticatorReturn = void;
  export interface AddCredentialParams {
    authenticatorId: WebAuthn.AuthenticatorId;
    credential: WebAuthn.Credential;
  }
  export type AddCredentialReturn = void;
  export interface GetCredentialParams {
    authenticatorId: WebAuthn.AuthenticatorId;
    credentialId: string;
  }
  export interface GetCredentialReturn {
    credential: WebAuthn.Credential;
  }
  export interface GetCredentialsParams {
    authenticatorId: WebAuthn.AuthenticatorId;
  }
  export interface GetCredentialsReturn {
    credentials: WebAuthn.Credential[];
  }
  export interface RemoveCredentialParams {
    authenticatorId: WebAuthn.AuthenticatorId;
    credentialId: string;
  }
  export type RemoveCredentialReturn = void;
  export interface ClearCredentialsParams {
    authenticatorId: WebAuthn.AuthenticatorId;
  }
  export type ClearCredentialsReturn = void;
  export interface SetUserVerifiedParams {
    authenticatorId: WebAuthn.AuthenticatorId;
    isUserVerified: boolean;
  }
  export type SetUserVerifiedReturn = void;
  export interface SetAutomaticPresenceSimulationParams {
    authenticatorId: WebAuthn.AuthenticatorId;
    enabled: boolean;
  }
  export type SetAutomaticPresenceSimulationReturn = void;
  export interface SetCredentialPropertiesParams {
    authenticatorId: WebAuthn.AuthenticatorId;
    credentialId: string;
    backupEligibility?: boolean;
    backupState?: boolean;
  }
  export type SetCredentialPropertiesReturn = void;
}

export namespace WebMCP {
  export interface EnableParams {}
  export type EnableReturn = void;
  export interface DisableParams {}
  export type DisableReturn = void;
  export interface InvokeToolParams {
    /** Frame in which to invoke the tool. */
    frameId: Page.FrameId;
    /** Name of the tool to invoke. */
    toolName: string;
    /** Input parameters for the tool, matching the tool's inputSchema. */
    input: Record<string, unknown>;
  }
  export interface InvokeToolReturn {
    /** Unique identifier for this invocation. Response is sent before tool events. */
    invocationId: string;
  }
  export interface CancelInvocationParams {
    /** Invocation identifier to cancel. */
    invocationId: string;
  }
  export type CancelInvocationReturn = void;
}

export interface Domains {
  Accessibility: {
    /** Disables the accessibility domain. */
    disable: () => Promise<void>;
    /**
     * Enables the accessibility domain which causes `AXNodeId`s to remain consistent between method calls.
     * This turns on accessibility for the page, which can impact performance until accessibility is disabled.
     */
    enable: () => Promise<void>;
    /** Fetches the accessibility node and partial accessibility tree for this DOM node, if it exists. */
    getPartialAXTree: (params?: Accessibility.GetPartialAXTreeParams) => Promise<Accessibility.GetPartialAXTreeReturn>;
    /** Fetches the entire accessibility tree for the root Document */
    getFullAXTree: (params?: Accessibility.GetFullAXTreeParams) => Promise<Accessibility.GetFullAXTreeReturn>;
    /**
     * Fetches the root node.
     * Requires `enable()` to have been called previously.
     */
    getRootAXNode: (params?: Accessibility.GetRootAXNodeParams) => Promise<Accessibility.GetRootAXNodeReturn>;
    /**
     * Fetches a node and all ancestors up to and including the root.
     * Requires `enable()` to have been called previously.
     */
    getAXNodeAndAncestors: (params?: Accessibility.GetAXNodeAndAncestorsParams) => Promise<Accessibility.GetAXNodeAndAncestorsReturn>;
    /**
     * Fetches a particular accessibility node by AXNodeId.
     * Requires `enable()` to have been called previously.
     */
    getChildAXNodes: (params: Accessibility.GetChildAXNodesParams) => Promise<Accessibility.GetChildAXNodesReturn>;
    /**
     * Query a DOM node's accessibility subtree for accessible name and role.
     * This command computes the name and role for all nodes in the subtree, including those that are
     * ignored for accessibility, and returns those that match the specified name and role. If no DOM
     * node is specified, or the DOM node does not exist, the command returns an error. If neither
     * `accessibleName` or `role` is specified, it returns all the accessibility nodes in the subtree.
     */
    queryAXTree: (params?: Accessibility.QueryAXTreeParams) => Promise<Accessibility.QueryAXTreeReturn>;
  };
  Animation: {
    /** Disables animation domain notifications. */
    disable: () => Promise<void>;
    /** Enables animation domain notifications. */
    enable: () => Promise<void>;
    /** Returns the current time of the an animation. */
    getCurrentTime: (params: Animation.GetCurrentTimeParams) => Promise<Animation.GetCurrentTimeReturn>;
    /** Gets the playback rate of the document timeline. */
    getPlaybackRate: () => Promise<Animation.GetPlaybackRateReturn>;
    /** Releases a set of animations to no longer be manipulated. */
    releaseAnimations: (params: Animation.ReleaseAnimationsParams) => Promise<void>;
    /** Gets the remote object of the Animation. */
    resolveAnimation: (params: Animation.ResolveAnimationParams) => Promise<Animation.ResolveAnimationReturn>;
    /** Seek a set of animations to a particular time within each animation. */
    seekAnimations: (params: Animation.SeekAnimationsParams) => Promise<void>;
    /** Sets the paused state of a set of animations. */
    setPaused: (params: Animation.SetPausedParams) => Promise<void>;
    /** Sets the playback rate of the document timeline. */
    setPlaybackRate: (params: Animation.SetPlaybackRateParams) => Promise<void>;
    /** Sets the timing of an animation node. */
    setTiming: (params: Animation.SetTimingParams) => Promise<void>;
  };
  Audits: {
    /**
     * Returns the response body and size if it were re-encoded with the specified settings. Only
     * applies to images.
     */
    getEncodedResponse: (params: Audits.GetEncodedResponseParams) => Promise<Audits.GetEncodedResponseReturn>;
    /** Disables issues domain, prevents further issues from being reported to the client. */
    disable: () => Promise<void>;
    /**
     * Enables issues domain, sends the issues collected so far to the client by means of the
     * `issueAdded` event.
     */
    enable: () => Promise<void>;
    /**
     * Runs the form issues check for the target page. Found issues are reported
     * using Audits.issueAdded event.
     */
    checkFormsIssues: () => Promise<Audits.CheckFormsIssuesReturn>;
  };
  Autofill: {
    /**
     * Trigger autofill on a form identified by the fieldId.
     * If the field and related form cannot be autofilled, returns an error.
     */
    trigger: (params: Autofill.TriggerParams) => Promise<void>;
    /** Set addresses so that developers can verify their forms implementation. */
    setAddresses: (params: Autofill.SetAddressesParams) => Promise<void>;
    /** Disables autofill domain notifications. */
    disable: () => Promise<void>;
    /** Enables autofill domain notifications. */
    enable: () => Promise<void>;
  };
  BackgroundService: {
    /** Enables event updates for the service. */
    startObserving: (params: BackgroundService.StartObservingParams) => Promise<void>;
    /** Disables event updates for the service. */
    stopObserving: (params: BackgroundService.StopObservingParams) => Promise<void>;
    /** Set the recording state for the service. */
    setRecording: (params: BackgroundService.SetRecordingParams) => Promise<void>;
    /** Clears all stored data for the service. */
    clearEvents: (params: BackgroundService.ClearEventsParams) => Promise<void>;
  };
  BluetoothEmulation: {
    /** Enable the BluetoothEmulation domain. */
    enable: (params: BluetoothEmulation.EnableParams) => Promise<void>;
    /** Set the state of the simulated central. */
    setSimulatedCentralState: (params: BluetoothEmulation.SetSimulatedCentralStateParams) => Promise<void>;
    /** Disable the BluetoothEmulation domain. */
    disable: () => Promise<void>;
    /**
     * Simulates a peripheral with |address|, |name| and |knownServiceUuids|
     * that has already been connected to the system.
     */
    simulatePreconnectedPeripheral: (params: BluetoothEmulation.SimulatePreconnectedPeripheralParams) => Promise<void>;
    /**
     * Simulates an advertisement packet described in |entry| being received by
     * the central.
     */
    simulateAdvertisement: (params: BluetoothEmulation.SimulateAdvertisementParams) => Promise<void>;
    /**
     * Simulates the response code from the peripheral with |address| for a
     * GATT operation of |type|. The |code| value follows the HCI Error Codes from
     * Bluetooth Core Specification Vol 2 Part D 1.3 List Of Error Codes.
     */
    simulateGATTOperationResponse: (params: BluetoothEmulation.SimulateGATTOperationResponseParams) => Promise<void>;
    /**
     * Simulates the response from the characteristic with |characteristicId| for a
     * characteristic operation of |type|. The |code| value follows the Error
     * Codes from Bluetooth Core Specification Vol 3 Part F 3.4.1.1 Error Response.
     * The |data| is expected to exist when simulating a successful read operation
     * response.
     */
    simulateCharacteristicOperationResponse: (params: BluetoothEmulation.SimulateCharacteristicOperationResponseParams) => Promise<void>;
    /**
     * Simulates the response from the descriptor with |descriptorId| for a
     * descriptor operation of |type|. The |code| value follows the Error
     * Codes from Bluetooth Core Specification Vol 3 Part F 3.4.1.1 Error Response.
     * The |data| is expected to exist when simulating a successful read operation
     * response.
     */
    simulateDescriptorOperationResponse: (params: BluetoothEmulation.SimulateDescriptorOperationResponseParams) => Promise<void>;
    /** Adds a service with |serviceUuid| to the peripheral with |address|. */
    addService: (params: BluetoothEmulation.AddServiceParams) => Promise<BluetoothEmulation.AddServiceReturn>;
    /** Removes the service respresented by |serviceId| from the simulated central. */
    removeService: (params: BluetoothEmulation.RemoveServiceParams) => Promise<void>;
    /**
     * Adds a characteristic with |characteristicUuid| and |properties| to the
     * service represented by |serviceId|.
     */
    addCharacteristic: (params: BluetoothEmulation.AddCharacteristicParams) => Promise<BluetoothEmulation.AddCharacteristicReturn>;
    /**
     * Removes the characteristic respresented by |characteristicId| from the
     * simulated central.
     */
    removeCharacteristic: (params: BluetoothEmulation.RemoveCharacteristicParams) => Promise<void>;
    /**
     * Adds a descriptor with |descriptorUuid| to the characteristic respresented
     * by |characteristicId|.
     */
    addDescriptor: (params: BluetoothEmulation.AddDescriptorParams) => Promise<BluetoothEmulation.AddDescriptorReturn>;
    /** Removes the descriptor with |descriptorId| from the simulated central. */
    removeDescriptor: (params: BluetoothEmulation.RemoveDescriptorParams) => Promise<void>;
    /** Simulates a GATT disconnection from the peripheral with |address|. */
    simulateGATTDisconnection: (params: BluetoothEmulation.SimulateGATTDisconnectionParams) => Promise<void>;
  };
  Browser: {
    /** Set permission settings for given embedding and embedded origins. */
    setPermission: (params: Browser.SetPermissionParams) => Promise<void>;
    /**
     * Grant specific permissions to the given origin and reject all others. Deprecated. Use
     * setPermission instead.
     */
    grantPermissions: (params: Browser.GrantPermissionsParams) => Promise<void>;
    /** Reset all permission management for all origins. */
    resetPermissions: (params?: Browser.ResetPermissionsParams) => Promise<void>;
    /** Set the behavior when downloading a file. */
    setDownloadBehavior: (params: Browser.SetDownloadBehaviorParams) => Promise<void>;
    /** Cancel a download if in progress */
    cancelDownload: (params: Browser.CancelDownloadParams) => Promise<void>;
    /** Close browser gracefully. */
    close: () => Promise<void>;
    /** Crashes browser on the main thread. */
    crash: () => Promise<void>;
    /** Crashes GPU process. */
    crashGpuProcess: () => Promise<void>;
    /** Returns version information. */
    getVersion: () => Promise<Browser.GetVersionReturn>;
    /**
     * Returns the command line switches for the browser process if, and only if
     * --enable-automation is on the commandline.
     */
    getBrowserCommandLine: () => Promise<Browser.GetBrowserCommandLineReturn>;
    /** Get Chrome histograms. */
    getHistograms: (params?: Browser.GetHistogramsParams) => Promise<Browser.GetHistogramsReturn>;
    /** Get a Chrome histogram by name. */
    getHistogram: (params: Browser.GetHistogramParams) => Promise<Browser.GetHistogramReturn>;
    /** Get position and size of the browser window. */
    getWindowBounds: (params: Browser.GetWindowBoundsParams) => Promise<Browser.GetWindowBoundsReturn>;
    /** Get the browser window that contains the devtools target. */
    getWindowForTarget: (params?: Browser.GetWindowForTargetParams) => Promise<Browser.GetWindowForTargetReturn>;
    /** Set position and/or size of the browser window. */
    setWindowBounds: (params: Browser.SetWindowBoundsParams) => Promise<void>;
    /** Set size of the browser contents resizing browser window as necessary. */
    setContentsSize: (params: Browser.SetContentsSizeParams) => Promise<void>;
    /** Set dock tile details, platform-specific. */
    setDockTile: (params?: Browser.SetDockTileParams) => Promise<void>;
    /** Invoke custom browser commands used by telemetry. */
    executeBrowserCommand: (params: Browser.ExecuteBrowserCommandParams) => Promise<void>;
    /**
     * Allows a site to use privacy sandbox features that require enrollment
     * without the site actually being enrolled. Only supported on page targets.
     */
    addPrivacySandboxEnrollmentOverride: (params: Browser.AddPrivacySandboxEnrollmentOverrideParams) => Promise<void>;
    /**
     * Configures encryption keys used with a given privacy sandbox API to talk
     * to a trusted coordinator.  Since this is intended for test automation only,
     * coordinatorOrigin must be a .test domain. No existing coordinator
     * configuration for the origin may exist.
     */
    addPrivacySandboxCoordinatorKeyConfig: (params: Browser.AddPrivacySandboxCoordinatorKeyConfigParams) => Promise<void>;
  };
  CacheStorage: {
    /** Deletes a cache. */
    deleteCache: (params: CacheStorage.DeleteCacheParams) => Promise<void>;
    /** Deletes a cache entry. */
    deleteEntry: (params: CacheStorage.DeleteEntryParams) => Promise<void>;
    /** Requests cache names. */
    requestCacheNames: (params?: CacheStorage.RequestCacheNamesParams) => Promise<CacheStorage.RequestCacheNamesReturn>;
    /** Fetches cache entry. */
    requestCachedResponse: (params: CacheStorage.RequestCachedResponseParams) => Promise<CacheStorage.RequestCachedResponseReturn>;
    /** Requests data from cache. */
    requestEntries: (params: CacheStorage.RequestEntriesParams) => Promise<CacheStorage.RequestEntriesReturn>;
  };
  Cast: {
    /**
     * Starts observing for sinks that can be used for tab mirroring, and if set,
     * sinks compatible with |presentationUrl| as well. When sinks are found, a
     * |sinksUpdated| event is fired.
     * Also starts observing for issue messages. When an issue is added or removed,
     * an |issueUpdated| event is fired.
     */
    enable: (params?: Cast.EnableParams) => Promise<void>;
    /** Stops observing for sinks and issues. */
    disable: () => Promise<void>;
    /**
     * Sets a sink to be used when the web page requests the browser to choose a
     * sink via Presentation API, Remote Playback API, or Cast SDK.
     */
    setSinkToUse: (params: Cast.SetSinkToUseParams) => Promise<void>;
    /** Starts mirroring the desktop to the sink. */
    startDesktopMirroring: (params: Cast.StartDesktopMirroringParams) => Promise<void>;
    /** Starts mirroring the tab to the sink. */
    startTabMirroring: (params: Cast.StartTabMirroringParams) => Promise<void>;
    /** Stops the active Cast session on the sink. */
    stopCasting: (params: Cast.StopCastingParams) => Promise<void>;
  };
  Console: {
    /** Does nothing. */
    clearMessages: () => Promise<void>;
    /** Disables console domain, prevents further console messages from being reported to the client. */
    disable: () => Promise<void>;
    /**
     * Enables console domain, sends the messages collected so far to the client by means of the
     * `messageAdded` notification.
     */
    enable: () => Promise<void>;
  };
  CrashReportContext: {
    /** Returns all entries in the CrashReportContext across all frames in the page. */
    getEntries: () => Promise<CrashReportContext.GetEntriesReturn>;
  };
  CSS: {
    /**
     * Inserts a new rule with the given `ruleText` in a stylesheet with given `styleSheetId`, at the
     * position specified by `location`.
     */
    addRule: (params: CSS.AddRuleParams) => Promise<CSS.AddRuleReturn>;
    /** Returns all class names from specified stylesheet. */
    collectClassNames: (params: CSS.CollectClassNamesParams) => Promise<CSS.CollectClassNamesReturn>;
    /** Creates a new special "via-inspector" stylesheet in the frame with given `frameId`. */
    createStyleSheet: (params: CSS.CreateStyleSheetParams) => Promise<CSS.CreateStyleSheetReturn>;
    /** Disables the CSS agent for the given page. */
    disable: () => Promise<void>;
    /**
     * Enables the CSS agent for the given page. Clients should not assume that the CSS agent has been
     * enabled until the result of this command is received.
     */
    enable: () => Promise<void>;
    /**
     * Ensures that the given node will have specified pseudo-classes whenever its style is computed by
     * the browser.
     */
    forcePseudoState: (params: CSS.ForcePseudoStateParams) => Promise<void>;
    /** Ensures that the given node is in its starting-style state. */
    forceStartingStyle: (params: CSS.ForceStartingStyleParams) => Promise<void>;
    getBackgroundColors: (params: CSS.GetBackgroundColorsParams) => Promise<CSS.GetBackgroundColorsReturn>;
    /** Returns the computed style for a DOM node identified by `nodeId`. */
    getComputedStyleForNode: (params: CSS.GetComputedStyleForNodeParams) => Promise<CSS.GetComputedStyleForNodeReturn>;
    /**
     * Resolve the specified values in the context of the provided element.
     * For example, a value of '1em' is evaluated according to the computed
     * 'font-size' of the element and a value 'calc(1px + 2px)' will be
     * resolved to '3px'.
     * If the `propertyName` was specified the `values` are resolved as if
     * they were property's declaration. If a value cannot be parsed according
     * to the provided property syntax, the value is parsed using combined
     * syntax as if null `propertyName` was provided. If the value cannot be
     * resolved even then, return the provided value without any changes.
     * Note: this function currently does not resolve CSS random() function,
     * it returns unmodified random() function parts.`
     */
    resolveValues: (params: CSS.ResolveValuesParams) => Promise<CSS.ResolveValuesReturn>;
    getLonghandProperties: (params: CSS.GetLonghandPropertiesParams) => Promise<CSS.GetLonghandPropertiesReturn>;
    /**
     * Returns the styles defined inline (explicitly in the "style" attribute and implicitly, using DOM
     * attributes) for a DOM node identified by `nodeId`.
     */
    getInlineStylesForNode: (params: CSS.GetInlineStylesForNodeParams) => Promise<CSS.GetInlineStylesForNodeReturn>;
    /**
     * Returns the styles coming from animations & transitions
     * including the animation & transition styles coming from inheritance chain.
     */
    getAnimatedStylesForNode: (params: CSS.GetAnimatedStylesForNodeParams) => Promise<CSS.GetAnimatedStylesForNodeReturn>;
    /** Returns requested styles for a DOM node identified by `nodeId`. */
    getMatchedStylesForNode: (params: CSS.GetMatchedStylesForNodeParams) => Promise<CSS.GetMatchedStylesForNodeReturn>;
    /** Returns the values of the default UA-defined environment variables used in env() */
    getEnvironmentVariables: () => Promise<CSS.GetEnvironmentVariablesReturn>;
    /** Returns all media queries parsed by the rendering engine. */
    getMediaQueries: () => Promise<CSS.GetMediaQueriesReturn>;
    /**
     * Requests information about platform fonts which we used to render child TextNodes in the given
     * node.
     */
    getPlatformFontsForNode: (params: CSS.GetPlatformFontsForNodeParams) => Promise<CSS.GetPlatformFontsForNodeReturn>;
    /** Returns the current textual content for a stylesheet. */
    getStyleSheetText: (params: CSS.GetStyleSheetTextParams) => Promise<CSS.GetStyleSheetTextReturn>;
    /**
     * Returns all layers parsed by the rendering engine for the tree scope of a node.
     * Given a DOM element identified by nodeId, getLayersForNode returns the root
     * layer for the nearest ancestor document or shadow root. The layer root contains
     * the full layer tree for the tree scope and their ordering.
     */
    getLayersForNode: (params: CSS.GetLayersForNodeParams) => Promise<CSS.GetLayersForNodeReturn>;
    /**
     * Given a CSS selector text and a style sheet ID, getLocationForSelector
     * returns an array of locations of the CSS selector in the style sheet.
     */
    getLocationForSelector: (params: CSS.GetLocationForSelectorParams) => Promise<CSS.GetLocationForSelectorReturn>;
    /**
     * Starts tracking the given node for the computed style updates
     * and whenever the computed style is updated for node, it queues
     * a `computedStyleUpdated` event with throttling.
     * There can only be 1 node tracked for computed style updates
     * so passing a new node id removes tracking from the previous node.
     * Pass `undefined` to disable tracking.
     */
    trackComputedStyleUpdatesForNode: (params?: CSS.TrackComputedStyleUpdatesForNodeParams) => Promise<void>;
    /**
     * Starts tracking the given computed styles for updates. The specified array of properties
     * replaces the one previously specified. Pass empty array to disable tracking.
     * Use takeComputedStyleUpdates to retrieve the list of nodes that had properties modified.
     * The changes to computed style properties are only tracked for nodes pushed to the front-end
     * by the DOM agent. If no changes to the tracked properties occur after the node has been pushed
     * to the front-end, no updates will be issued for the node.
     */
    trackComputedStyleUpdates: (params: CSS.TrackComputedStyleUpdatesParams) => Promise<void>;
    /** Polls the next batch of computed style updates. */
    takeComputedStyleUpdates: () => Promise<CSS.TakeComputedStyleUpdatesReturn>;
    /**
     * Find a rule with the given active property for the given node and set the new value for this
     * property
     */
    setEffectivePropertyValueForNode: (params: CSS.SetEffectivePropertyValueForNodeParams) => Promise<void>;
    /** Modifies the property rule property name. */
    setPropertyRulePropertyName: (params: CSS.SetPropertyRulePropertyNameParams) => Promise<CSS.SetPropertyRulePropertyNameReturn>;
    /** Modifies the keyframe rule key text. */
    setKeyframeKey: (params: CSS.SetKeyframeKeyParams) => Promise<CSS.SetKeyframeKeyReturn>;
    /** Modifies the rule selector. */
    setMediaText: (params: CSS.SetMediaTextParams) => Promise<CSS.SetMediaTextReturn>;
    /** Modifies the expression of a container query. */
    setContainerQueryText: (params: CSS.SetContainerQueryTextParams) => Promise<CSS.SetContainerQueryTextReturn>;
    /** Modifies the expression of a supports at-rule. */
    setSupportsText: (params: CSS.SetSupportsTextParams) => Promise<CSS.SetSupportsTextReturn>;
    /** Modifies the expression of a navigation at-rule. */
    setNavigationText: (params: CSS.SetNavigationTextParams) => Promise<CSS.SetNavigationTextReturn>;
    /** Modifies the expression of a scope at-rule. */
    setScopeText: (params: CSS.SetScopeTextParams) => Promise<CSS.SetScopeTextReturn>;
    /** Modifies the rule selector. */
    setRuleSelector: (params: CSS.SetRuleSelectorParams) => Promise<CSS.SetRuleSelectorReturn>;
    /** Sets the new stylesheet text. */
    setStyleSheetText: (params: CSS.SetStyleSheetTextParams) => Promise<CSS.SetStyleSheetTextReturn>;
    /** Applies specified style edits one after another in the given order. */
    setStyleTexts: (params: CSS.SetStyleTextsParams) => Promise<CSS.SetStyleTextsReturn>;
    /** Enables the selector recording. */
    startRuleUsageTracking: () => Promise<void>;
    /**
     * Stop tracking rule usage and return the list of rules that were used since last call to
     * `takeCoverageDelta` (or since start of coverage instrumentation).
     */
    stopRuleUsageTracking: () => Promise<CSS.StopRuleUsageTrackingReturn>;
    /**
     * Obtain list of rules that became used since last call to this method (or since start of coverage
     * instrumentation).
     */
    takeCoverageDelta: () => Promise<CSS.TakeCoverageDeltaReturn>;
    /** Enables/disables rendering of local CSS fonts (enabled by default). */
    setLocalFontsEnabled: (params: CSS.SetLocalFontsEnabledParams) => Promise<void>;
  };
  Debugger: {
    /** Continues execution until specific location is reached. */
    continueToLocation: (params: Debugger.ContinueToLocationParams) => Promise<void>;
    /** Disables debugger for given page. */
    disable: () => Promise<void>;
    /**
     * Enables debugger for the given page. Clients should not assume that the debugging has been
     * enabled until the result for this command is received.
     */
    enable: (params?: Debugger.EnableParams) => Promise<Debugger.EnableReturn>;
    /** Evaluates expression on a given call frame. */
    evaluateOnCallFrame: (params: Debugger.EvaluateOnCallFrameParams) => Promise<Debugger.EvaluateOnCallFrameReturn>;
    /**
     * Returns possible locations for breakpoint. scriptId in start and end range locations should be
     * the same.
     */
    getPossibleBreakpoints: (params: Debugger.GetPossibleBreakpointsParams) => Promise<Debugger.GetPossibleBreakpointsReturn>;
    /** Returns source for the script with given id. */
    getScriptSource: (params: Debugger.GetScriptSourceParams) => Promise<Debugger.GetScriptSourceReturn>;
    disassembleWasmModule: (params: Debugger.DisassembleWasmModuleParams) => Promise<Debugger.DisassembleWasmModuleReturn>;
    /**
     * Disassemble the next chunk of lines for the module corresponding to the
     * stream. If disassembly is complete, this API will invalidate the streamId
     * and return an empty chunk. Any subsequent calls for the now invalid stream
     * will return errors.
     */
    nextWasmDisassemblyChunk: (params: Debugger.NextWasmDisassemblyChunkParams) => Promise<Debugger.NextWasmDisassemblyChunkReturn>;
    /** This command is deprecated. Use getScriptSource instead. */
    getWasmBytecode: (params: Debugger.GetWasmBytecodeParams) => Promise<Debugger.GetWasmBytecodeReturn>;
    /** Returns stack trace with given `stackTraceId`. */
    getStackTrace: (params: Debugger.GetStackTraceParams) => Promise<Debugger.GetStackTraceReturn>;
    /** Stops on the next JavaScript statement. */
    pause: () => Promise<void>;
    pauseOnAsyncCall: (params: Debugger.PauseOnAsyncCallParams) => Promise<void>;
    /** Removes JavaScript breakpoint. */
    removeBreakpoint: (params: Debugger.RemoveBreakpointParams) => Promise<void>;
    /**
     * Restarts particular call frame from the beginning. The old, deprecated
     * behavior of `restartFrame` is to stay paused and allow further CDP commands
     * after a restart was scheduled. This can cause problems with restarting, so
     * we now continue execution immediatly after it has been scheduled until we
     * reach the beginning of the restarted frame.
     *
     * To stay back-wards compatible, `restartFrame` now expects a `mode`
     * parameter to be present. If the `mode` parameter is missing, `restartFrame`
     * errors out.
     *
     * The various return values are deprecated and `callFrames` is always empty.
     * Use the call frames from the `Debugger#paused` events instead, that fires
     * once V8 pauses at the beginning of the restarted function.
     */
    restartFrame: (params: Debugger.RestartFrameParams) => Promise<Debugger.RestartFrameReturn>;
    /** Resumes JavaScript execution. */
    resume: (params?: Debugger.ResumeParams) => Promise<void>;
    /** Searches for given string in script content. */
    searchInContent: (params: Debugger.SearchInContentParams) => Promise<Debugger.SearchInContentReturn>;
    /** Enables or disables async call stacks tracking. */
    setAsyncCallStackDepth: (params: Debugger.SetAsyncCallStackDepthParams) => Promise<void>;
    /**
     * Replace previous blackbox execution contexts with passed ones. Forces backend to skip
     * stepping/pausing in scripts in these execution contexts. VM will try to leave blackboxed script by
     * performing 'step in' several times, finally resorting to 'step out' if unsuccessful.
     */
    setBlackboxExecutionContexts: (params: Debugger.SetBlackboxExecutionContextsParams) => Promise<void>;
    /**
     * Replace previous blackbox patterns with passed ones. Forces backend to skip stepping/pausing in
     * scripts with url matching one of the patterns. VM will try to leave blackboxed script by
     * performing 'step in' several times, finally resorting to 'step out' if unsuccessful.
     */
    setBlackboxPatterns: (params: Debugger.SetBlackboxPatternsParams) => Promise<void>;
    /**
     * Makes backend skip steps in the script in blackboxed ranges. VM will try leave blacklisted
     * scripts by performing 'step in' several times, finally resorting to 'step out' if unsuccessful.
     * Positions array contains positions where blackbox state is changed. First interval isn't
     * blackboxed. Array should be sorted.
     */
    setBlackboxedRanges: (params: Debugger.SetBlackboxedRangesParams) => Promise<void>;
    /** Sets JavaScript breakpoint at a given location. */
    setBreakpoint: (params: Debugger.SetBreakpointParams) => Promise<Debugger.SetBreakpointReturn>;
    /** Sets instrumentation breakpoint. */
    setInstrumentationBreakpoint: (params: Debugger.SetInstrumentationBreakpointParams) => Promise<Debugger.SetInstrumentationBreakpointReturn>;
    /**
     * Sets JavaScript breakpoint at given location specified either by URL or URL regex. Once this
     * command is issued, all existing parsed scripts will have breakpoints resolved and returned in
     * `locations` property. Further matching script parsing will result in subsequent
     * `breakpointResolved` events issued. This logical breakpoint will survive page reloads.
     */
    setBreakpointByUrl: (params: Debugger.SetBreakpointByUrlParams) => Promise<Debugger.SetBreakpointByUrlReturn>;
    /**
     * Sets JavaScript breakpoint before each call to the given function.
     * If another function was created from the same source as a given one,
     * calling it will also trigger the breakpoint.
     */
    setBreakpointOnFunctionCall: (params: Debugger.SetBreakpointOnFunctionCallParams) => Promise<Debugger.SetBreakpointOnFunctionCallReturn>;
    /** Activates / deactivates all breakpoints on the page. */
    setBreakpointsActive: (params: Debugger.SetBreakpointsActiveParams) => Promise<void>;
    /**
     * Defines pause on exceptions state. Can be set to stop on all exceptions, uncaught exceptions,
     * or caught exceptions, no exceptions. Initial pause on exceptions state is `none`.
     */
    setPauseOnExceptions: (params: Debugger.SetPauseOnExceptionsParams) => Promise<void>;
    /** Changes return value in top frame. Available only at return break position. */
    setReturnValue: (params: Debugger.SetReturnValueParams) => Promise<void>;
    /**
     * Edits JavaScript source live.
     *
     * In general, functions that are currently on the stack can not be edited with
     * a single exception: If the edited function is the top-most stack frame and
     * that is the only activation of that function on the stack. In this case
     * the live edit will be successful and a `Debugger.restartFrame` for the
     * top-most function is automatically triggered.
     */
    setScriptSource: (params: Debugger.SetScriptSourceParams) => Promise<Debugger.SetScriptSourceReturn>;
    /** Makes page not interrupt on any pauses (breakpoint, exception, dom exception etc). */
    setSkipAllPauses: (params: Debugger.SetSkipAllPausesParams) => Promise<void>;
    /**
     * Changes value of variable in a callframe. Object-based scopes are not supported and must be
     * mutated manually.
     */
    setVariableValue: (params: Debugger.SetVariableValueParams) => Promise<void>;
    /** Steps into the function call. */
    stepInto: (params?: Debugger.StepIntoParams) => Promise<void>;
    /** Steps out of the function call. */
    stepOut: () => Promise<void>;
    /** Steps over the statement. */
    stepOver: (params?: Debugger.StepOverParams) => Promise<void>;
  };
  DeviceAccess: {
    /** Enable events in this domain. */
    enable: () => Promise<void>;
    /** Disable events in this domain. */
    disable: () => Promise<void>;
    /** Select a device in response to a DeviceAccess.deviceRequestPrompted event. */
    selectPrompt: (params: DeviceAccess.SelectPromptParams) => Promise<void>;
    /** Cancel a prompt in response to a DeviceAccess.deviceRequestPrompted event. */
    cancelPrompt: (params: DeviceAccess.CancelPromptParams) => Promise<void>;
  };
  DeviceOrientation: {
    /** Clears the overridden Device Orientation. */
    clearDeviceOrientationOverride: () => Promise<void>;
    /** Overrides the Device Orientation. */
    setDeviceOrientationOverride: (params: DeviceOrientation.SetDeviceOrientationOverrideParams) => Promise<void>;
  };
  DOM: {
    /** Collects class names for the node with given id and all of it's child nodes. */
    collectClassNamesFromSubtree: (params: DOM.CollectClassNamesFromSubtreeParams) => Promise<DOM.CollectClassNamesFromSubtreeReturn>;
    /**
     * Creates a deep copy of the specified node and places it into the target container before the
     * given anchor.
     */
    copyTo: (params: DOM.CopyToParams) => Promise<DOM.CopyToReturn>;
    /**
     * Describes node given its id, does not require domain to be enabled. Does not start tracking any
     * objects, can be used for automation.
     */
    describeNode: (params?: DOM.DescribeNodeParams) => Promise<DOM.DescribeNodeReturn>;
    /**
     * Scrolls the specified rect of the given node into view if not already visible.
     * Note: exactly one between nodeId, backendNodeId and objectId should be passed
     * to identify the node.
     */
    scrollIntoViewIfNeeded: (params?: DOM.ScrollIntoViewIfNeededParams) => Promise<void>;
    /** Disables DOM agent for the given page. */
    disable: () => Promise<void>;
    /**
     * Discards search results from the session with the given id. `getSearchResults` should no longer
     * be called for that search.
     */
    discardSearchResults: (params: DOM.DiscardSearchResultsParams) => Promise<void>;
    /** Enables DOM agent for the given page. */
    enable: (params?: DOM.EnableParams) => Promise<void>;
    /** Focuses the given element. */
    focus: (params?: DOM.FocusParams) => Promise<void>;
    /** Returns attributes for the specified node. */
    getAttributes: (params: DOM.GetAttributesParams) => Promise<DOM.GetAttributesReturn>;
    /** Returns boxes for the given node. */
    getBoxModel: (params?: DOM.GetBoxModelParams) => Promise<DOM.GetBoxModelReturn>;
    /**
     * Returns quads that describe node position on the page. This method
     * might return multiple quads for inline nodes.
     */
    getContentQuads: (params?: DOM.GetContentQuadsParams) => Promise<DOM.GetContentQuadsReturn>;
    /**
     * Returns the root DOM node (and optionally the subtree) to the caller.
     * Implicitly enables the DOM domain events for the current target.
     */
    getDocument: (params?: DOM.GetDocumentParams) => Promise<DOM.GetDocumentReturn>;
    /**
     * Returns the root DOM node (and optionally the subtree) to the caller.
     * Deprecated, as it is not designed to work well with the rest of the DOM agent.
     * Use DOMSnapshot.captureSnapshot instead.
     */
    getFlattenedDocument: (params?: DOM.GetFlattenedDocumentParams) => Promise<DOM.GetFlattenedDocumentReturn>;
    /** Finds nodes with a given computed style in a subtree. */
    getNodesForSubtreeByStyle: (params: DOM.GetNodesForSubtreeByStyleParams) => Promise<DOM.GetNodesForSubtreeByStyleReturn>;
    /**
     * Returns node id at given location. Depending on whether DOM domain is enabled, nodeId is
     * either returned or not.
     */
    getNodeForLocation: (params: DOM.GetNodeForLocationParams) => Promise<DOM.GetNodeForLocationReturn>;
    /** Returns node's HTML markup. */
    getOuterHTML: (params?: DOM.GetOuterHTMLParams) => Promise<DOM.GetOuterHTMLReturn>;
    /** Returns the id of the nearest ancestor that is a relayout boundary. */
    getRelayoutBoundary: (params: DOM.GetRelayoutBoundaryParams) => Promise<DOM.GetRelayoutBoundaryReturn>;
    /**
     * Returns search results from given `fromIndex` to given `toIndex` from the search with the given
     * identifier.
     */
    getSearchResults: (params: DOM.GetSearchResultsParams) => Promise<DOM.GetSearchResultsReturn>;
    /** Marks last undoable state. */
    markUndoableState: () => Promise<void>;
    /** Moves node into the new container, places it before the given anchor. */
    moveTo: (params: DOM.MoveToParams) => Promise<DOM.MoveToReturn>;
    /**
     * Searches for a given string in the DOM tree. Use `getSearchResults` to access search results or
     * `cancelSearch` to end this search session.
     */
    performSearch: (params: DOM.PerformSearchParams) => Promise<DOM.PerformSearchReturn>;
    /** Requests that the node is sent to the caller given its path. // FIXME, use XPath */
    pushNodeByPathToFrontend: (params: DOM.PushNodeByPathToFrontendParams) => Promise<DOM.PushNodeByPathToFrontendReturn>;
    /** Requests that a batch of nodes is sent to the caller given their backend node ids. */
    pushNodesByBackendIdsToFrontend: (params: DOM.PushNodesByBackendIdsToFrontendParams) => Promise<DOM.PushNodesByBackendIdsToFrontendReturn>;
    /** Executes `querySelector` on a given node. */
    querySelector: (params: DOM.QuerySelectorParams) => Promise<DOM.QuerySelectorReturn>;
    /** Executes `querySelectorAll` on a given node. */
    querySelectorAll: (params: DOM.QuerySelectorAllParams) => Promise<DOM.QuerySelectorAllReturn>;
    /**
     * Returns NodeIds of current top layer elements.
     * Top layer is rendered closest to the user within a viewport, therefore its elements always
     * appear on top of all other content.
     */
    getTopLayerElements: () => Promise<DOM.GetTopLayerElementsReturn>;
    /** Returns the NodeId of the matched element according to certain relations. */
    getElementByRelation: (params: DOM.GetElementByRelationParams) => Promise<DOM.GetElementByRelationReturn>;
    /** Re-does the last undone action. */
    redo: () => Promise<void>;
    /** Removes attribute with given name from an element with given id. */
    removeAttribute: (params: DOM.RemoveAttributeParams) => Promise<void>;
    /** Removes node with given id. */
    removeNode: (params: DOM.RemoveNodeParams) => Promise<void>;
    /**
     * Requests that children of the node with given id are returned to the caller in form of
     * `setChildNodes` events where not only immediate children are retrieved, but all children down to
     * the specified depth.
     */
    requestChildNodes: (params: DOM.RequestChildNodesParams) => Promise<void>;
    /**
     * Requests that the node is sent to the caller given the JavaScript node object reference. All
     * nodes that form the path from the node to the root are also sent to the client as a series of
     * `setChildNodes` notifications.
     */
    requestNode: (params: DOM.RequestNodeParams) => Promise<DOM.RequestNodeReturn>;
    /** Resolves the JavaScript node object for a given NodeId or BackendNodeId. */
    resolveNode: (params?: DOM.ResolveNodeParams) => Promise<DOM.ResolveNodeReturn>;
    /** Sets attribute for an element with given id. */
    setAttributeValue: (params: DOM.SetAttributeValueParams) => Promise<void>;
    /**
     * Sets attributes on element with given id. This method is useful when user edits some existing
     * attribute value and types in several attribute name/value pairs.
     */
    setAttributesAsText: (params: DOM.SetAttributesAsTextParams) => Promise<void>;
    /** Sets files for the given file input element. */
    setFileInputFiles: (params: DOM.SetFileInputFilesParams) => Promise<void>;
    /** Sets if stack traces should be captured for Nodes. See `Node.getNodeStackTraces`. Default is disabled. */
    setNodeStackTracesEnabled: (params: DOM.SetNodeStackTracesEnabledParams) => Promise<void>;
    /** Gets stack traces associated with a Node. As of now, only provides stack trace for Node creation. */
    getNodeStackTraces: (params: DOM.GetNodeStackTracesParams) => Promise<DOM.GetNodeStackTracesReturn>;
    /**
     * Returns file information for the given
     * File wrapper.
     */
    getFileInfo: (params: DOM.GetFileInfoParams) => Promise<DOM.GetFileInfoReturn>;
    /** Returns list of detached nodes */
    getDetachedDomNodes: () => Promise<DOM.GetDetachedDomNodesReturn>;
    /**
     * Enables console to refer to the node with given id via $x (see Command Line API for more details
     * $x functions).
     */
    setInspectedNode: (params: DOM.SetInspectedNodeParams) => Promise<void>;
    /** Sets node name for a node with given id. */
    setNodeName: (params: DOM.SetNodeNameParams) => Promise<DOM.SetNodeNameReturn>;
    /** Sets node value for a node with given id. */
    setNodeValue: (params: DOM.SetNodeValueParams) => Promise<void>;
    /** Sets node HTML markup, returns new node id. */
    setOuterHTML: (params: DOM.SetOuterHTMLParams) => Promise<void>;
    /** Undoes the last performed action. */
    undo: () => Promise<void>;
    /** Returns iframe node that owns iframe with the given domain. */
    getFrameOwner: (params: DOM.GetFrameOwnerParams) => Promise<DOM.GetFrameOwnerReturn>;
    /**
     * Returns the query container of the given node based on container query
     * conditions: containerName, physical and logical axes, and whether it queries
     * scroll-state or anchored elements. If no axes are provided and
     * queriesScrollState is false, the style container is returned, which is the
     * direct parent or the closest element with a matching container-name.
     */
    getContainerForNode: (params: DOM.GetContainerForNodeParams) => Promise<DOM.GetContainerForNodeReturn>;
    /**
     * Returns the descendants of a container query container that have
     * container queries against this container.
     */
    getQueryingDescendantsForContainer: (params: DOM.GetQueryingDescendantsForContainerParams) => Promise<DOM.GetQueryingDescendantsForContainerReturn>;
    /**
     * Returns the target anchor element of the given anchor query according to
     * https://www.w3.org/TR/css-anchor-position-1/#target.
     */
    getAnchorElement: (params: DOM.GetAnchorElementParams) => Promise<DOM.GetAnchorElementReturn>;
    /**
     * When enabling, this API force-opens the popover identified by nodeId
     * and keeps it open until disabled.
     */
    forceShowPopover: (params: DOM.ForceShowPopoverParams) => Promise<DOM.ForceShowPopoverReturn>;
  };
  DOMDebugger: {
    /** Returns event listeners of the given object. */
    getEventListeners: (params: DOMDebugger.GetEventListenersParams) => Promise<DOMDebugger.GetEventListenersReturn>;
    /** Removes DOM breakpoint that was set using `setDOMBreakpoint`. */
    removeDOMBreakpoint: (params: DOMDebugger.RemoveDOMBreakpointParams) => Promise<void>;
    /** Removes breakpoint on particular DOM event. */
    removeEventListenerBreakpoint: (params: DOMDebugger.RemoveEventListenerBreakpointParams) => Promise<void>;
    /** Removes breakpoint from XMLHttpRequest. */
    removeXHRBreakpoint: (params: DOMDebugger.RemoveXHRBreakpointParams) => Promise<void>;
    /** Sets breakpoint on particular CSP violations. */
    setBreakOnCSPViolation: (params: DOMDebugger.SetBreakOnCSPViolationParams) => Promise<void>;
    /** Sets breakpoint on particular operation with DOM. */
    setDOMBreakpoint: (params: DOMDebugger.SetDOMBreakpointParams) => Promise<void>;
    /** Sets breakpoint on particular DOM event. */
    setEventListenerBreakpoint: (params: DOMDebugger.SetEventListenerBreakpointParams) => Promise<void>;
    /** Sets breakpoint on XMLHttpRequest. */
    setXHRBreakpoint: (params: DOMDebugger.SetXHRBreakpointParams) => Promise<void>;
  };
  DOMSnapshot: {
    /** Disables DOM snapshot agent for the given page. */
    disable: () => Promise<void>;
    /** Enables DOM snapshot agent for the given page. */
    enable: () => Promise<void>;
    /**
     * Returns a document snapshot, including the full DOM tree of the root node (including iframes,
     * template contents, and imported documents) in a flattened array, as well as layout and
     * white-listed computed style information for the nodes. Shadow DOM in the returned DOM tree is
     * flattened.
     */
    getSnapshot: (params: DOMSnapshot.GetSnapshotParams) => Promise<DOMSnapshot.GetSnapshotReturn>;
    /**
     * Returns a document snapshot, including the full DOM tree of the root node (including iframes,
     * template contents, and imported documents) in a flattened array, as well as layout and
     * white-listed computed style information for the nodes. Shadow DOM in the returned DOM tree is
     * flattened.
     */
    captureSnapshot: (params: DOMSnapshot.CaptureSnapshotParams) => Promise<DOMSnapshot.CaptureSnapshotReturn>;
  };
  DOMStorage: {
    clear: (params: DOMStorage.ClearParams) => Promise<void>;
    /** Disables storage tracking, prevents storage events from being sent to the client. */
    disable: () => Promise<void>;
    /** Enables storage tracking, storage events will now be delivered to the client. */
    enable: () => Promise<void>;
    getDOMStorageItems: (params: DOMStorage.GetDOMStorageItemsParams) => Promise<DOMStorage.GetDOMStorageItemsReturn>;
    removeDOMStorageItem: (params: DOMStorage.RemoveDOMStorageItemParams) => Promise<void>;
    setDOMStorageItem: (params: DOMStorage.SetDOMStorageItemParams) => Promise<void>;
  };
  Emulation: {
    /** Tells whether emulation is supported. */
    canEmulate: () => Promise<Emulation.CanEmulateReturn>;
    /** Clears the overridden device metrics. */
    clearDeviceMetricsOverride: () => Promise<void>;
    /** Clears the overridden Geolocation Position and Error. */
    clearGeolocationOverride: () => Promise<void>;
    /** Requests that page scale factor is reset to initial values. */
    resetPageScaleFactor: () => Promise<void>;
    /** Enables or disables simulating a focused and active page. */
    setFocusEmulationEnabled: (params: Emulation.SetFocusEmulationEnabledParams) => Promise<void>;
    /** Automatically render all web contents using a dark theme. */
    setAutoDarkModeOverride: (params?: Emulation.SetAutoDarkModeOverrideParams) => Promise<void>;
    /** Enables CPU throttling to emulate slow CPUs. */
    setCPUThrottlingRate: (params: Emulation.SetCPUThrottlingRateParams) => Promise<void>;
    /**
     * Sets or clears an override of the default background color of the frame. This override is used
     * if the content does not specify one.
     */
    setDefaultBackgroundColorOverride: (params?: Emulation.SetDefaultBackgroundColorOverrideParams) => Promise<void>;
    /**
     * Overrides the values for env(safe-area-inset-*) and env(safe-area-max-inset-*). Unset values will cause the
     * respective variables to be undefined, even if previously overridden.
     */
    setSafeAreaInsetsOverride: (params: Emulation.SetSafeAreaInsetsOverrideParams) => Promise<void>;
    /**
     * Overrides the values of device screen dimensions (window.screen.width, window.screen.height,
     * window.innerWidth, window.innerHeight, and "device-width"/"device-height"-related CSS media
     * query results).
     */
    setDeviceMetricsOverride: (params: Emulation.SetDeviceMetricsOverrideParams) => Promise<void>;
    /**
     * Start reporting the given posture value to the Device Posture API.
     * This override can also be set in setDeviceMetricsOverride().
     */
    setDevicePostureOverride: (params: Emulation.SetDevicePostureOverrideParams) => Promise<void>;
    /**
     * Clears a device posture override set with either setDeviceMetricsOverride()
     * or setDevicePostureOverride() and starts using posture information from the
     * platform again.
     * Does nothing if no override is set.
     */
    clearDevicePostureOverride: () => Promise<void>;
    /**
     * Start using the given display features to pupulate the Viewport Segments API.
     * This override can also be set in setDeviceMetricsOverride().
     */
    setDisplayFeaturesOverride: (params: Emulation.SetDisplayFeaturesOverrideParams) => Promise<void>;
    /**
     * Clears the display features override set with either setDeviceMetricsOverride()
     * or setDisplayFeaturesOverride() and starts using display features from the
     * platform again.
     * Does nothing if no override is set.
     */
    clearDisplayFeaturesOverride: () => Promise<void>;
    setScrollbarsHidden: (params: Emulation.SetScrollbarsHiddenParams) => Promise<void>;
    setDocumentCookieDisabled: (params: Emulation.SetDocumentCookieDisabledParams) => Promise<void>;
    setEmitTouchEventsForMouse: (params: Emulation.SetEmitTouchEventsForMouseParams) => Promise<void>;
    /** Emulates the given media type or media feature for CSS media queries. */
    setEmulatedMedia: (params?: Emulation.SetEmulatedMediaParams) => Promise<void>;
    /** Emulates the given vision deficiency. */
    setEmulatedVisionDeficiency: (params: Emulation.SetEmulatedVisionDeficiencyParams) => Promise<void>;
    /** Emulates the given OS text scale. */
    setEmulatedOSTextScale: (params?: Emulation.SetEmulatedOSTextScaleParams) => Promise<void>;
    /**
     * Overrides the Geolocation Position or Error. Omitting latitude, longitude or
     * accuracy emulates position unavailable.
     */
    setGeolocationOverride: (params?: Emulation.SetGeolocationOverrideParams) => Promise<void>;
    getOverriddenSensorInformation: (params: Emulation.GetOverriddenSensorInformationParams) => Promise<Emulation.GetOverriddenSensorInformationReturn>;
    /**
     * Overrides a platform sensor of a given type. If |enabled| is true, calls to
     * Sensor.start() will use a virtual sensor as backend rather than fetching
     * data from a real hardware sensor. Otherwise, existing virtual
     * sensor-backend Sensor objects will fire an error event and new calls to
     * Sensor.start() will attempt to use a real sensor instead.
     */
    setSensorOverrideEnabled: (params: Emulation.SetSensorOverrideEnabledParams) => Promise<void>;
    /**
     * Updates the sensor readings reported by a sensor type previously overridden
     * by setSensorOverrideEnabled.
     */
    setSensorOverrideReadings: (params: Emulation.SetSensorOverrideReadingsParams) => Promise<void>;
    /**
     * Overrides a pressure source of a given type, as used by the Compute
     * Pressure API, so that updates to PressureObserver.observe() are provided
     * via setPressureStateOverride instead of being retrieved from
     * platform-provided telemetry data.
     */
    setPressureSourceOverrideEnabled: (params: Emulation.SetPressureSourceOverrideEnabledParams) => Promise<void>;
    /**
     * TODO: OBSOLETE: To remove when setPressureDataOverride is merged.
     * Provides a given pressure state that will be processed and eventually be
     * delivered to PressureObserver users. |source| must have been previously
     * overridden by setPressureSourceOverrideEnabled.
     */
    setPressureStateOverride: (params: Emulation.SetPressureStateOverrideParams) => Promise<void>;
    /**
     * Provides a given pressure data set that will be processed and eventually be
     * delivered to PressureObserver users. |source| must have been previously
     * overridden by setPressureSourceOverrideEnabled.
     */
    setPressureDataOverride: (params: Emulation.SetPressureDataOverrideParams) => Promise<void>;
    /** Overrides the Idle state. */
    setIdleOverride: (params: Emulation.SetIdleOverrideParams) => Promise<void>;
    /** Clears Idle state overrides. */
    clearIdleOverride: () => Promise<void>;
    /** Overrides value returned by the javascript navigator object. */
    setNavigatorOverrides: (params: Emulation.SetNavigatorOverridesParams) => Promise<void>;
    /** Sets a specified page scale factor. */
    setPageScaleFactor: (params: Emulation.SetPageScaleFactorParams) => Promise<void>;
    /** Switches script execution in the page. */
    setScriptExecutionDisabled: (params: Emulation.SetScriptExecutionDisabledParams) => Promise<void>;
    /** Enables touch on platforms which do not support them. */
    setTouchEmulationEnabled: (params: Emulation.SetTouchEmulationEnabledParams) => Promise<void>;
    /**
     * Turns on virtual time for all frames (replacing real-time with a synthetic time source) and sets
     * the current virtual time policy.  Note this supersedes any previous time budget.
     */
    setVirtualTimePolicy: (params: Emulation.SetVirtualTimePolicyParams) => Promise<Emulation.SetVirtualTimePolicyReturn>;
    /** Overrides default host system locale with the specified one. */
    setLocaleOverride: (params?: Emulation.SetLocaleOverrideParams) => Promise<void>;
    /** Overrides default host system timezone with the specified one. */
    setTimezoneOverride: (params: Emulation.SetTimezoneOverrideParams) => Promise<void>;
    /**
     * Resizes the frame/viewport of the page. Note that this does not affect the frame's container
     * (e.g. browser window). Can be used to produce screenshots of the specified size. Not supported
     * on Android.
     */
    setVisibleSize: (params: Emulation.SetVisibleSizeParams) => Promise<void>;
    setDisabledImageTypes: (params: Emulation.SetDisabledImageTypesParams) => Promise<void>;
    /** Override the value of navigator.connection.saveData */
    setDataSaverOverride: (params?: Emulation.SetDataSaverOverrideParams) => Promise<void>;
    setHardwareConcurrencyOverride: (params: Emulation.SetHardwareConcurrencyOverrideParams) => Promise<void>;
    /**
     * Allows overriding user agent with the given string.
     * `userAgentMetadata` must be set for Client Hint headers to be sent.
     */
    setUserAgentOverride: (params: Emulation.SetUserAgentOverrideParams) => Promise<void>;
    /** Allows overriding the automation flag. */
    setAutomationOverride: (params: Emulation.SetAutomationOverrideParams) => Promise<void>;
    /**
     * Allows overriding the difference between the small and large viewport sizes, which determine the
     * value of the `svh` and `lvh` unit, respectively. Only supported for top-level frames.
     */
    setSmallViewportHeightDifferenceOverride: (params: Emulation.SetSmallViewportHeightDifferenceOverrideParams) => Promise<void>;
    /**
     * Returns device's screen configuration. In headful mode, the physical screens configuration is returned,
     * whereas in headless mode, a virtual headless screen configuration is provided instead.
     */
    getScreenInfos: () => Promise<Emulation.GetScreenInfosReturn>;
    /** Add a new screen to the device. Only supported in headless mode. */
    addScreen: (params: Emulation.AddScreenParams) => Promise<Emulation.AddScreenReturn>;
    /** Updates specified screen parameters. Only supported in headless mode. */
    updateScreen: (params: Emulation.UpdateScreenParams) => Promise<Emulation.UpdateScreenReturn>;
    /** Remove screen from the device. Only supported in headless mode. */
    removeScreen: (params: Emulation.RemoveScreenParams) => Promise<void>;
    /**
     * Set primary screen. Only supported in headless mode.
     * Note that this changes the coordinate system origin to the top-left
     * of the new primary screen, updating the bounds and work areas
     * of all existing screens accordingly.
     */
    setPrimaryScreen: (params: Emulation.SetPrimaryScreenParams) => Promise<void>;
  };
  EventBreakpoints: {
    /** Sets breakpoint on particular native event. */
    setInstrumentationBreakpoint: (params: EventBreakpoints.SetInstrumentationBreakpointParams) => Promise<void>;
    /** Removes breakpoint on particular native event. */
    removeInstrumentationBreakpoint: (params: EventBreakpoints.RemoveInstrumentationBreakpointParams) => Promise<void>;
    /** Removes all breakpoints */
    disable: () => Promise<void>;
  };
  Extensions: {
    /**
     * Runs an extension default action.
     * Available if the client is connected using the --remote-debugging-pipe
     * flag and the --enable-unsafe-extension-debugging flag is set.
     */
    triggerAction: (params: Extensions.TriggerActionParams) => Promise<void>;
    /**
     * Installs an unpacked extension from the filesystem similar to
     * --load-extension CLI flags. Returns extension ID once the extension
     * has been installed. Available if the client is connected using the
     * --remote-debugging-pipe flag and the --enable-unsafe-extension-debugging
     * flag is set.
     */
    loadUnpacked: (params: Extensions.LoadUnpackedParams) => Promise<Extensions.LoadUnpackedReturn>;
    /**
     * Gets a list of all unpacked extensions.
     * Available if the client is connected using the --remote-debugging-pipe flag
     * and the --enable-unsafe-extension-debugging flag is set.
     */
    getExtensions: () => Promise<Extensions.GetExtensionsReturn>;
    /**
     * Uninstalls an unpacked extension (others not supported) from the profile.
     * Available if the client is connected using the --remote-debugging-pipe flag
     * and the --enable-unsafe-extension-debugging.
     */
    uninstall: (params: Extensions.UninstallParams) => Promise<void>;
    /**
     * Gets data from extension storage in the given `storageArea`. If `keys` is
     * specified, these are used to filter the result.
     */
    getStorageItems: (params: Extensions.GetStorageItemsParams) => Promise<Extensions.GetStorageItemsReturn>;
    /** Removes `keys` from extension storage in the given `storageArea`. */
    removeStorageItems: (params: Extensions.RemoveStorageItemsParams) => Promise<void>;
    /** Clears extension storage in the given `storageArea`. */
    clearStorageItems: (params: Extensions.ClearStorageItemsParams) => Promise<void>;
    /**
     * Sets `values` in extension storage in the given `storageArea`. The provided `values`
     * will be merged with existing values in the storage area.
     */
    setStorageItems: (params: Extensions.SetStorageItemsParams) => Promise<void>;
  };
  FedCm: {
    enable: (params?: FedCm.EnableParams) => Promise<void>;
    disable: () => Promise<void>;
    selectAccount: (params: FedCm.SelectAccountParams) => Promise<void>;
    clickDialogButton: (params: FedCm.ClickDialogButtonParams) => Promise<void>;
    openUrl: (params: FedCm.OpenUrlParams) => Promise<void>;
    dismissDialog: (params: FedCm.DismissDialogParams) => Promise<void>;
    /**
     * Resets the cooldown time, if any, to allow the next FedCM call to show
     * a dialog even if one was recently dismissed by the user.
     */
    resetCooldown: () => Promise<void>;
  };
  Fetch: {
    /** Disables the fetch domain. */
    disable: () => Promise<void>;
    /**
     * Enables issuing of requestPaused events. A request will be paused until client
     * calls one of failRequest, fulfillRequest or continueRequest/continueWithAuth.
     */
    enable: (params?: Fetch.EnableParams) => Promise<void>;
    /** Causes the request to fail with specified reason. */
    failRequest: (params: Fetch.FailRequestParams) => Promise<void>;
    /** Provides response to the request. */
    fulfillRequest: (params: Fetch.FulfillRequestParams) => Promise<void>;
    /** Continues the request, optionally modifying some of its parameters. */
    continueRequest: (params: Fetch.ContinueRequestParams) => Promise<void>;
    /** Continues a request supplying authChallengeResponse following authRequired event. */
    continueWithAuth: (params: Fetch.ContinueWithAuthParams) => Promise<void>;
    /**
     * Continues loading of the paused response, optionally modifying the
     * response headers. If either responseCode or headers are modified, all of them
     * must be present.
     */
    continueResponse: (params: Fetch.ContinueResponseParams) => Promise<void>;
    /**
     * Causes the body of the response to be received from the server and
     * returned as a single string. May only be issued for a request that
     * is paused in the Response stage and is mutually exclusive with
     * takeResponseBodyForInterceptionAsStream. Calling other methods that
     * affect the request or disabling fetch domain before body is received
     * results in an undefined behavior.
     * Note that the response body is not available for redirects. Requests
     * paused in the _redirect received_ state may be differentiated by
     * `responseCode` and presence of `location` response header, see
     * comments to `requestPaused` for details.
     */
    getResponseBody: (params: Fetch.GetResponseBodyParams) => Promise<Fetch.GetResponseBodyReturn>;
    /**
     * Returns a handle to the stream representing the response body.
     * The request must be paused in the HeadersReceived stage.
     * Note that after this command the request can't be continued
     * as is -- client either needs to cancel it or to provide the
     * response body.
     * The stream only supports sequential read, IO.read will fail if the position
     * is specified.
     * This method is mutually exclusive with getResponseBody.
     * Calling other methods that affect the request or disabling fetch
     * domain before body is received results in an undefined behavior.
     */
    takeResponseBodyAsStream: (params: Fetch.TakeResponseBodyAsStreamParams) => Promise<Fetch.TakeResponseBodyAsStreamReturn>;
  };
  FileSystem: {
    getDirectory: (params: FileSystem.GetDirectoryParams) => Promise<FileSystem.GetDirectoryReturn>;
  };
  HeadlessExperimental: {
    /**
     * Sends a BeginFrame to the target and returns when the frame was completed. Optionally captures a
     * screenshot from the resulting frame. Requires that the target was created with enabled
     * BeginFrameControl. Designed for use with --run-all-compositor-stages-before-draw, see also
     * https://goo.gle/chrome-headless-rendering for more background.
     */
    beginFrame: (params?: HeadlessExperimental.BeginFrameParams) => Promise<HeadlessExperimental.BeginFrameReturn>;
    /** Disables headless events for the target. */
    disable: () => Promise<void>;
    /** Enables headless events for the target. */
    enable: () => Promise<void>;
  };
  HeapProfiler: {
    /**
     * Enables console to refer to the node with given id via $x (see Command Line API for more details
     * $x functions).
     */
    addInspectedHeapObject: (params: HeapProfiler.AddInspectedHeapObjectParams) => Promise<void>;
    collectGarbage: () => Promise<void>;
    disable: () => Promise<void>;
    enable: () => Promise<void>;
    getHeapObjectId: (params: HeapProfiler.GetHeapObjectIdParams) => Promise<HeapProfiler.GetHeapObjectIdReturn>;
    getObjectByHeapObjectId: (params: HeapProfiler.GetObjectByHeapObjectIdParams) => Promise<HeapProfiler.GetObjectByHeapObjectIdReturn>;
    getSamplingProfile: () => Promise<HeapProfiler.GetSamplingProfileReturn>;
    startSampling: (params?: HeapProfiler.StartSamplingParams) => Promise<void>;
    startTrackingHeapObjects: (params?: HeapProfiler.StartTrackingHeapObjectsParams) => Promise<void>;
    stopSampling: () => Promise<HeapProfiler.StopSamplingReturn>;
    stopTrackingHeapObjects: (params?: HeapProfiler.StopTrackingHeapObjectsParams) => Promise<void>;
    takeHeapSnapshot: (params?: HeapProfiler.TakeHeapSnapshotParams) => Promise<void>;
  };
  IndexedDB: {
    /** Clears all entries from an object store. */
    clearObjectStore: (params: IndexedDB.ClearObjectStoreParams) => Promise<void>;
    /** Deletes a database. */
    deleteDatabase: (params: IndexedDB.DeleteDatabaseParams) => Promise<void>;
    /** Delete a range of entries from an object store */
    deleteObjectStoreEntries: (params: IndexedDB.DeleteObjectStoreEntriesParams) => Promise<void>;
    /** Disables events from backend. */
    disable: () => Promise<void>;
    /** Enables events from backend. */
    enable: () => Promise<void>;
    /** Requests data from object store or index. */
    requestData: (params: IndexedDB.RequestDataParams) => Promise<IndexedDB.RequestDataReturn>;
    /** Gets metadata of an object store. */
    getMetadata: (params: IndexedDB.GetMetadataParams) => Promise<IndexedDB.GetMetadataReturn>;
    /** Requests database with given name in given frame. */
    requestDatabase: (params: IndexedDB.RequestDatabaseParams) => Promise<IndexedDB.RequestDatabaseReturn>;
    /** Requests database names for given security origin. */
    requestDatabaseNames: (params?: IndexedDB.RequestDatabaseNamesParams) => Promise<IndexedDB.RequestDatabaseNamesReturn>;
  };
  Input: {
    /** Dispatches a drag event into the page. */
    dispatchDragEvent: (params: Input.DispatchDragEventParams) => Promise<void>;
    /** Dispatches a key event to the page. */
    dispatchKeyEvent: (params: Input.DispatchKeyEventParams) => Promise<void>;
    /**
     * This method emulates inserting text that doesn't come from a key press,
     * for example an emoji keyboard or an IME.
     */
    insertText: (params: Input.InsertTextParams) => Promise<void>;
    /**
     * This method sets the current candidate text for IME.
     * Use imeCommitComposition to commit the final text.
     * Use imeSetComposition with empty string as text to cancel composition.
     */
    imeSetComposition: (params: Input.ImeSetCompositionParams) => Promise<void>;
    /** Dispatches a mouse event to the page. */
    dispatchMouseEvent: (params: Input.DispatchMouseEventParams) => Promise<void>;
    /** Dispatches a touch event to the page. */
    dispatchTouchEvent: (params: Input.DispatchTouchEventParams) => Promise<void>;
    /** Cancels any active dragging in the page. */
    cancelDragging: () => Promise<void>;
    /** Emulates touch event from the mouse event parameters. */
    emulateTouchFromMouseEvent: (params: Input.EmulateTouchFromMouseEventParams) => Promise<void>;
    /** Ignores input events (useful while auditing page). */
    setIgnoreInputEvents: (params: Input.SetIgnoreInputEventsParams) => Promise<void>;
    /**
     * Prevents default drag and drop behavior and instead emits `Input.dragIntercepted` events.
     * Drag and drop behavior can be directly controlled via `Input.dispatchDragEvent`.
     */
    setInterceptDrags: (params: Input.SetInterceptDragsParams) => Promise<void>;
    /** Synthesizes a pinch gesture over a time period by issuing appropriate touch events. */
    synthesizePinchGesture: (params: Input.SynthesizePinchGestureParams) => Promise<void>;
    /** Synthesizes a scroll gesture over a time period by issuing appropriate touch events. */
    synthesizeScrollGesture: (params: Input.SynthesizeScrollGestureParams) => Promise<void>;
    /** Synthesizes a tap gesture over a time period by issuing appropriate touch events. */
    synthesizeTapGesture: (params: Input.SynthesizeTapGestureParams) => Promise<void>;
  };
  Inspector: {
    /** Disables inspector domain notifications. */
    disable: () => Promise<void>;
    /** Enables inspector domain notifications. */
    enable: () => Promise<void>;
  };
  IO: {
    /** Close the stream, discard any temporary backing storage. */
    close: (params: IO.CloseParams) => Promise<void>;
    /** Read a chunk of the stream */
    read: (params: IO.ReadParams) => Promise<IO.ReadReturn>;
    /** Return UUID of Blob object specified by a remote object id. */
    resolveBlob: (params: IO.ResolveBlobParams) => Promise<IO.ResolveBlobReturn>;
  };
  LayerTree: {
    /** Provides the reasons why the given layer was composited. */
    compositingReasons: (params: LayerTree.CompositingReasonsParams) => Promise<LayerTree.CompositingReasonsReturn>;
    /** Disables compositing tree inspection. */
    disable: () => Promise<void>;
    /** Enables compositing tree inspection. */
    enable: () => Promise<void>;
    /** Returns the snapshot identifier. */
    loadSnapshot: (params: LayerTree.LoadSnapshotParams) => Promise<LayerTree.LoadSnapshotReturn>;
    /** Returns the layer snapshot identifier. */
    makeSnapshot: (params: LayerTree.MakeSnapshotParams) => Promise<LayerTree.MakeSnapshotReturn>;
    profileSnapshot: (params: LayerTree.ProfileSnapshotParams) => Promise<LayerTree.ProfileSnapshotReturn>;
    /** Releases layer snapshot captured by the back-end. */
    releaseSnapshot: (params: LayerTree.ReleaseSnapshotParams) => Promise<void>;
    /** Replays the layer snapshot and returns the resulting bitmap. */
    replaySnapshot: (params: LayerTree.ReplaySnapshotParams) => Promise<LayerTree.ReplaySnapshotReturn>;
    /** Replays the layer snapshot and returns canvas log. */
    snapshotCommandLog: (params: LayerTree.SnapshotCommandLogParams) => Promise<LayerTree.SnapshotCommandLogReturn>;
  };
  Log: {
    /** Clears the log. */
    clear: () => Promise<void>;
    /** Disables log domain, prevents further log entries from being reported to the client. */
    disable: () => Promise<void>;
    /**
     * Enables log domain, sends the entries collected so far to the client by means of the
     * `entryAdded` notification.
     */
    enable: () => Promise<void>;
    /** start violation reporting. */
    startViolationsReport: (params: Log.StartViolationsReportParams) => Promise<void>;
    /** Stop violation reporting. */
    stopViolationsReport: () => Promise<void>;
  };
  Media: {
    /** Enables the Media domain */
    enable: () => Promise<void>;
    /** Disables the Media domain. */
    disable: () => Promise<void>;
  };
  Memory: {
    /** Retruns current DOM object counters. */
    getDOMCounters: () => Promise<Memory.GetDOMCountersReturn>;
    /** Retruns DOM object counters after preparing renderer for leak detection. */
    getDOMCountersForLeakDetection: () => Promise<Memory.GetDOMCountersForLeakDetectionReturn>;
    /**
     * Prepares for leak detection by terminating workers, stopping spellcheckers,
     * dropping non-essential internal caches, running garbage collections, etc.
     */
    prepareForLeakDetection: () => Promise<void>;
    /** Simulate OomIntervention by purging V8 memory. */
    forciblyPurgeJavaScriptMemory: () => Promise<void>;
    /** Enable/disable suppressing memory pressure notifications in all processes. */
    setPressureNotificationsSuppressed: (params: Memory.SetPressureNotificationsSuppressedParams) => Promise<void>;
    /** Simulate a memory pressure notification in all processes. */
    simulatePressureNotification: (params: Memory.SimulatePressureNotificationParams) => Promise<void>;
    /** Start collecting native memory profile. */
    startSampling: (params?: Memory.StartSamplingParams) => Promise<void>;
    /** Stop collecting native memory profile. */
    stopSampling: () => Promise<void>;
    /**
     * Retrieve native memory allocations profile
     * collected since renderer process startup.
     */
    getAllTimeSamplingProfile: () => Promise<Memory.GetAllTimeSamplingProfileReturn>;
    /**
     * Retrieve native memory allocations profile
     * collected since browser process startup.
     */
    getBrowserSamplingProfile: () => Promise<Memory.GetBrowserSamplingProfileReturn>;
    /**
     * Retrieve native memory allocations profile collected since last
     * `startSampling` call.
     */
    getSamplingProfile: () => Promise<Memory.GetSamplingProfileReturn>;
  };
  Network: {
    /** Sets a list of content encodings that will be accepted. Empty list means no encoding is accepted. */
    setAcceptedEncodings: (params: Network.SetAcceptedEncodingsParams) => Promise<void>;
    /** Clears accepted encodings set by setAcceptedEncodings */
    clearAcceptedEncodingsOverride: () => Promise<void>;
    /** Tells whether clearing browser cache is supported. */
    canClearBrowserCache: () => Promise<Network.CanClearBrowserCacheReturn>;
    /** Tells whether clearing browser cookies is supported. */
    canClearBrowserCookies: () => Promise<Network.CanClearBrowserCookiesReturn>;
    /** Tells whether emulation of network conditions is supported. */
    canEmulateNetworkConditions: () => Promise<Network.CanEmulateNetworkConditionsReturn>;
    /** Clears browser cache. */
    clearBrowserCache: () => Promise<void>;
    /** Clears browser cookies. */
    clearBrowserCookies: () => Promise<void>;
    /**
     * Response to Network.requestIntercepted which either modifies the request to continue with any
     * modifications, or blocks it, or completes it with the provided response bytes. If a network
     * fetch occurs as a result which encounters a redirect an additional Network.requestIntercepted
     * event will be sent with the same InterceptionId.
     * Deprecated, use Fetch.continueRequest, Fetch.fulfillRequest and Fetch.failRequest instead.
     */
    continueInterceptedRequest: (params: Network.ContinueInterceptedRequestParams) => Promise<void>;
    /** Deletes browser cookies with matching name and url or domain/path/partitionKey pair. */
    deleteCookies: (params: Network.DeleteCookiesParams) => Promise<void>;
    /** Disables network tracking, prevents network events from being sent to the client. */
    disable: () => Promise<void>;
    /**
     * Activates emulation of network conditions. This command is deprecated in favor of the emulateNetworkConditionsByRule
     * and overrideNetworkState commands, which can be used together to the same effect.
     */
    emulateNetworkConditions: (params: Network.EmulateNetworkConditionsParams) => Promise<void>;
    /**
     * Activates emulation of network conditions for individual requests using URL match patterns. Unlike the deprecated
     * Network.emulateNetworkConditions this method does not affect `navigator` state. Use Network.overrideNetworkState to
     * explicitly modify `navigator` behavior.
     */
    emulateNetworkConditionsByRule: (params: Network.EmulateNetworkConditionsByRuleParams) => Promise<Network.EmulateNetworkConditionsByRuleReturn>;
    /** Override the state of navigator.onLine and navigator.connection. */
    overrideNetworkState: (params: Network.OverrideNetworkStateParams) => Promise<void>;
    /** Enables network tracking, network events will now be delivered to the client. */
    enable: (params?: Network.EnableParams) => Promise<void>;
    /**
     * Configures storing response bodies outside of renderer, so that these survive
     * a cross-process navigation.
     * If maxTotalBufferSize is not set, durable messages are disabled.
     */
    configureDurableMessages: (params?: Network.ConfigureDurableMessagesParams) => Promise<void>;
    /**
     * Returns all browser cookies. Depending on the backend support, will return detailed cookie
     * information in the `cookies` field.
     * Deprecated. Use Storage.getCookies instead.
     */
    getAllCookies: () => Promise<Network.GetAllCookiesReturn>;
    /** Returns the DER-encoded certificate. */
    getCertificate: (params: Network.GetCertificateParams) => Promise<Network.GetCertificateReturn>;
    /**
     * Returns all browser cookies for the current URL. Depending on the backend support, will return
     * detailed cookie information in the `cookies` field.
     */
    getCookies: (params?: Network.GetCookiesParams) => Promise<Network.GetCookiesReturn>;
    /** Returns content served for the given request. */
    getResponseBody: (params: Network.GetResponseBodyParams) => Promise<Network.GetResponseBodyReturn>;
    /** Returns post data sent with the request. Returns an error when no data was sent with the request. */
    getRequestPostData: (params: Network.GetRequestPostDataParams) => Promise<Network.GetRequestPostDataReturn>;
    /** Returns content served for the given currently intercepted request. */
    getResponseBodyForInterception: (params: Network.GetResponseBodyForInterceptionParams) => Promise<Network.GetResponseBodyForInterceptionReturn>;
    /**
     * Returns a handle to the stream representing the response body. Note that after this command,
     * the intercepted request can't be continued as is -- you either need to cancel it or to provide
     * the response body. The stream only supports sequential read, IO.read will fail if the position
     * is specified.
     */
    takeResponseBodyForInterceptionAsStream: (params: Network.TakeResponseBodyForInterceptionAsStreamParams) => Promise<Network.TakeResponseBodyForInterceptionAsStreamReturn>;
    /**
     * This method sends a new XMLHttpRequest which is identical to the original one. The following
     * parameters should be identical: method, url, async, request body, extra headers, withCredentials
     * attribute, user, password.
     */
    replayXHR: (params: Network.ReplayXHRParams) => Promise<void>;
    /** Searches for given string in response content. */
    searchInResponseBody: (params: Network.SearchInResponseBodyParams) => Promise<Network.SearchInResponseBodyReturn>;
    /** Blocks URLs from loading. */
    setBlockedURLs: (params?: Network.SetBlockedURLsParams) => Promise<void>;
    /** Toggles ignoring of service worker for each request. */
    setBypassServiceWorker: (params: Network.SetBypassServiceWorkerParams) => Promise<void>;
    /** Toggles ignoring cache for each request. If `true`, cache will not be used. */
    setCacheDisabled: (params: Network.SetCacheDisabledParams) => Promise<void>;
    /** Sets a cookie with the given cookie data; may overwrite equivalent cookies if they exist. */
    setCookie: (params: Network.SetCookieParams) => Promise<Network.SetCookieReturn>;
    /** Sets given cookies. */
    setCookies: (params: Network.SetCookiesParams) => Promise<void>;
    /** Specifies whether to always send extra HTTP headers with the requests from this page. */
    setExtraHTTPHeaders: (params: Network.SetExtraHTTPHeadersParams) => Promise<void>;
    /** Specifies whether to attach a page script stack id in requests */
    setAttachDebugStack: (params: Network.SetAttachDebugStackParams) => Promise<void>;
    /**
     * Sets the requests to intercept that match the provided patterns and optionally resource types.
     * Deprecated, please use Fetch.enable instead.
     */
    setRequestInterception: (params: Network.SetRequestInterceptionParams) => Promise<void>;
    /**
     * Enables streaming of the response for the given requestId.
     * If enabled, the dataReceived event contains the data that was received during streaming.
     */
    streamResourceContent: (params: Network.StreamResourceContentParams) => Promise<Network.StreamResourceContentReturn>;
    /** Returns information about the COEP/COOP isolation status. */
    getSecurityIsolationStatus: (params?: Network.GetSecurityIsolationStatusParams) => Promise<Network.GetSecurityIsolationStatusReturn>;
    /**
     * Enables tracking for the Reporting API, events generated by the Reporting API will now be delivered to the client.
     * Enabling triggers 'reportingApiReportAdded' for all existing reports.
     */
    enableReportingApi: (params: Network.EnableReportingApiParams) => Promise<void>;
    /** Sets up tracking device bound sessions and fetching of initial set of sessions. */
    enableDeviceBoundSessions: (params: Network.EnableDeviceBoundSessionsParams) => Promise<void>;
    /** Deletes a device bound session. */
    deleteDeviceBoundSession: (params: Network.DeleteDeviceBoundSessionParams) => Promise<void>;
    /** Fetches the schemeful site for a specific origin. */
    fetchSchemefulSite: (params: Network.FetchSchemefulSiteParams) => Promise<Network.FetchSchemefulSiteReturn>;
    /** Fetches the resource and returns the content. */
    loadNetworkResource: (params: Network.LoadNetworkResourceParams) => Promise<Network.LoadNetworkResourceReturn>;
    /**
     * Sets Controls for third-party cookie access
     * Page reload is required before the new cookie behavior will be observed
     */
    setCookieControls: (params: Network.SetCookieControlsParams) => Promise<void>;
  };
  Overlay: {
    /** Disables domain notifications. */
    disable: () => Promise<void>;
    /** Enables domain notifications. */
    enable: () => Promise<void>;
    /** For testing. */
    getHighlightObjectForTest: (params: Overlay.GetHighlightObjectForTestParams) => Promise<Overlay.GetHighlightObjectForTestReturn>;
    /** For Persistent Grid testing. */
    getGridHighlightObjectsForTest: (params: Overlay.GetGridHighlightObjectsForTestParams) => Promise<Overlay.GetGridHighlightObjectsForTestReturn>;
    /** For Source Order Viewer testing. */
    getSourceOrderHighlightObjectForTest: (params: Overlay.GetSourceOrderHighlightObjectForTestParams) => Promise<Overlay.GetSourceOrderHighlightObjectForTestReturn>;
    /** Hides any highlight. */
    hideHighlight: () => Promise<void>;
    /**
     * Highlights owner element of the frame with given id.
     * Deprecated: Doesn't work reliably and cannot be fixed due to process
     * separation (the owner node might be in a different process). Determine
     * the owner node in the client and use highlightNode.
     */
    highlightFrame: (params: Overlay.HighlightFrameParams) => Promise<void>;
    /**
     * Highlights DOM node with given id or with the given JavaScript object wrapper. Either nodeId or
     * objectId must be specified.
     */
    highlightNode: (params: Overlay.HighlightNodeParams) => Promise<void>;
    /** Highlights given quad. Coordinates are absolute with respect to the main frame viewport. */
    highlightQuad: (params: Overlay.HighlightQuadParams) => Promise<void>;
    /**
     * Highlights given rectangle. Coordinates are absolute with respect to the main frame viewport.
     * Issue: the method does not handle device pixel ratio (DPR) correctly.
     * The coordinates currently have to be adjusted by the client
     * if DPR is not 1 (see crbug.com/437807128).
     */
    highlightRect: (params: Overlay.HighlightRectParams) => Promise<void>;
    /**
     * Highlights the source order of the children of the DOM node with given id or with the given
     * JavaScript object wrapper. Either nodeId or objectId must be specified.
     */
    highlightSourceOrder: (params: Overlay.HighlightSourceOrderParams) => Promise<void>;
    /**
     * Enters the 'inspect' mode. In this mode, elements that user is hovering over are highlighted.
     * Backend then generates 'inspectNodeRequested' event upon element selection.
     */
    setInspectMode: (params: Overlay.SetInspectModeParams) => Promise<void>;
    /** Highlights owner element of all frames detected to be ads. */
    setShowAdHighlights: (params: Overlay.SetShowAdHighlightsParams) => Promise<void>;
    setPausedInDebuggerMessage: (params?: Overlay.SetPausedInDebuggerMessageParams) => Promise<void>;
    /** Requests that backend shows debug borders on layers */
    setShowDebugBorders: (params: Overlay.SetShowDebugBordersParams) => Promise<void>;
    /** Requests that backend shows the FPS counter */
    setShowFPSCounter: (params: Overlay.SetShowFPSCounterParams) => Promise<void>;
    /** Highlight multiple elements with the CSS Grid overlay. */
    setShowGridOverlays: (params: Overlay.SetShowGridOverlaysParams) => Promise<void>;
    setShowFlexOverlays: (params: Overlay.SetShowFlexOverlaysParams) => Promise<void>;
    setShowScrollSnapOverlays: (params: Overlay.SetShowScrollSnapOverlaysParams) => Promise<void>;
    setShowContainerQueryOverlays: (params: Overlay.SetShowContainerQueryOverlaysParams) => Promise<void>;
    setShowInspectedElementAnchor: (params: Overlay.SetShowInspectedElementAnchorParams) => Promise<void>;
    /** Requests that backend shows paint rectangles */
    setShowPaintRects: (params: Overlay.SetShowPaintRectsParams) => Promise<void>;
    /** Requests that backend shows layout shift regions */
    setShowLayoutShiftRegions: (params: Overlay.SetShowLayoutShiftRegionsParams) => Promise<void>;
    /** Requests that backend shows scroll bottleneck rects */
    setShowScrollBottleneckRects: (params: Overlay.SetShowScrollBottleneckRectsParams) => Promise<void>;
    /** Deprecated, no longer has any effect. */
    setShowHitTestBorders: (params: Overlay.SetShowHitTestBordersParams) => Promise<void>;
    /** Deprecated, no longer has any effect. */
    setShowWebVitals: (params: Overlay.SetShowWebVitalsParams) => Promise<void>;
    /** Paints viewport size upon main frame resize. */
    setShowViewportSizeOnResize: (params: Overlay.SetShowViewportSizeOnResizeParams) => Promise<void>;
    /** Add a dual screen device hinge */
    setShowHinge: (params?: Overlay.SetShowHingeParams) => Promise<void>;
    /** Show elements in isolation mode with overlays. */
    setShowIsolatedElements: (params: Overlay.SetShowIsolatedElementsParams) => Promise<void>;
    /** Show Window Controls Overlay for PWA */
    setShowWindowControlsOverlay: (params?: Overlay.SetShowWindowControlsOverlayParams) => Promise<void>;
  };
  Page: {
    /** Deprecated, please use addScriptToEvaluateOnNewDocument instead. */
    addScriptToEvaluateOnLoad: (params: Page.AddScriptToEvaluateOnLoadParams) => Promise<Page.AddScriptToEvaluateOnLoadReturn>;
    /** Evaluates given script in every frame upon creation (before loading frame's scripts). */
    addScriptToEvaluateOnNewDocument: (params: Page.AddScriptToEvaluateOnNewDocumentParams) => Promise<Page.AddScriptToEvaluateOnNewDocumentReturn>;
    /** Brings page to front (activates tab). */
    bringToFront: () => Promise<void>;
    /** Capture page screenshot. */
    captureScreenshot: (params?: Page.CaptureScreenshotParams) => Promise<Page.CaptureScreenshotReturn>;
    /**
     * Returns a snapshot of the page as a string. For MHTML format, the serialization includes
     * iframes, shadow DOM, external resources, and element-inline styles.
     */
    captureSnapshot: (params?: Page.CaptureSnapshotParams) => Promise<Page.CaptureSnapshotReturn>;
    /** Creates an isolated world for the given frame. */
    createIsolatedWorld: (params: Page.CreateIsolatedWorldParams) => Promise<Page.CreateIsolatedWorldReturn>;
    /** Disables page domain notifications. */
    disable: () => Promise<void>;
    /** Enables page domain notifications. */
    enable: (params?: Page.EnableParams) => Promise<void>;
    /**
     * Gets the processed manifest for this current document.
     *   This API always waits for the manifest to be loaded.
     *   If manifestId is provided, and it does not match the manifest of the
     *     current document, this API errors out.
     *   If there is not a loaded page, this API errors out immediately.
     */
    getAppManifest: (params?: Page.GetAppManifestParams) => Promise<Page.GetAppManifestReturn>;
    getInstallabilityErrors: () => Promise<Page.GetInstallabilityErrorsReturn>;
    /** Deprecated because it's not guaranteed that the returned icon is in fact the one used for PWA installation. */
    getManifestIcons: () => Promise<Page.GetManifestIconsReturn>;
    /**
     * Returns the unique (PWA) app id.
     * Only returns values if the feature flag 'WebAppEnableManifestId' is enabled
     */
    getAppId: () => Promise<Page.GetAppIdReturn>;
    getAdScriptAncestry: (params: Page.GetAdScriptAncestryParams) => Promise<Page.GetAdScriptAncestryReturn>;
    /** Returns present frame tree structure. */
    getFrameTree: () => Promise<Page.GetFrameTreeReturn>;
    /** Returns metrics relating to the layouting of the page, such as viewport bounds/scale. */
    getLayoutMetrics: () => Promise<Page.GetLayoutMetricsReturn>;
    /** Returns navigation history for the current page. */
    getNavigationHistory: () => Promise<Page.GetNavigationHistoryReturn>;
    /** Resets navigation history for the current page. */
    resetNavigationHistory: () => Promise<void>;
    /** Returns content of the given resource. */
    getResourceContent: (params: Page.GetResourceContentParams) => Promise<Page.GetResourceContentReturn>;
    /** Returns present frame / resource tree structure. */
    getResourceTree: () => Promise<Page.GetResourceTreeReturn>;
    /** Accepts or dismisses a JavaScript initiated dialog (alert, confirm, prompt, or onbeforeunload). */
    handleJavaScriptDialog: (params: Page.HandleJavaScriptDialogParams) => Promise<void>;
    /** Navigates current page to the given URL. */
    navigate: (params: Page.NavigateParams) => Promise<Page.NavigateReturn>;
    /** Navigates current page to the given history entry. */
    navigateToHistoryEntry: (params: Page.NavigateToHistoryEntryParams) => Promise<void>;
    /** Print page as PDF. */
    printToPDF: (params?: Page.PrintToPDFParams) => Promise<Page.PrintToPDFReturn>;
    /** Reloads given page optionally ignoring the cache. */
    reload: (params?: Page.ReloadParams) => Promise<void>;
    /** Deprecated, please use removeScriptToEvaluateOnNewDocument instead. */
    removeScriptToEvaluateOnLoad: (params: Page.RemoveScriptToEvaluateOnLoadParams) => Promise<void>;
    /** Removes given script from the list. */
    removeScriptToEvaluateOnNewDocument: (params: Page.RemoveScriptToEvaluateOnNewDocumentParams) => Promise<void>;
    /** Acknowledges that a screencast frame has been received by the frontend. */
    screencastFrameAck: (params: Page.ScreencastFrameAckParams) => Promise<void>;
    /** Searches for given string in resource content. */
    searchInResource: (params: Page.SearchInResourceParams) => Promise<Page.SearchInResourceReturn>;
    /** Enable Chrome's experimental ad filter on all sites. */
    setAdBlockingEnabled: (params: Page.SetAdBlockingEnabledParams) => Promise<void>;
    /** Enable page Content Security Policy by-passing. */
    setBypassCSP: (params: Page.SetBypassCSPParams) => Promise<void>;
    /** Get Permissions Policy state on given frame. */
    getPermissionsPolicyState: (params: Page.GetPermissionsPolicyStateParams) => Promise<Page.GetPermissionsPolicyStateReturn>;
    /** Get Origin Trials on given frame. */
    getOriginTrials: (params: Page.GetOriginTrialsParams) => Promise<Page.GetOriginTrialsReturn>;
    /** Set generic font families. */
    setFontFamilies: (params: Page.SetFontFamiliesParams) => Promise<void>;
    /** Set default font sizes. */
    setFontSizes: (params: Page.SetFontSizesParams) => Promise<void>;
    /** Sets given markup as the document's HTML. */
    setDocumentContent: (params: Page.SetDocumentContentParams) => Promise<void>;
    /** Set the behavior when downloading a file. */
    setDownloadBehavior: (params: Page.SetDownloadBehaviorParams) => Promise<void>;
    /** Controls whether page will emit lifecycle events. */
    setLifecycleEventsEnabled: (params: Page.SetLifecycleEventsEnabledParams) => Promise<void>;
    /** Starts sending each frame using the `screencastFrame` event. */
    startScreencast: (params?: Page.StartScreencastParams) => Promise<void>;
    /** Force the page stop all navigations and pending resource fetches. */
    stopLoading: () => Promise<void>;
    /** Crashes renderer on the IO thread, generates minidumps. */
    crash: () => Promise<void>;
    /** Tries to close page, running its beforeunload hooks, if any. */
    close: () => Promise<void>;
    /**
     * Tries to update the web lifecycle state of the page.
     * It will transition the page to the given state according to:
     * https://github.com/WICG/web-lifecycle/
     */
    setWebLifecycleState: (params: Page.SetWebLifecycleStateParams) => Promise<void>;
    /** Stops sending each frame in the `screencastFrame`. */
    stopScreencast: () => Promise<void>;
    /**
     * Requests backend to produce compilation cache for the specified scripts.
     * `scripts` are appended to the list of scripts for which the cache
     * would be produced. The list may be reset during page navigation.
     * When script with a matching URL is encountered, the cache is optionally
     * produced upon backend discretion, based on internal heuristics.
     * See also: `Page.compilationCacheProduced`.
     */
    produceCompilationCache: (params: Page.ProduceCompilationCacheParams) => Promise<void>;
    /**
     * Seeds compilation cache for given url. Compilation cache does not survive
     * cross-process navigation.
     */
    addCompilationCache: (params: Page.AddCompilationCacheParams) => Promise<void>;
    /** Clears seeded compilation cache. */
    clearCompilationCache: () => Promise<void>;
    /**
     * Sets the Secure Payment Confirmation transaction mode.
     * https://w3c.github.io/secure-payment-confirmation/#sctn-automation-set-spc-transaction-mode
     */
    setSPCTransactionMode: (params: Page.SetSPCTransactionModeParams) => Promise<void>;
    /**
     * Extensions for Custom Handlers API:
     * https://html.spec.whatwg.org/multipage/system-state.html#rph-automation
     */
    setRPHRegistrationMode: (params: Page.SetRPHRegistrationModeParams) => Promise<void>;
    /** Generates a report for testing. */
    generateTestReport: (params: Page.GenerateTestReportParams) => Promise<void>;
    /** Pauses page execution. Can be resumed using generic Runtime.runIfWaitingForDebugger. */
    waitForDebugger: () => Promise<void>;
    /**
     * Intercept file chooser requests and transfer control to protocol clients.
     * When file chooser interception is enabled, native file chooser dialog is not shown.
     * Instead, a protocol event `Page.fileChooserOpened` is emitted.
     */
    setInterceptFileChooserDialog: (params: Page.SetInterceptFileChooserDialogParams) => Promise<void>;
    /**
     * Enable/disable prerendering manually.
     *
     * This command is a short-term solution for https://crbug.com/1440085.
     * See https://docs.google.com/document/d/12HVmFxYj5Jc-eJr5OmWsa2bqTJsbgGLKI6ZIyx0_wpA
     * for more details.
     *
     * TODO(https://crbug.com/1440085): Remove this once Puppeteer supports tab targets.
     */
    setPrerenderingAllowed: (params: Page.SetPrerenderingAllowedParams) => Promise<void>;
    /**
     * Get the annotated page content for the main frame.
     * This is an experimental command that is subject to change.
     */
    getAnnotatedPageContent: (params?: Page.GetAnnotatedPageContentParams) => Promise<Page.GetAnnotatedPageContentReturn>;
  };
  Performance: {
    /** Disable collecting and reporting metrics. */
    disable: () => Promise<void>;
    /** Enable collecting and reporting metrics. */
    enable: (params?: Performance.EnableParams) => Promise<void>;
    /**
     * Sets time domain to use for collecting and reporting duration metrics.
     * Note that this must be called before enabling metrics collection. Calling
     * this method while metrics collection is enabled returns an error.
     */
    setTimeDomain: (params: Performance.SetTimeDomainParams) => Promise<void>;
    /** Retrieve current values of run-time metrics. */
    getMetrics: () => Promise<Performance.GetMetricsReturn>;
  };
  PerformanceTimeline: {
    /**
     * Previously buffered events would be reported before method returns.
     * See also: timelineEventAdded
     */
    enable: (params: PerformanceTimeline.EnableParams) => Promise<void>;
  };
  Preload: {
    enable: () => Promise<void>;
    disable: () => Promise<void>;
  };
  Profiler: {
    disable: () => Promise<void>;
    enable: () => Promise<void>;
    /**
     * Collect coverage data for the current isolate. The coverage data may be incomplete due to
     * garbage collection.
     */
    getBestEffortCoverage: () => Promise<Profiler.GetBestEffortCoverageReturn>;
    /** Changes CPU profiler sampling interval. Must be called before CPU profiles recording started. */
    setSamplingInterval: (params: Profiler.SetSamplingIntervalParams) => Promise<void>;
    start: () => Promise<void>;
    /**
     * Enable precise code coverage. Coverage data for JavaScript executed before enabling precise code
     * coverage may be incomplete. Enabling prevents running optimized code and resets execution
     * counters.
     */
    startPreciseCoverage: (params?: Profiler.StartPreciseCoverageParams) => Promise<Profiler.StartPreciseCoverageReturn>;
    stop: () => Promise<Profiler.StopReturn>;
    /**
     * Disable precise code coverage. Disabling releases unnecessary execution count records and allows
     * executing optimized code.
     */
    stopPreciseCoverage: () => Promise<void>;
    /**
     * Collect coverage data for the current isolate, and resets execution counters. Precise code
     * coverage needs to have started.
     */
    takePreciseCoverage: () => Promise<Profiler.TakePreciseCoverageReturn>;
  };
  PWA: {
    /** Returns the following OS state for the given manifest id. */
    getOsAppState: (params: PWA.GetOsAppStateParams) => Promise<PWA.GetOsAppStateReturn>;
    /**
     * Installs the given manifest identity, optionally using the given installUrlOrBundleUrl
     *
     * IWA-specific install description:
     * manifestId corresponds to isolated-app:// + web_package::SignedWebBundleId
     *
     * File installation mode:
     * The installUrlOrBundleUrl can be either file:// or http(s):// pointing
     * to a signed web bundle (.swbn). In this case SignedWebBundleId must correspond to
     * The .swbn file's signing key.
     *
     * Dev proxy installation mode:
     * installUrlOrBundleUrl must be http(s):// that serves dev mode IWA.
     * web_package::SignedWebBundleId must be of type dev proxy.
     *
     * The advantage of dev proxy mode is that all changes to IWA
     * automatically will be reflected in the running app without
     * reinstallation.
     *
     * To generate bundle id for proxy mode:
     * 1. Generate 32 random bytes.
     * 2. Add a specific suffix at the end following the documentation
     *    https://github.com/WICG/isolated-web-apps/blob/main/Scheme.md#suffix
     * 3. Encode the entire sequence using Base32 without padding.
     *
     * If Chrome is not in IWA dev
     * mode, the installation will fail, regardless of the state of the allowlist.
     */
    install: (params: PWA.InstallParams) => Promise<void>;
    /** Uninstalls the given manifest_id and closes any opened app windows. */
    uninstall: (params: PWA.UninstallParams) => Promise<void>;
    /**
     * Launches the installed web app, or an url in the same web app instead of the
     * default start url if it is provided. Returns a page Target.TargetID which
     * can be used to attach to via Target.attachToTarget or similar APIs.
     */
    launch: (params: PWA.LaunchParams) => Promise<PWA.LaunchReturn>;
    /**
     * Opens one or more local files from an installed web app identified by its
     * manifestId. The web app needs to have file handlers registered to process
     * the files. The API returns one or more page Target.TargetIDs which can be
     * used to attach to via Target.attachToTarget or similar APIs.
     * If some files in the parameters cannot be handled by the web app, they will
     * be ignored. If none of the files can be handled, this API returns an error.
     * If no files are provided as the parameter, this API also returns an error.
     *
     * According to the definition of the file handlers in the manifest file, one
     * Target.TargetID may represent a page handling one or more files. The order
     * of the returned Target.TargetIDs is not guaranteed.
     *
     * TODO(crbug.com/339454034): Check the existences of the input files.
     */
    launchFilesInApp: (params: PWA.LaunchFilesInAppParams) => Promise<PWA.LaunchFilesInAppReturn>;
    /**
     * Opens the current page in its web app identified by the manifest id, needs
     * to be called on a page target. This function returns immediately without
     * waiting for the app to finish loading.
     */
    openCurrentPageInApp: (params: PWA.OpenCurrentPageInAppParams) => Promise<void>;
    /**
     * Changes user settings of the web app identified by its manifestId. If the
     * app was not installed, this command returns an error. Unset parameters will
     * be ignored; unrecognized values will cause an error.
     *
     * Unlike the ones defined in the manifest files of the web apps, these
     * settings are provided by the browser and controlled by the users, they
     * impact the way the browser handling the web apps.
     *
     * See the comment of each parameter.
     */
    changeAppUserSettings: (params: PWA.ChangeAppUserSettingsParams) => Promise<void>;
  };
  Runtime: {
    /** Add handler to promise with given promise object id. */
    awaitPromise: (params: Runtime.AwaitPromiseParams) => Promise<Runtime.AwaitPromiseReturn>;
    /**
     * Calls function with given declaration on the given object. Object group of the result is
     * inherited from the target object.
     */
    callFunctionOn: (params: Runtime.CallFunctionOnParams) => Promise<Runtime.CallFunctionOnReturn>;
    /** Compiles expression. */
    compileScript: (params: Runtime.CompileScriptParams) => Promise<Runtime.CompileScriptReturn>;
    /** Disables reporting of execution contexts creation. */
    disable: () => Promise<void>;
    /** Discards collected exceptions and console API calls. */
    discardConsoleEntries: () => Promise<void>;
    /**
     * Enables reporting of execution contexts creation by means of `executionContextCreated` event.
     * When the reporting gets enabled the event will be sent immediately for each existing execution
     * context.
     */
    enable: () => Promise<void>;
    /** Evaluates expression on global object. */
    evaluate: (params: Runtime.EvaluateParams) => Promise<Runtime.EvaluateReturn>;
    /** Returns the isolate id. */
    getIsolateId: () => Promise<Runtime.GetIsolateIdReturn>;
    /**
     * Returns the JavaScript heap usage.
     * It is the total usage of the corresponding isolate not scoped to a particular Runtime.
     */
    getHeapUsage: () => Promise<Runtime.GetHeapUsageReturn>;
    /**
     * Returns properties of a given object. Object group of the result is inherited from the target
     * object.
     */
    getProperties: (params: Runtime.GetPropertiesParams) => Promise<Runtime.GetPropertiesReturn>;
    /** Returns all let, const and class variables from global scope. */
    globalLexicalScopeNames: (params?: Runtime.GlobalLexicalScopeNamesParams) => Promise<Runtime.GlobalLexicalScopeNamesReturn>;
    queryObjects: (params: Runtime.QueryObjectsParams) => Promise<Runtime.QueryObjectsReturn>;
    /** Releases remote object with given id. */
    releaseObject: (params: Runtime.ReleaseObjectParams) => Promise<void>;
    /** Releases all remote objects that belong to a given group. */
    releaseObjectGroup: (params: Runtime.ReleaseObjectGroupParams) => Promise<void>;
    /** Tells inspected instance to run if it was waiting for debugger to attach. */
    runIfWaitingForDebugger: () => Promise<void>;
    /** Runs script with given id in a given context. */
    runScript: (params: Runtime.RunScriptParams) => Promise<Runtime.RunScriptReturn>;
    setCustomObjectFormatterEnabled: (params: Runtime.SetCustomObjectFormatterEnabledParams) => Promise<void>;
    setMaxCallStackSizeToCapture: (params: Runtime.SetMaxCallStackSizeToCaptureParams) => Promise<void>;
    /**
     * Terminate current or next JavaScript execution.
     * Will cancel the termination when the outer-most script execution ends.
     */
    terminateExecution: () => Promise<void>;
    /**
     * If executionContextId is empty, adds binding with the given name on the
     * global objects of all inspected contexts, including those created later,
     * bindings survive reloads.
     * Binding function takes exactly one argument, this argument should be string,
     * in case of any other input, function throws an exception.
     * Each binding function call produces Runtime.bindingCalled notification.
     */
    addBinding: (params: Runtime.AddBindingParams) => Promise<void>;
    /**
     * This method does not remove binding function from global object but
     * unsubscribes current runtime agent from Runtime.bindingCalled notifications.
     */
    removeBinding: (params: Runtime.RemoveBindingParams) => Promise<void>;
    /**
     * This method tries to lookup and populate exception details for a
     * JavaScript Error object.
     * Note that the stackTrace portion of the resulting exceptionDetails will
     * only be populated if the Runtime domain was enabled at the time when the
     * Error was thrown.
     */
    getExceptionDetails: (params: Runtime.GetExceptionDetailsParams) => Promise<Runtime.GetExceptionDetailsReturn>;
  };
  Schema: {
    /** Returns supported domains. */
    getDomains: () => Promise<Schema.GetDomainsReturn>;
  };
  Security: {
    /** Disables tracking security state changes. */
    disable: () => Promise<void>;
    /** Enables tracking security state changes. */
    enable: () => Promise<void>;
    /** Enable/disable whether all certificate errors should be ignored. */
    setIgnoreCertificateErrors: (params: Security.SetIgnoreCertificateErrorsParams) => Promise<void>;
    /** Handles a certificate error that fired a certificateError event. */
    handleCertificateError: (params: Security.HandleCertificateErrorParams) => Promise<void>;
    /**
     * Enable/disable overriding certificate errors. If enabled, all certificate error events need to
     * be handled by the DevTools client and should be answered with `handleCertificateError` commands.
     */
    setOverrideCertificateErrors: (params: Security.SetOverrideCertificateErrorsParams) => Promise<void>;
  };
  ServiceWorker: {
    deliverPushMessage: (params: ServiceWorker.DeliverPushMessageParams) => Promise<void>;
    disable: () => Promise<void>;
    dispatchSyncEvent: (params: ServiceWorker.DispatchSyncEventParams) => Promise<void>;
    dispatchPeriodicSyncEvent: (params: ServiceWorker.DispatchPeriodicSyncEventParams) => Promise<void>;
    enable: () => Promise<void>;
    setForceUpdateOnPageLoad: (params: ServiceWorker.SetForceUpdateOnPageLoadParams) => Promise<void>;
    skipWaiting: (params: ServiceWorker.SkipWaitingParams) => Promise<void>;
    startWorker: (params: ServiceWorker.StartWorkerParams) => Promise<void>;
    stopAllWorkers: () => Promise<void>;
    stopWorker: (params: ServiceWorker.StopWorkerParams) => Promise<void>;
    unregister: (params: ServiceWorker.UnregisterParams) => Promise<void>;
    updateRegistration: (params: ServiceWorker.UpdateRegistrationParams) => Promise<void>;
  };
  SmartCardEmulation: {
    /** Enables the |SmartCardEmulation| domain. */
    enable: () => Promise<void>;
    /** Disables the |SmartCardEmulation| domain. */
    disable: () => Promise<void>;
    /**
     * Reports the successful result of a |SCardEstablishContext| call.
     *
     * This maps to:
     * PC/SC Lite: https://pcsclite.apdu.fr/api/group__API.html#gaa1b8970169fd4883a6dc4a8f43f19b67
     * Microsoft: https://learn.microsoft.com/en-us/windows/win32/api/winscard/nf-winscard-scardestablishcontext
     */
    reportEstablishContextResult: (params: SmartCardEmulation.ReportEstablishContextResultParams) => Promise<void>;
    /**
     * Reports the successful result of a |SCardReleaseContext| call.
     *
     * This maps to:
     * PC/SC Lite: https://pcsclite.apdu.fr/api/group__API.html#ga6aabcba7744c5c9419fdd6404f73a934
     * Microsoft: https://learn.microsoft.com/en-us/windows/win32/api/winscard/nf-winscard-scardreleasecontext
     */
    reportReleaseContextResult: (params: SmartCardEmulation.ReportReleaseContextResultParams) => Promise<void>;
    /**
     * Reports the successful result of a |SCardListReaders| call.
     *
     * This maps to:
     * PC/SC Lite: https://pcsclite.apdu.fr/api/group__API.html#ga93b07815789b3cf2629d439ecf20f0d9
     * Microsoft: https://learn.microsoft.com/en-us/windows/win32/api/winscard/nf-winscard-scardlistreadersa
     */
    reportListReadersResult: (params: SmartCardEmulation.ReportListReadersResultParams) => Promise<void>;
    /**
     * Reports the successful result of a |SCardGetStatusChange| call.
     *
     * This maps to:
     * PC/SC Lite: https://pcsclite.apdu.fr/api/group__API.html#ga33247d5d1257d59e55647c3bb717db24
     * Microsoft: https://learn.microsoft.com/en-us/windows/win32/api/winscard/nf-winscard-scardgetstatuschangea
     */
    reportGetStatusChangeResult: (params: SmartCardEmulation.ReportGetStatusChangeResultParams) => Promise<void>;
    /**
     * Reports the result of a |SCardBeginTransaction| call.
     * On success, this creates a new transaction object.
     *
     * This maps to:
     * PC/SC Lite: https://pcsclite.apdu.fr/api/group__API.html#gaddb835dce01a0da1d6ca02d33ee7d861
     * Microsoft: https://learn.microsoft.com/en-us/windows/win32/api/winscard/nf-winscard-scardbegintransaction
     */
    reportBeginTransactionResult: (params: SmartCardEmulation.ReportBeginTransactionResultParams) => Promise<void>;
    /**
     * Reports the successful result of a call that returns only a result code.
     * Used for: |SCardCancel|, |SCardDisconnect|, |SCardSetAttrib|, |SCardEndTransaction|.
     *
     * This maps to:
     * 1. SCardCancel
     *    PC/SC Lite: https://pcsclite.apdu.fr/api/group__API.html#gaacbbc0c6d6c0cbbeb4f4debf6fbeeee6
     *    Microsoft: https://learn.microsoft.com/en-us/windows/win32/api/winscard/nf-winscard-scardcancel
     *
     * 2. SCardDisconnect
     *    PC/SC Lite: https://pcsclite.apdu.fr/api/group__API.html#ga4be198045c73ec0deb79e66c0ca1738a
     *    Microsoft: https://learn.microsoft.com/en-us/windows/win32/api/winscard/nf-winscard-scarddisconnect
     *
     * 3. SCardSetAttrib
     *    PC/SC Lite: https://pcsclite.apdu.fr/api/group__API.html#ga060f0038a4ddfd5dd2b8fadf3c3a2e4f
     *    Microsoft: https://learn.microsoft.com/en-us/windows/win32/api/winscard/nf-winscard-scardsetattrib
     *
     * 4. SCardEndTransaction
     *    PC/SC Lite: https://pcsclite.apdu.fr/api/group__API.html#gae8742473b404363e5c587f570d7e2f3b
     *    Microsoft: https://learn.microsoft.com/en-us/windows/win32/api/winscard/nf-winscard-scardendtransaction
     */
    reportPlainResult: (params: SmartCardEmulation.ReportPlainResultParams) => Promise<void>;
    /**
     * Reports the successful result of a |SCardConnect| call.
     *
     * This maps to:
     * PC/SC Lite: https://pcsclite.apdu.fr/api/group__API.html#ga4e515829752e0a8dbc4d630696a8d6a5
     * Microsoft: https://learn.microsoft.com/en-us/windows/win32/api/winscard/nf-winscard-scardconnecta
     */
    reportConnectResult: (params: SmartCardEmulation.ReportConnectResultParams) => Promise<void>;
    /**
     * Reports the successful result of a call that sends back data on success.
     * Used for |SCardTransmit|, |SCardControl|, and |SCardGetAttrib|.
     *
     * This maps to:
     * 1. SCardTransmit
     *    PC/SC Lite: https://pcsclite.apdu.fr/api/group__API.html#ga9a2d77242a271310269065e64633ab99
     *    Microsoft: https://learn.microsoft.com/en-us/windows/win32/api/winscard/nf-winscard-scardtransmit
     *
     * 2. SCardControl
     *    PC/SC Lite: https://pcsclite.apdu.fr/api/group__API.html#gac3454d4657110fd7f753b2d3d8f4e32f
     *    Microsoft: https://learn.microsoft.com/en-us/windows/win32/api/winscard/nf-winscard-scardcontrol
     *
     * 3. SCardGetAttrib
     *    PC/SC Lite: https://pcsclite.apdu.fr/api/group__API.html#gaacfec51917255b7a25b94c5104961602
     *    Microsoft: https://learn.microsoft.com/en-us/windows/win32/api/winscard/nf-winscard-scardgetattrib
     */
    reportDataResult: (params: SmartCardEmulation.ReportDataResultParams) => Promise<void>;
    /**
     * Reports the successful result of a |SCardStatus| call.
     *
     * This maps to:
     * PC/SC Lite: https://pcsclite.apdu.fr/api/group__API.html#gae49c3c894ad7ac12a5b896bde70d0382
     * Microsoft: https://learn.microsoft.com/en-us/windows/win32/api/winscard/nf-winscard-scardstatusa
     */
    reportStatusResult: (params: SmartCardEmulation.ReportStatusResultParams) => Promise<void>;
    /** Reports an error result for the given request. */
    reportError: (params: SmartCardEmulation.ReportErrorParams) => Promise<void>;
  };
  Storage: {
    /**
     * Returns a storage key given a frame id.
     * Deprecated. Please use Storage.getStorageKey instead.
     */
    getStorageKeyForFrame: (params: Storage.GetStorageKeyForFrameParams) => Promise<Storage.GetStorageKeyForFrameReturn>;
    /**
     * Returns storage key for the given frame. If no frame ID is provided,
     * the storage key of the target executing this command is returned.
     */
    getStorageKey: (params?: Storage.GetStorageKeyParams) => Promise<Storage.GetStorageKeyReturn>;
    /** Clears storage for origin. */
    clearDataForOrigin: (params: Storage.ClearDataForOriginParams) => Promise<void>;
    /** Clears storage for storage key. */
    clearDataForStorageKey: (params: Storage.ClearDataForStorageKeyParams) => Promise<void>;
    /** Returns all browser cookies. */
    getCookies: (params?: Storage.GetCookiesParams) => Promise<Storage.GetCookiesReturn>;
    /** Sets given cookies. */
    setCookies: (params: Storage.SetCookiesParams) => Promise<void>;
    /** Clears cookies. */
    clearCookies: (params?: Storage.ClearCookiesParams) => Promise<void>;
    /** Returns usage and quota in bytes. */
    getUsageAndQuota: (params: Storage.GetUsageAndQuotaParams) => Promise<Storage.GetUsageAndQuotaReturn>;
    /** Override quota for the specified origin */
    overrideQuotaForOrigin: (params: Storage.OverrideQuotaForOriginParams) => Promise<void>;
    /** Registers origin to be notified when an update occurs to its cache storage list. */
    trackCacheStorageForOrigin: (params: Storage.TrackCacheStorageForOriginParams) => Promise<void>;
    /** Registers storage key to be notified when an update occurs to its cache storage list. */
    trackCacheStorageForStorageKey: (params: Storage.TrackCacheStorageForStorageKeyParams) => Promise<void>;
    /** Registers origin to be notified when an update occurs to its IndexedDB. */
    trackIndexedDBForOrigin: (params: Storage.TrackIndexedDBForOriginParams) => Promise<void>;
    /** Registers storage key to be notified when an update occurs to its IndexedDB. */
    trackIndexedDBForStorageKey: (params: Storage.TrackIndexedDBForStorageKeyParams) => Promise<void>;
    /** Unregisters origin from receiving notifications for cache storage. */
    untrackCacheStorageForOrigin: (params: Storage.UntrackCacheStorageForOriginParams) => Promise<void>;
    /** Unregisters storage key from receiving notifications for cache storage. */
    untrackCacheStorageForStorageKey: (params: Storage.UntrackCacheStorageForStorageKeyParams) => Promise<void>;
    /** Unregisters origin from receiving notifications for IndexedDB. */
    untrackIndexedDBForOrigin: (params: Storage.UntrackIndexedDBForOriginParams) => Promise<void>;
    /** Unregisters storage key from receiving notifications for IndexedDB. */
    untrackIndexedDBForStorageKey: (params: Storage.UntrackIndexedDBForStorageKeyParams) => Promise<void>;
    /**
     * Returns the number of stored Trust Tokens per issuer for the
     * current browsing context.
     */
    getTrustTokens: () => Promise<Storage.GetTrustTokensReturn>;
    /**
     * Removes all Trust Tokens issued by the provided issuerOrigin.
     * Leaves other stored data, including the issuer's Redemption Records, intact.
     */
    clearTrustTokens: (params: Storage.ClearTrustTokensParams) => Promise<Storage.ClearTrustTokensReturn>;
    /** Gets details for a named interest group. */
    getInterestGroupDetails: (params: Storage.GetInterestGroupDetailsParams) => Promise<Storage.GetInterestGroupDetailsReturn>;
    /** Enables/Disables issuing of interestGroupAccessed events. */
    setInterestGroupTracking: (params: Storage.SetInterestGroupTrackingParams) => Promise<void>;
    /**
     * Enables/Disables issuing of interestGroupAuctionEventOccurred and
     * interestGroupAuctionNetworkRequestCreated.
     */
    setInterestGroupAuctionTracking: (params: Storage.SetInterestGroupAuctionTrackingParams) => Promise<void>;
    /** Gets metadata for an origin's shared storage. */
    getSharedStorageMetadata: (params: Storage.GetSharedStorageMetadataParams) => Promise<Storage.GetSharedStorageMetadataReturn>;
    /** Gets the entries in an given origin's shared storage. */
    getSharedStorageEntries: (params: Storage.GetSharedStorageEntriesParams) => Promise<Storage.GetSharedStorageEntriesReturn>;
    /** Sets entry with `key` and `value` for a given origin's shared storage. */
    setSharedStorageEntry: (params: Storage.SetSharedStorageEntryParams) => Promise<void>;
    /** Deletes entry for `key` (if it exists) for a given origin's shared storage. */
    deleteSharedStorageEntry: (params: Storage.DeleteSharedStorageEntryParams) => Promise<void>;
    /** Clears all entries for a given origin's shared storage. */
    clearSharedStorageEntries: (params: Storage.ClearSharedStorageEntriesParams) => Promise<void>;
    /** Resets the budget for `ownerOrigin` by clearing all budget withdrawals. */
    resetSharedStorageBudget: (params: Storage.ResetSharedStorageBudgetParams) => Promise<void>;
    /** Enables/disables issuing of sharedStorageAccessed events. */
    setSharedStorageTracking: (params: Storage.SetSharedStorageTrackingParams) => Promise<void>;
    /** Set tracking for a storage key's buckets. */
    setStorageBucketTracking: (params: Storage.SetStorageBucketTrackingParams) => Promise<void>;
    /** Deletes the Storage Bucket with the given storage key and bucket name. */
    deleteStorageBucket: (params: Storage.DeleteStorageBucketParams) => Promise<void>;
    /** Deletes state for sites identified as potential bounce trackers, immediately. */
    runBounceTrackingMitigations: () => Promise<Storage.RunBounceTrackingMitigationsReturn>;
    /**
     * Returns the effective Related Website Sets in use by this profile for the browser
     * session. The effective Related Website Sets will not change during a browser session.
     */
    getRelatedWebsiteSets: () => Promise<Storage.GetRelatedWebsiteSetsReturn>;
    setProtectedAudienceKAnonymity: (params: Storage.SetProtectedAudienceKAnonymityParams) => Promise<void>;
  };
  SystemInfo: {
    /** Returns information about the system. */
    getInfo: () => Promise<SystemInfo.GetInfoReturn>;
    /** Returns information about the feature state. */
    getFeatureState: (params: SystemInfo.GetFeatureStateParams) => Promise<SystemInfo.GetFeatureStateReturn>;
    /** Returns information about all running processes. */
    getProcessInfo: () => Promise<SystemInfo.GetProcessInfoReturn>;
  };
  Target: {
    /** Activates (focuses) the target. */
    activateTarget: (params: Target.ActivateTargetParams) => Promise<void>;
    /** Attaches to the target with given id. */
    attachToTarget: (params: Target.AttachToTargetParams) => Promise<Target.AttachToTargetReturn>;
    /** Attaches to the browser target, only uses flat sessionId mode. */
    attachToBrowserTarget: () => Promise<Target.AttachToBrowserTargetReturn>;
    /** Closes the target. If the target is a page that gets closed too. */
    closeTarget: (params: Target.CloseTargetParams) => Promise<Target.CloseTargetReturn>;
    /**
     * Inject object to the target's main frame that provides a communication
     * channel with browser target.
     *
     * Injected object will be available as `window[bindingName]`.
     *
     * The object has the following API:
     * - `binding.send(json)` - a method to send messages over the remote debugging protocol
     * - `binding.onmessage = json => handleMessage(json)` - a callback that will be called for the protocol notifications and command responses.
     */
    exposeDevToolsProtocol: (params: Target.ExposeDevToolsProtocolParams) => Promise<void>;
    /**
     * Creates a new empty BrowserContext. Similar to an incognito profile but you can have more than
     * one.
     */
    createBrowserContext: (params?: Target.CreateBrowserContextParams) => Promise<Target.CreateBrowserContextReturn>;
    /** Returns all browser contexts created with `Target.createBrowserContext` method. */
    getBrowserContexts: () => Promise<Target.GetBrowserContextsReturn>;
    /** Creates a new page. */
    createTarget: (params: Target.CreateTargetParams) => Promise<Target.CreateTargetReturn>;
    /** Detaches session with given id. */
    detachFromTarget: (params?: Target.DetachFromTargetParams) => Promise<void>;
    /**
     * Deletes a BrowserContext. All the belonging pages will be closed without calling their
     * beforeunload hooks.
     */
    disposeBrowserContext: (params: Target.DisposeBrowserContextParams) => Promise<void>;
    /** Returns information about a target. */
    getTargetInfo: (params?: Target.GetTargetInfoParams) => Promise<Target.GetTargetInfoReturn>;
    /** Retrieves a list of available targets. */
    getTargets: (params?: Target.GetTargetsParams) => Promise<Target.GetTargetsReturn>;
    /**
     * Sends protocol message over session with given id.
     * Consider using flat mode instead; see commands attachToTarget, setAutoAttach,
     * and crbug.com/991325.
     */
    sendMessageToTarget: (params: Target.SendMessageToTargetParams) => Promise<void>;
    /**
     * Controls whether to automatically attach to new targets which are considered
     * to be directly related to this one (for example, iframes or workers).
     * When turned on, attaches to all existing related targets as well. When turned off,
     * automatically detaches from all currently attached targets.
     * This also clears all targets added by `autoAttachRelated` from the list of targets to watch
     * for creation of related targets.
     * You might want to call this recursively for auto-attached targets to attach
     * to all available targets.
     */
    setAutoAttach: (params: Target.SetAutoAttachParams) => Promise<void>;
    /**
     * Adds the specified target to the list of targets that will be monitored for any related target
     * creation (such as child frames, child workers and new versions of service worker) and reported
     * through `attachedToTarget`. The specified target is also auto-attached.
     * This cancels the effect of any previous `setAutoAttach` and is also cancelled by subsequent
     * `setAutoAttach`. Only available at the Browser target.
     */
    autoAttachRelated: (params: Target.AutoAttachRelatedParams) => Promise<void>;
    /**
     * Controls whether to discover available targets and notify via
     * `targetCreated/targetInfoChanged/targetDestroyed` events.
     */
    setDiscoverTargets: (params: Target.SetDiscoverTargetsParams) => Promise<void>;
    /**
     * Enables target discovery for the specified locations, when `setDiscoverTargets` was set to
     * `true`.
     */
    setRemoteLocations: (params: Target.SetRemoteLocationsParams) => Promise<void>;
    /**
     * Gets the targetId of the DevTools page target opened for the given target
     * (if any).
     */
    getDevToolsTarget: (params: Target.GetDevToolsTargetParams) => Promise<Target.GetDevToolsTargetReturn>;
    /** Opens a DevTools window for the target. */
    openDevTools: (params: Target.OpenDevToolsParams) => Promise<Target.OpenDevToolsReturn>;
  };
  Tethering: {
    /** Request browser port binding. */
    bind: (params: Tethering.BindParams) => Promise<void>;
    /** Request browser port unbinding. */
    unbind: (params: Tethering.UnbindParams) => Promise<void>;
  };
  Tracing: {
    /** Stop trace events collection. */
    end: () => Promise<void>;
    /** Gets supported tracing categories. */
    getCategories: () => Promise<Tracing.GetCategoriesReturn>;
    /** Return a descriptor for all available tracing categories. */
    getTrackEventDescriptor: () => Promise<Tracing.GetTrackEventDescriptorReturn>;
    /** Record a clock sync marker in the trace. */
    recordClockSyncMarker: (params: Tracing.RecordClockSyncMarkerParams) => Promise<void>;
    /** Request a global memory dump. */
    requestMemoryDump: (params?: Tracing.RequestMemoryDumpParams) => Promise<Tracing.RequestMemoryDumpReturn>;
    /** Start trace events collection. */
    start: (params?: Tracing.StartParams) => Promise<void>;
  };
  WebAudio: {
    /** Enables the WebAudio domain and starts sending context lifetime events. */
    enable: () => Promise<void>;
    /** Disables the WebAudio domain. */
    disable: () => Promise<void>;
    /** Fetch the realtime data from the registered contexts. */
    getRealtimeData: (params: WebAudio.GetRealtimeDataParams) => Promise<WebAudio.GetRealtimeDataReturn>;
  };
  WebAuthn: {
    /**
     * Enable the WebAuthn domain and start intercepting credential storage and
     * retrieval with a virtual authenticator.
     */
    enable: (params?: WebAuthn.EnableParams) => Promise<void>;
    /** Disable the WebAuthn domain. */
    disable: () => Promise<void>;
    /** Creates and adds a virtual authenticator. */
    addVirtualAuthenticator: (params: WebAuthn.AddVirtualAuthenticatorParams) => Promise<WebAuthn.AddVirtualAuthenticatorReturn>;
    /** Resets parameters isBogusSignature, isBadUV, isBadUP to false if they are not present. */
    setResponseOverrideBits: (params: WebAuthn.SetResponseOverrideBitsParams) => Promise<void>;
    /** Removes the given authenticator. */
    removeVirtualAuthenticator: (params: WebAuthn.RemoveVirtualAuthenticatorParams) => Promise<void>;
    /** Adds the credential to the specified authenticator. */
    addCredential: (params: WebAuthn.AddCredentialParams) => Promise<void>;
    /**
     * Returns a single credential stored in the given virtual authenticator that
     * matches the credential ID.
     */
    getCredential: (params: WebAuthn.GetCredentialParams) => Promise<WebAuthn.GetCredentialReturn>;
    /** Returns all the credentials stored in the given virtual authenticator. */
    getCredentials: (params: WebAuthn.GetCredentialsParams) => Promise<WebAuthn.GetCredentialsReturn>;
    /** Removes a credential from the authenticator. */
    removeCredential: (params: WebAuthn.RemoveCredentialParams) => Promise<void>;
    /** Clears all the credentials from the specified device. */
    clearCredentials: (params: WebAuthn.ClearCredentialsParams) => Promise<void>;
    /**
     * Sets whether User Verification succeeds or fails for an authenticator.
     * The default is true.
     */
    setUserVerified: (params: WebAuthn.SetUserVerifiedParams) => Promise<void>;
    /**
     * Sets whether tests of user presence will succeed immediately (if true) or fail to resolve (if false) for an authenticator.
     * The default is true.
     */
    setAutomaticPresenceSimulation: (params: WebAuthn.SetAutomaticPresenceSimulationParams) => Promise<void>;
    /**
     * Allows setting credential properties.
     * https://w3c.github.io/webauthn/#sctn-automation-set-credential-properties
     */
    setCredentialProperties: (params: WebAuthn.SetCredentialPropertiesParams) => Promise<void>;
  };
  WebMCP: {
    /**
     * Enables the WebMCP domain, allowing events to be sent. Enabling the domain will trigger a toolsAdded event for
     * all currently registered tools.
     */
    enable: () => Promise<void>;
    /** Disables the WebMCP domain. */
    disable: () => Promise<void>;
    /** Invokes a registered tool. */
    invokeTool: (params: WebMCP.InvokeToolParams) => Promise<WebMCP.InvokeToolReturn>;
    /** Cancels a pending tool invocation. */
    cancelInvocation: (params: WebMCP.CancelInvocationParams) => Promise<void>;
  };
}

export function bindDomains(t: Transport): Domains {
  return {
    Accessibility: {
      disable: (params?: any) => t._call("Accessibility.disable", params) as any,
      enable: (params?: any) => t._call("Accessibility.enable", params) as any,
      getPartialAXTree: (params?: any) => t._call("Accessibility.getPartialAXTree", params) as any,
      getFullAXTree: (params?: any) => t._call("Accessibility.getFullAXTree", params) as any,
      getRootAXNode: (params?: any) => t._call("Accessibility.getRootAXNode", params) as any,
      getAXNodeAndAncestors: (params?: any) => t._call("Accessibility.getAXNodeAndAncestors", params) as any,
      getChildAXNodes: (params?: any) => t._call("Accessibility.getChildAXNodes", params) as any,
      queryAXTree: (params?: any) => t._call("Accessibility.queryAXTree", params) as any,
    },
    Animation: {
      disable: (params?: any) => t._call("Animation.disable", params) as any,
      enable: (params?: any) => t._call("Animation.enable", params) as any,
      getCurrentTime: (params?: any) => t._call("Animation.getCurrentTime", params) as any,
      getPlaybackRate: (params?: any) => t._call("Animation.getPlaybackRate", params) as any,
      releaseAnimations: (params?: any) => t._call("Animation.releaseAnimations", params) as any,
      resolveAnimation: (params?: any) => t._call("Animation.resolveAnimation", params) as any,
      seekAnimations: (params?: any) => t._call("Animation.seekAnimations", params) as any,
      setPaused: (params?: any) => t._call("Animation.setPaused", params) as any,
      setPlaybackRate: (params?: any) => t._call("Animation.setPlaybackRate", params) as any,
      setTiming: (params?: any) => t._call("Animation.setTiming", params) as any,
    },
    Audits: {
      getEncodedResponse: (params?: any) => t._call("Audits.getEncodedResponse", params) as any,
      disable: (params?: any) => t._call("Audits.disable", params) as any,
      enable: (params?: any) => t._call("Audits.enable", params) as any,
      checkFormsIssues: (params?: any) => t._call("Audits.checkFormsIssues", params) as any,
    },
    Autofill: {
      trigger: (params?: any) => t._call("Autofill.trigger", params) as any,
      setAddresses: (params?: any) => t._call("Autofill.setAddresses", params) as any,
      disable: (params?: any) => t._call("Autofill.disable", params) as any,
      enable: (params?: any) => t._call("Autofill.enable", params) as any,
    },
    BackgroundService: {
      startObserving: (params?: any) => t._call("BackgroundService.startObserving", params) as any,
      stopObserving: (params?: any) => t._call("BackgroundService.stopObserving", params) as any,
      setRecording: (params?: any) => t._call("BackgroundService.setRecording", params) as any,
      clearEvents: (params?: any) => t._call("BackgroundService.clearEvents", params) as any,
    },
    BluetoothEmulation: {
      enable: (params?: any) => t._call("BluetoothEmulation.enable", params) as any,
      setSimulatedCentralState: (params?: any) => t._call("BluetoothEmulation.setSimulatedCentralState", params) as any,
      disable: (params?: any) => t._call("BluetoothEmulation.disable", params) as any,
      simulatePreconnectedPeripheral: (params?: any) => t._call("BluetoothEmulation.simulatePreconnectedPeripheral", params) as any,
      simulateAdvertisement: (params?: any) => t._call("BluetoothEmulation.simulateAdvertisement", params) as any,
      simulateGATTOperationResponse: (params?: any) => t._call("BluetoothEmulation.simulateGATTOperationResponse", params) as any,
      simulateCharacteristicOperationResponse: (params?: any) => t._call("BluetoothEmulation.simulateCharacteristicOperationResponse", params) as any,
      simulateDescriptorOperationResponse: (params?: any) => t._call("BluetoothEmulation.simulateDescriptorOperationResponse", params) as any,
      addService: (params?: any) => t._call("BluetoothEmulation.addService", params) as any,
      removeService: (params?: any) => t._call("BluetoothEmulation.removeService", params) as any,
      addCharacteristic: (params?: any) => t._call("BluetoothEmulation.addCharacteristic", params) as any,
      removeCharacteristic: (params?: any) => t._call("BluetoothEmulation.removeCharacteristic", params) as any,
      addDescriptor: (params?: any) => t._call("BluetoothEmulation.addDescriptor", params) as any,
      removeDescriptor: (params?: any) => t._call("BluetoothEmulation.removeDescriptor", params) as any,
      simulateGATTDisconnection: (params?: any) => t._call("BluetoothEmulation.simulateGATTDisconnection", params) as any,
    },
    Browser: {
      setPermission: (params?: any) => t._call("Browser.setPermission", params) as any,
      grantPermissions: (params?: any) => t._call("Browser.grantPermissions", params) as any,
      resetPermissions: (params?: any) => t._call("Browser.resetPermissions", params) as any,
      setDownloadBehavior: (params?: any) => t._call("Browser.setDownloadBehavior", params) as any,
      cancelDownload: (params?: any) => t._call("Browser.cancelDownload", params) as any,
      close: (params?: any) => t._call("Browser.close", params) as any,
      crash: (params?: any) => t._call("Browser.crash", params) as any,
      crashGpuProcess: (params?: any) => t._call("Browser.crashGpuProcess", params) as any,
      getVersion: (params?: any) => t._call("Browser.getVersion", params) as any,
      getBrowserCommandLine: (params?: any) => t._call("Browser.getBrowserCommandLine", params) as any,
      getHistograms: (params?: any) => t._call("Browser.getHistograms", params) as any,
      getHistogram: (params?: any) => t._call("Browser.getHistogram", params) as any,
      getWindowBounds: (params?: any) => t._call("Browser.getWindowBounds", params) as any,
      getWindowForTarget: (params?: any) => t._call("Browser.getWindowForTarget", params) as any,
      setWindowBounds: (params?: any) => t._call("Browser.setWindowBounds", params) as any,
      setContentsSize: (params?: any) => t._call("Browser.setContentsSize", params) as any,
      setDockTile: (params?: any) => t._call("Browser.setDockTile", params) as any,
      executeBrowserCommand: (params?: any) => t._call("Browser.executeBrowserCommand", params) as any,
      addPrivacySandboxEnrollmentOverride: (params?: any) => t._call("Browser.addPrivacySandboxEnrollmentOverride", params) as any,
      addPrivacySandboxCoordinatorKeyConfig: (params?: any) => t._call("Browser.addPrivacySandboxCoordinatorKeyConfig", params) as any,
    },
    CacheStorage: {
      deleteCache: (params?: any) => t._call("CacheStorage.deleteCache", params) as any,
      deleteEntry: (params?: any) => t._call("CacheStorage.deleteEntry", params) as any,
      requestCacheNames: (params?: any) => t._call("CacheStorage.requestCacheNames", params) as any,
      requestCachedResponse: (params?: any) => t._call("CacheStorage.requestCachedResponse", params) as any,
      requestEntries: (params?: any) => t._call("CacheStorage.requestEntries", params) as any,
    },
    Cast: {
      enable: (params?: any) => t._call("Cast.enable", params) as any,
      disable: (params?: any) => t._call("Cast.disable", params) as any,
      setSinkToUse: (params?: any) => t._call("Cast.setSinkToUse", params) as any,
      startDesktopMirroring: (params?: any) => t._call("Cast.startDesktopMirroring", params) as any,
      startTabMirroring: (params?: any) => t._call("Cast.startTabMirroring", params) as any,
      stopCasting: (params?: any) => t._call("Cast.stopCasting", params) as any,
    },
    Console: {
      clearMessages: (params?: any) => t._call("Console.clearMessages", params) as any,
      disable: (params?: any) => t._call("Console.disable", params) as any,
      enable: (params?: any) => t._call("Console.enable", params) as any,
    },
    CrashReportContext: {
      getEntries: (params?: any) => t._call("CrashReportContext.getEntries", params) as any,
    },
    CSS: {
      addRule: (params?: any) => t._call("CSS.addRule", params) as any,
      collectClassNames: (params?: any) => t._call("CSS.collectClassNames", params) as any,
      createStyleSheet: (params?: any) => t._call("CSS.createStyleSheet", params) as any,
      disable: (params?: any) => t._call("CSS.disable", params) as any,
      enable: (params?: any) => t._call("CSS.enable", params) as any,
      forcePseudoState: (params?: any) => t._call("CSS.forcePseudoState", params) as any,
      forceStartingStyle: (params?: any) => t._call("CSS.forceStartingStyle", params) as any,
      getBackgroundColors: (params?: any) => t._call("CSS.getBackgroundColors", params) as any,
      getComputedStyleForNode: (params?: any) => t._call("CSS.getComputedStyleForNode", params) as any,
      resolveValues: (params?: any) => t._call("CSS.resolveValues", params) as any,
      getLonghandProperties: (params?: any) => t._call("CSS.getLonghandProperties", params) as any,
      getInlineStylesForNode: (params?: any) => t._call("CSS.getInlineStylesForNode", params) as any,
      getAnimatedStylesForNode: (params?: any) => t._call("CSS.getAnimatedStylesForNode", params) as any,
      getMatchedStylesForNode: (params?: any) => t._call("CSS.getMatchedStylesForNode", params) as any,
      getEnvironmentVariables: (params?: any) => t._call("CSS.getEnvironmentVariables", params) as any,
      getMediaQueries: (params?: any) => t._call("CSS.getMediaQueries", params) as any,
      getPlatformFontsForNode: (params?: any) => t._call("CSS.getPlatformFontsForNode", params) as any,
      getStyleSheetText: (params?: any) => t._call("CSS.getStyleSheetText", params) as any,
      getLayersForNode: (params?: any) => t._call("CSS.getLayersForNode", params) as any,
      getLocationForSelector: (params?: any) => t._call("CSS.getLocationForSelector", params) as any,
      trackComputedStyleUpdatesForNode: (params?: any) => t._call("CSS.trackComputedStyleUpdatesForNode", params) as any,
      trackComputedStyleUpdates: (params?: any) => t._call("CSS.trackComputedStyleUpdates", params) as any,
      takeComputedStyleUpdates: (params?: any) => t._call("CSS.takeComputedStyleUpdates", params) as any,
      setEffectivePropertyValueForNode: (params?: any) => t._call("CSS.setEffectivePropertyValueForNode", params) as any,
      setPropertyRulePropertyName: (params?: any) => t._call("CSS.setPropertyRulePropertyName", params) as any,
      setKeyframeKey: (params?: any) => t._call("CSS.setKeyframeKey", params) as any,
      setMediaText: (params?: any) => t._call("CSS.setMediaText", params) as any,
      setContainerQueryText: (params?: any) => t._call("CSS.setContainerQueryText", params) as any,
      setSupportsText: (params?: any) => t._call("CSS.setSupportsText", params) as any,
      setNavigationText: (params?: any) => t._call("CSS.setNavigationText", params) as any,
      setScopeText: (params?: any) => t._call("CSS.setScopeText", params) as any,
      setRuleSelector: (params?: any) => t._call("CSS.setRuleSelector", params) as any,
      setStyleSheetText: (params?: any) => t._call("CSS.setStyleSheetText", params) as any,
      setStyleTexts: (params?: any) => t._call("CSS.setStyleTexts", params) as any,
      startRuleUsageTracking: (params?: any) => t._call("CSS.startRuleUsageTracking", params) as any,
      stopRuleUsageTracking: (params?: any) => t._call("CSS.stopRuleUsageTracking", params) as any,
      takeCoverageDelta: (params?: any) => t._call("CSS.takeCoverageDelta", params) as any,
      setLocalFontsEnabled: (params?: any) => t._call("CSS.setLocalFontsEnabled", params) as any,
    },
    Debugger: {
      continueToLocation: (params?: any) => t._call("Debugger.continueToLocation", params) as any,
      disable: (params?: any) => t._call("Debugger.disable", params) as any,
      enable: (params?: any) => t._call("Debugger.enable", params) as any,
      evaluateOnCallFrame: (params?: any) => t._call("Debugger.evaluateOnCallFrame", params) as any,
      getPossibleBreakpoints: (params?: any) => t._call("Debugger.getPossibleBreakpoints", params) as any,
      getScriptSource: (params?: any) => t._call("Debugger.getScriptSource", params) as any,
      disassembleWasmModule: (params?: any) => t._call("Debugger.disassembleWasmModule", params) as any,
      nextWasmDisassemblyChunk: (params?: any) => t._call("Debugger.nextWasmDisassemblyChunk", params) as any,
      getWasmBytecode: (params?: any) => t._call("Debugger.getWasmBytecode", params) as any,
      getStackTrace: (params?: any) => t._call("Debugger.getStackTrace", params) as any,
      pause: (params?: any) => t._call("Debugger.pause", params) as any,
      pauseOnAsyncCall: (params?: any) => t._call("Debugger.pauseOnAsyncCall", params) as any,
      removeBreakpoint: (params?: any) => t._call("Debugger.removeBreakpoint", params) as any,
      restartFrame: (params?: any) => t._call("Debugger.restartFrame", params) as any,
      resume: (params?: any) => t._call("Debugger.resume", params) as any,
      searchInContent: (params?: any) => t._call("Debugger.searchInContent", params) as any,
      setAsyncCallStackDepth: (params?: any) => t._call("Debugger.setAsyncCallStackDepth", params) as any,
      setBlackboxExecutionContexts: (params?: any) => t._call("Debugger.setBlackboxExecutionContexts", params) as any,
      setBlackboxPatterns: (params?: any) => t._call("Debugger.setBlackboxPatterns", params) as any,
      setBlackboxedRanges: (params?: any) => t._call("Debugger.setBlackboxedRanges", params) as any,
      setBreakpoint: (params?: any) => t._call("Debugger.setBreakpoint", params) as any,
      setInstrumentationBreakpoint: (params?: any) => t._call("Debugger.setInstrumentationBreakpoint", params) as any,
      setBreakpointByUrl: (params?: any) => t._call("Debugger.setBreakpointByUrl", params) as any,
      setBreakpointOnFunctionCall: (params?: any) => t._call("Debugger.setBreakpointOnFunctionCall", params) as any,
      setBreakpointsActive: (params?: any) => t._call("Debugger.setBreakpointsActive", params) as any,
      setPauseOnExceptions: (params?: any) => t._call("Debugger.setPauseOnExceptions", params) as any,
      setReturnValue: (params?: any) => t._call("Debugger.setReturnValue", params) as any,
      setScriptSource: (params?: any) => t._call("Debugger.setScriptSource", params) as any,
      setSkipAllPauses: (params?: any) => t._call("Debugger.setSkipAllPauses", params) as any,
      setVariableValue: (params?: any) => t._call("Debugger.setVariableValue", params) as any,
      stepInto: (params?: any) => t._call("Debugger.stepInto", params) as any,
      stepOut: (params?: any) => t._call("Debugger.stepOut", params) as any,
      stepOver: (params?: any) => t._call("Debugger.stepOver", params) as any,
    },
    DeviceAccess: {
      enable: (params?: any) => t._call("DeviceAccess.enable", params) as any,
      disable: (params?: any) => t._call("DeviceAccess.disable", params) as any,
      selectPrompt: (params?: any) => t._call("DeviceAccess.selectPrompt", params) as any,
      cancelPrompt: (params?: any) => t._call("DeviceAccess.cancelPrompt", params) as any,
    },
    DeviceOrientation: {
      clearDeviceOrientationOverride: (params?: any) => t._call("DeviceOrientation.clearDeviceOrientationOverride", params) as any,
      setDeviceOrientationOverride: (params?: any) => t._call("DeviceOrientation.setDeviceOrientationOverride", params) as any,
    },
    DOM: {
      collectClassNamesFromSubtree: (params?: any) => t._call("DOM.collectClassNamesFromSubtree", params) as any,
      copyTo: (params?: any) => t._call("DOM.copyTo", params) as any,
      describeNode: (params?: any) => t._call("DOM.describeNode", params) as any,
      scrollIntoViewIfNeeded: (params?: any) => t._call("DOM.scrollIntoViewIfNeeded", params) as any,
      disable: (params?: any) => t._call("DOM.disable", params) as any,
      discardSearchResults: (params?: any) => t._call("DOM.discardSearchResults", params) as any,
      enable: (params?: any) => t._call("DOM.enable", params) as any,
      focus: (params?: any) => t._call("DOM.focus", params) as any,
      getAttributes: (params?: any) => t._call("DOM.getAttributes", params) as any,
      getBoxModel: (params?: any) => t._call("DOM.getBoxModel", params) as any,
      getContentQuads: (params?: any) => t._call("DOM.getContentQuads", params) as any,
      getDocument: (params?: any) => t._call("DOM.getDocument", params) as any,
      getFlattenedDocument: (params?: any) => t._call("DOM.getFlattenedDocument", params) as any,
      getNodesForSubtreeByStyle: (params?: any) => t._call("DOM.getNodesForSubtreeByStyle", params) as any,
      getNodeForLocation: (params?: any) => t._call("DOM.getNodeForLocation", params) as any,
      getOuterHTML: (params?: any) => t._call("DOM.getOuterHTML", params) as any,
      getRelayoutBoundary: (params?: any) => t._call("DOM.getRelayoutBoundary", params) as any,
      getSearchResults: (params?: any) => t._call("DOM.getSearchResults", params) as any,
      markUndoableState: (params?: any) => t._call("DOM.markUndoableState", params) as any,
      moveTo: (params?: any) => t._call("DOM.moveTo", params) as any,
      performSearch: (params?: any) => t._call("DOM.performSearch", params) as any,
      pushNodeByPathToFrontend: (params?: any) => t._call("DOM.pushNodeByPathToFrontend", params) as any,
      pushNodesByBackendIdsToFrontend: (params?: any) => t._call("DOM.pushNodesByBackendIdsToFrontend", params) as any,
      querySelector: (params?: any) => t._call("DOM.querySelector", params) as any,
      querySelectorAll: (params?: any) => t._call("DOM.querySelectorAll", params) as any,
      getTopLayerElements: (params?: any) => t._call("DOM.getTopLayerElements", params) as any,
      getElementByRelation: (params?: any) => t._call("DOM.getElementByRelation", params) as any,
      redo: (params?: any) => t._call("DOM.redo", params) as any,
      removeAttribute: (params?: any) => t._call("DOM.removeAttribute", params) as any,
      removeNode: (params?: any) => t._call("DOM.removeNode", params) as any,
      requestChildNodes: (params?: any) => t._call("DOM.requestChildNodes", params) as any,
      requestNode: (params?: any) => t._call("DOM.requestNode", params) as any,
      resolveNode: (params?: any) => t._call("DOM.resolveNode", params) as any,
      setAttributeValue: (params?: any) => t._call("DOM.setAttributeValue", params) as any,
      setAttributesAsText: (params?: any) => t._call("DOM.setAttributesAsText", params) as any,
      setFileInputFiles: (params?: any) => t._call("DOM.setFileInputFiles", params) as any,
      setNodeStackTracesEnabled: (params?: any) => t._call("DOM.setNodeStackTracesEnabled", params) as any,
      getNodeStackTraces: (params?: any) => t._call("DOM.getNodeStackTraces", params) as any,
      getFileInfo: (params?: any) => t._call("DOM.getFileInfo", params) as any,
      getDetachedDomNodes: (params?: any) => t._call("DOM.getDetachedDomNodes", params) as any,
      setInspectedNode: (params?: any) => t._call("DOM.setInspectedNode", params) as any,
      setNodeName: (params?: any) => t._call("DOM.setNodeName", params) as any,
      setNodeValue: (params?: any) => t._call("DOM.setNodeValue", params) as any,
      setOuterHTML: (params?: any) => t._call("DOM.setOuterHTML", params) as any,
      undo: (params?: any) => t._call("DOM.undo", params) as any,
      getFrameOwner: (params?: any) => t._call("DOM.getFrameOwner", params) as any,
      getContainerForNode: (params?: any) => t._call("DOM.getContainerForNode", params) as any,
      getQueryingDescendantsForContainer: (params?: any) => t._call("DOM.getQueryingDescendantsForContainer", params) as any,
      getAnchorElement: (params?: any) => t._call("DOM.getAnchorElement", params) as any,
      forceShowPopover: (params?: any) => t._call("DOM.forceShowPopover", params) as any,
    },
    DOMDebugger: {
      getEventListeners: (params?: any) => t._call("DOMDebugger.getEventListeners", params) as any,
      removeDOMBreakpoint: (params?: any) => t._call("DOMDebugger.removeDOMBreakpoint", params) as any,
      removeEventListenerBreakpoint: (params?: any) => t._call("DOMDebugger.removeEventListenerBreakpoint", params) as any,
      removeXHRBreakpoint: (params?: any) => t._call("DOMDebugger.removeXHRBreakpoint", params) as any,
      setBreakOnCSPViolation: (params?: any) => t._call("DOMDebugger.setBreakOnCSPViolation", params) as any,
      setDOMBreakpoint: (params?: any) => t._call("DOMDebugger.setDOMBreakpoint", params) as any,
      setEventListenerBreakpoint: (params?: any) => t._call("DOMDebugger.setEventListenerBreakpoint", params) as any,
      setXHRBreakpoint: (params?: any) => t._call("DOMDebugger.setXHRBreakpoint", params) as any,
    },
    DOMSnapshot: {
      disable: (params?: any) => t._call("DOMSnapshot.disable", params) as any,
      enable: (params?: any) => t._call("DOMSnapshot.enable", params) as any,
      getSnapshot: (params?: any) => t._call("DOMSnapshot.getSnapshot", params) as any,
      captureSnapshot: (params?: any) => t._call("DOMSnapshot.captureSnapshot", params) as any,
    },
    DOMStorage: {
      clear: (params?: any) => t._call("DOMStorage.clear", params) as any,
      disable: (params?: any) => t._call("DOMStorage.disable", params) as any,
      enable: (params?: any) => t._call("DOMStorage.enable", params) as any,
      getDOMStorageItems: (params?: any) => t._call("DOMStorage.getDOMStorageItems", params) as any,
      removeDOMStorageItem: (params?: any) => t._call("DOMStorage.removeDOMStorageItem", params) as any,
      setDOMStorageItem: (params?: any) => t._call("DOMStorage.setDOMStorageItem", params) as any,
    },
    Emulation: {
      canEmulate: (params?: any) => t._call("Emulation.canEmulate", params) as any,
      clearDeviceMetricsOverride: (params?: any) => t._call("Emulation.clearDeviceMetricsOverride", params) as any,
      clearGeolocationOverride: (params?: any) => t._call("Emulation.clearGeolocationOverride", params) as any,
      resetPageScaleFactor: (params?: any) => t._call("Emulation.resetPageScaleFactor", params) as any,
      setFocusEmulationEnabled: (params?: any) => t._call("Emulation.setFocusEmulationEnabled", params) as any,
      setAutoDarkModeOverride: (params?: any) => t._call("Emulation.setAutoDarkModeOverride", params) as any,
      setCPUThrottlingRate: (params?: any) => t._call("Emulation.setCPUThrottlingRate", params) as any,
      setDefaultBackgroundColorOverride: (params?: any) => t._call("Emulation.setDefaultBackgroundColorOverride", params) as any,
      setSafeAreaInsetsOverride: (params?: any) => t._call("Emulation.setSafeAreaInsetsOverride", params) as any,
      setDeviceMetricsOverride: (params?: any) => t._call("Emulation.setDeviceMetricsOverride", params) as any,
      setDevicePostureOverride: (params?: any) => t._call("Emulation.setDevicePostureOverride", params) as any,
      clearDevicePostureOverride: (params?: any) => t._call("Emulation.clearDevicePostureOverride", params) as any,
      setDisplayFeaturesOverride: (params?: any) => t._call("Emulation.setDisplayFeaturesOverride", params) as any,
      clearDisplayFeaturesOverride: (params?: any) => t._call("Emulation.clearDisplayFeaturesOverride", params) as any,
      setScrollbarsHidden: (params?: any) => t._call("Emulation.setScrollbarsHidden", params) as any,
      setDocumentCookieDisabled: (params?: any) => t._call("Emulation.setDocumentCookieDisabled", params) as any,
      setEmitTouchEventsForMouse: (params?: any) => t._call("Emulation.setEmitTouchEventsForMouse", params) as any,
      setEmulatedMedia: (params?: any) => t._call("Emulation.setEmulatedMedia", params) as any,
      setEmulatedVisionDeficiency: (params?: any) => t._call("Emulation.setEmulatedVisionDeficiency", params) as any,
      setEmulatedOSTextScale: (params?: any) => t._call("Emulation.setEmulatedOSTextScale", params) as any,
      setGeolocationOverride: (params?: any) => t._call("Emulation.setGeolocationOverride", params) as any,
      getOverriddenSensorInformation: (params?: any) => t._call("Emulation.getOverriddenSensorInformation", params) as any,
      setSensorOverrideEnabled: (params?: any) => t._call("Emulation.setSensorOverrideEnabled", params) as any,
      setSensorOverrideReadings: (params?: any) => t._call("Emulation.setSensorOverrideReadings", params) as any,
      setPressureSourceOverrideEnabled: (params?: any) => t._call("Emulation.setPressureSourceOverrideEnabled", params) as any,
      setPressureStateOverride: (params?: any) => t._call("Emulation.setPressureStateOverride", params) as any,
      setPressureDataOverride: (params?: any) => t._call("Emulation.setPressureDataOverride", params) as any,
      setIdleOverride: (params?: any) => t._call("Emulation.setIdleOverride", params) as any,
      clearIdleOverride: (params?: any) => t._call("Emulation.clearIdleOverride", params) as any,
      setNavigatorOverrides: (params?: any) => t._call("Emulation.setNavigatorOverrides", params) as any,
      setPageScaleFactor: (params?: any) => t._call("Emulation.setPageScaleFactor", params) as any,
      setScriptExecutionDisabled: (params?: any) => t._call("Emulation.setScriptExecutionDisabled", params) as any,
      setTouchEmulationEnabled: (params?: any) => t._call("Emulation.setTouchEmulationEnabled", params) as any,
      setVirtualTimePolicy: (params?: any) => t._call("Emulation.setVirtualTimePolicy", params) as any,
      setLocaleOverride: (params?: any) => t._call("Emulation.setLocaleOverride", params) as any,
      setTimezoneOverride: (params?: any) => t._call("Emulation.setTimezoneOverride", params) as any,
      setVisibleSize: (params?: any) => t._call("Emulation.setVisibleSize", params) as any,
      setDisabledImageTypes: (params?: any) => t._call("Emulation.setDisabledImageTypes", params) as any,
      setDataSaverOverride: (params?: any) => t._call("Emulation.setDataSaverOverride", params) as any,
      setHardwareConcurrencyOverride: (params?: any) => t._call("Emulation.setHardwareConcurrencyOverride", params) as any,
      setUserAgentOverride: (params?: any) => t._call("Emulation.setUserAgentOverride", params) as any,
      setAutomationOverride: (params?: any) => t._call("Emulation.setAutomationOverride", params) as any,
      setSmallViewportHeightDifferenceOverride: (params?: any) => t._call("Emulation.setSmallViewportHeightDifferenceOverride", params) as any,
      getScreenInfos: (params?: any) => t._call("Emulation.getScreenInfos", params) as any,
      addScreen: (params?: any) => t._call("Emulation.addScreen", params) as any,
      updateScreen: (params?: any) => t._call("Emulation.updateScreen", params) as any,
      removeScreen: (params?: any) => t._call("Emulation.removeScreen", params) as any,
      setPrimaryScreen: (params?: any) => t._call("Emulation.setPrimaryScreen", params) as any,
    },
    EventBreakpoints: {
      setInstrumentationBreakpoint: (params?: any) => t._call("EventBreakpoints.setInstrumentationBreakpoint", params) as any,
      removeInstrumentationBreakpoint: (params?: any) => t._call("EventBreakpoints.removeInstrumentationBreakpoint", params) as any,
      disable: (params?: any) => t._call("EventBreakpoints.disable", params) as any,
    },
    Extensions: {
      triggerAction: (params?: any) => t._call("Extensions.triggerAction", params) as any,
      loadUnpacked: (params?: any) => t._call("Extensions.loadUnpacked", params) as any,
      getExtensions: (params?: any) => t._call("Extensions.getExtensions", params) as any,
      uninstall: (params?: any) => t._call("Extensions.uninstall", params) as any,
      getStorageItems: (params?: any) => t._call("Extensions.getStorageItems", params) as any,
      removeStorageItems: (params?: any) => t._call("Extensions.removeStorageItems", params) as any,
      clearStorageItems: (params?: any) => t._call("Extensions.clearStorageItems", params) as any,
      setStorageItems: (params?: any) => t._call("Extensions.setStorageItems", params) as any,
    },
    FedCm: {
      enable: (params?: any) => t._call("FedCm.enable", params) as any,
      disable: (params?: any) => t._call("FedCm.disable", params) as any,
      selectAccount: (params?: any) => t._call("FedCm.selectAccount", params) as any,
      clickDialogButton: (params?: any) => t._call("FedCm.clickDialogButton", params) as any,
      openUrl: (params?: any) => t._call("FedCm.openUrl", params) as any,
      dismissDialog: (params?: any) => t._call("FedCm.dismissDialog", params) as any,
      resetCooldown: (params?: any) => t._call("FedCm.resetCooldown", params) as any,
    },
    Fetch: {
      disable: (params?: any) => t._call("Fetch.disable", params) as any,
      enable: (params?: any) => t._call("Fetch.enable", params) as any,
      failRequest: (params?: any) => t._call("Fetch.failRequest", params) as any,
      fulfillRequest: (params?: any) => t._call("Fetch.fulfillRequest", params) as any,
      continueRequest: (params?: any) => t._call("Fetch.continueRequest", params) as any,
      continueWithAuth: (params?: any) => t._call("Fetch.continueWithAuth", params) as any,
      continueResponse: (params?: any) => t._call("Fetch.continueResponse", params) as any,
      getResponseBody: (params?: any) => t._call("Fetch.getResponseBody", params) as any,
      takeResponseBodyAsStream: (params?: any) => t._call("Fetch.takeResponseBodyAsStream", params) as any,
    },
    FileSystem: {
      getDirectory: (params?: any) => t._call("FileSystem.getDirectory", params) as any,
    },
    HeadlessExperimental: {
      beginFrame: (params?: any) => t._call("HeadlessExperimental.beginFrame", params) as any,
      disable: (params?: any) => t._call("HeadlessExperimental.disable", params) as any,
      enable: (params?: any) => t._call("HeadlessExperimental.enable", params) as any,
    },
    HeapProfiler: {
      addInspectedHeapObject: (params?: any) => t._call("HeapProfiler.addInspectedHeapObject", params) as any,
      collectGarbage: (params?: any) => t._call("HeapProfiler.collectGarbage", params) as any,
      disable: (params?: any) => t._call("HeapProfiler.disable", params) as any,
      enable: (params?: any) => t._call("HeapProfiler.enable", params) as any,
      getHeapObjectId: (params?: any) => t._call("HeapProfiler.getHeapObjectId", params) as any,
      getObjectByHeapObjectId: (params?: any) => t._call("HeapProfiler.getObjectByHeapObjectId", params) as any,
      getSamplingProfile: (params?: any) => t._call("HeapProfiler.getSamplingProfile", params) as any,
      startSampling: (params?: any) => t._call("HeapProfiler.startSampling", params) as any,
      startTrackingHeapObjects: (params?: any) => t._call("HeapProfiler.startTrackingHeapObjects", params) as any,
      stopSampling: (params?: any) => t._call("HeapProfiler.stopSampling", params) as any,
      stopTrackingHeapObjects: (params?: any) => t._call("HeapProfiler.stopTrackingHeapObjects", params) as any,
      takeHeapSnapshot: (params?: any) => t._call("HeapProfiler.takeHeapSnapshot", params) as any,
    },
    IndexedDB: {
      clearObjectStore: (params?: any) => t._call("IndexedDB.clearObjectStore", params) as any,
      deleteDatabase: (params?: any) => t._call("IndexedDB.deleteDatabase", params) as any,
      deleteObjectStoreEntries: (params?: any) => t._call("IndexedDB.deleteObjectStoreEntries", params) as any,
      disable: (params?: any) => t._call("IndexedDB.disable", params) as any,
      enable: (params?: any) => t._call("IndexedDB.enable", params) as any,
      requestData: (params?: any) => t._call("IndexedDB.requestData", params) as any,
      getMetadata: (params?: any) => t._call("IndexedDB.getMetadata", params) as any,
      requestDatabase: (params?: any) => t._call("IndexedDB.requestDatabase", params) as any,
      requestDatabaseNames: (params?: any) => t._call("IndexedDB.requestDatabaseNames", params) as any,
    },
    Input: {
      dispatchDragEvent: (params?: any) => t._call("Input.dispatchDragEvent", params) as any,
      dispatchKeyEvent: (params?: any) => t._call("Input.dispatchKeyEvent", params) as any,
      insertText: (params?: any) => t._call("Input.insertText", params) as any,
      imeSetComposition: (params?: any) => t._call("Input.imeSetComposition", params) as any,
      dispatchMouseEvent: (params?: any) => t._call("Input.dispatchMouseEvent", params) as any,
      dispatchTouchEvent: (params?: any) => t._call("Input.dispatchTouchEvent", params) as any,
      cancelDragging: (params?: any) => t._call("Input.cancelDragging", params) as any,
      emulateTouchFromMouseEvent: (params?: any) => t._call("Input.emulateTouchFromMouseEvent", params) as any,
      setIgnoreInputEvents: (params?: any) => t._call("Input.setIgnoreInputEvents", params) as any,
      setInterceptDrags: (params?: any) => t._call("Input.setInterceptDrags", params) as any,
      synthesizePinchGesture: (params?: any) => t._call("Input.synthesizePinchGesture", params) as any,
      synthesizeScrollGesture: (params?: any) => t._call("Input.synthesizeScrollGesture", params) as any,
      synthesizeTapGesture: (params?: any) => t._call("Input.synthesizeTapGesture", params) as any,
    },
    Inspector: {
      disable: (params?: any) => t._call("Inspector.disable", params) as any,
      enable: (params?: any) => t._call("Inspector.enable", params) as any,
    },
    IO: {
      close: (params?: any) => t._call("IO.close", params) as any,
      read: (params?: any) => t._call("IO.read", params) as any,
      resolveBlob: (params?: any) => t._call("IO.resolveBlob", params) as any,
    },
    LayerTree: {
      compositingReasons: (params?: any) => t._call("LayerTree.compositingReasons", params) as any,
      disable: (params?: any) => t._call("LayerTree.disable", params) as any,
      enable: (params?: any) => t._call("LayerTree.enable", params) as any,
      loadSnapshot: (params?: any) => t._call("LayerTree.loadSnapshot", params) as any,
      makeSnapshot: (params?: any) => t._call("LayerTree.makeSnapshot", params) as any,
      profileSnapshot: (params?: any) => t._call("LayerTree.profileSnapshot", params) as any,
      releaseSnapshot: (params?: any) => t._call("LayerTree.releaseSnapshot", params) as any,
      replaySnapshot: (params?: any) => t._call("LayerTree.replaySnapshot", params) as any,
      snapshotCommandLog: (params?: any) => t._call("LayerTree.snapshotCommandLog", params) as any,
    },
    Log: {
      clear: (params?: any) => t._call("Log.clear", params) as any,
      disable: (params?: any) => t._call("Log.disable", params) as any,
      enable: (params?: any) => t._call("Log.enable", params) as any,
      startViolationsReport: (params?: any) => t._call("Log.startViolationsReport", params) as any,
      stopViolationsReport: (params?: any) => t._call("Log.stopViolationsReport", params) as any,
    },
    Media: {
      enable: (params?: any) => t._call("Media.enable", params) as any,
      disable: (params?: any) => t._call("Media.disable", params) as any,
    },
    Memory: {
      getDOMCounters: (params?: any) => t._call("Memory.getDOMCounters", params) as any,
      getDOMCountersForLeakDetection: (params?: any) => t._call("Memory.getDOMCountersForLeakDetection", params) as any,
      prepareForLeakDetection: (params?: any) => t._call("Memory.prepareForLeakDetection", params) as any,
      forciblyPurgeJavaScriptMemory: (params?: any) => t._call("Memory.forciblyPurgeJavaScriptMemory", params) as any,
      setPressureNotificationsSuppressed: (params?: any) => t._call("Memory.setPressureNotificationsSuppressed", params) as any,
      simulatePressureNotification: (params?: any) => t._call("Memory.simulatePressureNotification", params) as any,
      startSampling: (params?: any) => t._call("Memory.startSampling", params) as any,
      stopSampling: (params?: any) => t._call("Memory.stopSampling", params) as any,
      getAllTimeSamplingProfile: (params?: any) => t._call("Memory.getAllTimeSamplingProfile", params) as any,
      getBrowserSamplingProfile: (params?: any) => t._call("Memory.getBrowserSamplingProfile", params) as any,
      getSamplingProfile: (params?: any) => t._call("Memory.getSamplingProfile", params) as any,
    },
    Network: {
      setAcceptedEncodings: (params?: any) => t._call("Network.setAcceptedEncodings", params) as any,
      clearAcceptedEncodingsOverride: (params?: any) => t._call("Network.clearAcceptedEncodingsOverride", params) as any,
      canClearBrowserCache: (params?: any) => t._call("Network.canClearBrowserCache", params) as any,
      canClearBrowserCookies: (params?: any) => t._call("Network.canClearBrowserCookies", params) as any,
      canEmulateNetworkConditions: (params?: any) => t._call("Network.canEmulateNetworkConditions", params) as any,
      clearBrowserCache: (params?: any) => t._call("Network.clearBrowserCache", params) as any,
      clearBrowserCookies: (params?: any) => t._call("Network.clearBrowserCookies", params) as any,
      continueInterceptedRequest: (params?: any) => t._call("Network.continueInterceptedRequest", params) as any,
      deleteCookies: (params?: any) => t._call("Network.deleteCookies", params) as any,
      disable: (params?: any) => t._call("Network.disable", params) as any,
      emulateNetworkConditions: (params?: any) => t._call("Network.emulateNetworkConditions", params) as any,
      emulateNetworkConditionsByRule: (params?: any) => t._call("Network.emulateNetworkConditionsByRule", params) as any,
      overrideNetworkState: (params?: any) => t._call("Network.overrideNetworkState", params) as any,
      enable: (params?: any) => t._call("Network.enable", params) as any,
      configureDurableMessages: (params?: any) => t._call("Network.configureDurableMessages", params) as any,
      getAllCookies: (params?: any) => t._call("Network.getAllCookies", params) as any,
      getCertificate: (params?: any) => t._call("Network.getCertificate", params) as any,
      getCookies: (params?: any) => t._call("Network.getCookies", params) as any,
      getResponseBody: (params?: any) => t._call("Network.getResponseBody", params) as any,
      getRequestPostData: (params?: any) => t._call("Network.getRequestPostData", params) as any,
      getResponseBodyForInterception: (params?: any) => t._call("Network.getResponseBodyForInterception", params) as any,
      takeResponseBodyForInterceptionAsStream: (params?: any) => t._call("Network.takeResponseBodyForInterceptionAsStream", params) as any,
      replayXHR: (params?: any) => t._call("Network.replayXHR", params) as any,
      searchInResponseBody: (params?: any) => t._call("Network.searchInResponseBody", params) as any,
      setBlockedURLs: (params?: any) => t._call("Network.setBlockedURLs", params) as any,
      setBypassServiceWorker: (params?: any) => t._call("Network.setBypassServiceWorker", params) as any,
      setCacheDisabled: (params?: any) => t._call("Network.setCacheDisabled", params) as any,
      setCookie: (params?: any) => t._call("Network.setCookie", params) as any,
      setCookies: (params?: any) => t._call("Network.setCookies", params) as any,
      setExtraHTTPHeaders: (params?: any) => t._call("Network.setExtraHTTPHeaders", params) as any,
      setAttachDebugStack: (params?: any) => t._call("Network.setAttachDebugStack", params) as any,
      setRequestInterception: (params?: any) => t._call("Network.setRequestInterception", params) as any,
      streamResourceContent: (params?: any) => t._call("Network.streamResourceContent", params) as any,
      getSecurityIsolationStatus: (params?: any) => t._call("Network.getSecurityIsolationStatus", params) as any,
      enableReportingApi: (params?: any) => t._call("Network.enableReportingApi", params) as any,
      enableDeviceBoundSessions: (params?: any) => t._call("Network.enableDeviceBoundSessions", params) as any,
      deleteDeviceBoundSession: (params?: any) => t._call("Network.deleteDeviceBoundSession", params) as any,
      fetchSchemefulSite: (params?: any) => t._call("Network.fetchSchemefulSite", params) as any,
      loadNetworkResource: (params?: any) => t._call("Network.loadNetworkResource", params) as any,
      setCookieControls: (params?: any) => t._call("Network.setCookieControls", params) as any,
    },
    Overlay: {
      disable: (params?: any) => t._call("Overlay.disable", params) as any,
      enable: (params?: any) => t._call("Overlay.enable", params) as any,
      getHighlightObjectForTest: (params?: any) => t._call("Overlay.getHighlightObjectForTest", params) as any,
      getGridHighlightObjectsForTest: (params?: any) => t._call("Overlay.getGridHighlightObjectsForTest", params) as any,
      getSourceOrderHighlightObjectForTest: (params?: any) => t._call("Overlay.getSourceOrderHighlightObjectForTest", params) as any,
      hideHighlight: (params?: any) => t._call("Overlay.hideHighlight", params) as any,
      highlightFrame: (params?: any) => t._call("Overlay.highlightFrame", params) as any,
      highlightNode: (params?: any) => t._call("Overlay.highlightNode", params) as any,
      highlightQuad: (params?: any) => t._call("Overlay.highlightQuad", params) as any,
      highlightRect: (params?: any) => t._call("Overlay.highlightRect", params) as any,
      highlightSourceOrder: (params?: any) => t._call("Overlay.highlightSourceOrder", params) as any,
      setInspectMode: (params?: any) => t._call("Overlay.setInspectMode", params) as any,
      setShowAdHighlights: (params?: any) => t._call("Overlay.setShowAdHighlights", params) as any,
      setPausedInDebuggerMessage: (params?: any) => t._call("Overlay.setPausedInDebuggerMessage", params) as any,
      setShowDebugBorders: (params?: any) => t._call("Overlay.setShowDebugBorders", params) as any,
      setShowFPSCounter: (params?: any) => t._call("Overlay.setShowFPSCounter", params) as any,
      setShowGridOverlays: (params?: any) => t._call("Overlay.setShowGridOverlays", params) as any,
      setShowFlexOverlays: (params?: any) => t._call("Overlay.setShowFlexOverlays", params) as any,
      setShowScrollSnapOverlays: (params?: any) => t._call("Overlay.setShowScrollSnapOverlays", params) as any,
      setShowContainerQueryOverlays: (params?: any) => t._call("Overlay.setShowContainerQueryOverlays", params) as any,
      setShowInspectedElementAnchor: (params?: any) => t._call("Overlay.setShowInspectedElementAnchor", params) as any,
      setShowPaintRects: (params?: any) => t._call("Overlay.setShowPaintRects", params) as any,
      setShowLayoutShiftRegions: (params?: any) => t._call("Overlay.setShowLayoutShiftRegions", params) as any,
      setShowScrollBottleneckRects: (params?: any) => t._call("Overlay.setShowScrollBottleneckRects", params) as any,
      setShowHitTestBorders: (params?: any) => t._call("Overlay.setShowHitTestBorders", params) as any,
      setShowWebVitals: (params?: any) => t._call("Overlay.setShowWebVitals", params) as any,
      setShowViewportSizeOnResize: (params?: any) => t._call("Overlay.setShowViewportSizeOnResize", params) as any,
      setShowHinge: (params?: any) => t._call("Overlay.setShowHinge", params) as any,
      setShowIsolatedElements: (params?: any) => t._call("Overlay.setShowIsolatedElements", params) as any,
      setShowWindowControlsOverlay: (params?: any) => t._call("Overlay.setShowWindowControlsOverlay", params) as any,
    },
    Page: {
      addScriptToEvaluateOnLoad: (params?: any) => t._call("Page.addScriptToEvaluateOnLoad", params) as any,
      addScriptToEvaluateOnNewDocument: (params?: any) => t._call("Page.addScriptToEvaluateOnNewDocument", params) as any,
      bringToFront: (params?: any) => t._call("Page.bringToFront", params) as any,
      captureScreenshot: (params?: any) => t._call("Page.captureScreenshot", params) as any,
      captureSnapshot: (params?: any) => t._call("Page.captureSnapshot", params) as any,
      createIsolatedWorld: (params?: any) => t._call("Page.createIsolatedWorld", params) as any,
      disable: (params?: any) => t._call("Page.disable", params) as any,
      enable: (params?: any) => t._call("Page.enable", params) as any,
      getAppManifest: (params?: any) => t._call("Page.getAppManifest", params) as any,
      getInstallabilityErrors: (params?: any) => t._call("Page.getInstallabilityErrors", params) as any,
      getManifestIcons: (params?: any) => t._call("Page.getManifestIcons", params) as any,
      getAppId: (params?: any) => t._call("Page.getAppId", params) as any,
      getAdScriptAncestry: (params?: any) => t._call("Page.getAdScriptAncestry", params) as any,
      getFrameTree: (params?: any) => t._call("Page.getFrameTree", params) as any,
      getLayoutMetrics: (params?: any) => t._call("Page.getLayoutMetrics", params) as any,
      getNavigationHistory: (params?: any) => t._call("Page.getNavigationHistory", params) as any,
      resetNavigationHistory: (params?: any) => t._call("Page.resetNavigationHistory", params) as any,
      getResourceContent: (params?: any) => t._call("Page.getResourceContent", params) as any,
      getResourceTree: (params?: any) => t._call("Page.getResourceTree", params) as any,
      handleJavaScriptDialog: (params?: any) => t._call("Page.handleJavaScriptDialog", params) as any,
      navigate: (params?: any) => t._call("Page.navigate", params) as any,
      navigateToHistoryEntry: (params?: any) => t._call("Page.navigateToHistoryEntry", params) as any,
      printToPDF: (params?: any) => t._call("Page.printToPDF", params) as any,
      reload: (params?: any) => t._call("Page.reload", params) as any,
      removeScriptToEvaluateOnLoad: (params?: any) => t._call("Page.removeScriptToEvaluateOnLoad", params) as any,
      removeScriptToEvaluateOnNewDocument: (params?: any) => t._call("Page.removeScriptToEvaluateOnNewDocument", params) as any,
      screencastFrameAck: (params?: any) => t._call("Page.screencastFrameAck", params) as any,
      searchInResource: (params?: any) => t._call("Page.searchInResource", params) as any,
      setAdBlockingEnabled: (params?: any) => t._call("Page.setAdBlockingEnabled", params) as any,
      setBypassCSP: (params?: any) => t._call("Page.setBypassCSP", params) as any,
      getPermissionsPolicyState: (params?: any) => t._call("Page.getPermissionsPolicyState", params) as any,
      getOriginTrials: (params?: any) => t._call("Page.getOriginTrials", params) as any,
      setFontFamilies: (params?: any) => t._call("Page.setFontFamilies", params) as any,
      setFontSizes: (params?: any) => t._call("Page.setFontSizes", params) as any,
      setDocumentContent: (params?: any) => t._call("Page.setDocumentContent", params) as any,
      setDownloadBehavior: (params?: any) => t._call("Page.setDownloadBehavior", params) as any,
      setLifecycleEventsEnabled: (params?: any) => t._call("Page.setLifecycleEventsEnabled", params) as any,
      startScreencast: (params?: any) => t._call("Page.startScreencast", params) as any,
      stopLoading: (params?: any) => t._call("Page.stopLoading", params) as any,
      crash: (params?: any) => t._call("Page.crash", params) as any,
      close: (params?: any) => t._call("Page.close", params) as any,
      setWebLifecycleState: (params?: any) => t._call("Page.setWebLifecycleState", params) as any,
      stopScreencast: (params?: any) => t._call("Page.stopScreencast", params) as any,
      produceCompilationCache: (params?: any) => t._call("Page.produceCompilationCache", params) as any,
      addCompilationCache: (params?: any) => t._call("Page.addCompilationCache", params) as any,
      clearCompilationCache: (params?: any) => t._call("Page.clearCompilationCache", params) as any,
      setSPCTransactionMode: (params?: any) => t._call("Page.setSPCTransactionMode", params) as any,
      setRPHRegistrationMode: (params?: any) => t._call("Page.setRPHRegistrationMode", params) as any,
      generateTestReport: (params?: any) => t._call("Page.generateTestReport", params) as any,
      waitForDebugger: (params?: any) => t._call("Page.waitForDebugger", params) as any,
      setInterceptFileChooserDialog: (params?: any) => t._call("Page.setInterceptFileChooserDialog", params) as any,
      setPrerenderingAllowed: (params?: any) => t._call("Page.setPrerenderingAllowed", params) as any,
      getAnnotatedPageContent: (params?: any) => t._call("Page.getAnnotatedPageContent", params) as any,
    },
    Performance: {
      disable: (params?: any) => t._call("Performance.disable", params) as any,
      enable: (params?: any) => t._call("Performance.enable", params) as any,
      setTimeDomain: (params?: any) => t._call("Performance.setTimeDomain", params) as any,
      getMetrics: (params?: any) => t._call("Performance.getMetrics", params) as any,
    },
    PerformanceTimeline: {
      enable: (params?: any) => t._call("PerformanceTimeline.enable", params) as any,
    },
    Preload: {
      enable: (params?: any) => t._call("Preload.enable", params) as any,
      disable: (params?: any) => t._call("Preload.disable", params) as any,
    },
    Profiler: {
      disable: (params?: any) => t._call("Profiler.disable", params) as any,
      enable: (params?: any) => t._call("Profiler.enable", params) as any,
      getBestEffortCoverage: (params?: any) => t._call("Profiler.getBestEffortCoverage", params) as any,
      setSamplingInterval: (params?: any) => t._call("Profiler.setSamplingInterval", params) as any,
      start: (params?: any) => t._call("Profiler.start", params) as any,
      startPreciseCoverage: (params?: any) => t._call("Profiler.startPreciseCoverage", params) as any,
      stop: (params?: any) => t._call("Profiler.stop", params) as any,
      stopPreciseCoverage: (params?: any) => t._call("Profiler.stopPreciseCoverage", params) as any,
      takePreciseCoverage: (params?: any) => t._call("Profiler.takePreciseCoverage", params) as any,
    },
    PWA: {
      getOsAppState: (params?: any) => t._call("PWA.getOsAppState", params) as any,
      install: (params?: any) => t._call("PWA.install", params) as any,
      uninstall: (params?: any) => t._call("PWA.uninstall", params) as any,
      launch: (params?: any) => t._call("PWA.launch", params) as any,
      launchFilesInApp: (params?: any) => t._call("PWA.launchFilesInApp", params) as any,
      openCurrentPageInApp: (params?: any) => t._call("PWA.openCurrentPageInApp", params) as any,
      changeAppUserSettings: (params?: any) => t._call("PWA.changeAppUserSettings", params) as any,
    },
    Runtime: {
      awaitPromise: (params?: any) => t._call("Runtime.awaitPromise", params) as any,
      callFunctionOn: (params?: any) => t._call("Runtime.callFunctionOn", params) as any,
      compileScript: (params?: any) => t._call("Runtime.compileScript", params) as any,
      disable: (params?: any) => t._call("Runtime.disable", params) as any,
      discardConsoleEntries: (params?: any) => t._call("Runtime.discardConsoleEntries", params) as any,
      enable: (params?: any) => t._call("Runtime.enable", params) as any,
      evaluate: (params?: any) => t._call("Runtime.evaluate", params) as any,
      getIsolateId: (params?: any) => t._call("Runtime.getIsolateId", params) as any,
      getHeapUsage: (params?: any) => t._call("Runtime.getHeapUsage", params) as any,
      getProperties: (params?: any) => t._call("Runtime.getProperties", params) as any,
      globalLexicalScopeNames: (params?: any) => t._call("Runtime.globalLexicalScopeNames", params) as any,
      queryObjects: (params?: any) => t._call("Runtime.queryObjects", params) as any,
      releaseObject: (params?: any) => t._call("Runtime.releaseObject", params) as any,
      releaseObjectGroup: (params?: any) => t._call("Runtime.releaseObjectGroup", params) as any,
      runIfWaitingForDebugger: (params?: any) => t._call("Runtime.runIfWaitingForDebugger", params) as any,
      runScript: (params?: any) => t._call("Runtime.runScript", params) as any,
      setCustomObjectFormatterEnabled: (params?: any) => t._call("Runtime.setCustomObjectFormatterEnabled", params) as any,
      setMaxCallStackSizeToCapture: (params?: any) => t._call("Runtime.setMaxCallStackSizeToCapture", params) as any,
      terminateExecution: (params?: any) => t._call("Runtime.terminateExecution", params) as any,
      addBinding: (params?: any) => t._call("Runtime.addBinding", params) as any,
      removeBinding: (params?: any) => t._call("Runtime.removeBinding", params) as any,
      getExceptionDetails: (params?: any) => t._call("Runtime.getExceptionDetails", params) as any,
    },
    Schema: {
      getDomains: (params?: any) => t._call("Schema.getDomains", params) as any,
    },
    Security: {
      disable: (params?: any) => t._call("Security.disable", params) as any,
      enable: (params?: any) => t._call("Security.enable", params) as any,
      setIgnoreCertificateErrors: (params?: any) => t._call("Security.setIgnoreCertificateErrors", params) as any,
      handleCertificateError: (params?: any) => t._call("Security.handleCertificateError", params) as any,
      setOverrideCertificateErrors: (params?: any) => t._call("Security.setOverrideCertificateErrors", params) as any,
    },
    ServiceWorker: {
      deliverPushMessage: (params?: any) => t._call("ServiceWorker.deliverPushMessage", params) as any,
      disable: (params?: any) => t._call("ServiceWorker.disable", params) as any,
      dispatchSyncEvent: (params?: any) => t._call("ServiceWorker.dispatchSyncEvent", params) as any,
      dispatchPeriodicSyncEvent: (params?: any) => t._call("ServiceWorker.dispatchPeriodicSyncEvent", params) as any,
      enable: (params?: any) => t._call("ServiceWorker.enable", params) as any,
      setForceUpdateOnPageLoad: (params?: any) => t._call("ServiceWorker.setForceUpdateOnPageLoad", params) as any,
      skipWaiting: (params?: any) => t._call("ServiceWorker.skipWaiting", params) as any,
      startWorker: (params?: any) => t._call("ServiceWorker.startWorker", params) as any,
      stopAllWorkers: (params?: any) => t._call("ServiceWorker.stopAllWorkers", params) as any,
      stopWorker: (params?: any) => t._call("ServiceWorker.stopWorker", params) as any,
      unregister: (params?: any) => t._call("ServiceWorker.unregister", params) as any,
      updateRegistration: (params?: any) => t._call("ServiceWorker.updateRegistration", params) as any,
    },
    SmartCardEmulation: {
      enable: (params?: any) => t._call("SmartCardEmulation.enable", params) as any,
      disable: (params?: any) => t._call("SmartCardEmulation.disable", params) as any,
      reportEstablishContextResult: (params?: any) => t._call("SmartCardEmulation.reportEstablishContextResult", params) as any,
      reportReleaseContextResult: (params?: any) => t._call("SmartCardEmulation.reportReleaseContextResult", params) as any,
      reportListReadersResult: (params?: any) => t._call("SmartCardEmulation.reportListReadersResult", params) as any,
      reportGetStatusChangeResult: (params?: any) => t._call("SmartCardEmulation.reportGetStatusChangeResult", params) as any,
      reportBeginTransactionResult: (params?: any) => t._call("SmartCardEmulation.reportBeginTransactionResult", params) as any,
      reportPlainResult: (params?: any) => t._call("SmartCardEmulation.reportPlainResult", params) as any,
      reportConnectResult: (params?: any) => t._call("SmartCardEmulation.reportConnectResult", params) as any,
      reportDataResult: (params?: any) => t._call("SmartCardEmulation.reportDataResult", params) as any,
      reportStatusResult: (params?: any) => t._call("SmartCardEmulation.reportStatusResult", params) as any,
      reportError: (params?: any) => t._call("SmartCardEmulation.reportError", params) as any,
    },
    Storage: {
      getStorageKeyForFrame: (params?: any) => t._call("Storage.getStorageKeyForFrame", params) as any,
      getStorageKey: (params?: any) => t._call("Storage.getStorageKey", params) as any,
      clearDataForOrigin: (params?: any) => t._call("Storage.clearDataForOrigin", params) as any,
      clearDataForStorageKey: (params?: any) => t._call("Storage.clearDataForStorageKey", params) as any,
      getCookies: (params?: any) => t._call("Storage.getCookies", params) as any,
      setCookies: (params?: any) => t._call("Storage.setCookies", params) as any,
      clearCookies: (params?: any) => t._call("Storage.clearCookies", params) as any,
      getUsageAndQuota: (params?: any) => t._call("Storage.getUsageAndQuota", params) as any,
      overrideQuotaForOrigin: (params?: any) => t._call("Storage.overrideQuotaForOrigin", params) as any,
      trackCacheStorageForOrigin: (params?: any) => t._call("Storage.trackCacheStorageForOrigin", params) as any,
      trackCacheStorageForStorageKey: (params?: any) => t._call("Storage.trackCacheStorageForStorageKey", params) as any,
      trackIndexedDBForOrigin: (params?: any) => t._call("Storage.trackIndexedDBForOrigin", params) as any,
      trackIndexedDBForStorageKey: (params?: any) => t._call("Storage.trackIndexedDBForStorageKey", params) as any,
      untrackCacheStorageForOrigin: (params?: any) => t._call("Storage.untrackCacheStorageForOrigin", params) as any,
      untrackCacheStorageForStorageKey: (params?: any) => t._call("Storage.untrackCacheStorageForStorageKey", params) as any,
      untrackIndexedDBForOrigin: (params?: any) => t._call("Storage.untrackIndexedDBForOrigin", params) as any,
      untrackIndexedDBForStorageKey: (params?: any) => t._call("Storage.untrackIndexedDBForStorageKey", params) as any,
      getTrustTokens: (params?: any) => t._call("Storage.getTrustTokens", params) as any,
      clearTrustTokens: (params?: any) => t._call("Storage.clearTrustTokens", params) as any,
      getInterestGroupDetails: (params?: any) => t._call("Storage.getInterestGroupDetails", params) as any,
      setInterestGroupTracking: (params?: any) => t._call("Storage.setInterestGroupTracking", params) as any,
      setInterestGroupAuctionTracking: (params?: any) => t._call("Storage.setInterestGroupAuctionTracking", params) as any,
      getSharedStorageMetadata: (params?: any) => t._call("Storage.getSharedStorageMetadata", params) as any,
      getSharedStorageEntries: (params?: any) => t._call("Storage.getSharedStorageEntries", params) as any,
      setSharedStorageEntry: (params?: any) => t._call("Storage.setSharedStorageEntry", params) as any,
      deleteSharedStorageEntry: (params?: any) => t._call("Storage.deleteSharedStorageEntry", params) as any,
      clearSharedStorageEntries: (params?: any) => t._call("Storage.clearSharedStorageEntries", params) as any,
      resetSharedStorageBudget: (params?: any) => t._call("Storage.resetSharedStorageBudget", params) as any,
      setSharedStorageTracking: (params?: any) => t._call("Storage.setSharedStorageTracking", params) as any,
      setStorageBucketTracking: (params?: any) => t._call("Storage.setStorageBucketTracking", params) as any,
      deleteStorageBucket: (params?: any) => t._call("Storage.deleteStorageBucket", params) as any,
      runBounceTrackingMitigations: (params?: any) => t._call("Storage.runBounceTrackingMitigations", params) as any,
      getRelatedWebsiteSets: (params?: any) => t._call("Storage.getRelatedWebsiteSets", params) as any,
      setProtectedAudienceKAnonymity: (params?: any) => t._call("Storage.setProtectedAudienceKAnonymity", params) as any,
    },
    SystemInfo: {
      getInfo: (params?: any) => t._call("SystemInfo.getInfo", params) as any,
      getFeatureState: (params?: any) => t._call("SystemInfo.getFeatureState", params) as any,
      getProcessInfo: (params?: any) => t._call("SystemInfo.getProcessInfo", params) as any,
    },
    Target: {
      activateTarget: (params?: any) => t._call("Target.activateTarget", params) as any,
      attachToTarget: (params?: any) => t._call("Target.attachToTarget", params) as any,
      attachToBrowserTarget: (params?: any) => t._call("Target.attachToBrowserTarget", params) as any,
      closeTarget: (params?: any) => t._call("Target.closeTarget", params) as any,
      exposeDevToolsProtocol: (params?: any) => t._call("Target.exposeDevToolsProtocol", params) as any,
      createBrowserContext: (params?: any) => t._call("Target.createBrowserContext", params) as any,
      getBrowserContexts: (params?: any) => t._call("Target.getBrowserContexts", params) as any,
      createTarget: (params?: any) => t._call("Target.createTarget", params) as any,
      detachFromTarget: (params?: any) => t._call("Target.detachFromTarget", params) as any,
      disposeBrowserContext: (params?: any) => t._call("Target.disposeBrowserContext", params) as any,
      getTargetInfo: (params?: any) => t._call("Target.getTargetInfo", params) as any,
      getTargets: (params?: any) => t._call("Target.getTargets", params) as any,
      sendMessageToTarget: (params?: any) => t._call("Target.sendMessageToTarget", params) as any,
      setAutoAttach: (params?: any) => t._call("Target.setAutoAttach", params) as any,
      autoAttachRelated: (params?: any) => t._call("Target.autoAttachRelated", params) as any,
      setDiscoverTargets: (params?: any) => t._call("Target.setDiscoverTargets", params) as any,
      setRemoteLocations: (params?: any) => t._call("Target.setRemoteLocations", params) as any,
      getDevToolsTarget: (params?: any) => t._call("Target.getDevToolsTarget", params) as any,
      openDevTools: (params?: any) => t._call("Target.openDevTools", params) as any,
    },
    Tethering: {
      bind: (params?: any) => t._call("Tethering.bind", params) as any,
      unbind: (params?: any) => t._call("Tethering.unbind", params) as any,
    },
    Tracing: {
      end: (params?: any) => t._call("Tracing.end", params) as any,
      getCategories: (params?: any) => t._call("Tracing.getCategories", params) as any,
      getTrackEventDescriptor: (params?: any) => t._call("Tracing.getTrackEventDescriptor", params) as any,
      recordClockSyncMarker: (params?: any) => t._call("Tracing.recordClockSyncMarker", params) as any,
      requestMemoryDump: (params?: any) => t._call("Tracing.requestMemoryDump", params) as any,
      start: (params?: any) => t._call("Tracing.start", params) as any,
    },
    WebAudio: {
      enable: (params?: any) => t._call("WebAudio.enable", params) as any,
      disable: (params?: any) => t._call("WebAudio.disable", params) as any,
      getRealtimeData: (params?: any) => t._call("WebAudio.getRealtimeData", params) as any,
    },
    WebAuthn: {
      enable: (params?: any) => t._call("WebAuthn.enable", params) as any,
      disable: (params?: any) => t._call("WebAuthn.disable", params) as any,
      addVirtualAuthenticator: (params?: any) => t._call("WebAuthn.addVirtualAuthenticator", params) as any,
      setResponseOverrideBits: (params?: any) => t._call("WebAuthn.setResponseOverrideBits", params) as any,
      removeVirtualAuthenticator: (params?: any) => t._call("WebAuthn.removeVirtualAuthenticator", params) as any,
      addCredential: (params?: any) => t._call("WebAuthn.addCredential", params) as any,
      getCredential: (params?: any) => t._call("WebAuthn.getCredential", params) as any,
      getCredentials: (params?: any) => t._call("WebAuthn.getCredentials", params) as any,
      removeCredential: (params?: any) => t._call("WebAuthn.removeCredential", params) as any,
      clearCredentials: (params?: any) => t._call("WebAuthn.clearCredentials", params) as any,
      setUserVerified: (params?: any) => t._call("WebAuthn.setUserVerified", params) as any,
      setAutomaticPresenceSimulation: (params?: any) => t._call("WebAuthn.setAutomaticPresenceSimulation", params) as any,
      setCredentialProperties: (params?: any) => t._call("WebAuthn.setCredentialProperties", params) as any,
    },
    WebMCP: {
      enable: (params?: any) => t._call("WebMCP.enable", params) as any,
      disable: (params?: any) => t._call("WebMCP.disable", params) as any,
      invokeTool: (params?: any) => t._call("WebMCP.invokeTool", params) as any,
      cancelInvocation: (params?: any) => t._call("WebMCP.cancelInvocation", params) as any,
    },
  };
}

// Stats: 56 domains, 652 commands (excl. redirects), 608 types
