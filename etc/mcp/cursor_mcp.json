{
  "mcpServers": {
    "gateway": {
      "command": "uvx",
      "args": ["agent-mcp-gateway"]
    },
    "utcp": {
      "command": "npx",
      "args": ["-y", "@utcp/code-mode-mcp"],
      "env": {
        "UTCP_CONFIG_FILE": "${env:UTCP_CONFIG_FILE}"
      }
    }
  }
}
